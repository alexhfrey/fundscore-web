# Day plan 2026-07-17 — owner-approved priorities (paste-in session brief)

Owner decisions recorded this morning: X-Ray solver SERVICE is **PAUSED** (do not work it);
FMP licensing **resolved** ("consider sorted"); the Jul-5 payload contraction was **NOT
intended** → restore. Fee source for SPY-type funds: default = Option A below unless the owner
says otherwise. State of the world: all gated branches are merged (fund_score main d697721+,
web main 729cb32+, unpushed), local dev serves manifest 46, backlog re-grounded 2026-07-17.

Work top-down. One item at a time, each in a fresh fund_score worktree + branch (branch-guard
blocks main commits; owner merges or asks for merges). Standard gates: measure before-state →
fix at source → rebuild → /check-data + inconsistency sweep → data-reviewer + codex → commit.
NEVER `git push` the web repo (push = prod deploy). Serving reloads: LOCAL dev only.

## 1. Restore the 71 dropped funds (data, reviewed lane) — top priority

The Jul-5 beta-skill rebuild of `data/gold/value_offering_payload.parquet` (10,731→10,435
rows) silently removed 71 funds, 49 of them live-priced (TRP intl cluster PRIDX/PRASX/PRLAX…).
Evidence base: `value_offering_payload.parquet.pre-beta-skill-bak` (has all 71 as EQ+ticker);
their tickers appear under NO successor sid. Owner: not intended.

- Root-cause the payload builder (why did the beta-skill basis change drop live funds — a join
  that went inner? a universe filter keyed on the new skill panel?). Fix at SOURCE — no
  copy-backs from the .bak.
- Rebuild: payload → `build_factor_exposure.py` (~30 s) → `build_exposure_divergence.py`.
  Both now carry guards (winner-rule ambiguity exclusion, fresher-β PIT drop) — expect their
  warnings, don't silence them.
- Verify PER-FUND: the 71 back in the payload; served-β fund count recovers from 4,572 toward
  ~4,62x (net of the 21 genuinely-dead + EGRAX/DFLVX honest exclusions — those stay out).
- Reload local serving (`build_serving_facts.py`, manifest 47) + re-run the serving checks
  (fund_holdings_full, l2_served_refit).

## 2. ETF/UIT expense-ratio source (data) — fixes the SPY fee gap

SEC-filed ER history has no rows for UIT-structured ETFs (SPY, QQQ, DIA…), so SPY blend legs
and SPY-as-input serve `expense_ratio_bps: null` → fee gap partial/missing.

**Default: Option A — FMP etf-info ingestion** (owner said FMP is sorted): extend the existing
client `src/fundscore/reference/fmp.py` (has key handling + backoff) with the ETF-info
endpoint; emit a small `etf_expense_ratios` table with per-row provenance (source, fetch date).
Rules: SEC-filed ER stays PRIMARY everywhere it exists — FMP fills only null-ER ETFs; bound
values to (0, 2%]; VALIDATE against known figures (SPY 9.45 bps, VTI 3, IWF 19) and, for
ETFs with BOTH sources, check FMP≈filed (commensurability — investigate any >2 bps gap).
Wire into the solver's `_ticker_er` fallback + blend-leg ER lookup. Acceptance: the
{VTSAX 40, AGTHX 30, SPY 30} CLI solve returns fee.coverage_state="available" with a real gap;
a SPY 100% book shows SPY's own ER. (Fallback Option B if FMP's ETF data disappoints in
validation: a tiny curated table for the handful of UIT mega-ETFs, each row citing its
prospectus + as-of date — honest and reviewable, just manual.)

## 3. Adjudicate the value_score northstars drift (analysis, no rebuild)

`check_value_score_northstars.py` fails: 2022 cohort median −80 bps vs ~−60 band, starting
with the 07-16 L2 rebuild of its inputs (passive_alt_daily_nav / l2_replica_quality — the
canonical-price + 2026-06-30 refit migration). Determine whether the move is fully explained
by the input-basis change (compare the 2022 cohort on old-vs-new passive NAV if recoverable,
or decompose which leg moved). If explained → re-baseline the check's tolerance with an
era-stamp comment; if not → file the defect with evidence. Present the conclusion to the
owner either way. This UNBLOCKS item 4.

## 4. Then, in order (each unblocked by the one before)

- **Price-quarantine tightening** (backlog: sub-50× rescales slip `build_fund_daily_adj_close`;
  BSCL-class, ~35 in-window tickers). Rebuilds the canonical panel — only start AFTER item 3
  lands so drift attribution stays clean. Root-fix the quarantine threshold logic; expect
  downstream basis deltas; full sweep required.
- **Data-dictionary entry** (5 min): add `data/gold/fund_daily_adj_close.parquet` as the
  canonical price read to `docs/context/data-dictionary.md` (file exists, entry missing).
- **fund_the_take ↔ takeaways citation drift** (183/1,431 rows, re-measured 07-17): rebuild
  the two panels coherently.
- **Stretch — metadata-freshness chain census**: five backlog items chain off the same frozen
  inputs (`fund_metadata.last_nport` @2024-12-31, class_ticker_mappings @2024-12-31, N-CEN
  FY2025 not ingested). Do the census-first step only (what refresh would move, cost, risks)
  and file the plan — do NOT run a multi-hour ingest without the owner seeing the census.

## Out of scope today (owner-directed or deferred)

- X-Ray solver HTTP service — PAUSED by owner.
- Pricing-store compaction (multi-hour store rewrite — schedule separately).
- Foreign-holdings residuals, MFRR refresh-vs-deprecate, X-ray book-basis decisions,
  holdings-vs-β adjudication, serve-l2 fit-floor, panel-refresh pass — unchanged in backlog.
- Anything touching prod: web push, prod DB — owner only.
