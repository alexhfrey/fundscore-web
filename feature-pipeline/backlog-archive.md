# Backlog archive — shipped items (newest first)

Full history of `- [x]` Done entries moved out of `backlog.md` (which keeps only the 3 most
recent for loop context). Same entry format; move items here verbatim, newest on top.
- [x] (data) **DONE 2026-07-17/18 — adjudicated AND fixed; fund_score `1c186a8` on `fix/benchmark-nav-dedup-rebuild`; northstars 10/10 PASS at median −70 (band untouched); local serving manifest 49.** The data-reviewer's blocking catch en route: `positioning_context.parquet` (built 8 min after the corrupt NAV) still embedded the corrupt TE basis inside the manifest-48 staging (664 served te_bps wrong, HAINX 2.7×) — rebuilt from the clean panel (2,036/2,036 verified) and staging re-assembled before the pass. Correction of record: 3 strict sign flips (not 19; 474/16 movers exact). ADJUDICATION: Controlled decomposition (input swaps E0–E2b + corrected-basis counterfactual): membership/current-era-R² effect ≈ 0, metadata-restore effect exactly 0 — the whole −80 is a VALUE effect from corrupted ETF gap-fill legs in `passive_alt_daily_nav`. Mechanism proven end-to-end: the 2026-07-10 full-history re-fetch landed a SECOND adjustment vintage in the raw tiingo store; the Jul-16 rebuild's un-deduped raw-glob read (`build_benchmark_nav.load_etf_daily`) concatenated both (BND ~3.98 rows/date) → seam garbage on 61 pool ETFs (HAINX benchmark Nov-2022 +15.33% vs true IEFA +13.16%; 175/1,378 2022-cohort funds shifted >50bps/yr, one-directionally penalized). The f011953 loader fix is already merged but its gold rebuild was deferred on a "~1-2bp" estimate — measured: ~450bps/yr on intl legs, 10bps on the [A] median, and 474/2,036 SERVED value_score rows ≥10bps (16 ≥50bps, MLOIX verdict flip +10→+80). Counterfactual with the fixed loader: med22 −70, IN BAND, same cohort → check needs NO tolerance change. FIX (running now as the next loop): rebuild passive_alt_daily_nav with the fixed builder, re-run northstars (expect 10/10), rebuild value_score, local re-serve, full gates. Era note for the record: legitimate Jul-12 canonical-refresh drift accounts for ~−59→−70 within band. [fund_score: build_benchmark_nav / passive_alt_daily_nav]
  **Owner summary:** The failing self-check caught a real data bug, not a moved goalpost: the "passive alternative" price series for ~60 ETFs got scrambled by duplicate downloads, quietly dragging down scores for international funds (474 live scores affected, one flips verdict). The cleanup uses a fix we already shipped — we're rebuilding the affected series and re-checking now.
- [x] (bug) **DONE 2026-07-17 — both halves. (b) shipped as fund_score `d04253c` on `feat/etf-uit-expense-ratios` (worktree `fund_score-wt-etf-er`): FMP etf/info ingestion (owner Option A), 154-ticker solver universe, SEC-filed PRIMARY via anti-join, per-row provenance, anchors+bounds gates. Acceptance met: mixed {VTSAX,AGTHX,SPY} fee partial→available (gap 16.48 bps); SPY:100 serves its own 9.0 bps (fmp_etf_info). Gates: codex clean; data-reviewer PASS (SPY/GLD/DBC — the only serving vendor rows — verified vs issuer/EDGAR; filed primacy proven over 41,536 tickers).** THREE things the owner should know: (1) reviewer falsified FMP's basis — `expenseRatio` is GROSS under a fee waiver (BKLN 67 vs filed net 65); harmless today (serving rows are waiver-free, filed quarantines the rest) but any NEW vendor-only ticker must be waiver-checked before serving (rule documented in fmp.py); (2) QQQ/DIA are NOT covered — they're entirely outside the solver universe (unresolvable as inputs, honest absence); extending input support to them is a separate small item if wanted (QQQ's 18bps was externally verified — UIT→open-end conversion 2025-12-22 cut 0.20%→0.18%); (3) DBC's 89bps is total-cost basis (mgmt+brokerage per 10-K) — watch UI copy that says "expense ratio". Original: (b) UIT expense-ratio sourcing (owner: pick the source — SPY files no SEC ER). The mixed book {VTSAX,AGTHX,SPY} now solves available (VTI 83.2+IWF 16.8, r² .997), exposure available incl. SPY look-through, fee honestly partial pending (b). Cosmetic follow-up adjudicated: VTSAX typed `etf` is series-true (VTSAX+VTI = classes of one series); class-level typing later. Original: **`SPY` is `unsupported` as a Portfolio X-Ray input — and 30% of it suppresses the entire solve.** Verified live: `POST /api/portfolio/solve` with `{VTSAX 40, AGTHX 30, SPY 30}` returns `resolution_state: "unsupported"` for SPY, `excluded_weight_pct: 0.3`, and `coverage_state: "suppress"` with **zero blend legs** — the excluded weight crosses the >25% threshold and kills the whole result. SPY is the most widely held ETF in existence, and the landing page tells people to paste in what they hold. Note the asymmetry: SPY is fine as a blend LEG (the solver picks it), it just cannot be resolved as an INPUT — and separately it has no SEC-filed expense ratio (it files as a UIT), which is why a blend that lands on SPY returns `fee.coverage_state='missing'` and suppresses the fee gap. Both need fixing; they are different bugs with the same victim. Also worth checking: VTSAX/VFIAX resolve but come back typed `etf` when they are mutual-fund share classes. [fundscore-web: portfolio-solver / fund_score: solver ticker resolution + expense-ratio source for UITs]
  **Owner summary:** If someone types SPY — the single most commonly held fund in America — into the Portfolio X-Ray, they don't just lose that line: the whole analysis refuses to run. We're about to tell people on the homepage to paste in their holdings, so this is the first thing a real user is likely to hit.
- [x] (data) **DONE 2026-07-17 — restored at source; fund_score `8456dbd` on `fix/payload-contraction-71` (worktree `fund_score-wt-payload-restore`), local serving manifest 47.** Root cause was a TIME BOMB, not the beta-skill code: `last_nport_filing_date` derived solely from the frozen class_ticker_mappings (ends 2024-12-31); the 548-day recency window crossed that horizon 2026-07-03, so the Jul-5 build (first after) dropped every fund whose only fresh evidence was an unwitnessed N-PORT. Fix: N-PORT recency = max(CTM, raw holding store report_period_end, 3 partition years) + `--recency-only` in-place refresh with a fail-closed no-regression guard. Result: payload 10,435→10,860 (240 of the 315 restored incl. PRIDX/PRASX/PRLAX + 185 live filers dropped by EARLIER builds crossing the same horizon; 75 stay out EDGAR-verified dead — 0 recoverable-missing). Coherent downstream rebuild (contraction had propagated Jul-15): factor_exposure 4,576→4,657, holdings_complete +226, xray_panel +156, divergence +65, return_attribution +24, fund_alternatives↔SI leak healed; ZERO series lost anywhere; EGRAX/DFLVX/TBD/ZZZ exclusions preserved. Gates: data-reviewer PASS (independent 26,155/26,155 re-derivation), codex r2 clean, 5 falsification tests, check suite deltas all pre-existing. **Owner should know (reviewer W1/W2): the serving diff is not only the restore — peer-percentile re-ranking over the restored universe flipped `value_offering_label` on 54 retained funds (incl. 11 Strong→Mixed, 19 Mixed→Weak), and the xray rebuild also picked up the Jul-15/16 diversification+blend input refresh on retained funds.** Original: **OWNER DECIDED 2026-07-17: the drop was NOT intended — RESTORE the 71 funds at source.** Root-cause why the Jul-5 beta-skill payload rebuild dropped live funds (evidence base: `value_offering_payload.parquet.pre-beta-skill-bak`), fix the payload builder (no copy-backs), rebuild payload → factor_exposure → exposure_divergence, reload local serving, verify per-fund restoration. Original: **Jul-5 `value_offering_payload` contraction silently drops served factor-β for 71 funds at the next serving reload.** The beta-skill payload rebuild (2026-07-05, 10,731→10,435 rows; evidence: `value_offering_payload.parquet.pre-beta-skill-bak` has all 71 as EQ+ticker, current has none, and their tickers appear under NO successor sid) removed live funds (49/71 have prices through 2026-07-10; TRP intl cluster PRIDX/PRASX/PRLAX…). Manifest 45 still serves their β from the pre-contraction factor artifact; ANY serving reload from current gold drops them. Confirm the contraction was intentional (skill-basis scope change?) or restore them to the payload universe BEFORE the launch reload. Surfaced by the 2026-07-17 fresher-β per-fund sweep + data-reviewer. [fund_score: value_offering_payload / beta-skill universe]
  **Owner summary:** A universe rebuild two weeks ago quietly removed 71 funds — most of them alive and priced — from the list that gets factor analysis. Their fund pages still show it only because we're serving an old snapshot; the next data publish would silently blank those sections. You should decide whether that removal was intended before we publish.
- [x] (story) **RESOLVED by owner 2026-07-17 ("consider FMP sorted for now") — no gating needed.** Original: **OWNER ACTION — FMP display licensing unresolved, and FMP-derived sectors are now on the serving path.** The EODHD/FMP validation spike's own risk register marked display licensing BLOCKING before any UI ship (R1); the FMP foreign-sector enrichment has since merged to main and serves locally (manifest 46) — at the launch reload those sectors go public. Resolve the licensing question (the drafted inquiry email is at fund_score reports/legal/eodhd_licensing_inquiry_email.md) or gate the FMP-derived rows before prod. Surfaced by the 2026-07-17 backlog re-grounding. [fund_score: FMP enrichment / legal]
  **Owner summary:** We're using a data vendor's industry classifications on fund pages, and our own earlier research note said "check the license before showing this to users" — that check never happened. Needs a yes from the vendor (draft email already written) or we hide those rows at launch.
- [x] (data) **DONE — fresher-β invariant restored at source; MERGED to fund_score main (d96098b; served locally in manifest 46, FBGRX invariant verified in DB).** factor_exposure re-evaled at the ff6 frontier 2026-05-22 (root cause: refresh path never rebuilt it after the 07-12 ff6 refresh); fail-honest guard drops holdings-newer-than-β rows (16 rows / 2 funds, self-heals at next ff6 advance) + synthetic falsification test; ticker→series dedup rewritten (live-registration winner, tied live claimants EXCLUDED: EGRAX/DFLVX/TBD/ZZZ → see identity-hygiene item); both red tests green, 41/41 pass. Gates: data-reviewer r2 PASS (r1 correctly BLOCKED the interim lowest-sid rule), codex r3 pass. DISCLOSED: EGRAX 1-fund honest β drop; and see the NEW payload-contraction item below — owner must confirm before any serving reload. Original: **Stale exposure / risk-attribution gold violates the fresher-β invariant (holdings NEWER than factor eval).** Surfaced 2026-07-16 during the L2 serving reload: `exposure_divergence` / `risk_attribution` gold is ~3 months stale (April vintage) — the holdings snapshot (`2026-04-30`) is NEWER than the factor β eval date (`2026-04-24`), so β reflects returns that predate the holdings it's attributed to. Two tests are red on it (`test_exposure_divergence::test_artifact_structural_invariants`, `test_serving_fact_assembler::test_risk_betas_eval_is_factor_date_not_holdings`; FBGRX among the affected). PRE-EXISTING — fails at 76020e1 and independent of the L2 work (confirmed) and of the FMP merge (those files/tests untouched). It is now SERVED on local dev (manifest 45) next to the fresh 2026-06-30 L2 data (owner accepted this for the local preview). Fix: rebuild the exposure/factor/holdings chain coherently so factor_eval_date ≥ holdings_as_of before the PROD/launch reload. [fund_score: exposure_divergence / factor betas / holdings snapshot freshness]
  **Owner summary:** The risk/exposure section of the fund pages is running on ~3-month-old data whose internal dates are out of order (the holdings are newer than the returns the exposure is measured on), which trips two of our own guardrail tests. It's not new and not from today's price fix, but it's now visible in the local preview and should be refreshed before we publish to real users.
- [x] (data) **DONE (gold + local serving; prod rides the launch reload) — re-verified 2026-07-17 on the rebuilt exposure_xray_panel: 678/706 panel-present INTL funds (96.0%) now carry high/medium sector rows (was 1.1% of 845), via the merged FMP foreign-sector enrichment; served locally in manifest 46.** Original: **International funds now have NO sector X-Ray section** — only 1.1% of the 845 INTL funds get a served sector row (vs 74.9% of domestic). Surfaced by the 2026-07-13 coverage gate and confirmed HONEST-MISSING against the raw source: `cusip_reference` carries a sector on just 5.5% of rows; median sector coverage for an INTL fund is **9.8% fund-side / 1.8% benchmark-side** (SSNLF/TCTZF/NVSEF → sector=None while the ADRs TSM/ASML/BABA classify fine). The suppression is CORRECT — the old panel was computing sector tilts from ~10% of the book, which is precisely the fabrication the data doctrine forbids, so this is not a regression to undo. **But it is the single biggest product hole, and it is RECOVERABLE: the FMP foreign-sector path takes foreign sector coverage 6.3% → 99.6%** (spike done; licensing blocks DISPLAY, not use as an internal bound — so even an unlicensed FMP feed could legitimately un-suppress these panels by shrinking the error bound). This is the strongest argument for building the FMP enrichment next. [fund_score: FMP foreign enrichment]
- [x] (data) **DONE — theme re-key SHIPPED in the merged FMP work (a5a9895 via ebdb9c8): canonical theme_key over the FMP ISIN→US-ticker bridge for matching AND decomposability; ASML-Amsterdam holders 0→3 semi/AI rows, 271 funds newly covered, high-confidence theme rows 25,557→41,195.** Original: Curated theme library is keyed on US TICKERS (`build_curated_theme_library.py:121`) while `holdings_complete.security_id` falls back to ISIN for foreign lines → **a fund holding ASML's Amsterdam line gets ZERO semiconductor-theme credit while an ASML-ADR holder gets full** (same for TSM/NVO/RHHBY). Same economic exposure, opposite answer. The 2026-07-13 coverage gate makes this HONEST (theme confidence de-rates and the tilt is suppressed) but does not FIX it. Root fix = re-key the library off a canonical issuer id (LEI or an ISIN→issuer crosswalk). FMP sector/ISIN enrichment is a natural enabler. [fund_score: curated_theme_library / holdings_complete security_id]
- [x] (chore) **DONE — both L2-cadence hardening follow-ups; MERGED to fund_score main (f011953)** (benchmark-NAV ETF reads → canonical panel, gold rebuild deferred to the quarterly runbook post-merge given the open northstars adjudication; served_l2 resolvers — date AND path — now share a fail-closed partial-partition guard + 3 unit tests; codex pass, 47 tests). Original: Two L2-cadence hardening follow-ups from the 2026-07-16 codex review (both P2, non-blocking): (1) `build_benchmark_nav.py` still sources ETF gap-fill NAV from the raw Tiingo glob, not the canonical panel — move it to the canonical basis so the passive daily-NAV (value_score's beta basis) matches the solver's prices (~1-2bp today). (2) `served_l2_refit_date()` picks the latest partition assuming it's a full-universe refit; add a guard/warn if a partial new-date partition is ever written (the `l2_served_refit` check is the current backstop). [fund_score: build_benchmark_nav.py / passive_match/io_utils.py]
  **Owner summary:** Two small robustness cleanups the code reviewer suggested on the pricing work — neither affects correctness today, both make the quarterly refresh sturdier.
- [x] (data) **DONE — L2 passive-match batch on the canonical price panel + 2026-06-30 served refit + quarterly cadence; served on local dev.** SHIPPED to fund_score `main` 2026-07-16 (merge `ebdb9c8`; L2 work `76020e1`, pushed to origin). `run_benchmarks.py` `--funds-daily`/`--tiingo-daily` default to the canonical de-duped `fund_daily_adj_close.parquet` (fixes the arbitrary-adjustment-vintage bug — CPAI in-sample TE 6360→0.10). Owner advanced the served refit to the current quarter-end **2026-06-30** (matches X-Ray `PORTFOLIO_SOLVER_AS_OF`, verified no override) + established a **quarterly cadence**: shared `served_l2_refit_date()` resolver (latest partition) used by all 5 served consumers (fixed a latent cross-panel desync), `l2_served_refit` guard check, documented refit runbook. value_score coherence fix (scored ⇒ served passive_alt; GSCOX corrupt-price → not_comparable, served `missing_passive_match`). **Gates green:** codex HIGH pass (0 P0/P1), data-reviewer PASS (0 fabrication / 0 recoverable misses; 313 drops all honest), 197 tests, `l2_served_refit` PASS. **FMP sector branch merged to main too** (clean, disjoint files) so serving is coherent. **Reloaded local dev Postgres** 2026-07-16 (build_serving_facts → manifest 45 active: 5,675 facts + 1.38M holdings + 1,941 attribution rows, one txn). Blast radius: ~4,781 served labels move (~19%, mostly cosmetic same-asset tie-flips, ~203 cross-asset); coverage net −143 vs prior weights partition (honest attrition), value_score 2,036 scored. **PROD serving reload happens at the gated X-Ray launch** (from this same main). Closes the X-Ray↔fund-page split. [fund_score ebdb9c8]
  **Owner summary:** DONE and previewable locally. We fixed the price-data bug under the live fund pages' "closest passive alternative," brought those pages to the same current date as the Portfolio X-Ray, and built a repeatable quarterly refresh so they never drift apart again. Independent review confirmed the numbers are clean. It's merged to main (alongside the sector work) and loaded into the local preview database; the live/production publish happens at the gated X-Ray launch.
- [x] (bug) **DONE (part a) — X-Ray default example swapped to the landing worked book; merged to web main (2e8a419; unpushed — push = deploy).** EXAMPLES now leads with FXAIX 40 / FCNTX 35 / VWIGX 25 → verified live (worktree @ fund_score main ebdb9c8, as-of 2026-06-30): IWF 62.5 + VEU 37.5, fee 35.8→12.75 bps, gap 23.1 bps, fit high — consistent with the landing DEMO_* story. Build+lint green, CODEX_GATE pass. Repro note (honest): on post-fix main the OLD book no longer suppresses — it solves to VTI 100% with an available 57 bps gap (the filed failure was the pre-fix solver era); the swap proceeded per the owner's "do (a) now regardless" and also de-risks future tie-flips back to ER-less SPY. Part (b) (SPY UIT expense ratio) remains OPEN in the SPY-unsupported item above. Original: **The X-Ray's default "Active / passive mix" example solves to SPY 100% → a SUPPRESSED fee gap, i.e. the first thing a visitor clicks demonstrates the product FAILING to answer its headline question.** Verified: clicking that built-in example and running it returns `coverage_state: suppress`, no blend, and the fee-gap panel reads "we can't show a blended fee gap … no SEC-filed expense ratio" (SPY files as a UIT). A tilted book (DODGX/POAGX/VWIGX/FLPSX) instead solves cleanly to VO+VEU with a real +54bps gap. Two things: (a) change the built-in example portfolios so the default demonstrates a *successful* solve with a real fee gap (the marketing screenshots use FXAIX/FCNTX/VWIGX → IWF+VEU, 36→13bps — reuse that book); (b) is downstream of the SPY UIT fix above — once SPY carries an expense ratio, the suppression goes away. Do (a) now regardless; it's a one-line example-set change and it's what a first-time visitor sees. [fundscore-web: src/components/portfolio/PortfolioXray.tsx EXAMPLES]
  **Owner summary:** The sample portfolio we ship on the X-Ray page — the one a curious visitor clicks first — happens to produce a blank "we can't answer this" result, which is the worst possible first impression for the product's flagship feature. Swap it for a sample that returns a real, punchy answer.
- [x] (data) **DONE — methodology as-ofs re-derived from shipped gold; SHIPPED 729cb32 (merged to web main, not pushed).** 10 of 17 sections were stale (value_score → v0.3/2026-07-11, skill → v1/2026-07-05, xray → v0.4, return_attr → v0.2, frontier → 2026-04-30 per-product 'up to' phrasing; takeaways/the-take/VO verified genuinely still 2025-10-31 and left alone; risk-behavior as-of honestly unverifiable → build-date phrasing; stale-β state now disclosed where the method text claimed the opposite). Gates: data-reviewer PASS (12/12 exact vs gold, served==gold==registry), codex pass (3 P2s folded in). FOLLOW-UP (kept Open below): derive registry values programmatically at build time so they can't drift — third drift-fix in a month (facts.ts caught it 07-13, this item 07-16). Original: `src/lib/methodology/registry.ts` still stamped `HOLDINGS_FRONTIER = "2025-10-31"` (10 occurrences of that date) and several per-artifact `asOf` strings, but the N-PORT refresh advanced most funds to **2026-03-31 / 2026-04-30** (FCNTX profile shows holdings as of 2026-03-31). So the public, indexable trust page — the one every score deep-links to for provenance — tells users the data is ~5–6 months older than it is. This is a data-integrity/trust defect on the one surface whose entire job is honesty about freshness. Fix: re-derive the registry's `methodVersion`/`asOf`/`HOLDINGS_FRONTIER` values from the actual shipped gold artifacts (the file header says these are "copied from the real shipped data products … do NOT invent numbers"), and consider deriving them programmatically so they can't drift again. [fundscore-web: src/lib/methodology/registry.ts]
  **Owner summary:** Our public methodology page — the page that exists specifically to be honest about how fresh our data is — is itself showing dates that are about half a year out of date. It's not wrong data in the product, but it undercuts the exact trust the page is meant to build, so it should be corrected and ideally wired to update itself. **Corrected 2026-07-16 night run; the "wired to update itself" half is the follow-up item below.**
- [x] (data) X-Ray pricing — read the canonical panel + ticker-scope the load + fail-honest bad-price guard — SHIPPED 2026-07-16 (fund_score d92213c, branch `fix/xray-canonical-pricing-panel`; web branch `fix/xray-as-of-and-exclusion-copy`; **committed on branches, NOT merged/pushed, NOT reloaded to Postgres**). The filed "compaction" was really an arbitrary-adjustment-vintage bug, not pure I/O: the solver read the raw 3,993-file glob (205M rows, 86.9M duplicate re-fetch vintages) and `.last()`-over-date-sort picked a RANDOM vintage per week (seams: a fund reading +206%/wk; 24,000,000% on near-zero prices). Fix repoints funds_daily/tiingo_daily to the canonical de-duplicated `fund_daily_adj_close.parquet` (already built + read by ~30 pipelines) and ticker-scopes each solve (scan+filter on ONE file, predicate pushdown): **180s → ~1–4s** (fund load 25s→0.05s). Adds a fail-honest guard (>200%/wk excluded → `implausible_return_series`; 113/32,419 tickers, all genuine artifacts; leveraged funds not false-rejected) and advances SOLVER_AS_OF 2026-02-28 → 2026-06-30 (web) + Python/CLI default. Web: friendly exclusion copy. Gates: data-reviewer PASS (5/5 — no fabrication, exact scoped==full parity, +189 net funds covered, no leak), codex HIGH pass (no P0/P1), 165 tests. Acquisition confirmed already-incremental (only-when-needed) — no fetch change. **Owner-accepted interim** (codex P1): the L2 batch still on the raw glob → the top Open item (required-before-launch). [fund_score d92213c]
  **Owner summary:** The Portfolio X-Ray now answers in about a second instead of two-to-four minutes, on corrected prices. We found the slowness was really a data bug — the tool was silently picking a random version of each fund's price history — and fixed both at once by pointing it at the clean price file the rest of the system already uses; bad-price funds now fail honestly instead of producing garbage, and the shown data is ~4 months fresher. One follow-up (now top of the list) must run the same correction through the pipeline feeding the fund pages before we open the X-Ray to users.
- [x] (bug) te_decomposition build gate hardened (fail-closed) + registered check — SHIPPED 2026-07-13 (fund_score a9b2dc0, branch `fix/te-decomp-build-gate`; **committed on branch, NOT yet merged to main** — deferred to avoid disturbing a concurrent session on the shared main checkout; ready to merge). Closes two codex P2 advisories from ee72d15. (1) `build_te_decomposition.py` ran `ted.validate()` AFTER `write_parquet` and only LOGGED PASS/FAIL (and skipped validation entirely on an empty panel), so `make build-te-decomposition` exited 0 and could silently promote a bad/empty `te_decomposition.parquet` on a future rebuild → now validates BEFORE writing (new `_enforce_gate`): an empty panel or ANY failed invariant raises `SystemExit`, on-disk parquet left untouched. (2) No `te_decomposition` entry existed in `run_checks.py` so `make check FEATURE=te_decomposition` selected nothing → added the entry + new `scripts/reports/check_te_decomposition.py` re-running the 11 invariants + non-empty/named-bet coverage on the shipped panel (P2-fixed inline: the coverage row FAILS, not silently passes, when a by-bet panel has 0 bet rows). Realized impact nil (live panel 26,594 rows / 2,054 funds passes all 11 invariants → gate won't false-block); latent on rebuilds. Ran in an isolated fund_score worktree (zero branch contention with the concurrent foreign-holdings session on `feat/sector-rollup-and-coverage-gate`). Gates: pytest 8/8 (new `_enforce_gate` test — valid passes, empty + invariant-violating both raise), check PASS on the live panel, ruff clean, `run_checks --feature te_decomposition` selects+runs+PASS, codex HIGH pass (0 P0/P1, 0 advisories). [fund_score a9b2dc0]
  **Owner summary:** Closed a latent hole in the tracking-error product's build: its quality check ran only AFTER the data was already written and merely logged problems, so a future bad rebuild could have shipped silently. It now blocks the build before writing, and the same checks are wired into our standard check command. Today's live data passes everything — nothing customer-facing moved. Committed on a branch and ready to merge (held back only to not disturb another in-flight backend session).
- [x] (data) exposure_xray vs_benchmark zero-fill fabrication FIXED (Class A) — SHIPPED 2026-07-13 (fund_score eed1b00, branch fix/exposure-xray-vs-benchmark-zerofill, merged+pushed to main). Root cause: a full-outer join + `fill_null(0.0)` on the passive weight (build_stock_rows / _group_exposure / build_theme_rows) fabricated `reference_value=0.0`, `difference=holdings_value` for every key of a fund with NO row in `passive_blend_holdings` → RYZAX (leveraged Rydex S&P 500, 158.6% US) read "+159pp overweight US" at confidence_state=high; 1,975 funds / 100,355 vs_benchmark rows fabricated. Fix: gate each vs_l2_passive builder on the fund being in the passive blend (semi-join); no-blend funds emit NO vs_benchmark / sector_active_share rows; genuine per-key zeros for has-blend funds preserved (EPDPX = all-foreign passive → real 0% US). Measured (rebuild pinned --today 2026-06-15): vs_benchmark 341,734→241,379 (−100,355), +3,626 fabricated sector_active_share removed; **241,377/241,379 surviving rows byte-identical, 0 value changes**; 0 has-blend funds lost their comparison; provenance gold==source at machine epsilon. **User steered the root-cause**: censused the coverage gap first (1,564/79% PASSIVE by design, 377/19% too-young, 34 bond-matched) → no rebuild fills it → owner chose suppression. Guards: build invariant [17] + data-backed test. Gates: /check-data exit 0 (WARN pre-existing/orthogonal), data-reviewer PASS (all 5 claims + 4 adversarial angles), codex PASS 0 P0/P1, 6/6 tests. Residuals filed (Class-B look-through depression, passive-match coverage, downstream panel-refresh propagation). [fund_score eed1b00]
  **Owner summary:** The portfolio-comparison panel stopped inventing a "0% benchmark" for funds that have no passive alternative — an S&P 500 fund no longer reads as "+159 points overweight the US." We proved the ~2,000 affected funds are overwhelmingly index products (which correctly have no alternative) or brand-new funds (too young to match), not a database we forgot to populate; the honest "no comparison yet" now shows instead of a fabricated number, and every fund we CAN benchmark is untouched.
- [x] (story) Spike: evaluate FMP to enrich foreign holdings — **DONE 2026-07-13. Verdict: GO on sector/industry/market-cap (foreign-only); NO-GO for now on returns; BLOCKED on display licensing.** Full report: `fund_score/reports/product/fmp_foreign_enrichment_spike.md`. Measured live against `holdings_complete.parquet` (1.66M rows / 5,528 funds) with the real key. **SECTOR: coverage 98.0% value-weighted of the previously-unmapped set; panel sector coverage 81.2%→99.6% of USD (52.4%→98.2% of rows); funds below an 80% classification floor 2,092→113.** Deterministic ISIN join (not fuzzy name-match); 15/15 atomic spot checks correct (MJSC = MUFG Japan Small Cap, 100% unclassified today → full plausible breakdown). **Commensurability: sector vocabulary is IDENTICAL (same 11 Morningstar labels), 98.2% value-wtd agreement on the 4,865-security overlap → safe to mix foreign-FMP with US-Sharadar at SECTOR level. INDUSTRY is NOT commensurable (149 vs 158 labels, only 82 shared) → adopt FMP for the whole book or not at all, never blend.** Cost shape is 4 bulk `/stable/profile-bulk` calls (90,305 profiles), not 27K lookups; `marketCap` 99.9% populated comes free → unlocks the style box (`holdings_size.py` `cap_unverified`). **RETURNS: coverage is fine (40/40 sampled foreign names priced, 35/40 ≥5Y) but prices are LOCAL-CURRENCY — Toyota 2021-24 = +121% JPY vs +45% USD, a 76pp distortion. Needs a daily FX layer + non-synchronous-close handling = its own spec, not a join. The features it would unlock (Return Attribution, Brinson, Exposure Path for intl) are currently SUPPRESSED not wrong, i.e. failing safe → defer.** Residual after FMP = 0.37% of panel value, split honestly: 0.15% fund/trust wrappers (OUR FoF look-through leak, not a stock), 0.09% RECOVERABLE (issuer is in FMP under a different ISIN — N-PORT files secondary lines: L'Oreal FR0011149590 vs primary FR0000120321; needs an issuer-name/LEI fallback), 0.08% no-ISIN-filed, 0.06% Gulf/ADX = TRUE honest vendor gap (FMP has no ADX coverage, confirmed). **RISKS: (R1 BLOCKING) FMP individual plans do NOT permit displaying/redistributing data to end users — needs a separate Data Display & Licensing Agreement; FundScore.ai displays sector breakdowns publicly. Resolve before ANY UI ship. (R3) never let FMP overwrite US names — 9.1% of overlap disagrees; coalesce sharadar→fmp only. (R5) profiles are current-sector not point-in-time; snapshot with as_of.** [fund_score: spike complete, implementation not started]
  **Owner summary:** FMP works, and works well, for the sector half of the problem — it fills in the sector for foreign stocks on a clean ISIN match and would fix ~2,000 funds whose sector breakdown is currently blank or partial. It uses the exact same sector names we already use, so it drops in cleanly. Market cap comes free, which also fixes the style box for those funds. The returns half is a bigger project than it looks (foreign prices come back in yen/euro, not dollars — ignoring that would overstate a Japanese stock's return by 76 points), so we should defer it. **The one thing that can kill this is legal, not technical: FMP's standard plan doesn't let us show their data to end users. We need to check the licence before building anything.**
- [x] (data) FOLLOW-UP from FMP spike: Sharadar sector ROLLUP defect — **FIXED 2026-07-13** (branch `feat/sector-rollup-and-coverage-gate`). Root cause: it is **vendor data, not our bug** — Sharadar's `industry` is correct but its `sector` column is misrolled against it, and NO industry→sector rollup existed anywhere in the repo. Fixed at the vendor boundary (`src/fundscore/reference/sector_rollup.py`, applied in `sharadar.build_cusip_sector_map`): 6 overrides — Travel Services→Consumer Cyclical, Building Products & Equipment→Industrials, Thermal Coal→Energy, Uranium→Energy, Publishing→Comm Services, Shell Companies→Financial Services. `industry` never touched. 2,722 securities re-sectored; verified surgical (ticker/name/industry diff = 0; 0 non-overridden industries moved; 0 landed on a wrong target). Deliberately did NOT override `Solar`→Technology or `Electronic Gaming`→Comm Services: Sharadar matches Morningstar there and FMP is the outlier. Validated by the affected funds' own NAMES: SPCZ (RiverNorth **Pre-Merger SPAC** fund) 91% "Industrials" → 77.5% Financial Services; OOTO (Direxion **Travel & Vacation**) → Consumer Cyclical; URA/URAN/NLR (**uranium** ETFs) → Energy. Rebuild path added: `download_sharadar_tickers.py --from-cache` (no API call, raw vendor copy untouched) + `run_cusip_mapping.py --no-openfigi`. [fund_score]
- [x] (bug) LATENT, found while fixing the above: `cusip_mapping._prefix_match` was **NON-DETERMINISTIC** — `unique(subset=["cusip_prefix"], keep="first")` with no sort. polars' `unique()` is hash/parallel, so "first" is only stable for byte-identical input: editing an UNRELATED column (the sector relabel) silently reassigned the **TICKER for 3,267 prefix-matched CUSIPs**. The reference artifact was irreproducible under any upstream edit. Fixed with an explicit `.sort(["cusip_prefix","cusip"])` before `unique(..., maintain_order=True)`; regression test added. Verified benign: for all 6,842 multi-issue prefixes the lowest cusip is a NUMERIC issue code (the common-stock line), never a lettered bond line (ASCII digits sort before letters). **Any `unique(keep="first")` without an explicit sort in this repo is a latent bug of the same class — worth a sweep.** [fund_score]
- [x] (data) FOLLOW-UP from FMP spike: X-Ray coverage flags were computed and IGNORED — **FIXED 2026-07-13** (same branch). The X-Ray already computed `classified` (fund) + `passive_cov` (passive) and bucketed them into `confidence_state`; **no downstream consumer read it.** `named_bets_from_panels` and `select_candidates` filtered on tilt magnitude alone, so tilts manufactured entirely by an unclassifiable book were served as user-facing named bets and used to pick risk-model regressors. **Fix is an ERROR-BOUND gate, not a confidence filter** — new panel cols `fund_unclassified_value`/`reference_unclassified_value` (in **pct_nav units**, NOT the EC-sleeve `classified` ratio) + `exposure_xray.credible_tilt(floor_pp)`, which charges an OVERweight against the benchmark's unclassified weight and an UNDERweight against the fund's (each direction has its own one-sided error). A naive confidence_state filter would have been WRONG: it deletes CSRIX (Cohen & Steers Realty, +96pp real-estate, `confidence_state='low'`) whose fund side is 99.8% classified — only its BENCHMARK is dirty, and 96−38=58pp ≫ floor. Real fabrications killed: **SPAQ "−83pp underweight industrials" on a 100%-unclassifiable book**; SAPH "+96pp technology" vs a 98.5%-unclassified benchmark; MSTQ −50pp. Also fixed the **one genuinely silent axis**: theme `confidence_state` was hardcoded `lit("high")` → now real, based on KEYABILITY (100% "high" across 150,083 rows → high 75k/medium 50k/low 16k/unavailable 9k). Impact: sector named-bet rows 6,148→2,230, funds with a bet_tag 1,396→856 (555 fabricated removed); **owner chose to ship the dip rather than serve fabricated bets**, and FMP enrichment restores them legitimately (→ ~1,880 funds). Gates: `make check` 5 PASS/2 WARN (both pre-existing)/1 FAIL (pre-existing `data_freshness`: rolling_factor_alpha row-drop from 87bdd3a, unrelated); 1,158 tests pass, 8 fail = all pre-existing on main. STILL OPEN (root cause): the curated theme library is keyed on **US TICKERS** so a fund holding ASML's Amsterdam line gets ZERO semiconductor-theme credit while an ASML-ADR holder gets full — re-key the library off a canonical issuer id. [fund_score]
- [x] (bug) Ken French factor refresh repaired + factor-alpha rebuilt — SHIPPED 2026-07-12 (fund_score 87bdd3a, branch fix/ken-french-factor-refresh). Root cause: the Momentum CSV pads every row with a trailing comma (header ",Mom,") → `split(",")` gave 3 fields vs a 2-col (date,mom) schema → `polars ComputeError`; FF5 has no trailing comma so only the momentum leg failed and ff6/factor-alpha froze at 2026-04-24. Fix: strip trailing EMPTY fields only (a genuine extra column still raises → no silent drop). Refreshed ff6_daily→2026-05-29 / ff6_weekly→2026-05-22 (overlap corr 0.9995 = whole-history CRSP `202604`→`202605` vintage revision, all 6 premia textbook), rebuilt base rolling_factor_alpha 463,440→466,217 rows / 3,929 funds / →2026-05-22 — freshness lag **79→51 days** (51 = source floor, Ken French publishes to 2026-05-29). Row-additive (0 funds lost rows, +2,777 tail rows, n_obs byte-identical; sub-bp historical value revision). Row-drop FAIL adjudicated = NOT a silent drop (fund count ROSE 3,926→3,929, shared-window keys identical to the prior build at 454,985, baseline is pre-dedup) — the documented 2026-07-10 dedup input-state shift, left standing until the owner-gated panel-refresh re-baselines. Verified vs raw: all 15,833 daily rows × 7 cols match Ken French /100 exactly. Gates: /check-data PASS, data-reviewer PASS, codex PASS; 19 tests pass. Served variant (rolling_factor_alpha_adaptive) untouched. Residual filed (refresh loop never re-fetches ff6). [fund_score 87bdd3a]
  **Owner summary:** The style-factor data downloader is fixed and the analytics layer that was ~11 weeks stale is now current to the source's own publishing frontier (the provider only releases monthly). We confirmed the row-count dip the audit flagged is the expected fund-universe cleanup from an earlier change, not lost data — fund count actually went up.
- [x] (data) Run the price/alpha refresh loop — COMPLETED 2026-07-12 17:42 (run started before this session; completion verified from the store + the regenerated freshness audit): full-universe Tiingo pull (last batch 00744) + fund_daily_adj_close rebuilt (119.4M rows, max 2026-07-10, 2-day calendar lag) + expense history + rolling_alpha rebuilt (max 2026-07-10) + freshness audit regenerated. SHY current through 2026-07-10 → the rf zero-fill latent was not armed. Residual: the factor-alpha leg did NOT advance (ff6 frozen 2026-04-24 — the Ken French fetcher was broken) — **RESOLVED 2026-07-12 by the Ken French factor fix above (87bdd3a): parser repaired, ff6→2026-05-22, factor-alpha rebuilt lag 79→51d, the −6.61% rows FAIL adjudicated as the dedup input-state shift (not a silent drop).** [fund_score: make refresh-current-features]
  **Owner summary:** The big price refresh finished today — fund prices and the core alpha analytics are now current to within two days, site-wide. One downstream layer (style-factor analytics) is still stale because its data downloader has a bug; that fix is now first in the queue.
- [x] (bug) TE bets-table presentation batch from the 2026-07-12 DQ-critic — SHIPPED 2026-07-12 inside the positioning_context cutover flip (fundscore-web branch feat/profile-v2-section-flips): (a) blend-aware "Active vs blend (pp)" header + baseline-composition chip (weights only when the positioning cohort provably covers the full blend — qualifying_weight===1; else names-only from public l2_blend_etfs; DTEYX "IXN 50% + FDN 50%", IENAX "IXC + XOP" — mitigates the vs_benchmark P1 until the panel fix); (b) percentile copy rounded + shared cohortPhrase ("2nd percentile of the 160 funds benchmarked to IWF"; fee ruler imports the same helper); (c) split freshness stamp "returns through 2026-04-24 · built 2026-05-09" (window_end added to the TE proof point + golden assertion). Gates: lint/build/golden ALL PASS, codex high pass 0 P0/P1. [fundscore-web: CurrentPositioning.tsx / BetsTable.tsx / format.ts / gating.ts]
- [x] (bug×3) Serving hygiene trio — X-ray as-of / passive_fund reason / top-holdings recency — SHIPPED 2026-07-12 (fund_score 005e24c, branch fix/serving-hygiene-trio, merged+pushed; LIVE as manifest 34). (#25) X-ray header no longer reads eval-date-as-today: 2,737→0 funds (class-keyed stray filter — dormant funds' honest eval==real-date headers survive; producer fill_null(eval_date) also removed at source w/ per-series date joined pre-truncation, converges next rebuild; FCNTX header 2026-03-31). (#30) 1,717 truly-passive funds (1,661 vo-filed + 56 is_index_fund, 0 leaks) serve active_fee_over_passive_missing_reason='passive_fund' (VOO); condition uses RAW vo style, never identity's ETF fallback — two review rounds killed the broad-flag versions that would have mislabeled 451 then 18 active ETFs (ULTY-class). (#31) 63 funds w/ books older than frontier−548d no longer serve top_holdings (teaser untouched, 0 live-by-snapshot funds lost) + deterministic tie-break makes holdings/fees/xray byte-reproducible. Data-reviewer FAIL→PASS: B2 found 53/63 'dormant' funds are LIVE filers = upstream ingestion gap → filed as new Open item; codex ×3 pass 0 P0/P1. [fund_score 005e24c]
- [x] (data) theme::emerging_markets_exjapan label "EM ex-China" → "EM ex-Japan" — FIXED AT SOURCE + BYTE-IDENTITY MIGRATION 2026-07-11 (fund_score e4c877f, branch fix/em-exjapan-label, merged to main). Catalog name+rationale fixed (basket holds BABA/JD/PDD China + TW/IN/BR/MX/KR, no Japan — old label contradicted constituents); propagated to ALL 17 label-bearing artifacts (13,061 strings: xray 3,372, return_attr 1,853, positioning 2,177, te_decomp 260, divergence 938, staging 3,230 across 1,310 funds, screeners, prose panels incl. nested named_bets). Pinned rebuilds FAILED identity (input-state drift post-dedup → filed as the panel-refresh Open item); shipped as restore+keyed/prose patch (scripts/maintenance/label_fix_em_exjapan_20260711.py, idempotent) with verify 18/18 identical-mod-label. **te-decomposition RENDER spec UNBLOCKED** (gate WARN C2 resolved). Gates: make check 5 PASS/3 pre-existing WARN/0 FAIL; data-reviewer PASS 0-blocking (independent positional identity on 8M rows; 272-context collision audit — zero real ex-China fund names touched; advisories: consider "Broad EM" rename, manifest26 snapshot retains old label); codex ×3 → pass 0 P0/P1 (P1 en route was the PRE-EXISTING the_take citation drift the honest report exposed → new Open item + proven-pre-existing on backups). Postgres reload of the fixed staging pending owner sign-off. Exposed + filed: panel-refresh, the_take drift, tie-break nondeterminism, screener-from-retire-branch. [fund_score e4c877f]
- [x] (story) Fee-fairness peer band on the profile page — FULL STORY SHIPPED 2026-07-11. PRD red-teamed, 5 owner decisions folded in (same-passive-alt cohort, blend-weighted; canonical active_fee_over_passive_bps; FREE; sentence + marker in the existing fee ruler; shared percentile convention via positioning-context-percentiles). BACKEND fund_score ed64397 (merged): FCNTX 56 bps over passive = 36.9th pctile of the 160-fund IWF cohort. WEB fundscore-web branch feat/fee-peer-band-web (lean lane): typed peer_percentile contract in FeeFairnessV2, cohort sentence quoting the ruler's own bps + ordinal marker on the fund's ruler mark, blend phrasing "(weighted across IXN 50% / FDN 50%)" (DTEYX verified), null → nothing (VOO verified); served data live in Postgres manifest 32. Gates: lint/build clean, gating-golden ALL PASS, CODEX_GATE pass 0 P0/P1 (2 P2 advisories disproven against the live payload, documented in the spec). Specs: done/fee-peer-band-backend.md + done/fee-peer-band-web.md
- [x] (bug) build_serving_facts.py clobbers the canonical staging parquet on partial/masked runs — FIXED 2026-07-10 (fund_score 84d0cdf, branch fix/staging-clobber-guard; MERGE DEFERRED until the in-flight positioning-context workflow commits — the canonical checkout holds its uncommitted fact_assembler work). Four fail-closed guards: (1) `--series` serving builds divert to data/_tmp/serving_facts_sample.parquet (verified: canonical mtime untouched); (2) FULL builds refuse to overwrite when any section OR nested contract key collapses populated→0 vs existing staging (`--allow-section-drop` override) — nested trackers gates.holdings_full + holdings.holdings_full catch the manifest-26 shape a section-level count cannot see (codex P1, verified vs real staging 5,628); (3) fact_assembler teaser input FAILS FAST when fund_holdings_full_staging.parquet is missing (codex P2 from 5ddf855); (4) build_fund_holdings_full: empty slice → honest 0-row parquet at the SAMPLE path (verified on dormant S000060195; was a None-aggregate crash, codex P3), and a FULL zero-row build fails closed instead of writing an empty canonical long table (codex P1). Gates: functional verification of all four paths; CODEX_GATE blocked→pass (2 P1s fixed in round 2; 0 P0/P1 final, 2 advisories). [fund_score 84d0cdf]
- [x] (data) Fund-reorg duplicate series / ticker collisions — ROOT-FIXED IN SERVING SCOPE 2026-07-10 (fund_score 8993b29, branch fix/fund-reorg-dedup, MERGED TO MAIN; staging rebuilt, Postgres reload pending owner sign-off). `build_universe` now enforces ONE SERVED ROW PER TICKER via evidence-tiered ownership (tier3 witnessed+alive / tier2 primary-identity+alive / tier1 freshness; aliveness = portfolio-report evidence only via the RAW nport store — never gold holdings [feeder hole: DFLVX], never N-CEN [final-filing corpse: FEIRX], never MFRR [trailing paperwork: DHAMX]; placeholder tickers nulled; as-of point-in-time capped). Staging dup canonical_tickers 14→0; null-canonical 134→8 (honest VA); ALL 16 collision tickers EDGAR-ground-truthed to the live owner; +51 live funds RECOVERED (frozen metadata gate had silently excluded raw-store-only filers); tmap fallthrough recovered 460 inventory statuses (287 missing→available incl. GCSLX factor_alpha). G2 alternatives dedup patch now redundant defense-in-depth (kept). Universe 5,799→5,706. Gates: 9 build rounds; data-reviewer FAIL→PASS→PASS (EDGAR ground-truthing killed 2 rule drafts); CODEX_GATE pass ×3 (2 P1s fixed: membership gate + tmap fallthrough; advisories → residual items); checks 30/30 + positioning + value_offering PASS; tests 18/18. Residuals → 2 new Open items. [fund_score 8993b29]

- [x] (bug) applyGates fact-row path fails OPEN on malformed gate values — FIXED 2026-07-10 (fundscore-web fd5b2c3, stacked on feat/serve-full-holdings-web — same gating.ts as 2bf678d, merges together conflict-free). VERIFIED FIRST: live `gates` JSONB carries only public/free/paid (zero malformed) ⇒ behavior-neutral hardening. Unknown gate value → rank Infinity: section stripped for EVERY tier and HARD-locked (no preview proof point — a malformed gate has no valid tier policy; codex P2). TIER_RANK/GATE_RANK → null-prototype maps: a "toString"/"constructor" gate value no longer resolves via inherited Object properties (function → NaN comparison → silent fail-open; also hardens hasHoldingsFullList + the effectiveHoldingsTier `in` check; codex P2 round 2). gating-golden +7 assertions (malformed anon+pro lock, hard-lock-no-preview, sibling undisturbed, prototype-key cases ×3 — first two test targets hit UNPOPULATED fixture sections and proved nothing before being caught: test the test). Gates: golden ALL PASS, lint 0 errors, build clean; codex ×3 rounds → final pass 0 P0/P1, 0 advisories. [fundscore-web fd5b2c3]
- [x] (data) exposure_xray country-region aggregate exceeds 100% of NAV — ROOT-CAUSED + GATED 2026-07-10 (fund_score 72b9a30, branch fix/xray-country-rollup, UNMERGED; machinery-only, no data changed). **Premise disproven across 4 adversarial data-review rounds**: not an aggregation/look-through bug, not misfiled pctVal. Three classes (29/5,249 served >105%): liability-explained 24 (RYZAX files totLiabs $47.6M vs netAssets $60.0M, EDGAR-verified → holdings lawfully 179% of NET assets, pctVal's filed basis; cause not itemized in filing), SERVED-ABOVE-FILED 4+26 (holdings_complete drops filed negative legs — collar funds served above filed net; JAIGX filed −178%, served +94%), feeder master-basis (DFCSX/ECGIX). New gating check `check_exposure_xray_country_coherence.py` (registered): [A] panel≤input, [G1] gold≤raw long book +2pp (double-count tripwire), [G2] served gross-over-net ≤ filed liabilities +3pp (blocks served S000089010-class corruption regardless of which rows gold kept — simulated → G2 FAIL); WARN-lists honest cohorts + ~75 filed-incoherent balance sheets (EDGU) + gold-vs-raw drift. Review history is itself doctrine: round 1 killed a TAUTOLOGICAL reconciliation (pctVal==valUSD/net row-wise ⇒ any subset "reconciles"); round 2 killed a `continue` that diverted 75 funds around the gate; round 3 killed a hardcoded entity claim the check didn't compute (reviewer's own mislabel, copied unverified). Gates: data-reviewer PASS (round 4, reproduced 0-FAIL vs raw N-PORT + EDGAR); CODEX_GATE pass (0 P0/P1, 0 advisories). Residuals → 2 new Open items (vs_benchmark zero-ref; 4-decision owner story). [fund_score 72b9a30]
- [x] (story) Look-through positioning-changes panel for ETF-wrapper funds — SHIPPED + LIVE 2026-07-10 (fund_score fd982cd, MERGED to main + pushed origin; loaded to serving DB as manifest 30 active — 5,799 facts / 2,254 positioning sections / 51 partial-coverage flagged, holdings 1,398,936 + attribution 1,963 unregressed; local 127.0.0.1:54322, no prod URL on this box). Windowed as-of look-through resolves wrapper/ETF sleeves into underlying holdings; positioning_changes now computed on the looked-through basis so wrapper-heavy funds show real sector/theme shifts instead of opaque ETF lines. New: product/lookthrough_window.py (windowed frame + pre-selection coherence guard — drops filings Σ pct_nav>1.5 BEFORE endpoint pick, so a corrupt latest filing can't strand a fund), build_holdings_lookthrough_window.py, build_positioning_changes_panel.py --direct-book-baseline (fair same-eval control), check_positioning_changes_panel.py Check 8 (served→missing vs the fair baseline, honest/recoverable split, fail-closed on any recoverable downgrade). Results (eval 2026-04-30 vs fair same-eval direct-book baseline): net +89 available (4,107→4,196), **0 recoverable downgrades** — the 16 downgrades all honest (10 dormant/departed, 5 corrupt-filing raw N-PORT Σ pctVal 160–329%, 1 insufficient-history); THEQ Technology 29.7% looked-through vs 3.0% raw; X-Ray coherence 3,409/3,412 shared-basis funds within 1pp (99.9%); 4,859 full / 203 partial look-through, 118 wrappers fixed. Gates: data-reviewer re-review PASS (0 blocking); FINAL DATA GATE (served==gold + /check-data) PASS (7 PASS / 1 benign WARN / 0 FAIL); codex --high PASS (0 P0/P1, 2 P2 advisories → fast-follow: non-twin-map ETF wrappers unresolved-by-LEI not flagged honest-missing [lookthrough_window.py:223, e.g. VOO/IVV/VEA]; partial-coverage metadata dropped for funds with no surfaced shifts [fact_assembler.py:837]). Reviewed-lane workflow crashed at finalize (a workflow subagent can't await the long codex bg run → StructuredOutput timeout, no commit made); main loop owned the codex gate + hand-finalized. Spec → done/. [fund_score fd982cd]
- [x] (story) Serve the full holdings list per fund — COMPLETE END-TO-END 2026-07-10 (web: fundscore-web `feat/serve-full-holdings-web` 2bf678d, UNMERGED overnight-run branch — merge to main to go live on /preview; backend: fund_score 5ddf855 → main 30e8c75, live via manifest 28, 5,708/5,799 funds / 1,398,936 as-filed rows, 91 misses honest EDGAR-adjudicated). WEB SHIPPED: lazy server-action fetch with entitlement resolved from the REAL session inside the action (forged action POST → 0 rows, proven on a production build; preview `?tier=` override env-gated to non-prod); teaser/entitlement/rows all key off `gates.holdings_full`, FAIL CLOSED on malformed values, teaser⇔gate⇔rows 0 mismatches across the universe; drawer renders as-filed incl. negatives (JAIGX −309.17%, sign-aware −$2.4B; dust floors display-only, rows never dropped); 26,433-row worst case DOM-capped; FCNTX paid drawer 428 rows @2026-03-31 META 10.29% SpaceX 5 lines, anon locked teaser count+as-of. Gates: build+lint+gating-golden PASS (new suite: tier matrix, forged-override, malformed-gate); codex --uncommitted ×4 rounds → pass 0 P0/P1 (fixed en route: negative-weight display P2, anon-teaser P2, action-tier-trust P1, fail-open-gate P1). Residual advisories → Open item (applyGates sibling) + spec Implementation Result. Spec serve-full-holdings-web → done/. [fundscore-web 2bf678d + fund_score 30e8c75]
- [x] (bug) Pipeline admin console pre-commit fixes [codex P1/P2] — SHIPPED 2026-07-09 (fundscore-web 1c73e53, branch feat/pipeline-admin-console): committed the untracked admin-console WIP (src/app/admin/pipeline/ + src/lib/pipeline/admin.ts) with both gate findings fixed. [P1] env-only `adminEnabled()` → `adminConfigured()` (env flag AND secret `PIPELINE_ADMIN_TOKEN`) + `isAdminAuthenticated()` token-cookie session (constant-time compare, httpOnly, path-scoped set+clear); every mutation awaits `assertAdminAuthenticated()`, every page/sub-page gates on it, + sign-in/sign-out. [P2] `moveSpec` now runs `reconcileBacklogForSpecMove()` — rewrites the `specs/<status>/<file>` pointer, flips the checkbox, relocates the item (done→Done [x] / queue→Specced [~] / rejected→Open [ ]); pointer always preserved so the lifecycle round-trips (Reopen re-finds it), no-op for non-story specs. Codex --high surfaced 7 more P2s across 6 rounds, ALL fixed: cookie-delete path (sign-out was ineffective), specced-item Close/triage/spec buttons (backlog↔queue desync), spawn-handler-before-await (bad CODEX_BIN async-ENOENT crash), running-vs-terminal job-write race → serialized + atomic temp+rename, saveSpec stale-form resurrection, backlog-edit stale-hash 404 → dashboard redirect, multiline backlog-edit guard. Gates: build+lint pass; final codex --high sign-off CLEAN (0 P0/P1, 0 discrete findings). Owner: set `PIPELINE_ADMIN_TOKEN` in `.env.local` + sign in to use the console. [fundscore-web 1c73e53]
- [x] (bug) apply_serving_schema fresh-DB + upgrade robustness [codex P1/P2] — FIXED 2026-07-09 (fund_score 2e8fe61, MERGED TO MAIN 2026-07-09 — main @ 3d5622d, local only, not pushed). Defined the missing `asset_class_code` enum (EQ/FI/MU/MA/ALT/RE/OT — mirrors the web enum; the column stores taxonomy codes, not legacy long labels) and replaced the hand-maintained partial ADD COLUMN list with generation from `fact_assembler.SECTION_COLUMNS`, so every assembler section gets `ADD COLUMN IF NOT EXISTS` on an upgraded DB. Data-neutral DDL — no gold/product data touched, no rebuild. Unblocked the 2026-07-09 admin-run patch from stash@{0} via an isolated worktree (first use of `new-worktree.sh` + the branch/worktree doctrine; stash dropped after verifying full containment in the commit). Gates: 3 regression tests pass (every referenced enum defined; every SECTION_COLUMNS entry guarded); data-reviewer PASS (admin run, patch unchanged since); CODEX_GATE pass (high, 0 P0/P1, 2 advisories — the machine-local worktree data symlink correctly left uncommitted). [fund_score 2e8fe61]
- [x] (bug) implement-backend-spec pipeline: finalize can skip the mandatory codex gate + a killed finalize reports status:done — FIXED 2026-07-09 (fundscore-web a21897f, chore/pipeline-streamline). The workflow now FAILS CLOSED: finalize must return `codex_gate: pass` + `commit_sha` + `spec_moved_to_done` (all FINALIZE_SCHEMA-required) or the run returns `stopped` — never `done`; a null finalize result (session-limit death) also returns `stopped`; the gate protocol lives in the finalize prompt AND backend-implementer.md (gate BEFORE commit, max 2 rounds, an unrunnable gate is blocked — never a pass). Same batch also collapsed the verification tail (revision rounds 2→1; checkpoint-3 + /check-data folded into ONE final data gate). Machinery change per /review-prompts conventions. Gates: node --check on all 5 workflows; codex high review of the full diff — 0 P0/P1 in these files (1 P2 fixed pre-commit: spec_moved_to_done required; the unrelated admin-console [P1] filed as its own Open item). [fundscore-web a21897f]
- [x] (data) Misfiled out-of-range pct_nav coherence guard — FIXED 2026-07-08 (fund_score `fix/pct-nav-coherence-guard` f7ec601). Split from the FOF/ETF-wrapper item: `build_holdings_snapshots.py` already filtered valUSD>0 but carried filed N-PORT pctVal/100 into `pct_nav` blindly, so filings pairing a real positive valUSD with an internally-incoherent/misfiled pctVal corrupted the %-of-NAV display panels. Guard nulls pct_nav outside [0,1.5] at source (weight/valUSD untouched — honest-missing, never a synthetic fill): **272 rows** (269 negative incl. Enerpac S000001631 $19.0M @ pctVal −10212.53%, S000072700 whole-filing NAV-denom corruption 233%/231%/225%; +3 lines >150% of NAV). serving `_top_holdings_by_series` drops null-weight rows so a quarantined holding can't rank as a top holding. 1.5 ceiling keeps legit single-ETF-wrapper funds (~100-103% of NAV). Rebuilt holdings_snapshots + positioning_changes_panel (all invariants OVERALL PASS; +15 concentration rows correctly surface as removing corrupt cross-sectional outliers de-inflates change_z sd, 0 lost; 1 real change on dormant/unserved S000030654) + serving STAGING (no Postgres push). exposure_xray unaffected (reads already-clean holdings_complete). Gates: /check-data positioning OVERALL PASS (5/5 fund spot-checks to 0.00pp; z-tail WARN improved 11→10); **data-reviewer PASS** (272 nulls all trace to internally-incoherent raw N-PORT; served==gold==raw exact, FCNTX META 10.2875%; no synthetic data); **CODEX_GATE pass** (--high, 0 P0/P1, 1 advisory P2 = downstream null-vs-zero robustness, verified zero served impact). Residuals filed in data_gap_analysis.md: port the guard to holdings_complete (sibling, currently clean); exposure_xray RYZAX/COHOX country aggregate >100% (pre-existing); positioning null→0 hardening. The look-through incoherence → (story) in Open (owner: build a full-history looked-through positioning panel). Pre-existing unrelated test failure: test_risk_betas_eval_is_factor_date_not_holdings (stale risk_attribution date assertion, fails on pre-fix backup too). [fund_score f7ec601]
- [x] (data) Cross-panel holdings-weight incoherence at the same as-of — FIXED 2026-07-08 (fund_score main 32db3f6, pushed). Root cause: **three** denominators, not two — every displayed holding weight differed by an exact per-fund scalar because panels read different tables: filed pctVal ÷ net assets (fund_holdings_full drawer) / EC-equity-sleeve ÷ Σ EC valUSD renormalised (exposure_xray ← holdings_complete) / US-ticker-equity renormalised (positioning + top_holdings ← holdings_snapshots). FCNTX META was 10.29% / 10.89% / 11.75% across surfaces. **Owner decision: standardize everything on the SEC-filed pctVal (% of overall fund NAV)** — evidence-forward and the cleanest basis (NAV is a fund constant so any panel summing the same filed pctVal agrees; no renormalization games). Carried a decimal `pct_nav` (= filed pctVal/100) through holdings_complete (look-through multiplicative, not renormalised) + holdings_snapshots + the passive blend (`passive_pct_nav`); exposure_xray/positioning/top_holdings now display pct_nav, `classified`/coverage kept on the EC `weight` as a basis-invariant ratio, internal replication `weight` left byte-identical. Result: exposure_xray == positioning == drawer == top_holdings to ~1e-15 (FCNTX Technology 23.85%, META 10.29% everywhere); low-equity funds (frozen Russia funds) now honestly read ~0% instead of a renormalised 100%. Gates: affected test suites green; positioning /check-data PASS (5/5 spot checks); **data-reviewer PASS** (11 raw-N-PORT spot checks exact, weight byte-identical 1.6e-15, 95.9% cross-panel coherent, no fabrication); **CODEX_GATE pass** (--high, 0 P0/P1, 3 advisories). Gold + serving STAGING rebuilt; **Postgres push still pending owner sign-off** (staging only). Residual FOF-wrapper look-through incoherence + misfiled-pctVal guard filed above + in data_gap_analysis.md. [fund_score main 32db3f6]
- [x] (data) Pricing/derived-alpha staleness flagged by the freshness monitor — ROOT-CAUSED + auditor calibrated 2026-07-07 (fund_score 0d912b5). Diagnosis (every source spot-checked): the price/alpha WARNs are an **operational refresh-loop cadence gap, not a data defect** — `gold.fund_daily_adj_close` (2026-05-08) faithfully reflects its Tiingo source, which itself caps at 2026-05-08 for the full universe (only a 40-ticker smoke `pricing_refresh_*` runs to 06-09; last full pull ~2026-05-18); `rolling_alpha` (2026-04-24) is bounded by the price panel; `rolling_factor_alpha` (2026-01-30) is hard-capped by the Ken French factor source (`ff6_weekly` inner-join, currently 2026-04-24 — publication lag). Recovering fresh prices needs a **live Tiingo re-fetch** → filed as the operational residual above + data_gap_analysis.md (NOT force-recovered, per "spot-check the source before recovering"). MFRR "Q1-vs-Q2" WARNs VERIFIED normal filing lag — **live SEC fetch confirmed 2026Q1 is the latest published bundle, no 2026Q2** (6 days after quarter close), so 2026Q1 is the freshest that exists. Auditor FIX (`audit_data_freshness.py`): added `SEC_QUARTERLY_PUBLICATION_LAG_DAYS=45` grace via `_expected_latest_quarter()` — cleared 8 false-positive WARNs (17→9) while keeping every genuine one (price/alpha, nport, + the 4 legacy-path mfrr outputs genuinely stuck at 2025Q4, filed above). BONUS (owner ask, same session): codified the canonical SEC fair-access User-Agent (`FundScore.ai alex.h.frey@gmail.com`, env-overridable) — repointed mfrr/download.py, reference/config.py, backfill_tickers.py, build_fund_succession.py (were support@/example.com/research@fundscore.com/no-contact) to `fundscore.sec.SEC_HEADERS`, documented in docs/agent_context_map.md; + pricing/alpha map & freshness-lag reference in pipeline_status.md. Gates: 11 freshness + 11 mfrr tests pass; **data-reviewer PASS** (7/7 root-cause claims verified vs raw sources); **CODEX_GATE pass** (--high, 0 findings, isolated `--commit`). Scoped commit avoided a concurrent session's exposure_path edits in the shared checkout. [fund_score 0d912b5]
- [x] (bug) L2 solver screened candidate ETFs on ABSOLUTE correlation — FIXED 2026-07-06 (fund_score 54e1b17, branch fix/passive-solver-sign-guard). Root cause: the correlation screen ranked by |corr| and always_include benchmark ETFs (get_benchmark_etfs → always_include in L2 routing) bypassed the screen entirely; because the blend is long-only (w>=0, sum=1) a negatively-correlated ETF can't beat a good match, but a fund with NO good positive candidate was still served its least-bad anti-correlated ETF at w=1 (100% AGG at enormous TE — the serve-l2 inverse-ETF failure class), and an anti-correlated always_include ETF could enter the solver as a beta-trimmer. Fix: require positive fund-vs-ETF correlation in the screen AND apply the same rule to always_include → such funds now return an honest null. Inverse/bear FUNDS are unaffected (they match an inverse ETF via POSITIVE corr, e.g. bear ~ +SH). Owner de-scoped the long+anti-correlated-diversifier corner case (codex --high P2 "preserve negatively-correlated blend components") — the screen-level guard structurally excludes it by design. Also fixed stale "prefix"→"exhaustive combination" docs (top-of-file comment, __init__, dead runner preset metadata, README). Gates: 4 new unit tests (proven to FAIL against pre-fix code — it served tickers_chosen=['AGG']) + 8 real-data integration tests green; CODEX_GATE pass ×2 (medium: fixed the always_include P2 it found; --high: pass, 2 advisory P2s both = "not supporting the de-scoped diversifier case"). [fund_score passive_match]
- [x] (story) Named portfolio managers on the profile — fund-named-manager-source + fund-named-manager-ui SHIPPED end-to-end 2026-07-05/06. Backend: consumed the codex-signed r4 extraction build (10,845 rows / 3,666 series), ran the FULL-universe web-verify lane (4,035 series, ~$50: 76 promoted / 736 demoted; 2 lane defects fixed en route — 'CFA(R)' trademark poisoning name-match + the **dated-EDGAR evidence guard** after hand-adjudicating the demotion mass: qwen:online cited 2011-2025 EDGAR docs as "current" in 143/281 sec.gov demotions), wired canonical `manager_people_v1.0` panels (real rows ONLY for post-web-verify confident series; honest placeholders elsewhere), served `managers[]`/`manager_transition`/`manager_as_of` (adviser-firm synthesis REMOVED). **Coverage: 2,778/4,035 (68.9%) auto-served; 1,257 review-quarantined with evidence (promotion queue, not a data gap)**. FCNTX serves Danoff-since-1990 + 2025 co-PMs + filed retirement 2026-12-31 (verbatim evidence). Frontend: PM roster + tenure ("since YYYY", year-precision honored) + amber successor-less transition flag in Selection Evidence, honest firm-only state for empty rosters, anon leak-checked. Gates: data-reviewer checkpoints PASS (wire + serving full-universe served==gold; caught the staging-clobber provenance issue → immutable serving_facts_manifest26.parquet), /check-data PASS (3 WARNs filed: cross-registrant change disclosures e.g. GFOA Blair Frank, AGTHX F-2 ticker keying, placeholder provenance flattening), codex initial 4 findings fixed (d5d83f3) → **SIGN-OFF yes, 0 findings**. Survived a session-limit kill (workflow journal resume) and a two-session serving clobber war (manifests 26/27) — resolved by merging BOTH branches to fund_score main (7c1c0f7 manager + 30e8c75 holdings, pushed) and reloading combined **manifest 28** (2,778 rosters + 1.4M holdings rows, one transaction, holdings check 35/35). (fund_score main 30e8c75; web 9077fdf + 0d06f2b)
- [x] (data) Skill evidence on the beta-adjusted basis — the served Bayesian skill read (p_skill, alpha_ir → `manager_parent.skill_evidence`) is recomputed on the SAME beta-adjusted gross-vs-gross basis as value_score (fund − β·L2; β = full-sample weekly covariance beta; own time-varying ER add-back; blend grossed by clip(β,0,2)·passive_fee via the identical fee_efficiency lookup) — **hierarchical_skill_v2**, served with explicit `basis`/`beta`/`ir_is_gross` fields (schema-derived so legacy panels keep honest v1 labels). **FCNTX: alpha_ir −0.124 → +0.223, p_skill 4.2% → 11.5%, β 0.9032 (value_score 0.9044; universe β-corr 0.9997)** — the contradiction is resolved (small positive edge, statistically modest — one coherent story with the +10bps value score). Externally validated: PRWCX p 0.78 (Giroux 17-yr streak), FMAGX p_neg 0.55 (post-Lynch). **3 defects found & fixed at source along the way**: (1) Storey pi0=1.0 → degenerate mixture weights → 8000/8000 divergences (capped 0.99, finite-gradient verified); (2) **manufactured skill IRs up to 10.9** for ~910 funds whose L2 blend was mis-assigned (option-income ETFs in FI groups, β −60..+20) or a trace exposure (cash-like funds, β≈0.07 → IR = own Sharpe) → basis-validity gate β≥0.25 ∧ corr≥0.5 (`ba_corr` column added; honest exclusion; only 2 were served — the serve-l2 lesson applied); (3) **panel nondeterminism** (unordered ticker→series unique + untied share-class dedup: 878 ticker flips / 2,322 winning-class flips run-to-run) → deterministic N-PORT-priority resolution with scorable-join-before-dedup (byte-reproducible panel; junk series 14→1; +68 valid fallback series recovered). Final: 8,150 funds/147 groups, fit 0 div / r_hat 1.01 / ESS 678; staging **5,799 rows, unrelated sections byte-identical**, served skill 3,276→3,273 (4 honest gate misses); fee_fairness relabels 190 series (perf-fee leg re-priced by new p_skill, by design). Gold + serving staging (as-of pinned 2026-06-29); **LIVE in Postgres since 2026-07-05/06 (manifests 26-28, merged main 30e8c75)**; the P(skill) headline restore in the redesigned profile UI rides on the in-flight profile-simple work (payload is ready). Gates: 155 tests, /check-data PASS, **data-reviewer PASS ×2** (independent byte-exact reproduction from source), **codex CODEX_GATE pass** (final run 0 findings; 3 P2s fixed across rounds). Residuals filed (sigma-basis convention, FI low-β tier floor + comparability-parity owner decision, passive-fee coverage beyond EQ, tiingo dup captures, CGHAX mapping conflict, Fidelity NAV depth) in fund_score docs/status/data_gap_analysis.md. (fund_score 825e1cd, on branch feat/fund-family-panel)
- [x] (data) NPORT ingest refresh — ingested SEC N-PORT period-ends 2025-11-30 → 2026-04-30 (Q4-2025=2025-12-31 6,301 series; Q1-2026=2026-03-31 6,350 series; **27,449/27,472 filings ok**, 0 SEC 429/403) and rebuilt every holdings-derived gold panel + serving **STAGING**. Freshens the profile: **FCNTX holdings 2025-09-30 → 2026-03-31**, FBGRX/VDIGX → 2026-04-30; holdings_snapshots/complete/exposure_path/asset_alloc → 2026-05-31, positioning/return_attribution/manager → 2026-04-30. **Fixed 4 defects the refresh exposed**: (1) holdings builders pinned `year=2025` → **recent-2yr glob** (new `src/fundscore/nport/paths.py`) so year=2026-partition filings no longer freeze at a stale 2025 period (+75 fresh funds, 52 dormant excluded; holdings_complete pull 22min→27s); (2) exposure_path `HOLDINGS_WINDOW` hardcoded "2019-09..2025-11" stamp → **dynamic `holdings_window_label()`** (now 2020-06..2026-05) + `infer_schema_length=None` schema-crash fix; (3) **valUSD double-count** [data-reviewer P1] — original+NPORT-P/A amendment summed → **2× for 41 served funds** (URNM CCJ $882.9M→$441.5M); dedup to canonical lexmax `acc_no` per (series,period); (4) **within-filing duplicate rows** [codex P1] — byte-identical dup rows (Lassonde) summed → `DISTINCT`/`.unique()` (~49 funds/$57M excess) + bare-glob normalize [codex P2]. Weight byte-identical across 564k rows (immune — renormalises per series). Gates: 28+ tests, positioning **20/20**, return_attribution **14/14**, exposure_path **11/11** + check-data PASS, **data-reviewer PASS ×2** (served==gold==raw provenance; 0 residual double-count across all 62 multi-filing series), **codex CODEX_GATE pass** (fixed 1 P1 + 1 P2, re-ran clean). **LIVE in Postgres since 2026-07-05/06** (manifests 26-28; branch merged to main 30e8c75). 3 pre-existing residuals filed above. (fund_score 84cd770)
- [x] (data) Fee-correction ripple sweep — FCNTX's fee (net 74 bps, 2026Q1) is a **REAL performance-adjusted management fee**, not a bug: SEC-verified 0.39→0.63→0.74 across three 485BPOS/497K prospectuses (owner flagged the 39→74 rise as suspicious; confirmed the ±0.20%-vs-S&P-500 mechanic verbatim in the filings; all-inclusive net=gross, mgmt≈total ±0.01% "other"). The correction had landed in `fund_metadata` but two consumers still priced older vintages. **(2) value_score** applied the 75-day filing lag even to the CURRENT served snapshot → used 2025Q1 (63) at as_of 2026-05-09; gated the lag on `--as-of` so the served build uses the latest-filed fee (matches the page / `fund_metadata`) while historical vintages keep the lag (no look-ahead). Fee-only isolation (identical data): 435 fees move (189↑/246↓) → 43 coarsened scores (30↓/13↑); **0 residual staleness** (2076/2076 scored named-class == latest-filed, both directions). Backlog guess was wrong: FCNTX served value_bps **10→10 / score 50→50** (raw vb 13.4→5.9, both round to nearest-10) — not "≈0/negative". **(3) alternatives** was a stale Jun-23 snapshot on 2024Q1 fees (self=39); rebuild → current fees. Backlog "$7 should be ~$42" was wrong: FCNKX's OWN fee also rose 32→67 (both share classes carry the same perf adjustment) so the $7 saving is coincidentally correct — but the displayed K-class ER (32→67) was stale and cross-wrapper savings understated (SFY $34→$69, MGC $32.5→$69), now fixed. Rebuild EXPOSED a pre-existing G2 grain violation from fund-reorg ticker collisions → grain-dedup in `alternatives.finalize` (filed as a follow-up for the upstream root). **(4) fee_fairness** labels **UNCHANGED** universe-wide (null 3177 / Weak 1435 / Mixed 724 / Strong 463 identical before/after); FCNTX "Mixed"@2025-12-19 is a DELIBERATE holdings-lag (`diversification_missing` while Q1-2026 NPORT awaits ingest — resolves via the NPORT item), not stale in this item's sense. **(1) Postgres** the fee-74 push was prior; the NEW value_score + alternatives are in gold + serving **STAGING only** → a **sign-off-gated Postgres push is still required** for the live page (couldn't re-verify the live DB — `psql` blocked by permissions). Rebuilt chain: value_score → alternatives → serving staging (`--staging-only`) → screener_base → canonical queries (19 ER updates). Gates: **57 value_score+alternatives tests** (+2 regressions: lag gating, reorg dedup), **/check-data value_score PASS** (0 nulls, basis receipt 100% within 10bps), **21 alternatives invariants PASS**, **data-reviewer PASS** (6/6 claims — FCNTX served==gold==raw==74; 3 non-blocking advisories), **codex CODEX_GATE pass** (0 P0/P1, 0 advisories). Residual filed (fund-reorg upstream dedup + Postgres push). (fund_score a87ee0c)
- [x] (data) Residual gross<net (C1) 68→44 + serving coherence quarantine + recency-scoped served universe. **AS-OF commensurability** (`load_expenses`): the surfaced gross is now sourced AT net's filing quarter (`net_q`, the same pin `load_fees` uses for management_fee) instead of the primary class's latest gross quarter — removed the ~23 stale-gross-vs-fresher-net artifacts (+1 coherent same-quarter recovery); a within-filing fallback derives `gross=net−reimbursed` when a filing reports net+waiver but omits the explicit total (BWTG, the SEC fee identity — can't make a C1). New **44 ⊂ 68** (zero new C1 per-series), net & management_fee byte-identical, gross fill 16,215→15,851; ratchet re-pinned **C1≤44**; bootstrap builds keep the latest-quarter gross fallback. **Within-filing residual** (the 44 are bad SEC filings, e.g. WCPIX total 0.07% vs net 1.75%, unrecoverable) is **quarantined at serving** (`fact_assembler._fees` nulls a displayed gross<net — missing>incoherent) while gold keeps the honest residual so the gate still catches regressions. **is_defunct / Castlemaine**: the served-only-scoping leak is fixed by an **18-month recency gate in serving scope** (`source_inventory.build_universe` → `universe.recently_active_filter`, factored out of `active_retail_filter` + forwarded the build as_of), NOT by widening `is_defunct` — keeping it a permanent confirmed-deregistration fact so point-in-time backtest universes stay survivorship-correct (codex P2). Served EQ **8,622→5,799**, Castlemaine excluded (0 served), `is_defunct` unchanged at 390. Gold + serving **STAGING** rebuilt; no Postgres push. Gates: **129 tests** (+recency/quarantine/stale-exclusion units), /check-data PASS (aggregate + external SEC spot-checks), **data-reviewer PASS ×2** (no new C1 per-series, net/mgmt byte-identical, no synthetic data, is_defunct unchanged), **codex CODEX_GATE pass** (survivorship + build-date + bootstrap-fallback P2s all fixed). (fund_score f213b11)
- [x] (data) MFRR gross/total ÷100 ROOT CAUSE — it was OUR `_normalize_ratio`, not the SEC source. It ÷100'd any pure-uom expense value ≥0.10 as a presumed mis-scaled percent, but tiny micro funds legitimately have gross/waiver >10% of NAV before a large adviser waiver (net stays <10%). Proven via raw num.tsv + inline-XBRL scale + SEC fee table: MDP Low Volatility (MDPMX) raw gross 0.2786 / waiver 0.2604 / net 0.0182 (coherent fractions; SEC = 27.86% gross) was ÷100'd → fabricated gross<net; Royce Global Value is a genuine 85.14% gross (SEC-confirmed), not the "degenerate ~2.3%" earlier assumed. Fix: `extract_expense_ratios` is filing-aware — per (adsh,series,class,document) it keeps a value in [0.10,1.0] when NET is a plausible fee (<10%) AND the within-filing identity reconciles at the raw scale (`gross == net+|waiver|`); guards: require the waiver identity (not just gross≥net — rejects mixed-scale percent-total filings), `nulls_equal` join (class-null single-class filings), document-scoping (no cross-document leak). PIMCO-style uniform mis-scalers (net ≥10%) still ÷100. Full-universe: **C1 194→68, C2-corruption 10→6**, ~5.8k high-fraction groups across ~1.1k series corrected at source; net leg unchanged (max 0.0986); **zero new C1 by construction**; the gold-layer `_recover_divide_by_100_scaling` now fires 0 keys (subsumed, kept as backstop). Ratchet re-pinned 68/6. Residual (68) filed as a follow-up. Gates: 11 mfrr-transform tests (+ gross/coherence/expense suites), data-reviewer PASS (kept grosses verified gold==raw==SEC; no net inflation; no new C1), codex CODEX_GATE pass (0 P0/P1). (fund_score b82dfee)
- [x] (data) MFRR gross/total ÷100 scaling recovery — a cohort of SEC filers tags the gross/total ER 100× too small in the XBRL `pure` field while net in the SAME filing is correct (e.g. Water Island L/S ATQCX 2018Q4: stored total 0.001334 / net 0.0317; real gross 13.34%). `_recover_divide_by_100_scaling` (build_gold_metadata.py, before `_select_primary_gross`) ×100's the gross/total leg ONLY when proven against the filing's own fee identity — gross-only (`gross×100 == net+|face waiver|`) or both-×100 (`gross×100 == net+|waiver|×100`) confirmed by a sibling-quarter coherent gross — never a synthetic fill. Three anti-fabrication guards (two caught by data-reviewer, one by codex): the degenerate symmetric both-×100 identity is never a proof alone (would inflate Royce Global Value to ~85%); `geff` = MAX across gross AND total concepts (a garbage tiny gross beside a real total isn't ×100'd past it — CIPSX); only the rows achieving the proven `geff` are scaled. Un-provable defects stay in the honest C1 residual (WCPIX 0.0007 not inflated to 7%). Full-universe: C1 242→194 (48 surfaced recovered; recovered values reconcile to ~machine epsilon and match SEC fee tables; max 17.5%), C2-corruption 10→10, gross fill 16,215 unchanged, ZERO new C1 per-series (194 ⊂ 242); ratchet re-pinned 194/10. Residual filed as a follow-up (132 large-gap corrupt grosses + serving quarantine + is_defunct). Gates: 19 gross-selection tests (+ fee/expense suites), /check-data PASS, data-reviewer PASS (3 rounds, external SEC spot-checks), codex CODEX_GATE pass. (fund_score c3ad4d7)
- [x] (bug) MFRR mixed-schema fragility [codex P1] — both MFRR expense readers required every parquet to share the `document` column, so a partial re-extract / pre-`document` restore would throw a SchemaError before class recovery (`backfill_class_from_document`) ran. Root cause: `pl.read_parquet(files)` pins the schema to the first sorted file (later document-bearing quarter → "extra column"), and `_read_parquets`' `vertical_relaxed` requires identical column sets ("schema lengths differ"). Fix: read per-file and concat `diagonal_relaxed` in both — unions columns (null-fills absentees), coerces dtypes, no-op in the happy path. Added a regression test that reproduces the exact SchemaError (document-less quarter sorts first) and confirms recovery still runs. Gates: 11 expense + 44 fee/MFRR tests pass, codex CODEX_GATE pass (0 P0/P1, 0 advisories). (fund_score a834553)
- [x] (data) Gross-expense class-recovery asymmetry [codex P2] — hardened the gross leg of the coherence triple to match net/mgmt: backfill class from `document`, drop negatives, and take MAX (largest actually-filed; never an interpolated median) of conflicting rows per class/quarter — preserving genuine zero-fee `gross=net=0` (Fidelity ZERO). Naive backfill alone regressed coherence; root-caused the spurious filed `0.0` + non-deterministic `.first()`. Full-universe C1 280→242, C2-corruption 12→10 (0 new per-series), net/mgmt unchanged; ratchet re-pinned 242/10. Surfaced + filed a pre-existing MFRR ÷100 gross-scaling source defect (~205 series). Gates: data-reviewer PASS, codex CODEX_GATE pass; 23 tests. (fund_score a03f95f)
- [x] (data) Canonical fee-over-passive + coherent net/gross/management triple + build-failing gate; root-caused FCNTX staleness → real 0.74%. (fund_score cc180d4, spec → done)
