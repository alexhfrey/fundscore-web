# Pipeline run summary — 2026-07-09 19:00 → 2026-07-11 08:50

One continuous loop across ~38 hours (4 session-limit resets survived). Every shipped item
passed its full gate chain: adversarial data-reviewer where data changed, codex (OpenAI)
cross-vendor review on every diff, build/lint/tests, and honest coverage reporting.

## Shipped — 9 items, all merged

| # | Item | Commit | What it did |
|---|------|--------|-------------|
| 1 | Full-holdings drawer + paid gate (`serve-full-holdings-web`) | web `2bf678d` | Lazy server-action fetch, entitlement resolved server-side (a forged POST gets 0 rows — proven on a production build); FCNTX 428 rows verified live. Codex ×4 rounds caught a paid-data exfiltration P1 en route. |
| 2 | `applyGates` fail-closed hardening | web `fd5b2c3` | Unknown gate values strip for every tier (was: served PUBLIC); null-prototype rank maps kill the `"toString"` fail-open. |
| 3 | X-ray country >100% root-cause + gates (B1) | fund_score `72b9a30` | Premise disproven: RYZAX legitimately files holdings at 179% of net assets ($47.6M liabilities). New served-basis coherence gates [A]/[G1]/[G2]; 4 data-review rounds killed two of my own check designs (tautological metric; gate-diverting control flow). |
| 4 | Fund-reorg ticker dedup (B2) | fund_score `8993b29` | One served row per ticker via evidence-tiered ownership. Duplicate tickers 14→0; all 16 collisions EDGAR-adjudicated to the live owner; **+51 live funds recovered**; 460 source-inventory statuses improved. Exposed that metadata filing-dates + class-ticker mappings are **frozen at 2024-12-31**. |
| 5 | Staging clobber guard (item 25) | fund_score `84d0cdf` | Partial/masked builds can never overwrite canonical staging: 4 fail-closed guards incl. nested-gate collapse detection (the manifest-26 incident class). |
| 6 | Positioning-context percentiles | fund_score `5a0268c` | Beta/TE percentiles vs shared-passive-alt cohorts; ships the **shared `cohort_percentiles` utility** (your blend-weighted rule, N_MIN=20). 100% of scored funds covered. The workflow's own guards caught its implementer doing an unpinned rebuild + an unauthorized DB load. |
| 7 | TE decomposition by bet | fund_score `ee72d15` | Factor sleeve + stock-selection sleeve per fund, Fable implementer, all checkpoints pass. FCNTX's top factor source is now Financial Services under the standardized basis (mock copy is stale by design). |
| 8 | Fee-over-passive peer percentile (A2 backend) | fund_score `ed64397` | Your five answers, end-to-end: PRD → red-team → spec → shipped. **FCNTX flipped**: 56 bps over passive = 36.9th percentile of the 160-fund IWF cohort (cheaper than ~63% — the old "71% more expensive" example predated the fee fix). |
| 9 | NAV 2025-04-30 cutoff root-caused (B6) | investigation | Not an ingestion bug: 284/317 frozen tickers are honestly-dead merged funds (Putnam→Franklin etc.), 29 are retired-class-ticker basis losses, only **2 true vendor gaps** (JMVNX, JCAVX). Precise fix re-filed: current-ticker-basis NAV rebuild after the Tiingo refresh. |

## Also done
- **A2 story fully resolved**: PRD written, adversarially red-teamed, your 5 decisions folded in,
  two specs queued — backend now shipped (#8), `fee-peer-band-web` (lean) unblocked.
- **4 memory lessons** saved (non-degenerate verification metrics; workflow-finalize codex
  limitation; te-decomp basis coherence; plus the night's process lessons).
- **6 new backlog items** filed from findings: evidence-producer refresh (the frozen feeds),
  metadata identity hygiene, EM-ex-China label defect (**blocks the te-decomp render spec**),
  vs_benchmark zero-vs-null suspicion, X-ray >100%/book-basis owner decisions, fee-panel gaps.

## Still running
- **Tiingo full-universe refresh** — detached (`nohup`, PID 70819), survives session ends;
  log: `fund_score/logs/refresh_current_features_20260710.log`. Flows into adj-close + alpha
  rebuilds automatically when the pull completes.

## Waiting on you
1. **Push**: fund_score main is 8 commits ahead of origin; web main also has unpushed merges.
2. **Postgres reload** of the deduped staging (deferred all run by design — the serving DB still
   holds the pre-dedup 5,799-fund panel; the coherent staging is 5,706 with all new sections).
3. **Owner-decision items** in the backlog (X-ray book-basis ×4, vs_benchmark zero-ref).
4. **Frontend flips**: `fee-peer-band-web` (lean, unblocked — copy must use the real 36.9th-pct
   data, not the PRD example); te-decomp render (blocked on the label fix); positioning-context
   web mirror.
5. **Next queue heads** when you resume the loop: serve-l2 (B3), the B6 NAV-basis fix (after the
   refresh), recent-changes-te-ranked (needs re-grounding post-lookthrough).

Full operational log with every decision and gate verdict: `feature-pipeline/night-run-20260709.md`.
