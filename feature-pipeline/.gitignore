# Capture artifacts (screenshots, extracted text, served facts) are regenerated
# on every critique run — never committed.
captures/
# Review reports (JSON + HTML) are regenerated by /review-specs and /review-prompts.
reviews/
