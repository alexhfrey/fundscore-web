# Triage Backlog

The intake list. `/triage` drains the **Open** section: it picks the top item and routes it by
`(type)`. A `(bug)` or `(data)` item is fixed end-to-end and moves straight to **Done**. A `(story)`
that gets turned into a queued spec moves to **Specced (in queue)** — it has left the backlog for the
spec queue and is no longer re-triageable — and only reaches **Done** when `/implement-next` ships
that spec. Each item lives in exactly one state at a time (never Open *and* specced).

**Format:** `- [ ] (type) Title — one-line context [area]`
**Owner summary (required):** every Open/Specced item carries an indented `**Owner summary:**`
line directly beneath it — one or two sentences in plain English, written the way a CPO briefs a
board: what this is about and why it matters, no code paths, no jargon. It supplements (never
replaces) the detailed line. Anyone filing an item writes both; `/triage` should treat a missing
owner summary as a formatting defect and add one.
**Optional model tag:** `[model: fable|opus|sonnet, effort: xhigh|high|...]` — a routing hint for judgment-heavy items. Fix loops run in the MAIN session, so the dispatcher should surface the tag ("this item recommends X — /model to switch, or proceed") rather than silently ignore it. Specs carry the same hint as `model:`/`effort:` frontmatter, which `/implement-next` passes to the implementer agents automatically.
**Types & routing:**
- `(bug)` → `fundscore-data:fix-bug` (fundscore-harness plugin) — lean loop: reproduce → fix → verify → review (Claude + codex) → commit.
- `(data)` → `fundscore-data:fix-data` (fundscore-harness plugin) — lean loop: root-cause at source → fix → rebuild → /check-data + data-reviewer → review (Claude + codex) → commit. (No synthetic data; fix upstream.)
- `(story)` → `spec-story`: clarity-gate → a lean spec in `specs/queue/` (or escalate to the owner if vague). Discovery-heavy, net-new product direction goes to `/critique-funds` → review-proposals → `/spec-approved` instead. Once a spec is queued, the item moves to **Specced (in queue)** (below), not Done.

Keep items small and single-purpose. Put the type first so the dispatcher can route without reading the whole line.

---

## Open
Organized by thread (2026-07-22 restructure). **Working set** = the drain queue — `/loop /triage` works it top-down; items whose text calls for a focused data-scientist/reviewed session sit at its end so the lean loop never stalls mid-list. **Owner decisions** wait on the owner. The **Refresh campaign** executes as ONE supervised reviewed arc (shared root causes and rebuild/review overhead — do NOT lean-loop its members piecemeal), in roughly the listed order, ending with the terminal panel-refresh pass and the owner-gated serving reload. The **Hardening sweep** batches verified-nil-impact latents into one session. **Deploy** holds the paused solver service; the **Value-prop group** is post-launch roadmap. File new items into the matching thread (Working set by default). The serve-l2 fit-floor SHIPPED 2026-07-23 (see Done). **Beta bar (owner decisions, 2026-08-06):** beta scope = FULL experience (fund pages + screener + X-Ray); items tagged **BETA BLOCKER** must close before invites — the four user-visible-wrongness items PLUS V-spike and the foreign-holdings enrichment (owner chose the stricter bar). The **Beta-launch group** (below, before Value-prop) holds the launch-thread items filed the same day; the solver service (Deploy) is un-paused.

**Working set.** Distinct near-term correctness and coverage items in priority order — user-visible wrongness first, then coverage recovery, then hygiene chores; the focused-session item (holdings-vs-β) sits last so the lean loop never stalls on it.
- [ ] (data) **BETA BLOCKER (owner bar 2026-08-06).** **RE-GROUNDED 2026-07-17 — the CORE shipped with the merged FMP enrichment (holdings_complete sector 81.3→99.4% of USD; funds >20% unclassified 1,651→134; flows through to passive_blend_holdings). Residuals now the item: (1) per-SECTOR (vs per-fund) confidence; (2) web suppressor gate for low-confidence sector rows; (3) ~$10.7B recoverable secondary-ISIN/cusip=='N/A' misses; (4) Gulf/ADX honest gap.** Original: Foreign-holdings classification gap — sector/theme understated on BOTH the fund side and the benchmark side (systemic, ~1/3 of the catalog) — ROOT CAUSE (traced in code + data 2026-07-13): FOREIGN securities held as their **foreign local line** are filed in N-PORT by foreign ISIN with no US ticker and no US CUSIP (e.g. GB00BP6MXD84 = London Shell, FR0000120271 = Paris Total). Our only security-level sector/industry/theme references are US-based (`cusip_reference`=Sharadar, keyed cusip/ticker; curated themes keyed by US-ticker security_id). Both pipelines classify via the SAME `cusip→cusip_reference` join, so foreign local lines drop out of every sector/theme aggregation. NOT passive-only and NOT a vendor issue (both sides are SEC N-PORT): international funds' FUND side classifies foreign holdings' sector at just **11.7%** (vs 95% US); a US-heavy fund like IENAX only looks fine (fund 85%) because it holds US ADR lines (SHEL/TTE/BP), while its global-ETF benchmark (IXC) holds the local lines (~40% of NAV unclassified → energy reads 76% vs ~97%). The ETF-lookthrough table (`etf_holdings_snapshots`→`passive_blend_holdings`) is even a regression vs the fund side — it DROPPED the `cusip` and `security_name` columns. Blast radius (fund-side only): **1,836 EQ funds >30% foreign, 1,465 >50%**, plus every global-ETF benchmark. **FIX (owner directive: one SHARED enrichment path for both sides, since the source is identical; enrich as much as possible on both):** (1) carry `cusip`+`security_name` through the ETF-lookthrough to parity with the fund side; (2) Option A core — normalized issuer-name/ISIN → sector via `cusip_reference.name` + the union of already-resolved US-ADR securities + an ISIN→US-ADR crosswalk (Shell PLC ↔ SHEL → energy); confident matches only, uncertain stays null (no fabrication); (3) apply in the common identifier-resolution step so `build_holdings_complete` AND the ETF look-through both improve. Interim SHIPPED 2026-07-13 (fund_score 564d32f): vs_benchmark `confidence_state`/`coverage_state` now key on `min(fund_classified, passive_coverage)` so an under-classified benchmark reads low-confidence, not a confident false overweight (IENAX energy medium→low; 336 funds/3,317 sector rows re-rated, all downgrades, 0 served figures moved). RESIDUAL from that interim: (a) it's per-FUND coverage applied fund-wide — a fund with unclassified mass in one sector downgrades ALL its sectors (conservative, never over-confident); the enrichment fix should make it per-SECTOR. (b) The web renders `confidence_state` only as a caveat label today (not a suppressor) — a fast-follow web gate could suppress/strongly-caveat low-confidence bets on the flagship TE table. DOWNSTREAM BLAST RADIUS (16 features, see companion): HIGH — Return Attribution + Holdings Brinson are **silently withheld** for intl/EM funds (Gate A `insufficient_coverage`); Value-Offering **named_bets / bet-profile headline** mis-sized (user-facing); Exposure X-Ray sector+theme (fund & passive); theme exposure understated wherever a theme lives abroad (semis/china-internet/EV); Exposure Path suppressed for >20%-foreign funds (COVERAGE_FLOOR 0.80); risk-decomp regressor selection + security_characteristics/size 2nd-order. NOT affected (bounds it): L2 passive-blend solver / passive_alt_blend (fits fund NAV vs ETF NAV — needs no foreign-security returns), factor betas, te_decomposition basis, and the COUNTRY axis (inv_country filed directly). [fund_score: build_holdings_complete.py + build_passive_holdings_foundation.py + cusip_reference / shared identifier resolution] [model: opus, effort: high]
  **Owner summary:** Foreign stocks that trade on their home exchange (London Shell, Paris TotalEnergies) arrive from the SEC with no US identifier, and our sector/theme reference only covers US securities — so we can't tag them by sector. This isn't just the benchmark: about a third of our equity funds hold enough foreign stock that their OWN sector breakdown is understated, and for truly international funds two features (return attribution, style-box) are silently withheld entirely. The fix is one shared enrichment that recovers the sector/theme for foreign holdings on both sides at once. Country data is unaffected, and the core "which passive alternative" match doesn't depend on this.
- [ ] (data) L2 energy pool has no natural-resources/materials candidate — TRNEX-class twins under-span their mandate (owner question 2026-07-28; investigated same day, read-only, scripts in session scratchpad). The `EQ.GLOBAL.ENERGY` allowlist (`src/fundscore/passive_match/etf_pools.py:65`) is broad-world ∪ energy only; MXI/XLB/VAW/GNR/GUNR/IGE/PICK/COPX appear in no energy pool (several nowhere in the module) and are never even priced into the solver panel (`runner.py:525`), so an energy+materials blend was NEVER a candidate — pool exclusion, not an objective artifact. Measured on the exact production window/method (reproduced the 2026-06-30 TRNEX solve to 4 decimals: IXC .5539/VT .4461, R² 0.9101, TE 556 bps): **IGE alone corr 0.948** (best of any candidate tested; IXC is 0.880); production rule over pool ∪ NR ETFs picks **IGE 68%/VT 32% — R² 0.943, TE 443 bps, wins the penalized objective by 20%**; IXC+MXI 0.936/469 confirms the materials sleeve (TRNEX holds 64.4% energy / 19.2% materials / 11.3% other) is the biggest hole; VT keeps a real seat for the non-resource sleeve either way. FIX: add IGE+GNR+MXI to `EQ.GLOBAL.ENERGY` (strict pool ADD — monotonic, can't worsen any fund per `etf_pools.py:49-54` pattern). **OWNER APPROVED 2026-07-28; SHIPPED 2026-07-29 (fund_score commit on `home/value-prop-refresh`: full-audit 21-ETF add list + EQ.CN.BROAD, 2026-06-30 re-solve, all gold panels rebuilt + gated — codex x3 + data-reviewer x4 PASS; ONLY the serving/Postgres reload remains, owner-gated)** (etf_pools.py add + etf_classification.py: IGE "Sector", GNR "Global Sector"; MXI already classified; pool routing + all_etf_tickers verified). Fee-vintage correction: the investigator's "stale 2017 fee rows" claim was WRONG — gold expense_ratio_history is current (IGE 39 bps @2025Q3, GNR 40 @2026Q1, MXI 39 @2025Q3), so no fee refresh needed and the blended IGE/VT twin fee is ≈28 bps ⇒ TRNEX surcharge ~32→~29 (not ~22 as first estimated). Remaining: companion all-pool gap audit (running), then ONE full refit at 2026-06-30 per the passive-match runbook — **changes served twins/R²/TE/surcharge for every fund routed to the energy pool (plus any audit-approved pool adds); owner-gated serving reload per docs/context/passive-match.md**. TRNEX effect: R² 0.906→~0.94, TE 6.3%→~4.4% (tighter Crescent; the fund honestly looks modestly better). [fund_score: passive_match pools + refit]
  **Owner summary:** You asked whether we ever tried an energy + materials mix as New Era's benchmark — we never did: the candidate list for energy funds simply contains no materials or natural-resources ETF. Tested properly, a natural-resources ETF (IGE) plus world stocks beats our current twin decisively (fit 0.94 vs 0.91, tracking error 4.4% vs 5.6%), and it wins under our own selection rule the moment it's allowed to compete. The fix is to add three tickers to the candidate list and refit — it changes the served comparison for every energy fund, so it runs as a gated refit with fresh fee data first.
- [ ] (data) **BETA BLOCKER (owner bar 2026-08-06).** V-spike price corruption degrades 174 L2-served funds' fits — one-week ±12-47% reversal pairs (net≈0) inside the production window in `data/gold/fund_daily_adj_close.parquet` (SLPAX ±23% Dec-2021, MOTNX ±46%, DFMLX −69%/+222% Nov-2021): bad NAV prints / distribution mis-adjustments injecting fake variance. Median served blend R² for affected funds 0.593 vs 0.858 clean — their twins/TE/verdicts are systematically degraded. Fund list in session scratchpad `pool_audit/fund_scan_v2.parquet` (2026-07-29 pool audit §7.1). Root-cause at the pricing source (adjustment pipeline), fix, then these funds heal at the next L2 refit. [fund_score: fund pricing / adj_close]
  **Owner summary:** 174 funds have a corrupted week in our price history (a bogus giant down-then-up spike, usually a mishandled distribution), which quietly wrecks their passive-twin fit and makes their verdicts look noisier than they are. Fix the price data at the source and they heal on the next refit.
- [ ] (data) **BETA BLOCKER (owner bar 2026-08-06).** Wrong price series served under fund tickers — pricing-resolver ticker collisions: WMSIX ("Wilmington Small-Cap Strategy") tracks a MUNI index at corr 0.97 (VTEAX; 0.33 vs VB), PMAAX ("Small Cap Growth") 0.96 vs MINO (PIMCO muni), CGBAX 0.91 vs IBTE, JSGTX 0.983 vs IWF — their served fits are meaningless and they dominate the EQ.US.BROAD.SMALL flag list (2026-07-29 pool audit §7.3). Sweep the resolver for collision class + audit how many more exist. Related but distinct: QQQ/DIA/MDY (UITs) + IAU/SLV (grantor trusts) have NO prices in any store (not N-PORT filers, not in the curated Sharadar 101 — QQQM is present as the natural Nasdaq-100 candidate), and VIG is unreachable under its ETF ticker (maps to Admiral-class ticker) — price-store coverage gaps that also blocked audit evaluation (§7.4-5). [fund_score: pricing resolver + ETF price store]
  **Owner summary:** A handful of funds are being graded against the WRONG price history entirely (a small-cap fund whose "prices" are actually a muni-bond fund's) — their pages are garbage until the ticker-resolution bug is fixed; and a few household ETFs (QQQ, DIA, gold/silver trusts) have no prices in our stores at all, so they can never be candidates.
- [ ] (data) **BETA BLOCKER (owner bar 2026-08-06).** Taxonomy misroutes put funds in the wrong L2 pool — surfaced fund-level by the 2026-07-29 pool audit (§5-6): MSCFX (Mairs & Power Small Cap, an equity fund) is served from the FI.US.IG_BROAD pool; WAINX + 2 more India funds and 3 LatAm funds sit in EQ.EM.BROAD instead of country routing; CSRIX (realty) in EQ.US.CONSDISC; AIIFX (alt income, blend R² −4.96) in EQ.US.FINANC; 17 funds whose best twin is an AOR-family allocation ETF are allocation funds misclassified as pure equity. Also the converse-gap set: 163 clean funds with max corr <0.85 against ALL 1,932 scanned ETFs — pre-merger SPAC, equity market-neutral, multi-strategy/tactical — need ALT/market-neutral classification so the R² floor suppresses their scores honestly instead of serving garbage twins. [fund_score: taxonomy / classification]
  **Owner summary:** Some funds are simply in the wrong bucket — an equity fund graded against bond ETFs, India funds graded against broad EM, and a set of hedge-fund-like funds that have no passive twin by construction but sit in equity pools anyway. Reclassify them so each is graded against the right shelf — or honestly not graded at all.
- [ ] (data) l2_passive_candidate_fit omits off-pool always-include blend legs — 15 funds (0.35% of 4,319; e.g. ROGSX/IGM+SPY, CONWX/VYM+…) have a blend second leg that entered via `always_include` (stated-benchmark ETFs: SPY/AGG/SHY/BNDX/IEFA/VTI) rather than the candidate pool; the builder (`src/fundscore/product/l2_passive_candidate_fit.py`) emits pool candidates only, so the served panel shows k_chosen=2 with ONE component and weights that don't sum to 1 — contradicting the serving docstring "every candidate the L2 solver actually considered" (fact_assembler.py:469). Blend composition itself (passive_alt_blend) is complete; this is the evidence panel under-reporting. Emit always-include legs as candidate rows (flagged `entered_via=always_include`) or document + serve the missing-leg weight. Surfaced by the 2026-07-29 pool-refit data review (non-blocking). [fund_score: l2_passive_candidate_fit]
  **Owner summary:** For a handful of funds the "why this passive alternative" evidence table shows only half the twin (the half that came from the candidate pool) — the composition users see elsewhere is right, but the evidence table under-explains it. Small fix: include the benchmark-leg rows too.
- [ ] (data) `value_offering_reframed_panel.l2_blend_etfs` is NON-DETERMINISTIC across rebuilds — two consecutive builds of the SAME code on the SAME inputs disagreed on **117 of 5,937 funds** (PABBX served `USMV` on one run and `SPY` on the next; PSGIX `USMV`/`IWO`; BACCX `IXC`/`VEU`). The differing values are not a re-ordering — the ETF **sets** differ (0 of 117 were set-identical). ROOT CAUSE (traced 2026-07-30): `build_value_offering_reframed.py` builds `tmap` as a concat of three ticker→series_id sources (`fund_metadata.primary_ticker` ∪ `hierarchical_skill_posteriors.fund` ∪ `class_ticker_mappings`), in which **6,654 series_ids map to >1 ticker** (share classes), then does `blend.join(tmap, on="ticker").unique(subset="series_id")` — so an ARBITRARY share class's L2 blend wins, and polars' unmaintained-order `unique` picks a different one run to run. Different share classes get different solver fits (fee-differentiated NAV series), hence genuinely different ETF sets. Discovered incidentally while verifying that a segment-2 guard was a no-op (the rebuild-twice diff surfaced it); **PRE-EXISTING and independent of the unify work** — the `l2` block is untouched by that diff. FIX: do NOT sort-then-unique — per the `deterministic-wrong-worse-than-nondeterministic` rule that just freezes one arbitrary winner. Adjudicate which share class is authoritative at the join surface (primary_ticker should win over posterior/CTM aliases), and fail honestly where claimants genuinely tie. Check the same concat→unique shape in sibling consumers before closing. MERGED 2026-08-06: the round-4 W3 "(bug) add a deterministic sort key before the unique" item was this SAME defect (116/117-fund cohort) — folded here; the sort-key quick fix is explicitly forbidden (it freezes an arbitrary wrong winner — see the dedup lesson). **BETA BLOCKER (owner bar 2026-08-06).** [fund_score: build_value_offering_reframed.py tmap/blend join]
  **Owner summary:** The named ETFs we show as a fund's cheap alternative can change between two identical rebuilds — 117 funds flip, and not just in ordering: one fund is shown a low-volatility ETF on one build and the S&P 500 on the next. The cause is that a fund's share classes each get their own passive match and we pick one at random. We need to decide which share class speaks for the fund rather than letting the tie break itself.
- [ ] (story) **Passive-fit coverage investigation — raise the share of funds with a GOOD twin (owner-requested 2026-07-31).** GOAL: more funds legitimately clearing the fit floor, NOT a greener metric. The fit floor (`l2_passive_candidate_fit_v1.0`: winner selection-corr <0.60 OR `replica_r2` <0.75 → `no_good_match`, fails closed) exists because serving a bad twin is worse than an honest null — any threshold change must be justified by evidence that the twin is genuinely usable, never by coverage convenience. **Grounding measured 2026-07-31 (do NOT re-derive):** on the VO panel 1,944/4,202 scored funds (46.3%) carry a measured `idio_risk_share` — below the check's 50% PASS bar, hence a standing WARN. The 2,258 missing split: **too_new 1,340 · not_comparable 394 · not in value_score 363 · our own decomposition gate 120 · fee-level 41.** Only the 394 `not_comparable` are twin-fit-addressable → **ceiling 46.3% → 55.6%**, and within them `replica_r2` is 0.70–0.75 on 120 (near-miss), 0.65–0.70 on 76, 0.60–0.65 on 42, **<0.60 on 152** (the last group are market-neutral / multi-strategy / SPAC-arb with no passive analogue — they need ALT classification and honest non-scoring, see the taxonomy-misroute item, not a better pool). Realistic pool-driven upside is ~2–4 coverage points, not nine. NOTE the 2026-07-29 21-ETF pool add is ALREADY live in the lakehouse (served refit `asof_refit_date=2026-06-30`; IGE/GNR/MXI appear in 9/6/15 blends) and was a fit-QUALITY fix (TRNEX R² 0.906→~0.94), not a coverage fix — coverage only moves when a fund crosses the floor from below. **LEVERS to evaluate (owner-listed; each needs measured yield + risk, then an owner call):** (1) **shorter fit window** — lets shorter-history funds qualify; costs fit precision and interacts with `te_decomposition`'s `MAX_ANCHOR_LAG_WEEKS=13` alignment gate and the ~7-week structural FF6 lag, so quantify the noise/precision trade before adopting. (2) **fall back to the stated/official benchmark when history is short** (`src/fundscore/stated_benchmark/` already exists) — BIGGEST CONCEPTUAL CALL: the product promise is "vs the best passive alternative you could actually buy," which is not the same question as "vs the benchmark the prospectus names"; if adopted it must be a distinctly LABELLED comparison, never silently mixed into the same field. (3) **candidate-pool expansion** — the companion all-pool gap audit is already running; target the 120 near-misses specifically and measure which are pool-limited vs structurally un-twinnable. (4) **revisit the R² threshold** — re-derive where 0.75/0.60 should sit from the relationship between fit quality and downstream correctness (do served TE / idio / verdicts actually degrade below the cut, and where?), not from the coverage it yields; report the served-figure damage at each candidate cut. **PREREQUISITE:** resolve the filed "222 `too_new` mislabels" defect first (Crescent-v2 item 3) — it decides whether headroom is ~4 points or materially more. I could NOT verify it 2026-07-31: `l2_replica_quality.n_obs` is a WINDOWED fit count (capped ~180w), not total history, so it cannot distinguish a genuinely young fund from an old one; this needs a real total-history field before the 222 is confirmed or refuted. **Prize is wider than the WARN:** the passive twin drives the Value Score itself, te_decomposition/the bets table and the fee-over-passive figure — twin coverage lifts every one of those surfaces, so scope the win across them, not just this metric. [fund_score: passive_match fit floor + window + pools + stated_benchmark fallback] [model: opus, effort: high]
  **Owner summary:** Fewer than half our funds currently get the "how much of this is real stock-picking" read, because it requires a passive twin that genuinely tracks the fund and many funds don't have one. This investigation asks how to give more funds a good twin — by shortening the measurement window, using the fund's official benchmark when its history is short, widening the candidate list, or revisiting where we set the quality bar. The rule is that we improve the actual match, not lower the bar to make the number look better: a wrong benchmark is worse than admitting we don't have one. Realistically this recovers a few points on its own, and the bigger prize is that a better twin improves the headline Value Score and the fee comparison too — not just this one metric.
- [ ] (data) fee_efficiency_score vintage/label coherence after refits — two warts surfaced by the 2026-07-29 pool-refit review: (a) rows are stamped `eval_date` (e.g. 2026-04-10) but a post-refit rebuild embeds blends solved on data through the refit date — as-of/label incoherence in a dated panel; (b) value_score no longer reads its `passive_fee_bps` (v0.3.2 computes the fee directly from the served blend after the stale-vintage desync defect — 64 funds up to ~54 bps net-fee distortion, fixed 2026-07-29), so decide fee_efficiency's remaining contract (fair-fee/peer content) and either re-stamp eval_date honestly on rebuilds or version the panel per refit. [fund_score: fee_efficiency]
  **Owner summary:** The fair-fee panel's date labels can quietly disagree with the data inside them after a benchmark refit, and its blend-fee column is no longer what the Value Score uses (the score now computes the twin's fee directly, immune to staleness). Tidy the panel's dating and its remaining role.
- [ ] (data) **BETA BLOCKER (owner bar 2026-08-06).** value_score SCORES ~139 funds on stale/mis-keyed ticker fees — the scoring-basis fee (`expense_ratio_history` ticker-keyed, resolve_fee `fee_basis=named_class`) can bind a DEAD predecessor series' fee to a reused ticker (SNWCX scored at 226 bps from a series ending 2021Q2; the live series S000073425 files 85 bps; TSWEX same pattern) and the 3% ER_CAP winsorization is a scoring choice that must never reach display. Display surfaces were healed 2026-07-29 (canonical_fee sources the display-basis series fee), but the SCORE still ingests the stale ticker fee for these funds — fees dominate the metric, so this misprices their value_bps/score100. Fix at source: expense_ratio_history ticker keying must resolve series reuse (join through class_ticker_mappings vintages), and resolve_fee should flag staleness (last quarter << as_of). Surfaced by the 2026-07-29 pool-refit review round 3 (warning class). [fund_score: expense pipeline / value_score fee resolver]
  **Owner summary:** About 139 funds' Value Scores are computed with the wrong fee — usually a dead predecessor fund's fee that got attached to a recycled ticker. The displayed fee numbers are now correct; the score's input still isn't for these funds. Fix the fee history's identity handling and their scores correct themselves.
- [ ] (data) Un-gated fallback fee path serves stale evals — `fact_assembler._fee_fairness_by_series` falls back to fee_efficiency's latest-eval `active_fee_over_passive_bps` for the 618 served funds NOT priced by canonical_fee; 13 contradict their displayed net ER (worst WIEMX 87 bps, evals back to 2021) and 34 serve a fee gap with NULL `passive_fee_bps` (a gap without its baseline in the payload). Same defect family the 2026-07-29 canonical_fee fix closed for the priced path. Gate the fallback (serve an honest missing_reason when the eval's actual disagrees with displayed net or passive is null) or extend canon coverage. Surfaced by the round-4 acceptance review (W1/W2, non-blocking). [fund_score: serving fact_assembler]
  **Owner summary:** ~600 funds that our new always-fresh fee source can't price still fall back to old dated fee evaluations; for 13 of them the displayed fee math visibly contradicts itself (some using 2021 numbers). Gate the fallback so those serve an honest "unavailable" instead.
- [ ] (bug) **LIVE mixed-basis: the 2026-07-29 pool refit published a new twin alongside attribution still computed against the OLD twin.** The refit rebuilt the twin/value/fee chain (passive_alt_blend, replica_quality, benchmark_nav, value_score, fee_efficiency, candidate_fit, positioning/fee-peer/VOR) and was reloaded to serving — but **`te_decomposition` (built 2026-07-15), `factor_attribution` (07-15) and `profile_nav_series`/`profile_nav_period_table` (07-04) were NOT rebuilt, and all four are in the serving path** (`serving/load.py:43-54`). So for funds whose twin changed, the served profile shows the NEW twin's identity/fit/fee next to bets, TE-decomposition shares and a growth chart measured against the OLD twin. Measured exposure: **877 funds took a new-pool ETF into their twin; 302 of them carry te_decomposition rows and 405 carry factor_attribution rows on the old basis** (TRNEX included — served twin IGE 68/VT 32 at fit 0.943, but its 9.4%/44-24-22-10 anatomy, its HOLD/SHIFT/PICK figures and its growth chart are all vs IXC+VT at fit 0.910). ROOT CAUSE: the refit runbook in `docs/context/passive-match.md` (corrected 2026-07-29 to 12 steps) still does not list these three panels — the same class of omission as the TD-cache and fee-efficiency gaps that correction caught. FIX: add them to the runbook, rebuild (`make build-profile-nav-series`, `make build-factor-attribution`, `make build-te-decomposition`, `make build-exposure-path`), re-gate, reload. **SEQUENCING RESOLVED (owner, 2026-07-30): the te_decomposition arm is FOLDED INTO the queued `unify-te-decomposition-global-basis` spec** — that spec must rebuild te_decomposition anyway (its `y` changes with the new twins regardless of basis), so unify rebuilds it exactly ONCE on the new twins AND the 35-factor basis. The earlier "partly throwaway" read was backwards. **This item's remaining scope = `factor_attribution` (405 funds) + `profile_nav_series`/`profile_nav_period_table` only** — both basis-independent, so rebuilding them is never wasted; do them independently and don't wait on unify. Runbook fix (adding all four panels to `docs/context/passive-match.md`) still belongs here. [fund_score: refit runbook + attribution panels; owner-gated reload]
    **te_decomposition arm CLOSED 2026-07-30** (unify segment 1, fund_score `feature/unify-te-decomposition-global-basis`): rebuilt on the new twins AND the 35-factor basis in one pass. The violation was much larger than this item estimated — v0.1 disagreed with the served `te_current` on **1,972/2,043 funds** (not ~302) and carried a stale `passive_alt_label` on **509**. v0.2 now matches value_score on `as_of`, `te_total_bps` (≤1e-9) and `passive_alt_label` for all 1,949 funds, **0 mismatches**. Still owner-gated on the serving reload, so the ~302 remain in violation *on the live site* until that runs.
  **Owner summary:** Yesterday's benchmark upgrade went live for the twin, the fees and the fit — but the bet-by-bet breakdown and the growth chart for those same funds are still measured against the OLD benchmark, because the refit runbook doesn't list those three panels. Roughly 300-400 funds are currently showing two benchmarks on one page. The fix is to rebuild those panels and reload; the runbook needs the missing steps added so the next quarterly refit doesn't repeat it.
- [ ] (data) te_decomposition serves 94 funds whose bets were fit on a 13–53 week STALE window — measured 2026-07-30 on the served panel (2,054 funds): 95.2% fit within 2 weeks of their `te_current` anchor, but 94 funds (4.6%) have `as_of − window_end` of 13–53 weeks (worst SSVSX/RSCYX/GAGVX/THBVX at 53 weeks — bets fit through 2025-05-02 against a 2026-05-09 anchor), every one sitting exactly on the `MIN_OBS_FIT=104` floor. ROOT CAUSE: the te-anchor gate is NAV-only (`build_te_decomposition.py:191`); the fit rows are then silently intersected with FF6+factor availability (`:196-206`) and a short aligned window passes with no gate — only `MIN_OBS_FIT` binds. So a fund's displayed bet MIX can be a year older than the tracking-error level it's scaled to, with all 11 invariants green. **FIX IS SPEC'D: the anchor-alignment gate in `unify-te-decomposition-global-basis` segment 4 suppresses these fail-honest.** File separately so it isn't lost if unify slips — the same gate is a ~10-line standalone fix on v0.1. Adjacent question worth one look: these funds' NAV series appear to end ~a year early, yet they pass `value_score` as `scored` — check whether the recency filter should exclude them upstream. [fund_score: build_te_decomposition alignment gate / value_score recency]
  **Owner summary:** For 94 funds (about 5%) the "where does your tracking error come from" table was computed on price data up to a year older than the tracking-error number it sits next to — the fund's bet mix is stale even though the headline figure is current. Our checks all pass because nothing verifies the two cover the same period. The fix is already written into the unify spec; filing it separately so it can ship on its own if that spec slips.
- [ ] (data) **LIKELY FIXED-BY-UNIFY — retest before closing (2026-07-30).** Tested RYLSX directly on the rebuilt 35-factor global basis: the served `Financial Services +318.7 bps` (rank 1, "high" confidence) + `US Mega Banks +258.6 bps` pair **disappears entirely** from the top-5, and `sector::consumer_cyclical +64 bps` — where leisure actually lives — surfaces at rank 4 behind Japan/Germany/China. Same pattern on the other archetypes: TRNEX's spurious `theme::mag_7 +105 bps` rank-1 bet and FBGRX's financial-services/real-estate leaders both vanish. Root cause is confirmed as the per-fund collinear basis, which `unify-te-decomposition-global-basis` replaces. Do NOT close on this evidence alone — consumer_cyclical is rank 4, not rank 1 — re-check on the full rebuild. Original item below.
- [ ] (bug) **Consumers assert a "biggest bet" while `top_bet_confident=False`.** `te_decomposition` serves `top_bet_confident` + `top_bet_gap` precisely so no consumer claims a single largest bet when the top two aren't separable — the contract says "no consumer may assert a 'biggest bet' on it". TRNEX: `top_bet_confident=False`, `top_bet_gap=0.0078` against a required 0.10, on a **0.4 bps** difference (Canada 57.50 vs Gold 57.06) — yet the V4 mockup called Canada "the single largest bet in the fund" and Gold "the second-largest" (struck 2026-07-30). Two things needed: (a) an automated check that no served/rendered copy asserts a superlative when the flag is False, and (b) note that **stock selection (332.6 bps) is larger than any factor bet** for this fund, so even a confident factor superlative would mislead unless it says "largest *factor* bet". Surfaced by the RIA critic 2026-07-30. [fund_score: te_decomposition consumers + display contract]
  **Owner summary:** Our data explicitly flags when the top two bets are too close to call a winner, and the page ignored that flag and named a winner anyway — on a 0.4 basis-point difference. Add a check so no page can claim a "biggest bet" the data won't support.
- [ ] (data) **Beta/weight sign disagreement has no detector — and country-of-registration is a weak proxy for exposure.** For TRNEX, 2 of 8 mappable bets disagree in sign between `te_decomposition` (returns beta) and `exposure_xray`/holdings (weight gap): **Australia** (beta over, holds 0.6% vs twin 1.7%) and **United Kingdom** (beta over, holds 6.5% vs 7.1%). Neither is a coverage artifact — country `fund_unclassified` is 0.0000 and the foreign book IS captured (38.4% of NAV, 55 non-US-ticker names). Root cause is conceptual: **filed country is domicile, not economic exposure.** This fund's "UK" is Shell/BP/TechnipFMC (energy, not Britain); its "Ireland" is mostly Linde (industrial gas); it holds Australian-registered names that mine elsewhere. Two asks: (a) add the sign-agreement join as a `/check-data` sibling-coherence check (both inputs are in gold, ~8 lines), auto-emitting a display annotation where they diverge; (b) decide whether country-by-domicile should carry a standing caveat or be supplemented by a revenue/listing-based view for global funds. Surfaced 2026-07-30 by the RIA critic + owner hypothesis. [fund_score: te_decomposition ↔ exposure_xray coherence]
  **Owner summary:** Our two views of "which countries does this fund bet on" disagree for a couple of bets — not because data is missing, but because the filed country tells you where a company is registered, not what its business is. A global resources fund's "UK" is Shell and BP. We should detect these disagreements automatically and say so on the page rather than letting the two views contradict each other.
- [ ] (data) Narrow-industry funds serve implausible top TE bets — RYLSX (Rydex LEISURE, holds NFLX/PM/MCD, zero financials held) leads with "Financial Services +320 bps (50% of factor variance, high confidence)" + near-duplicate "US Mega Banks +258 bps" (collinear FWL var-shares). Gold-exact and method-consistent, but an expert double-takes; review theme/sector bet labeling + collinearity handling for narrow industry funds before the section is marketed. Surfaced by the 2026-07-12 TE-flip data-quality-critic (P2). [fund_score: te_decomposition FWL basis / risk_model targets]
  **Owner summary:** For a handful of narrowly focused funds (e.g. a leisure-industry fund), our risk model names "Financial Services" as the biggest bet — statistically defensible, but jarring to anyone who knows the fund. Review how bets are labeled for narrow funds before this feature is marketed.
- [ ] (data) Exposure-path "Fees + trading" line is mostly a coverage artifact for foreign-heavy funds, and the COVERAGE_FLOOR gate never binds — ROOT CAUSE (traced 2026-07-27 on TRNEX): `holdings_snapshots.parquet` is US-ticker-keyed, so foreign local lines never enter the frozen book (TRNEX filed `pct_nav` sums to 0.58–0.71/quarter; 53–67 of 115–127 filed lines priced); `frozen_returns` renormalizes the priced sub-book to 1, and `weight_coverage` (COVERAGE_FLOOR 0.80, `exposure_path_attribution.py`) is measured vs the ALREADY-SUBSET snapshot book (reads 0.91–0.97 for TRNEX) instead of vs filed NAV (0.59–0.71) → the intended low-coverage suppression never fires for exactly the funds that need it (same bug class as the 2026-06-20 fix inside `frozen_returns`, one level up). Result: `reconciliation_gap_bps` — served on the profile as "Fees + trading" — absorbs what the unpriced 29–42% of NAV did differently. Measured: corr(gap, expense ratio) = 0.07 across 2,469 funds; median gap 112 bps where ≥90% of NAV is priced vs 277 in the 60–75% bucket (TRNEX +330, 78th pctile) and med-|gap| 560 below 60%. Fix: (1) gate on filed-NAV coverage (`pct_nav` sum), not priced-subset weight; (2) coverage-aware serving label — "Fees & trading" only when priced-NAV ≥ ~0.9, else "Fees, trading & unpriced holdings (~X% of NAV)" and suppress any "costs are the gap"-style takeaway; (3) the foreign-pricing vendor fix (EODHD path) shrinks the artifact at source later. Related to the foreign-classification CORE item above (same US-keyed-reference root) but ship the gate+label fix independently so the profile stops mislabeling now. [fund_score: product/exposure_path_attribution.py coverage denominator / serving fact_assembler + web copy]
  **Owner summary:** On foreign-heavy fund pages, the attribution line we call "fees and trading" is actually dominated by the third of the portfolio our US price data can't see — and the safety valve meant to suppress those cases checks the wrong denominator, so it never fires. Fix is two-part: measure coverage against the real filed portfolio, and when coverage is low, label the residual honestly instead of implying a 0.57% fee explains a 3.3% annual drag.
- [ ] (bug) Crescent v2 preview Hurdle mis-states its own bar — the preview says "must beat IXC by ≈32 bps/yr just to cover its extra fee" and draws AFTER-FEE excess bars against a +32 line, but after-fee excess already includes the fee difference: the honest bar on that basis is ZERO, so the current framing double-charges the surcharge (caught by the adversarial RIA panel on the V3 mockup, 2026-07-27; V3 is fixed — copy now "after all fees, beat the twin — the bar is zero; its $32 surcharge is already inside these numbers"). Two sibling label fixes from the same panel to carry into cutover: "Closest passive alternative IXC · 0.25%/yr" mislabels the BLEND's fee as IXC's (IXC alone is 40 bps; the twin is IXC 55%+VT 45%), and the twin growth series needs a HYPOTHETICAL · MIX-AS-OF chip (2026-06 refit weights backcast to 2017). [fundscore-web: src/components/fund/profile/v2 Hurdle framing + identity/twin labels]
  **Owner summary:** The preview tells users a fund must beat its twin by 0.32%/yr to cover its fee, but the numbers displayed already have fees taken out — the honest bar is zero, and the current framing counts the fee against the fund twice. Plus two labeling fixes: the twin's fee belongs to the two-ETF mix (not IXC alone), and the twin's long history should be marked hypothetical.
- [ ] (story) Returns-based realized-attribution fallback for low-coverage funds — the served "what the bets did" chain is frozen-holdings-based BY DESIGN (the bias/timing split needs point-in-time per-quarter exposures; a rolling NAV regression smears a fresh rotation into a persistent tilt — `version_c_model_brainstorm.md:93-97`; per-theme returns-based contributions are collinearity-unstable, cluster-level only — `value_offering_risk_replicability_v0_3.md:123-139`), but that trade buys constituent-pricing dependence, so foreign-heavy funds either get suppressed (448/2,484 low_coverage today, once the gate-denominator fix makes the gate honest this GROWS) or served a coverage-artifact residual. Documented fact: the returns side needs only fund+factor NAV series — no holdings, no constituent prices, and it's fresher (`13_risk_attribution.md:112-124`). Proposal to spec: when filed-NAV coverage < the floor, serve the returns-space attribution instead — aggregate replicable-vs-idio split + CLUSTER-level factor contributions (β × factor return over the window) on the unified 35-factor basis, labeled "estimated from returns — this fund's book is mostly foreign-listed", explicitly WITHOUT the picks-vs-tilts split and WITHOUT bias/timing (honest capability downgrade, not a silent one). Natural sequencing: after `unify-te-decomposition-global-basis` ships (same basis, same factor return series); the alternative — never showing realized attribution for ~20%+ of funds incl. most intl/EM — is the status quo this replaces. [fund_score: product/exposure_path_attribution.py + risk_model/global_* / new returns-attribution builder] [model: opus, effort: high]
  **Owner summary:** For the sizeable slice of funds whose holdings we can't price (mostly international), we today either hide the "what did the bets pay" story or serve a misleading residual. A returns-only version of the attribution — less granular but honest and coverage-proof — can cover exactly those funds, clearly labeled as the coarser read. Best built right after the already-queued risk-model unification since it reuses the same 35-factor machinery.
- [ ] (story) The One Ledger — full performance breakdown anchored on NAV, with fees broken out and static-vs-timing displayed (owner ask 2026-07-27) — one served decomposition per fund/window that always reconciles to the NAV-vs-twin gap: NET active (NAV, β-adj, exact incl. foreign) → + FEES (exact per-quarter filed ER from `series_expense_ratio_history.parquet` — break the known fee OUT of today's conflated `reconciliation_gap` residual) → factor sleeves with drill-downs (sector/theme/macro rows each splitting into STATIC tilt = `bias_bps` and MOVING-the-tilt = `timing_bps` — both already computed in `factor_attribution` AND already in the served payload, `fact_assembler.py:1693-94`, just never displayed; per-row drill to Brinson #10 stock/sector/theme receipts where priced) → stock selection (priced names, Brinson drill) → residual honestly split as "trading + unpriced sleeve", with the unpriced sleeve's COMPOSITION named via the FMP ISIN→sector enrichment (classification is GO even though foreign returns are NO-GO — "~35% of NAV, mostly foreign-listed energy" instead of a bare number). Display language lock: timing_bps is "the realized contribution of moving the exposure", NEVER "timing skill" (three probes killed timing-skill claims). Dependencies/sequencing: coverage-gate+label fix (above) first; `unify-te-decomposition-global-basis` gives one bet vocabulary; the returns-based fallback (above) covers low-coverage funds; EODHD foreign pricing later shrinks the unpriced-sleeve line toward pure fees+trading. TRNEX proof the drill-down pops: Energy total −666 = static −220 + moving −447; Big Oil Majors +466 = +325 static +141 moving. [fund_score: serving fact_assembler ledger assembly + web profile chain redesign; reference rendering docs/product/strategy/mockup_fund_profile_crescent_v3.html] [model: opus, effort: high]
  **Owner summary:** One performance ledger per fund that always adds up: start from the real after-fee gap vs the twin (which covers foreign holdings by construction), break out the fee we know exactly, show each bet family with "standing tilt" vs "moving the tilt around" — numbers we already compute but never show — with click-through receipts to individual stocks, and label the honest remainder for what it is (trading costs plus the foreign sleeve we can classify but not yet price). Turns three disconnected attribution views into one story a retail investor can follow line by line.
- [ ] (data) te_decomposition names a STALE passive alternative next to the new candidates table — 131/2,038 series serving both sections have `te_decomposition.passive_alt_label` ≠ the served blend rank-1 ETF (FCNTX: te_decomp says IWF, passive_baseline says SPY) because te_decomp predates the 2026-07-16 L2 canonical-panel winner flips. Each figure is self-labeled (payload not mislabeled), but two sections name a different "the passive alternative" on one page. Rebuild te_decomposition on the migrated blends BEFORE the frontend renders both sections — fold into the queued `unify-te-decomposition-global-basis` spec's stale-chain refresh rather than a standalone rebuild (do not duplicate). Surfaced by the serve-l2-passive-candidate-fit final data gate W2 (2026-07-23). [fund_score: build_te_decomposition inputs / specs/queue/unify-te-decomposition-global-basis.md]
  **Owner summary:** One fund page can currently name two different "closest passive" ETFs — the tracking-error section still reflects last quarter's winner while the new alternatives table shows the current one. The already-planned tracking-error rebuild fixes this; it needs to land before both sections appear on the same page.
- [ ] (data) 8 intl/EM ETF style tags honest-null on the new passive-candidates table — 441/2,969 served winner cards (14.9%; 2,702/37,963 candidate rows) omit `style_tag`/`one_word_summary` for IEMG/IEFA/EFG/EFV/FDN/MCHI/GMF/EPP because `fund_taxonomy`/`etf_classification_signal` carry holdings-derived focus dims that are WRONG for these ETFs (IEFA/IEMG/MCHI → CONSDISC, EFV → HEALTH); the builder correctly omits rather than serve a wrong tag (foreign-sector lesson), but the right tags are knowably curatable (the module's curated mechanism already covers VWO/VEA) and the gap reads incoherent on-page (EAGMX: VWO tagged "Emerging-markets" beside untagged IEMG). Fix: extend the curated style map for the 8 (12 total affected) ETFs + root-fix the upstream taxonomy focus dims for intl/EM ETFs. Surfaced by the serve-l2-passive-candidate-fit final data gate W1 (2026-07-23). [fund_score: product/l2_passive_candidate_fit.py curated styles / fund_taxonomy focus dims]
  **Owner summary:** On the new "closest passive alternative" table, about one winner card in seven shows no one-word style tag — all international/emerging-market ETFs whose auto-derived style would be wrong, so we honestly show nothing. Hand-curating the correct tags for eight well-known ETFs (and fixing the upstream classification) closes the gap and removes the odd look of a tagged and an untagged ETF side by side.
- [ ] (fast-follow) Serve-l2 candidate-fit serving-boundary hardening — two codex `--high` P2 advisories from the shipped serve-l2-passive-candidate-fit (fund_score `71b5e4b`), nil realized impact today, latent at the next quarterly refit: (1) `_passive_baseline`'s fail-closed coherence gate compares winner TICKER == blend rank-1 only — if a refit advances and the rank-1 ETF happens to stay the same, a stale `l2_passive_candidate_fit` panel would merge silently with current `passive_alt_blend` (contradictory `selected_weight` vs `etf_weights`, outdated floor verdict); also validate the candidate panel's `asof_refit_date` against the served refit (`served_l2_refit_date()`) at the serving boundary. (2) candidate rows expose `expense_ratio_bps` but drop `expense_ratio_source` (only `selected_summary` carries it) — non-selected candidates can't be fee-basis-labeled in the UI even though the gold panel mixes filed_erh / fmp_reference per row; carry the source on every candidate row. [fund_score: serving/fact_assembler.py `_l2_candidate_fit_by_series` / `_passive_baseline`]
  **Owner summary:** Two small hardening notes from the independent reviewer on the just-shipped alternatives table: make the staleness check strict enough to catch an out-of-date sibling artifact after the next quarterly refresh, and carry the "where this fee number comes from" label on every row, not just the winner. Nothing is wrong today.
- [ ] (data) N-CEN adviser pick is order-dependent → wrong fund-family identity — `ncen/parser.py` (~lines 250–258) takes the FIRST `<investmentAdvisers>` entry; DREVX's N-CEN lists Newton NIMNA (the SUB-adviser per its own 485BPOS + BNY fact sheet) first and BNY Mellon Investment Adviser third with `<subAdvisers>` empty → DREVX serves family "Newton Investment Management North America, LLC / 1 scored fund / too small to rank" when it should be member 27 of the RANKED BNY Mellon Investment Adviser family (74/115, 26 funds, $27.6B); `has_sub_adviser=false` also wrong. Measured blast radius today: 1 fund, but the mechanism is systemic for any filer listing the primary adviser non-first — the fund-family spec's called-out fragmentation risk realized. Fix the pick (e.g. registrant-sibling coherence gate / prospectus cross-check) + rebuild fund_family_panel/members + fund_metadata adviser fields. Surfaced by the 2026-07-12 fund-family-flip data-quality-critic (P1). [fund_score: ncen/parser.py / fund_family]
  **Owner summary:** We identify a fund's "family" from its SEC filing but take whichever management firm is listed first — sometimes a sub-adviser rather than the real manager. One fund is currently shown in the wrong family with a false "too small to rank" message; the fix makes family identity trustworthy across the catalog.
- [ ] (data) Metadata identity hygiene from the dedup review — three small source fixes: (1) null stale `fund_effective_ticker` relic claims at source (MDIIX/MCLRX/FEIRX-class VA relics; 3 occupied tickers RYCWX/TMAAX/EKOCX have witnessed owners outside the universe); (2) real primary_tickers for the two Federated Hermes MDT ETFs registered with placeholder `TBD`; (3) backfill `fund_name` for raw-store-only arrivals with no frozen metadata row (HEQQ S000090211 + 1 serve NULL names — recoverable from their filings). (4) NEW 2026-07-17 (fresher-β dedup review): adjudicate ticker→series identity for tied LIVE claimants now EXCLUDED from the β universe — EGRAX (S000005287 Floating-Rate vs S000029759 Global Macro ARA; previously served under the latter), DFLVX (S000000961 vs S000000988), and payload placeholder tickers TBD + ZZZ (junk claims in value_offering_payload). class_ticker_mappings is FROZEN at 2024-12-31 so recency can't adjudicate — needs the raw N-PORT/prospectus claim check. Surfaced 2026-07-10. [fund_score: fund_effective_ticker / fund_metadata / identity backfill]
  **Owner summary:** Three small identity cleanups — stale ticker claims, two placeholder tickers, missing fund names — each causing one-off wrong or empty labels. Low effort, prevents embarrassing single-fund errors.
- [ ] (data) 6 incoherent expense triples — bad FILED total/net ER at SEC source for single-class ETFs (e.g. First Trust Natural Gas ETF total ER 0.0% vs 40bps mgmt; MILITIA L/S 10.9bps vs 130bps). Per-filing parser fix to drive the coherence-gate baseline toward 0. [fund_score: mfrr]
  **Owner summary:** Six ETFs have internally impossible fee figures in the SEC source itself (e.g. a total fee below the management fee). Per-filing parser fixes drive our fee-coherence baseline toward zero exceptions.
- [ ] (data) Tighten the gold price-quarantine to catch 2–3× adjustment rescales — the X-Ray's fail-honest guard (>200%/wk) is a magnitude backstop, not full recall; `build_fund_daily_adj_close.py`'s quarantine uses a ~50× regime threshold, so a ~2× (+150%/wk) bad-price rescale (BSCL-class terminated / near-zero-price tickers, ~35 in-window) still slips into the canonical panel. Root-fix the quarantine to catch sub-50× rescales at source. Surfaced by the 2026-07-16 X-Ray data-reviewer. [fund_score: build_fund_daily_adj_close.py quarantine]
  **Owner summary:** Our price-cleaning step catches only very large data glitches; a handful of funds still carry medium-sized bad prices. Tightening the cleaner removes them at the source, so no downstream feature has to guard against them.
- [ ] (data) **RE-GROUNDED 2026-07-17 — the named test no longer exists (Gate A rebuilt by ab5aa01; replacement coverage_gate tests 10/10 green and keep LSHEX-style books). The RESIDUAL is the data, not the test: real LSHEX is still suppressed `insufficient_coverage` (3 periods) on the Jul-15 rebuild — root-cause its pricing/coverage gap.** Original: return_attribution north-star `test_gate_a_keeps_north_stars_and_domestic_concentrated` FAILS: LSHEX/TPL real domestic concentrated bet is dropped. Confirmed PRE-EXISTING (failing on `main` before the 2026-07-13 work) and NOT sector-related — LSHEX is suppressed `insufficient_coverage`, and Gate A keys on PRICED coverage (Sharadar SEP), not sector labels. A fresh rebuild does not fix it, so it is a genuine pricing/coverage gap worth root-causing. [fund_score: return_attribution]
  **Owner summary:** One genuinely concentrated fund is still honestly suppressed from return attribution because of a pricing-coverage gap. The old failing test was replaced and is green — the remaining work is root-causing that fund's data, not fixing a test.
- [ ] (data) Value Score 62-fund replica gap — 62 EQ funds have ≥200wk of returns but no `replica_r2` computed in `l2_replica_quality`, so they fall to `not_comparable` in `value_score` and get no score. Run/expand the L2 replica fit (mandate pools) for them to recover ~62 scored funds. [fund_score: l2_replica_quality / value_score]
  **Owner summary:** 62 otherwise-eligible funds show no Value Score because one intermediate model never ran for them. Running it recovers scores for ~62 funds — pure coverage upside.
- [ ] (data) FCNTX-class class recovery — ~85% of class-null fee rows still unrecovered (funds with no class-bearing `(series,document)` precedent in the SEC bulk dataset stay null). Explore per-filing R-file parsing or a ticker-based map. [fund_score: alpha/expense.py]
  **Owner summary:** For ~85% of a fund cohort we can't tie fees to the specific share class a customer would buy, so those pages show less precise fee info. Deeper filing parsing could recover many of them.
- [ ] (data) **RE-GROUNDED 2026-07-17 — the periodic-refresh half is DONE (refit advanced to 2026-06-30, quarterly cadence + fail-closed resolvers shipped). Remaining: the min-history/young-fund policy decision + re-measure how many of the 377 now match at the new refit.** Original: Passive-match coverage: 377 young scorable funds + bond-matched funds have no equity benchmark — after the Class-A fix (2026-07-13) funds with no passive blend correctly show no vs_benchmark comparison, which makes an honest coverage gap visible: **377 SCORABLE active funds** (median ~1yr return history — AKRE Focus ETF, Argent Large Cap, ARGA EM Value, American Century Insights) aren't in the L2 solver (latest refit 2026-02-28) because they're too young; **34 funds** are matched to BOND ETFs (AGG/SHY/SGOV) so the equity look-through is empty; **1,564 are scorability=PASSIVE index funds** (IVE/NASDX/DMIDX) correctly excluded by design. Not a rebuild gap today — the young funds recover as they accumulate history + a solver re-run. Also filed by the data-reviewer: GSIB (S000081041) holds 28.95% US but is matched to an all-foreign passive → passive-match quality, not coverage. Also: `passive_alt_blend.parquet` is stale (built 2026-04-09) — refresh feeds fact_assembler `_passive_baseline` / l2_passive_candidate_fit. Track for a periodic passive-match refresh + a min-history/solver-cadence decision. [fund_score: L2 solver universe / passive-match cadence]
  **Owner summary:** With the fabricated benchmark removed, ~400 mostly-new active funds honestly show "no passive comparison yet" because they're too young to match reliably; they'll light up as they age. A periodic passive-match refresh (and a decision on how young a fund can be matched) would recover them sooner.
- [ ] (data) X-Ray does NOT look through third-party ETF wrappers, but `holdings_lookthrough_window` (feeding positioning_changes) DOES. The 2026-07-13 gate makes the X-Ray honest about it (a wrapper's weight is charged to the error bound, so MSTQ — sole holding QQQ — no longer claims "underweight Magnificent 7"), but the real fix is to look through the wrapper so a QQQ-heavy fund gets its TRUE sector/theme/stock exposure instead of a suppressed panel. `etf_holdings_snapshots` already exists for the passive side. Would restore exposure for the 446 funds with >25% wrapper books. [fund_score: exposure_xray / holdings_complete]
  **Owner summary:** Funds that mostly hold ETFs instead of stocks currently show a suppressed exposure panel because we don't look inside the wrapper. Looking through would restore honest sector/theme/stock exposure for ~450 funds.
- [ ] (story) Auto-derive the methodology registry's as-of/method-version values from the serving pipeline so they can't drift — third manual drift-fix in a month (facts.ts side-stepped the registry 2026-07-13 for this reason; the registry itself re-derived 2026-07-16 night run). Candidate shape: emit a `methodology_asofs.json` from build_serving_facts (per-artifact method_version + as-of maxima, same panels the reviewer verified) and have registry.ts import it, keeping prose strings hand-written but dates/versions generated. [fundscore-web: src/lib/methodology/registry.ts / fund_score: build_serving_facts]
  **Owner summary:** The trust page's dates have now drifted stale twice and been hand-fixed twice. This wires the dates to come straight from the data pipeline so the page can never quietly fall out of sync again — only the explanatory prose stays hand-written.
- [ ] (bug) ff6 factor-source refresh — **OWNER DECIDED 2026-07-23: automate with a fail-loud staleness gate.** Add a `fetch-factor-data` Make target and wire it into `refresh-current-features`/`refresh-features`: attempt the Ken French download on each refresh (monthly-idempotent); if the source is unreachable, proceed only when on-disk `ff6_weekly`/`ff6_daily` are <45 days old, else FAIL the refresh visibly — never silently rebuild on stale factors. Original: Factor refresh loop never re-fetches the ff6 source (factor-alpha re-lags) — OWNER DECISION — `refresh-current-features`/`refresh-features` rebuild `build-factor-alpha` but there is **no `fetch-factor-data` Make target and no fetch step**, so the loop rebuilds factor-alpha from whatever `ff6_weekly` is already on disk. Even with the parser now fixed (87bdd3a), the automated refresh will perpetually rebuild the factor leg from a frozen ff6 and re-lag unless someone runs `scripts/pipeline/fetch_factor_data.py` by hand first. `fetch_factor_data.py` is currently a deliberate manual step (Ken French publishes ~monthly, and wiring a network download into every incremental price refresh adds a fragile external dependency). Decide: add a `fetch-factor-data` target + step into the refresh compound (auto-refresh, monthly-idempotent), or keep it manual and document the two-step "fetch factors, then refresh" runbook. Surfaced 2026-07-12 during the Ken French parser fix. [fund_score: Makefile refresh-current-features / fetch_factor_data.py]
  **Owner summary:** The parser that keeps our style-factor data current is fixed, but the automated refresh routine never actually re-downloads that source — so the layer will drift stale again on its own. Small decision: wire the download into the routine, or keep it a deliberate monthly manual step with a clear runbook. **Decided 2026-07-23: the download joins the automated refresh with a loud failure mode — if the source can't be fetched and our copy is older than ~45 days, the refresh stops instead of quietly rebuilding on stale data.**
- [ ] (chore) Pricing-store hygiene — **(2) DONE 2026-07-17: canonical-price entry added to the data dictionary (on fix/benchmark-nav-dedup-rebuild), incl. the duplicate-vintage hazard with the Jul-16 NAV corruption as the cautionary case.** Remaining: (1) the append-only `data/vendors/tiingo/daily_pricing/` store grows unboundedly (205M rows / 3,993 files / 86.9M duplicate re-fetch vintages); add a periodic in-store compaction — NOW HIGHER PRIORITY: the duplicate vintages are no longer a latent cost, they actively corrupted the Jul-16 passive-NAV rebuild via an un-deduped read; compaction removes the hazard class at source. Surfaced 2026-07-16, re-grounded 07-17. [fund_score: daily_pricing store]
  **Owner summary:** Two small maintenance items: the raw price folder keeps growing because we never prune old re-downloads, and we should officially document which cleaned price file everything is supposed to read from.
- [ ] (data) Holdings-direction vs active-β sign disagreement needs adjudication before retail launch — FCNTX renders a famous Technology UNDERWEIGHT on the holdings basis (25.9% held vs IWF 53.8%; MSFT −6.0pp, AAPL −8.6pp) while the attribution basis shows a POSITIVE avg active tech β (+0.146, per-quarter path peaks +0.38 in 2023–24) adding +461 bps/yr — two readings of the same exposure pointing opposite ways on one page with nothing reconciling them (DODGX us_megabanks shows the same held−3.1pp vs β+0.11 pattern, though there the TE table's t agrees). Baselines legitimately differ (joint-WLS return regression vs L2 blend vs holdings pp vs IWF) so not provably wrong; needs a data-scientist pass + an on-page basis-reconciliation note ("held weight and exposure β can disagree in sign — why") — related magnitude double-take: gross legs ±1800 bps/yr nearly canceling on a 4.8%TE fund, colliding with the +110 bps/yr gross-selection skill read (the known idio_alpha_bps tension). Surfaced by the 2026-07-12 attribution-flip data-quality-critic (P2). [fund_score: exposure_path_attribution basis / fundscore-web: basis note; data-scientist]
  **Owner summary:** Two sections of the fund page can describe the same bet in opposite directions — the portfolio shows a famous underweight to Technology while our return-based model says the fund behaves as if overweight, and nothing on the page reconciles them. Both measurements are defensible, but to a customer this reads as a contradiction; needs a research pass and a plain-English note before launch.

**Owner decisions.** Blocked on an owner call, not engineering — each is a paragraph of judgment that unblocks already-filed work. Once decided, re-file the resulting work into the Working set or the campaign. (Empty right now — both 2026-07-22 decisions were made and re-filed; new decision items go here.)

**Refresh campaign (one supervised arc — not lean-loop items).** The stale-producer / coherent-rebuild debt, worked as ONE campaign: land the VO-emitter branch first (screener-rebuild prerequisite), refresh the frozen evidence producers and ingest gaps, rebuild the dependent chain coherently, then close with the terminal reviewed panel-refresh pass and the owner-gated serving reload. Worked piecemeal, every member re-pays the reviewed-rebuild tax these items share; most were filed with "fold into the panel-refresh pass" notes already.
- [ ] (data) NAV panel serves dead-class tickers — ROOT-CAUSED 2026-07-10 (was: "NAV feed cuts off ~2025-04-30"; investigation in night-run-20260709.md): NOT an ingestion bug — Tiingo 200s with genuinely-ended series. The 317 frozen tickers in passive_alt_daily_nav = **284 dead series** (Putnam→Franklin mergers/closures — honest NAV end) + **23 retired share-class tickers** whose funds live under CURRENT tickers already fresh in the vendor store + **6 degraded classes** with fresh sibling-class coverage + **2 TRUE vendor gaps** (JMVNX, JCAVX: single-class, alive 2026 N-PORT, Tiingo stopped) + 1 recovered by the 2026-07-10 refresh + 2 unmapped. REMAINING FIX (UNBLOCKED — the Tiingo refresh completed 2026-07-12 17:42, store fresh through 2026-07-10): rebuild fund_daily_adj_close + the passive-match NAV pairing on the CURRENT-ticker basis (series→best-covered class ticker; use the 8993b29 deduped universe + class mappings — a per-series ticker can die while the series lives), then re-measure the profile-nav recovery (the original "~123 recoverable" premise mixes honest-dead with class-basis losses). JMVNX/JCAVX → alternate source (N-PORT monthly NAV) or honest null. Sibling: passive-blend micro-gaps item below shares the rebuild. [fund_score: passive-match / build_fund_adj_close ticker basis] [model: fable, effort: xhigh]
  **Owner summary:** Root-caused: most "stale price" funds are genuinely dead or renamed share classes, not a data bug. The fix is to rebuild price histories on funds' current tickers — recovering growth charts for live funds and stopping us from chasing dead ones.
- [ ] (data) Passive-blend micro-gaps fragment live-fund NAV tails — `passive_alt_daily_nav` drops 1–2 month runs of `benchmark_nav` for funds that ARE continuously priced in `fund_daily_adj_close` those same months, right before the series end (e.g. LSLTX: paired run 2002-12..2025-01, gap 2025-02/03, then a 1-month 2025-04 stub; MRTHX: 2017-01..2025-02, gap 2025-03, 2025-04 stub). 122/123 profile-nav "recoverable" funds show this fund-alive-in-gap pattern; the tiny post-gap stub then trips the MIN_MONTHS length filter and nulls the whole fund. Root-cause the passive-match/`benchmark_nav` build's month gaps for continuously-priced funds (a continuous blend would keep these funds served without any trim heuristics). Surfaced 2026-07-04 by the profile-nav-series coverage review. [fund_score: passive-match / build_benchmark_nav → passive_alt_daily_nav]
  **Owner summary:** Short gaps in our reconstructed index-blend prices cause ~120 live funds' growth charts to be suppressed entirely. Fixing the gap-handling at the source recovers those charts without lowering our no-fabrication standard.
- [ ] (data) SPY + DBC ingested — rebuild fund_metadata 3Y risk fields to light up benchmark-relative stats for 1,477 funds — RAW PRICES LANDED 2026-07-12 22:25 (SPY 6,669 rows 2000-01-03→2026-07-10; DBC 5,138 rows 2006-02-06→, via fetch_tiingo_pricing --ticker-list-override; store now uniformly fresh incl. SHY→2026-07-10 so the rf zero-fill latent is NOT armed). REMAINING (gated fix-data loop): rebuild build_gold_metadata risk fields (beta_3y/alpha/TE/IR/captures populate for the 1,430 SPY + 47 DBC funds incl. FCNTX), /check-data + data-reviewer on the new relative stats, correct the risk-behavior registry limitation wording (fundscore-web), then serving rebuild+reload picks it up. Original evidence: 2026-07-12 riskBehavior-flip DQ-critic (P1). [fund_score: build_gold_metadata / fundscore-web: registry]
  **Owner summary:** The missing S&P 500 benchmark prices are now loaded — done today. One reviewed rebuild remains to actually light up beta/tracking-error/capture stats for ~1,500 funds on the new risk panel, including our flagship example fund.
- [ ] (data) Member AUM stamps one N-CEN cycle stale (recoverable) — FCNTX/DREVX `aum_as_of_date` = 2024-12-31 while both registrants filed FY2025 N-CENs on 2026-03-12 (EDGAR-verified, not ingested; external FCNTX AUM $145.9B vs our $140.6B). Page discloses the range honestly; ingest the FY2025 N-CEN cycle + rebuild metadata AUM. Likely same root as the frozen universe-evidence producers item (sibling). Surfaced by the 2026-07-12 fund-family-flip data-quality-critic (P3). [fund_score: ncen ingest / fund_metadata AUM]
  **Owner summary:** Fund size figures are one annual filing cycle behind — the 2025 filings exist but aren't ingested (we show $140.6B for Contrafund vs ~$146B current). Cosmetic today and disclosed on-page, but easy to bring current.
- [ ] (data) **RE-GROUNDED 2026-07-30 (item 9): premise was WRONG — 3 of the 4 legacy outputs ARE consumed, and the producer is GONE (not re-runnable).** `fees`/`fund_metadata`/`tickers`/`turnover` were last produced 2025Q4 by an extraction path no longer in the codebase (current `mfrr_pipeline.py` emits only expense_ratios/fee_components/benchmarks/narratives per the manifest; no `extract_tickers` in code or git history; the ticker isn't even in the standard SEC MFRR TSVs). Consumer audit (corrected — the original "no consumers" was a literal-path grep that missed `build_gold_metadata.py`'s `load_*` helpers; codex P1 caught it): **`turnover`→`load_turnover`→`portfolio_turnover`; `tickers`→`load_tickers`→`mfrr_ticker` (coalesced BEFORE nport_ticker for `primary_ticker`) + alpha/expense.py; `fund_metadata`→`load_inception_dates`+`load_mfrr_identity`→`inception_date`/company name+CIK — all feed `gold/fund_metadata.parquet`. Only `fees` is genuinely unconsumed** (`load_fees` reads `fee_components`, the current panel, NOT this dir). DONE 2026-07-30: `fees` retired from `audit_data_freshness.MFRR_QUARTER_DIRS`; the other 3 KEPT in the audit (their 2025Q4 staleness is real, must not be silenced). **REMAINING (this item): re-source `tickers`/`fund_metadata`/`turnover` in `build_gold_metadata` from CURRENT feeds — N-PORT `class_ticker_mappings` (already fresh 2026-07-01) for tickers, N-CEN for inception/company identity, a current turnover source (N-PORT? TBD) — removing the frozen-MFRR-legacy dependency; then all four are fully retire-able. Impact of the current staleness is LOW (slow-changing fields: tickers stable, turnover ~annual, inception/identity static) but real.** [fund_score: build_gold_metadata load_tickers/load_turnover/load_inception_dates/load_mfrr_identity → current N-PORT/N-CEN feeds]
  **Owner summary:** Correcting an earlier mistake: three of these four datasets (fund tickers, turnover, and fund identity/inception) DO still feed our core fund-metadata table — they're just frozen at last-year's-Q4 with no way to rebuild them the old way. Only the fourth ("fees") was truly unused and is now retired. The real fix is to re-point those three at data we already keep current (N-PORT tickers, N-CEN identity) so they stop drifting stale; impact today is small because these fields barely change.
- [ ] (data) Manager-people gold is stale vs the serving universe — `manager_people_assignments` still contains adjudicated-dead series (MAVFX, TANX…) that source_inventory correctly excludes; `test_gold_universe_matches_source_inventory` red (pre-existing; inputs Jul-15, independent of the 07-17 restore). Also pre-existing reds worth one look in the same pass: `test_openfigi::test_resolve_batch_splits` (pure unit, batch-split logic) and the tracked the_take↔takeaways drift pair. Rebuild/trim manager gold against the current scope. [fund_score: manager_people / source_inventory]
  **Owner summary:** The fund-manager roster table still lists a handful of funds that have shut down, which trips one of our own consistency tests. A small refresh brings it back in line with the catalog we actually serve.
- [ ] (data) exposure_path active-universe lags fund_metadata — 22 funds with fresh 2026 holdings are excluded from `holdings_exposure_path` because `eq_active_universe()` → `active_retail_universe()` reads **unrefreshed** `fund_metadata`; rebuilding at a later as-of shifts the recency window and drops them even though their holdings are current (net −47 exposure_path series, 36 genuinely dormant + 22 metadata-lagged). Refresh `fund_metadata` alongside holdings, or have the active universe consider holdings recency. Surfaced 2026-07-03 by the NPORT-refresh sweep. [fund_score: build_exposure_path_attribution.eq_active_universe / universe]
  **Owner summary:** 22 funds with current portfolios get dropped from the trend analysis because a staleness check reads outdated reference data. Fixing the check recovers them.
- [ ] (data) `fund_taxonomy.parquet` (Jun 20) still reads the PRE-rollup `cusip_reference` sector as a classification SIGNAL. Not a served sector (so it is not a second sector truth in the UI), but the 4D classification could shift marginally for funds with material Travel-Services / Building-Products / Uranium weight. A refresh needs the full `run-taxonomy` LLM pipeline — deliberately NOT run inside the 2026-07-13 change. [fund_score: taxonomy]
  **Owner summary:** The fund-classification table still reads pre-correction sector data as one input signal. A refresh needs the full LLM classification pipeline, so it's tracked separately rather than folded into the quick sector fix.
- [ ] (data) **RE-GROUNDED 2026-07-17 — most of the cascade HAS been rebuilt (return_attribution + holdings_exposure_path + fund_alternatives Jul-15; exposure_divergence Jul-17). Still stale: theme_bet_attribution (Jun-25), fund_the_take + fund_takeaways (Jul-11, pre-relabel), fund_taxonomy (Jun-20).** Original: Downstream cascade still stale after the 2026-07-13 sector/coverage rebuild: `holdings_exposure_path` (Jul 7), theme_bet_attribution, return_attribution, exposure_divergence, alternatives, the_take. Several of their tests were ALREADY failing on `main` before that work (pre-existing staleness debt), but the sector relabel makes them formally incoherent with the rebuilt X-Ray. Rebuilt in that change: cusip_reference → exposure_xray → risk_decomposition → value_offering_reframed → positioning_changes → serving_facts. [fund_score: panel refresh]
  **Owner summary:** A few analytics tables are still computed from inputs that predate this month's sector fixes (theme attribution, the fund summaries pair, taxonomy) — internally consistent but stale. Rebuilding them brings every downstream number onto the corrected inputs.
- [ ] (data) fund_the_take ↔ fund_takeaways citation drift — re-measured 2026-07-17: now 183/1,431 (was 269) SERVED Take rows cite takeaway rows that no longer exist (invariants 3+9 fail; report was stale 25/25 evidence from the 2026-06-23 ship until regenerated 2026-07-11). Pre-existing: proven identical on pre-fix backup images; a later run rebuilt fund_takeaways without fund_the_take. Served impact: up to 269 funds' Take sentences may quote numerics from retired takeaways. Rebuild the pair coherently as its own gated fix or fold into the panel-refresh pass (sibling item). [fund_score: build_fund_takeaways.py + build_fund_the_take.py]
  **Owner summary:** Up to 269 fund summaries may quote supporting facts that have since been regenerated, so a summary sentence and its cited evidence can disagree. Rebuilding the two together restores the guarantee that every claim traces to live evidence.
- [ ] (data) Reviewed panel-refresh pass — gold panels no longer reproduce at their pinned as-ofs (input-state moved: 2026-07-10 dedup universe 5,799→5,706 et al.). Measured 2026-07-11 during the label fix: return_attribution −146k rows (−1.8%), exposure_xray 75 target rows + ~4.8k value cells, positioning change_z shifted on ~72k/78k rows, security_characteristics −3,752 rows, value_offering named-bet mass ~5k rows. Shipped panels are coherent but frozen pre-dedup. Refresh ALL panels together as a REVIEWED pass (numbers move, owner-visible), incl. the takeaways+the_take coherent pair rebuild (sibling item) and the mixed-state risk_model catalog internals (attribute_def/model_factor/target_membership regenerated 2026-07-11 without pre-images — converge here). Owner option to fold in: rename "EM ex-Japan" → "Broad EM" (data-reviewer advisory: Japan isn't EM, qualifier is vacuous; same migration pattern). **MUST fold in (2026-07-13): the exposure_xray Class-A zero-fill fix (eed1b00) already landed in gold, but its consumers on disk were built from the PRE-fix exposure_xray and still carry fabricated tilts — `value_offering_reframed_panel.named_bets` for 337 SERVED (status=scored) no-blend funds (+1,217 unsupported), plus risk_decomposition (33 rows), theme_bet_attribution/rollup (65/22), holdings_exposure_path (3,489 rows). Rebuild these from the fixed exposure_xray in this pass so the propagated fabrication clears before the manifest-35 reload.** Evidence: fund_score docs/status/data_gap_analysis.md 2026-07-11 + 2026-07-13 sections. Terminal step of the refresh queue: after the upstream refreshes above land, this pass + a serving rebuild/reload (manifest 35, owner-gated) put everything in front of customers. [fund_score: panel builders / risk_model catalog]
  **Owner summary:** Our published analytics were computed on a slightly older fund universe, so rebuilding anything today produces small unexplained shifts. One supervised, all-at-once refresh brings every analytic onto the current universe consistently — numbers will visibly move, so the owner reviews it.

**Fail-honest hardening sweep (batch as one session).** Latent defects with verified-nil live impact today — determinism, guards, null-vs-zero semantics. One focused pass with a single review; no served number should move, which is itself the acceptance check.
- [ ] (data) SWEEP: `unique(keep="first")` with NO explicit sort is a latent nondeterminism bug CLASS in this repo. polars' `unique()` is hash/parallel — "first" is only stable for byte-identical input, so the winner flips between rebuilds. Root case (fixed 2026-07-13): `sharadar.build_cusip_sector_map` wrote `cusip_sector_map.parquet` with a DIFFERENT ROW ORDER on every rebuild (6 orderings in 6 runs from identical input); `cusip_mapping._prefix_match` inherited it and silently reassigned the TICKER for ~3,000–3,800 prefix-matched CUSIPs per rebuild, with no code or data change. Fixed at both layers + `sharadar.py`'s ticker dedup (whose "prefer SEP over SFP" comment was never actually guaranteed) + both cluster-dedups (`value_offering_reframed.named_bets_from_panels`, `risk_decomposition.select_candidates`). **Remaining: grep the whole repo for `unique(` without `maintain_order=True` and audit each one that PICKS A WINNER (`keep=`/subset-based) — any such site is irreproducible.** [fund_score] NEW EVIDENCE 2026-07-17 (payload-restore data-reviewer W3): `build_holdings_complete.py`'s enrichment dedup (`unique(keep="first")` over an unordered DuckDB pull) re-bound isin/cusip/name/sector/lei on 307 series between two same-input runs (weights/membership stable; did not reach serving — staging md5-identical). Live confirmed instance for this sweep.
  **Owner summary:** A code pattern used in several places can silently pick a different "winner" row on every rebuild, making outputs unreproducible — one live instance re-bound identity fields for 307 funds between two identical runs. A one-pass audit fixes the whole class, not just the instances we've caught.
- [ ] (bug) build_gold_metadata rf leg silently zero-fills — `_load_risk_metrics` does `rf_return.fill_null(0.0)` (build_gold_metadata.py:1648), so months where SHY has no return get rf=0 instead of being dropped; the vendor store demonstrably runs mixed vintages (fund NAVs 2026-07-10 while SHY sat at 2026-05-08 mid-refresh), so a rebuild in that state would zero-fill trailing rf months and silently flatter Sharpe/Sortino. Fail-honest: drop unmatched months or gate the build on rf coverage. Latent today (shipped build predates the drift). Surfaced by the 2026-07-12 riskBehavior-flip data-quality-critic (P2). [fund_score: build_gold_metadata._load_risk_metrics]
  **Owner summary:** A latent calculation bug: if our price data were partially refreshed, the risk-adjusted-return math would quietly treat missing Treasury yields as zero and flatter every fund's Sharpe ratio. Nothing wrong is live today; we're closing the hole before a refresh can trigger it.
- [ ] (bug) Coherence guard missing on the holdings_complete sibling path — the misfiled-pct_nav guard (fund_score f7ec601) covers `holdings_snapshots` (→ positioning, top_holdings) but NOT `holdings_complete` (→ exposure_xray), a parallel N-PORT read. holdings_complete is currently clean (0 pct_nav<0, 0 >1.5, max 1.03) so nothing leaks today, but the same class of corrupt *latest* filing would reach exposure_xray unguarded. Port the identical `[0,1.5]` null guard to `build_holdings_complete.py` for symmetry (no current data change expected). Surfaced by the pct_nav-guard data-reviewer 2026-07-08. [fund_score: build_holdings_complete.py]
  **Owner summary:** A data-corruption guard was added to one of two parallel ingestion paths; the second is currently clean but unguarded. Porting the same guard means one bad SEC filing can't reach the portfolio X-ray unfiltered.
- [ ] (bug) Positioning aggregation turns a quarantined null pct_nav into 0 (false 0% / false exit) — [codex --high P2 from f7ec601] Polars `sum()` returns 0.0 for an all-null group, and `build_positioning_changes_panel.build_positions` pivots with `aggregate_function="sum"` before treating null/zero as absence, so a quarantined holding that is the ONLY value in a ticker/sector/theme/top-10 bucket at an endpoint reports 0% weight (or a false exit) instead of degraded/missing. Verified **zero served impact today** (only the dormant/unserved S000030654 has an all-null latest quarter; no served fund's corrupt quarter lands on a positioning endpoint). Harden positioning null-vs-zero semantics so an all-null bucket surfaces as missing/degraded, not 0. [fund_score: build_positioning_changes_panel.py]
  **Owner summary:** A latent bug where a quarantined bad data point could display as "position went to 0%" — a false exit story — instead of "data unavailable". No live fund is affected; fixing the handling prevents a future false narrative.
- [ ] (bug) build_gold_metadata fee circular-dep on clean rebuild [codex P2] — `_resolve_primary_class`/`load_fees` read `data/gold/fund_metadata.parquet`, the file the script is about to write, so a first clean `build-gold`/`full-rebuild` returns empty primary classes → all-null management fees (and gross-fee path) until a second pass. Derive primary classes from the in-memory metadata/ticker inputs for the current build instead of the prior output. [fund_score: build_gold_metadata.py]
  **Owner summary:** A from-scratch rebuild of our metadata would produce empty management-fee fields on the first pass because the build reads its own prior output. Only affects clean rebuilds — but those are exactly the moments we can least afford silent gaps.
- [ ] (bug) fact_assembler rank tie-break is unstable across identical-input runs — return_attribution section `rank_within_dimension` swaps tied rows between assembly runs on byte-identical gold (S000000137: mag_7 ↔ ev_battery_chain at ranks 7/8, bit-identical contributions). Cosmetic today; makes staging non-reproducible byte-for-byte and can flap served ordering on reloads. Add a deterministic secondary sort key (member_id) to the rank ordering. Surfaced by the 2026-07-11 label-fix identity verification. [fund_score: serving/fact_assembler.py]
  **Owner summary:** A reproducibility nit: tied rows can swap order between two identical builds, which makes our byte-for-byte verification noisy. One deterministic sort key fixes it; customers see at most flickering order of tied rows.
- [ ] (chore) Pipeline-state hygiene 2026-08-06 — three rot spots from the stock-take audit: (1) three queue specs (`exposure-screener`, `portfolio-exposure-parity`, `serve-full-exposure-panel`) cite `source_proposal: proposals/approved/homepage-promise.md`, which never existed in git — fix the pointers (likely intended: `docs/product/page_specs/home.md`) or reconstruct the proposal; (2) `prds/fee-fairness-peer-band.md` status still reads "awaiting red-team + owner resolution" though both derived specs shipped — mark RESOLVED; (3) `specs/rejected/plain-english-verdict-and-jargon-anchors.md` frontmatter still says `status: queued`. [feature-pipeline]
  **Owner summary:** Three bookkeeping fixes in the work-tracking files themselves — a broken provenance pointer and two stale status lines that would confuse future automation. No product impact.

**Deploy — UN-PAUSED 2026-08-06 (beta-critical).** The public site stays gated (landing + waitlist only; `LAUNCHED=false`). Of the three items that blocked opening the Portfolio X-Ray to a real user, two shipped (canonical-panel L2 refit + quarterly cadence; SPY/UIT expense ratios — see archive); the solver HTTP service below is the third. Owner chose a FULL-EXPERIENCE beta (2026-08-06) including the X-Ray, so this is back on the critical path. The PROD serving reload happens at the gated X-Ray launch.
- [ ] (story) **UN-PAUSED by owner 2026-08-06 (full-experience beta); PRD RESOLVED same day (all 4 questions: cheapest-of-Fly.io/Railway; owner verifies vendor licenses BEFORE any image push; compaction de-scope confirmed; freshness = fund-page cadence with an AUTOMATED as-of deploy gate) — ready to spec, spec-writer dispatched 2026-08-06.** Run the passive solver as an HTTP service so the app can deploy. (Still the eventual prerequisite for the public X-Ray; the PRD + 4 questions stand for when it resumes.) — was AWAITING OWNER: 4 open questions in `feature-pipeline/prds/solver-http-service.md` (vendor+cost, Sharadar/Tiingo license-into-container, confirm compaction de-scope, between-refit freshness stance). PRD red-teamed + revised 2026-07-16 night run; facts corrected therein: real input footprint ~690 MB across NINE files (not 2.2 GB — raw store no longer read post-d92213c) and solves are ~1–4 s, so sync HTTP needs no queue and compaction is no longer a prerequisite. `runSolver()` shells out with `spawn(uv, ["run","python",...], { cwd: FUND_SCORE_REPO })` — it needs the fund_score checkout, uv, Python, CVXPY and the pricing panel on local disk. That cannot run on Vercel (no Python runtime, no persistent filesystem, execution caps well under the current 170–220s). Everything ELSE in the app is a clean Vercel fit (RSC pages over Postgres). Fix: stand the solver up as a small always-on service (vendor = owner Q1 in the PRD; ~690 MB of inputs across nine files, verified — see PRD) exposing `POST /solve`, and replace `spawn()` with `fetch()` behind the same `SolveResponse` contract so nothing downstream changes. Compaction-first prerequisite RETIRED (superseded by the 2026-07-16 solver fix — warm solves ~1–4 s, no job queue needed; owner confirmation is PRD Q3). [fundscore-web: src/lib/serving/portfolio-solver.ts]
  **Owner summary:** The X-Ray currently only works on a laptop, because it launches a Python program that reads files off the local disk. To put it on the internet it needs to become its own small always-on service that the website calls over the network. Worth doing straight after the speed fix, since a fast solver can answer in a normal web request and a slow one would need a whole queueing system around it.

**Beta-launch group (filed 2026-08-06).** The launch thread the backlog lacked — everything between "local serving is coherent" and "a beta user on fundscore.ai sees the full experience." Owner scope decision 2026-08-06: FULL experience (fund pages + screener + X-Ray). Sequencing: prod data load + screener port start once the local reload verifies; the solver service is the un-paused Deploy item above; beta ops lands before any invite. The V4 page itself is driven by `specs/queue/profile-v2-production-cutover.md` (re-grounded on V4 2026-08-06), not a backlog item here.
- [ ] (story) Prod serving-data load + beta runbook — prod Supabase (`henxcsknsjfadetomjeu`) has ONLY `waitlist_signups`+`early_access`; the beta needs the four serving tables (`fund_profile_facts`, `fund_holdings_full`, `fund_attribution_blocks`, `serving_manifest`) loaded from the SAME verified staging parquet the local reload uses (TRUNCATE+COPY, one transaction, in-transaction coherence checks), plus env wiring (`LAUNCHED` stays false; access via `early_access` grants) and a WRITTEN prod-reload runbook — the serving-db-ahead-of-branches rules apply to prod doubly (never load from a branch missing another feature's emitters). ALSO loads the PREVIEW Supabase (`yqyyvhcrmcwarxweusbw`) with the same verified tables + a gate-passing test user: the solver-http-service spec's end-to-end acceptance (AC3) runs against preview, so the preview load is its external prerequisite (flagged in that spec 2026-08-06). [fundscore-web: docs/DEPLOYMENT.md + scripts / fund_score: serving load target]
  **Owner summary:** Production's database has zero fund data — a beta user literally cannot see a fund page until we copy the verified serving tables up. This files the missing "ship the data to production" step with the same one-transaction discipline the local reload uses, plus a written runbook so future prod reloads aren't improvised.
- [ ] (story) Screener beta port — `/screener` and `/q/[slug]` read DuckDB over LOCAL parquet (`src/lib/serving/screener.ts`, `QUERY_PARQUET_DIR` defaulting into the local fund_score checkout) — they cannot run on Vercel. Decide the approach (serve the query surface from Postgres like the profiles, vs deployable parquet storage) and port. In-scope for beta per the 2026-08-06 full-experience decision. [fundscore-web: src/lib/serving/screener.ts / fund_score: query panel emit]
  **Owner summary:** The screener works only on your laptop today — it reads analysis files straight off local disk. A full-experience beta needs it served properly in production; this decides between porting it to the database or shipping the files to cloud storage, then does the port.
- [ ] (story) Beta ops minimum — error monitoring, a user feedback channel, and basic page analytics wired BEFORE invites go out, so beta users' problems reach the owner instead of dying silently. Scope deliberately small: one error tracker, one feedback affordance (even a mailto/form), one pageview counter. [fundscore-web]
  **Owner summary:** Before we invite strangers, we need to see what they see: error tracking, a way for them to tell us what's broken or confusing, and basic usage numbers. Small setup, but without it the beta teaches us nothing.
- [ ] (story) Per-stock receipts backend for V4 movement 05 ("manager & their names") — V4's receipts table (per-stock holding-period receipts: filed holdings × prices, "the picking cleared the fee") has NO backend spec; the cutover spec's sub-blocks `top10VsIwf`/`positioningBetBridges` are fixture-only with no serving source. Spec + build the per-stock panel (honest gating for foreign-heavy books, same coverage doctrine as everything else) — OR the cutover descopes the receipts sub-block for beta (owner decision, surfaced in the re-grounded cutover spec's batch-3 list). [fund_score: per-stock receipts panel + serving / feature-pipeline: new spec]
  **Owner summary:** V4's most persuasive section — the per-stock receipts proving the manager's picks did or didn't cover the fee — has no data pipeline behind it yet. Either we build that panel before cutover or the beta page ships the section without receipts; this forces the choice and does the work if it's a build.

**Value-prop delivery group (filed 2026-07-17).** These deliver homepage promises (`docs/product/page_specs/home.md`) that the already-queued exposure specs (`serve-full-exposure-panel`, `portfolio-exposure-parity`, `exposure-screener`) do NOT cover. Confirmed gaps via a coverage-matrix pass — none duplicate a queued spec. NOT launch blockers; they build out the value prop after the gated launch, so they sit at the bottom of Open.
- [ ] (story) Portfolio X-Ray — cross-fund overlap & "cancel each other out" — make the hero promise literal: show which funds in a book duplicate each other (two funds holding the same names; an active fund that mirrors an ETF you also own). Pairwise holdings/factor overlap + a "do these funds diversify or cancel each other out" readout, on top of the aggregate concentration (top10/HHI/effective-N) that `portfolio-exposure-parity` already adds. NOT covered by any queued spec — parity does aggregate concentration only, not fund-vs-fund overlap. Spec'd but deferred: `docs/product/page_specs/portfolio_xray.md` §6. [fundscore-web + fund_score: portfolio look-through / overlap]
  **Owner summary:** The homepage's whole thesis — "you might own the AI trade seven times and call it diversification" — needs the X-Ray to actually show which of your funds overlap each other. We total up concentration today, but we don't yet show the fund-vs-fund duplication that makes "the same trade in seven wrappers" concrete and personal. This builds it.
- [ ] (story) Portfolio-aware screening — "funds that diversify your current holdings instead of duplicating them" — screen the universe against the user's own X-ray'd book (low overlap / complementary exposure to what they already hold), not just absolute or vs-passive exposure. Homepage screener promise + the deferred `portfolio_xray.md` §5 inline "find funds with less [X] for your portfolio" CTA. Depends on `exposure-screener` + the cross-fund overlap work above. [fundscore-web + fund_score: screener + portfolio context]
  **Owner summary:** Our screener searches the whole universe by exposure. The homepage also promises "funds that diversify what you already own," which needs the screen to know your portfolio and rank for what complements it — not just what matches a filter. It's the feature that connects the X-Ray and the screener into one loop.
- [ ] (story) Screener predicate — "active managers with broad-based security-selection value" — let the screener filter/rank on the security-selection-skill signal (idio / selection evidence), which ships as a fund-page feature but is not yet a screenable dimension. Homepage screener example NOT covered by `exposure-screener` (that spec is exposure / vs-passive only). Must stay backward-looking evidence, never a forecast or ranking-as-advice. [fund_score: expose selection-skill as a screen dimension / fundscore-web: screener]
  **Owner summary:** One of the homepage's screener examples asks for manager skill, not exposure — "active managers with broad-based selection value." We already show that evidence on a fund page, but you can't search on it yet. This makes it a searchable dimension, carefully framed as historical evidence rather than a prediction.
- [ ] (story) Fund attribution — "how much of the excess came from one bet" — a single-bet excess-return concentration readout ("N% of this fund's outperformance traced to semiconductor exposure, not broad selection"), the homepage's "did the manager earn it?" payoff. The single-fund TE / return decomposition already ships (`specs/done/te-decomposition-by-bet.md`, `attribution-factor-path-serving.md`); this is the concentration framing on top — what share of EXCESS RETURN (correctly labeled, not TE) traces to the top one/few bets vs broad selection. No forecast. [fund_score: return_attribution framing / fundscore-web: profile]
  **Owner summary:** The sharpest line on the homepage — "the manager outperformed, but 95% of it came from one sector, not skill" — is a framing we can almost tell but don't yet. The decomposition numbers exist; what's missing is the one-line "how concentrated was the outperformance in a single bet" readout on top of them. Small feature, but it's the punchline of the active-management story.
- [ ] (story) Monitoring & Alerts — the homepage's "know when the investment changes": notify a user when a fund they follow drifts from the role they expected it to play (positioning / exposure / factor / manager change past a threshold). NET-NEW experience — no route, spec, or prior backlog item exists anywhere. Reuses the shipped positioning-changes / exposure panels as the drift signal; new surface = a watch list + thresholds + a delivery channel. PRD `feature-pipeline/prds/monitoring-and-alerts.md` — **OWNER-DECIDED (all 7) 2026-07-27, ready to spec**: baseline = fund's own recent history; dimensions = sector/theme + manager + fee hike + factor/concentration/turnover; noise = quiet ~1/qtr/fund; scope = fund-by-fund explicit Follow; delivery = in-app feed + email (both from v1); tier = paid-only (whole surface). Spec notes: (1) owner included the noisier factor/concentration/turnover dims AND chose quiet — needs per-dimension threshold calibration; (2) email requires consent/unsubscribe/cadence scope. [fundscore-web + fund_score: new surface] [model: opus, effort: high]
  **Owner summary:** The homepage promises we'll tell you when a fund quietly turns into a different investment. Nothing like this exists yet — it's a whole new surface (a watchlist plus alerts on positioning drift), built on the change-detection we already compute per fund. All the product calls are now made (2026-07-27): follow individual funds, get a quiet ~once-a-quarter alert when one drifts, delivered in-app and by email, as a paid-only feature. Ready to write the spec.
- [ ] (story) Crescent v2 productionization — backend follow-ons for the five-block Fund Profile shipped on the v2 preview 2026-07-21 (web branch `feature/crescent-profile-v2`; concept `fund_score/docs/product/strategy/crescent_signature_artifacts.md`, status BUILDING). The page runs on served data EXCEPT the Archetype, which is a fixture (real A2 prototype outputs, 7 exemplar tickers, chipped). To take the fixture out and finish the system: (1) **archetype classifier data product** in fund_score — locked cutoffs from `reports/product/crescent_data_validation.md` (6 archetypes + borderline ±20% + Unclassifiable), new gold panel + serving field + check registration; prerequisites: FF6 style-panel refresh (11.2% coverage → Style Tilter under-detects; DODGX misfalls to Broad) and the naming/legal pass on pejoratives ("Closet Indexer"). (2) **Serve turnover** (in diversification_panel, not in fact_assembler) — two archetype rules key on it. (3) **Fix the 222 `too_new` mislabels** (funds with 4–30y pricing whose real blocker is twin fit — coverage_state should say so; DEFECT filed in the validation memo). (4) Runner-up twins for the TwinPanel = the `serve-l2-passive-candidate-fit` backend, SHIPPED 2026-07-23 (per-candidate corr/TE/beta + fit floor served in `passive_baseline.candidates`/`selected_summary`, staging; Postgres load owner-gated) — consume the served field, do not duplicate. (5) Production cutover of the five-block page folds into `specs/queue/profile-v2-production-cutover.md`; X-Ray crescent composite is a separate later story. [fund_score: archetype classifier + serving / fundscore-web: swap fixture → served field] Also fold in: delete the dead `ai_summary` fixture key from profile-v2-fcntx.json (section dropped from the Crescent page; ai-summary-generation spec rejected 2026-07-22).
  **Owner summary:** The new Crescent fund page is live on the local preview with real data everywhere except the archetype word, which still comes from the validated prototype's outputs for a handful of demo funds. This story builds the real classifier as a data product (plus the style-panel refresh and the legal pass on names like "Closet Indexer"), serves turnover, fixes the mislabeled "too new" funds, and then swaps the fixture out — after which the page is production-cutover ready.
- [ ] (story) Per-fund share-class comparison — a fund runs many share classes with the SAME portfolio but different ticker / net-ER / load / minimum (e.g. AB Global Bond `ANABX`/`ANAGX`/`ANAIX`; American Funds run up to 16). The profile today surfaces ONE representative class — the retail-accessible flagship the item-1 `primary_ticker` rule picks (Morningstar-style: retail before institutional, then flagship). Add a share-class table on the fund profile: every live class with its ticker, net ER, load, and minimum, so a user can find the class they can actually access and see the fee spread across classes. Fits the fee-transparency thesis (Morningstar/Vanguard both let a user self-select the class). Data: per-class ER exists (`data/mfrr/outputs/expense_ratios`, class-keyed) + class liveness (`fund_daily_adj_close`); load/minimum need a prospectus/MFRR source. NOT a launch blocker. [fund_score: per-class fee/load panel + serving / fundscore-web: profile share-class table] [owner-requested 2026-07-31, during the item-1 flagship-class decision]
  **Owner summary:** Every mutual fund has multiple share classes — same investments, different fees depending on how you buy it. We show one representative class per fund; this adds a small table listing every class with its ticker, fee, load, and minimum, so you can see the fee range and find the version you can actually buy. It's a natural fee-transparency feature and the industry standard (how Morningstar and Vanguard both present it).

## Specced (in queue)
Stories that became a queued spec — they've left the backlog for the spec queue and are **not**
re-triageable. `/implement-next` flips one to Done when it ships the linked spec.
Format: `- [~] (story) Title — context → specs/queue/<slug>.md`

- [~] (story) X-ray >100%-of-NAV and book-basis presentation — **OWNER DECIDED 2026-07-22/23, all four calls; doctrine: disclose as filed, never renormalize or suppress.** (a) serve the honest liability-explained >100% cohort as filed + a plain leverage flag; (b) show the SHORT book as well — carry short legs through gold and display both legs labeled (until shipped, label the long book "long positions only" with the filed net alongside); (c) keep dropping filed-incoherent rows but DOCUMENT the inclusion contract and surface the dropped count; (d) feeders anchor coherence to the master's own filing. Original: X-ray >100%-of-NAV and book-basis presentation — FOUR owner decisions from the 2026-07-10 country-coherence investigation (full evidence: fund_score docs/status/data_gap_analysis.md holdings section + check_exposure_xray_country_coherence.py WARN cohorts): (a) how to PRESENT the honest liability-explained >100% cohort (24 funds, RYZAX 178.9%) — serve as-is on the filed %-of-net basis vs flag vs suppress (no renormalization per the 2026-07-08 basis decision); (b) negative-leg serving semantics — holdings_complete carries only the LONG book, so 30 collar-style funds serve above their filed net totals (ASPCX +8.4pp; JAIGX filed −178% net, served +94%): long book or filed net book?; (c) holdings_complete row-inclusion contract for filed-incoherent rows (~75 funds file valUSD>totAssets, EDGU canonically — gold silently drops such rows today, undocumented); (d) feeder gate anchor — feeders (DFCSX/ECGIX) are G1/G2-exempt; link the master's own filing as the coherence anchor. [fund_score: exposure_xray / holdings_complete; owner] → specs/queue/holdings-book-basis-disclosure.md
  **Owner summary:** Four product decisions about presenting funds that use leverage or short positions (portfolios legitimately summing over 100% of assets). Needs owner judgment on presentation, not engineering — until decided, a small set of funds shows technically-true-but-confusing totals. **Decided 2026-07-22/23: show everything as filed — flag leverage, add the short book (specced), document what we exclude, and check feeders against their master's filing.**
- [~] (fast-follow) Look-through coverage-honesty hardening — two codex `--high` P2 advisories from the shipped look-through positioning feature (fund_score fd982cd), both honest-partial-coverage disclosure gaps. (1) `lookthrough_window.py:315` flags `is_unresolved_wrapper` only when `msid` resolves, so a held ETF outside the twin-map with a non-mapping/absent LEI (VOO/IVV/VEA) is counted as a resolved leaf → `lookthrough_coverage` over-reports; flag it honest-missing (or extend resolution), exclude from the resolved numerator. (2) `fact_assembler.py:785` emits coverage metadata only inside the per-`is_surfaced`-series dict, so a partial-coverage fund with zero surfaced shifts gets NO positioning_changes section → UI can't show its partial-coverage flag; emit `rows:[]` + coverage metadata for partial funds. Backend (fund_score), reviewed lane, depends_on look-through-positioning-changes. Realized impact nil today (data-reviewer verified), latent as data evolves. → specs/queue/look-through-coverage-honesty.md
  **Owner summary:** Two disclosure gaps in the fund-of-funds feature: coverage can be over-reported, and partially covered funds can lose their coverage flag entirely. Fixing both keeps the "we tell you what we can't see" promise intact as data evolves.
- [~] (story) Crescent v2 productionization — **SUPERSEDED 2026-07-30 into the cutover spec's V4 target.** The five-block preview on `feature/crescent-profile-v2` is no longer the thing to promote; V4 (`fund_score/docs/product/strategy/mockup_fund_profile_v4_2026-07-28.html`) is. The branch stays unmerged as the running preview + component donor. Its remaining backend prerequisites (archetype classifier, serve turnover, the 222 `too_new` mislabels) survive as V4 items where V4 still uses them — the archetype chip is absent from V4, so the classifier is no longer a cutover blocker. → specs/queue/profile-v2-production-cutover.md
  **Owner summary:** The redesign we were going to promote has itself been replaced by the newer V4 page, so this story now lives inside the cutover spec rather than standing alone. The old preview branch stays put as a working reference — nothing needs merging.
- [~] (story) Unify the TE decomposition onto the 35-factor global basis (ONE idio machine) — owner decision 2026-07-22 after the promotion investigation: the global basis merged 2026-06-25 (`3120e05`, headline idio into vo_reframe_v0.4) but the deferred te_decomposition rewire never happened, leaving a sector-crowded per-fund bets vocabulary AND two idio siblings on two bases. Spec: refresh the stale chain (inputs end ~mid-June; te anchors to value_score at 1e-9), repoint `build_te_decomposition` onto the fixed 35-factor vocabulary with an EDA-gated display rule + theme-handling decision, rewire vo_reframe idio to the repointed panel (v0.5), retire `global_decomposition` after a consumer census (theme_ride provenance!). Scoring basis untouched. → specs/queue/unify-te-decomposition-global-basis.md
  **Owner summary:** The bets table gets one universal risk vocabulary — macro and geography guaranteed a seat instead of being crowded out by sectors — and the site drops to exactly one "how much is stock-picking" number computed one way, everywhere. The model was already promoted a month ago; this finishes the wiring it was promised.

## Done

**Recent only — the 3 most recent shipped items** (full history: `backlog-archive.md`). When a new
item ships, add it on top and move the overflow entry to the TOP of `backlog-archive.md` — the
archive is the full changelog; this section is a bounded buffer so the operational backlog (loaded
on every `/triage`) stays small.

- [x] (data) **RE-GROUNDED 2026-07-28 — NOT a defect (campaign item 4; fund_score doc `eca7af2`, no code change).** The 2026-07-17 reviewer flagged BGHDX's terminal filing as absent; re-measured, it IS ingested (accession 0001752724-25-044443) as a genuine FINAL filing (`isFinalFiling=Y`, seriesId S000074799, period 2024-12-31) with **0 holdings** — the fund liquidated, so nothing to recover (the panel correctly ends at its last non-empty quarter 2024-09-30). Systematic completeness check: of **52,678 EDGAR 2025-filed N-PORT accessions, only 13 (0.025%) are absent from the raw store, and ZERO are `isFinalFiling=Y`** (all 13 are redundant duplicate accessions — ProFund Bitcoin/derivative + Fidelity central funds + 2 seriesId-None — whose series are all present via other accessions with periods through 2025-12-31). The ingest does not miss dying funds' final filings; premise falsified. Original: N-PORT holding-store ingest can miss a dying fund's FINAL filing — BGHDX's 2025-02-27 NPORT-P (period 2024-12-31) appeared absent from data/nport/holding (raw max 2024-09-30); decision-neutral, surfaced by the 2026-07-17 payload-restore data-reviewer (W4). [fund_score: nport ingest]
  **Owner summary:** Our copy of SEC filings sometimes seemed to miss a closing fund's last report. **Re-grounded 2026-07-28: measured a non-issue — the filing copy is 99.98% complete and misses no closing fund's final report; BGHDX's final filing is present and genuinely empty (the fund liquidated with no portfolio). No fix needed.**
- [x] (data) **RE-GROUNDED 2026-07-28 — NOT a recoverable ingestion gap; owner de-scoped, subsumed into the foreign-holdings item (fund_score `9023a48` documents the measured root cause; no code change).** The filings ARE ingested (S000041190/WisdomTree Japan is in the raw holding store @ 2026-03-31 with 100 EC holdings), but `build_holdings_snapshots.py:135` INNER-joins on US `cusip` (docstring: "Foreign/cash/preferred names are excluded here (US-ticker inner-join)") and these funds' equity is FOREIGN — cusip `000000000` + foreign ISINs (Marubeni JP3877600001, Mitsubishi, SoftBank…) → 0 matches → 0 panel rows → the series freezes at its last US-cusip quarter (2019 for WisdomTree Japan). Measured: of the **88 panel-stale funds (max quarter_end < 2024-11-29) with a 2026 EC filing, 0.0% of EC holdings resolve to a US cusip; 88/88 all-foreign**; derivative leveraged funds (Direxion Brazil 2X = swaps) additionally carry no `assetCat=='EC'` rows. **A rebuild recovers NOTHING** — the panel honestly excludes foreign by design. This is the US-cusip/ticker-keyed-reference FOREIGN-HOLDINGS gap; the 53 recover only when the shared foreign-holdings enrichment (foreign ISIN→ticker/sector) lands — see the Working-set **Foreign-holdings classification gap** item and the **Exposure-path "Fees + trading"** item (same US-ticker-keyed `holdings_snapshots` root). Original: holdings_snapshots upstream ingestion gap — 53 LIVE N-PORT filers' recent filings never reached the panel (S000041190 WisdomTree Japan filed 2026-03-31, panel ends 2019-09-30; JPEM, Direxion Brazil Bull 2X); framed as recoverable ingestion-path fix + rebuild, falsified by measurement. [fund_score: nport ingestion → holdings_snapshots]
  **Owner summary:** 53 funds that actively file portfolios with the SEC show older holdings than they should. **Re-grounded 2026-07-28: the investigation proved this ISN'T a fixable ingestion bug — the filings are already in our database; these funds hold foreign stocks (Japanese, Brazilian, EM) our US-based security reference can't identify, so the portfolio panel honestly can't display them. They light up only when the separate foreign-holdings enrichment lands — there is no rebuild that recovers them now.**
- [x] (data) **DONE 2026-07-27 — RE-GROUNDED then SHIPPED (fund_score `af7ecee` on `campaign/refresh-2026-07`; refresh-campaign item 2).** Measured that 2 of the 3 named feeds were already fresh — `last_nport_filing_date`→2026-05 (payload-restore 8456dbd), `last_ncen_filing_date`→2025-10 (one cycle behind late-2025-FY funds, = item 8) — so item 2 was scoped (owner-approved) to the real residual: the frozen `class_ticker_mappings` WITNESS tier (last_ncen deferred to item 8). New ACTIVE producer `scripts/pipeline/refresh_class_ticker_witnesses.py` (`make refresh-ctm-witnesses`) re-derives the witness tier (first/last_seen/filing_count) from the ACTUAL classId membership of the ingested 2025-26 N-PORT headers (local EDGAR cache, 80,120/80,120 cache hits, 0 network; advance/add only, historical never overwritten, no fabrication; date_filed basis). CTM 413,653→419,048 rows, last_seen 2024-12-31→2026-07-01; 31,904 fresh witnesses / 13,821 series; 5,395 new rows; 0 regressions. Un-censors witness recency → dissolves the tier2 coin-flip (BRIF null→2026-04-23) and admits live reorg successors (GOFCX→2026-04-28, dead C000027932→2025-01-24, PEQKX/PYCGX→2026-06-25). Gates: full data-review PASS (12 groups reconstructed vs EDGAR, no fabrication) + focused-delta re-review PASS (recovered coverage / fund_name / counts independently reconstructed) + codex PASS ×3 (fixed W1 early-2025 coverage, W2 fund_name, W3 null-count, W4 idempotency). Fast-follows: W-A (renamed-class new-row fund_name pairs the earliest name vs the latest accession — one-line fix, self-heals next refresh) + a registry-stub filing_count sweep. CTM is an intermediate producer (data gitignored); the dedup benefit lands when fund_metadata rebuilds (item 7 / panel-refresh). Original: Refresh the stale universe-evidence producers — fund_metadata.last_nport/ncen + class_ticker_mappings frozen at 2024-12-31 while the raw N-PORT store spans →2026-05; the 8993b29 ticker-dedup works around it via latest_raw_nport_recency but the witness tier is censored. [fund_score: metadata/ctm producers + source_inventory]
  **Owner summary:** The reference data we use to decide which funds are alive and currently trading had stopped updating at end-2024. **Done 2026-07-27: 2 of the 3 feeds were already current, and the frozen ticker-witness evidence is now refreshed from SEC filings we already had on disk — so fund-identity tie-breaks (which of two claimants is the live one) use fresh evidence, dissolving coin-flips and admitting live successor funds.**
