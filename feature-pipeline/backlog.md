# Triage Backlog

The intake list. `/triage` drains the **Open** section: it picks the top item and routes it by
`(type)`. A `(bug)` or `(data)` item is fixed end-to-end and moves straight to **Done**. A `(story)`
that gets turned into a queued spec moves to **Specced (in queue)** — it has left the backlog for the
spec queue and is no longer re-triageable — and only reaches **Done** when `/implement-next` ships
that spec. Each item lives in exactly one state at a time (never Open *and* specced).

**Format:** `- [ ] (type) Title — one-line context [area]`
**Owner summary (required):** every Open/Specced item carries an indented `**Owner summary:**`
line directly beneath it — one or two sentences in plain English, written the way a CPO briefs a
board: what this is about and why it matters, no code paths, no jargon. It supplements (never
replaces) the detailed line. Anyone filing an item writes both; `/triage` should treat a missing
owner summary as a formatting defect and add one.
**Optional model tag:** `[model: fable|opus|sonnet, effort: xhigh|high|...]` — a routing hint for judgment-heavy items. Fix loops run in the MAIN session, so the dispatcher should surface the tag ("this item recommends X — /model to switch, or proceed") rather than silently ignore it. Specs carry the same hint as `model:`/`effort:` frontmatter, which `/implement-next` passes to the implementer agents automatically.
**Types & routing:**
- `(bug)` → `fundscore-data:fix-bug` (fundscore-harness plugin) — lean loop: reproduce → fix → verify → review (Claude + codex) → commit.
- `(data)` → `fundscore-data:fix-data` (fundscore-harness plugin) — lean loop: root-cause at source → fix → rebuild → /check-data + data-reviewer → review (Claude + codex) → commit. (No synthetic data; fix upstream.)
- `(story)` → `spec-story`: clarity-gate → a lean spec in `specs/queue/` (or escalate to the owner if vague). Discovery-heavy, net-new product direction goes to `/critique-funds` → review-proposals → `/spec-approved` instead. Once a spec is queued, the item moves to **Specced (in queue)** (below), not Done.

Keep items small and single-purpose. Put the type first so the dispatcher can route without reading the whole line.

---

## Open
Organized by thread (2026-07-22 restructure). **Working set** = the drain queue — `/loop /triage` works it top-down; items whose text calls for a focused data-scientist/reviewed session sit at its end so the lean loop never stalls mid-list. **Owner decisions** wait on the owner. The **Refresh campaign** executes as ONE supervised reviewed arc (shared root causes and rebuild/review overhead — do NOT lean-loop its members piecemeal), in roughly the listed order, ending with the terminal panel-refresh pass and the owner-gated serving reload. The **Hardening sweep** batches verified-nil-impact latents into one session. **Deploy** holds the paused solver service; the **Value-prop group** is post-launch roadmap. File new items into the matching thread (Working set by default). The serve-l2 fit-floor SHIPPED 2026-07-23 (see Done). **Beta bar (owner decisions, 2026-08-06):** beta scope = FULL experience (fund pages + screener + X-Ray); items tagged **BETA BLOCKER** must close before invites — the four user-visible-wrongness items PLUS V-spike and the foreign-holdings enrichment (owner chose the stricter bar). The **Beta-launch group** (below, before Value-prop) holds the launch-thread items filed the same day; the solver service (Deploy) is un-paused. **The beta push is drained via `feature-pipeline/beta-execution-plan.md`** — the rank-ordered assembly-line queue (owner contract, fences, model/effort routing, usage-limit resilience); beta-path items listed there are worked through that plan, not ad-hoc.

**Working set.** Distinct near-term correctness and coverage items in priority order — user-visible wrongness first, then coverage recovery, then hygiene chores; the focused-session item (holdings-vs-β) sits last so the lean loop never stalls on it.

- [ ] (data) **Merge the wrapper look-through fix through 3 conflicted files, rebuild, gates — OWNER RULED GO 2026-08-26 (register: Path-to-Live entry).** The shipped flag+suppress fix (fund_score `076562f` on `fix/d83-wrapper-lookthrough`, codex-verdicted, reviewer-passed 2026-08-18) kills 10/10 false "entered" rows and 97/97 phantom exits. `git merge-tree` vs main `e34d480` conflicts in `build_holdings_lookthrough_window.py`, `build_positioning_changes_panel.py`, `src/fundscore/product/positioning_changes.py` (the 2026-08-26 positioning merges touched the same code). Reviewed lane: resolve in a worktree, rebuild the panel, re-run the positioning ratchet + check-data + data-reviewer, codex gate. Unparks L3 Phase B and the R1 surfacing precondition (P7-D1). [fund_score: fix/d83-wrapper-lookthrough → main]
  **Owner summary:** You approved merging the fix that stops us inventing trades when a fund's ETF wrapper fails to resolve. The merge collides with this morning's other positioning fixes in three files, so it gets a careful reviewed merge rather than a quick one — then two downstream fixes you also approved can proceed.
- [ ] (data) **Quarantine 4 charts serving fabricated price steps — OWNER RULED 2026-08-26 (register: Path-to-Live entry).** BRLIX +28.3% (2021-12-15), CSIUX +62.72% (2022-07-13, Segment 0's 3rd-largest served mid-series step), JOPSX +28.1% (2021-12-15), SMGAX −52.7% (2005-03-29) — adjudicated OUT of the capital-gain distribution class by capgain S1-F14 (the distribution evidence does not explain their steps), so no existing rule owns them. Lean fix-data loop: extend the ratified W3 chart-withholding pattern (6 funds today) to these four; honest interim while `bad-dividend-record` (below) chases the root cause. [fund_score: chart quarantine list / value_score withholding]
  **Owner summary:** Four funds still show a price spike that never happened. Until the underlying dividend-data problem is fixed, we stop showing those four charts rather than show a wrong one — the same honest-withholding pattern you already approved for six other funds.
- [ ] (data) **bad-dividend-record: adjudicate the DISTRIBUTIONS FEED at source — filed 2026-08-26 from capgain S1-F14 (the report's own "FILE THIS", stranded on the unmerged worktree until now).** Owns the question "is Tiingo's distributions record itself wrong?" — covering: the 4 quarantined funds above; the **516 `suspect_yield = True` events**; the **~40% of events where the stamped distribution never leaves the price** (S1-F13); and the MAPOX-type doubled-print family (S1-F3). Distinct from the P5 trigger class by design — that rule tests whether the price ADJUSTMENT matches a real distribution; this item tests whether the recorded DISTRIBUTION is real. Fix at source; evidence lives in the raw vendor files (`close_price`, `dividend`, `split_factor`) that `build_fund_daily_adj_close.py` currently discards. [fund_score: distributions feed / price ingestion]
  **Owner summary:** You asked whether the better fix for the four broken charts is fixing the price history at its source — yes, and this is that item. The evidence says some of the vendor's dividend records are themselves wrong (a recorded payout that never shows up in the price, or shows up twice). This adjudicates the dividend feed directly, which is the durable fix behind the quarantine.
- [ ] (data) **Wrapper suppression refinement (error bound) — OWNER RULED 2026-08-26 (register: Path-to-Live entry).** Replace the fund-level over-suppression (143 surfaced rows / 43 funds suppressed; for 69 of them the basis break is arithmetically too small to explain the move) with the measured per-fund error bound: suppress a holdings-derived row iff it names a basis-broken wrapper OR `|change_magnitude| <= break_pp`. Measured: 1,700 rows / 69 surfaced suppressed instead of 2,281 / 143, still removing 10/10 false entries + 97/97 phantom exits; `top10_weight` stays on the fund-level rule (rank-based, bound not applicable). Sequenced AFTER the d83 merge above. [fund_score: positioning suppression gate]
  **Owner summary:** The approved phantom-trade fix errs on the side of hiding too much — it also suppresses some real moves that are too big to be explained by the data gap. This refinement keeps those real moves visible while still killing every phantom row.
- [ ] (story) **Resolve the wrappers — real positioning for ~390 wrapper funds, X-Ray included — OWNER RULED 2026-08-26 (register: Path-to-Live entry).** Feed the wrapper's recovered identity into look-through so the ETF's own filed book expands instead of collapsing to one line: 2,552 of 3,459 affected wrapper lines (73.8%, Σ137.4 of 205.3 fund-NAV units) have the master's N-PORT filing inside the existing 120-day lookback and would resolve; the rest stay honestly flagged. Fold in the X-Ray's identical LEI-only defect (`build_holdings_complete.py::lookthrough()` — SPYC/HEQT/SHUS/GBXA serve a single opaque line there today) so both books share one identity rule. Reviewed lane; changes served positioning for ~390 funds. Sequenced after the d83 merge + refinement. [fund_score: lookthrough identity resolution]
  **Owner summary:** Beyond suppressing the phantom trades, you approved actually fixing the view: for about 390 funds whose big ETF holding we failed to open, we already hold the ETF's own filing and can show the real underlying positions instead of a blank. This is that build, applied to both the fund page and the X-Ray so they can't disagree.
- [ ] (data) **88,350 served rows are labelled "stale" by a hardcoded constant, and 93.4% of them are NOT stale** — found 2026-08-26 by the frozen-clock sweep. `build_stock_rows` sets `coverage_state = lit("stale")` unconditionally on `stock` and `vs_benchmark` rows. Measured: **82,475 of 88,350 rows (93.4%), across 3,299 funds, sit on a holdings book ≤180 days old** — median age **148 days**, i.e. comfortably inside the freshness bar every other axis is judged by. On this frame the label does not mean "computed from an old book"; it means nothing, because it was never computed. Same never-computed-constant class as the frozen `--today` clock (fixed) and the fabricated `EVAL_DATE` stamp — a value asserted rather than derived. **Deliberately not fixed in the clock change:** relabelling 82K served rows is a different change from unfreezing a clock, and it is entangled with the parked suppress-vs-flag decision on how these rows surface. Needs its own loop: derive the state from the row's own book age like every other axis, then measure what the honest labels do to the section. [fund_score: exposure_xray.py::build_stock_rows]
  **Owner summary:** On one part of the exposure view we stamp every row "stale" no matter how fresh it is — and it turns out roughly 19 in 20 of those rows are based on data well inside our own freshness limit. We are telling readers data is old when it isn't. Nothing is miscalculated; the label was simply hardcoded and never worked out.
- [ ] (data) **`value_score.AS_OF_DEFAULT = 2026-04-24` is another frozen clock, and it moves the served universe** — found 2026-08-26 by the frozen-clock sibling sweep, filed rather than fixed because it is an as-of call the owner should make. This constant bootstraps which series get scored at all: at the frozen date it admits **3,965 series**; at the real current date, **3,892 (83 in, 10 out)**. Moving it shifts a shared recency window, so it is not a local edit — it changes who is in the product. Three further latent frozen dates found in the same sweep and NOT yet biting, listed for whoever takes this: `portfolio_passive_solver` class default `as_of_date="2026-06-30"` (the live service and CLI both pass explicit values, so it is dormant), `BASKET_END = 2026-06-30` (the SEP store ends 2026-06-15 and this binds the day it passes), and `build_fmp_reference --as-of 2026-07-15` (a vintage selector with only one snapshot on disk). [fund_score: value_score + 3 latent sites]
  **Owner summary:** Another hardcoded date, this one deciding which funds are eligible to be scored at all. Unfreezing it would add 83 funds and drop 10. That is a judgement about who appears in the product rather than a bug fix, so it is yours to call.

- [ ] (data) **`value_offering_reframed` FABRICATES a holdings date on 3,484 of 5,963 rows (58.4%), and ZERO of them are real** — found 2026-08-26 by the as-of-mislabel worker, then independently re-sized by the dispatcher, which made it worse than filed. `scripts/pipeline/build_value_offering_reframed.py:306` does `pl.coalesce(pl.col("quarter_end_used"), pl.lit(EVAL_DATE)).alias("holdings_as_of")` — when the true holdings date is unknown it substitutes the constant `EVAL_DATE = 2025-10-31` (`:64`) and labels the result `holdings_as_of`. **Dispatcher check at the panel's own nearest eval date (2025-10-24): `quarter_end_used` is NULL for 2,982 of 5,461 series, and exactly 0 series genuinely carry 2025-10-31.** So every one of the 3,484 stamps is manufactured, none corresponds to a filed date, and nothing in the output distinguishes a fabricated stamp from a real one. This is a direct violation of the data-integrity rule ("never synthesize, impute, or default-fill; missing surfaces as missing"), not a staleness question. **OWNER RULED 2026-08-26: blank it** (*"if we really don't know leave it blank"*) — but the owner also asked why we wouldn't know, and **investigating showed we mostly DO**, so this is a RECOVERABLE miss (a DEFECT) rather than honest-missing. Of the 3,484 stamped funds, **3,263 (93.7%) have a filed book in `holdings_complete`** (which spans 2025-01-31..2026-05-31). Two frozen constants cause it: the panel takes its date from the RISK MODEL frame (`quarter_end_used`, an as-of-backward attach capped at 100d off the risk grid) instead of the holdings store; and `EVAL_DATE = 2025-10-31` (`:64`) is ~10 months stale, so **3,042 of the 3,484 have books only AFTER that frozen date**. Only **221 have no book at all**; **221 are directly datable today** (the constant was off by a median 153d, max 273d). **So: serve NULL as the immediate honesty fix, then unfreeze `EVAL_DATE` and source the date from the holdings store — after which most of these get a REAL date, not a blank.** Same class as the frozen `--today` clock; sweep both together. **Check every other `coalesce(..., lit(...))` on a date or provenance column in the same sweep.** [fund_score: build_value_offering_reframed.py:306]
  **Owner summary:** On more than half the funds in one of our panels, the "as of" date we attach to the holdings is simply made up — we substitute one fixed date whenever the real one is missing, and it turns out not a single fund actually has that date. Nobody looking at the output can tell the invented dates from the real ones. We should show nothing rather than something invented.
- [ ] (data) **Two served `active_share` figures disagree for 2,370 of 2,479 shared funds** (median 1.34pp, worst 56.8pp) — found 2026-08-26 by the as-of worker. The X-Ray concentration block and `value_offering_reframed` both serve an `active_share` derived from the SAME diversification panel but under different selection rules, so one fund can show two different numbers on one product. This is the same-page-contradiction class this project ranks above an honest gap. Adjudicate ONE selection rule and make both consumers read it; do not median them. [fund_score: exposure_xray.py + build_value_offering_reframed.py]
  **Owner summary:** The same statistic about how different a fund is from the index is being calculated two ways, and for almost every fund the two answers disagree — usually a little, occasionally a lot. A reader could see both numbers on the same product. We need to pick one method.
- [ ] (data) **X-Ray panel is built with a HARDCODED `--today 2026-06-15`, now 72 days stale, and moving it flips 13,212 rows / 848 funds to `stale`** — `scripts/pipeline/build_exposure_xray_panel.py:41` defaults `--today` to `2026-06-15` and `make build-exposure-xray` passes nothing, so every freshness verdict in the panel is judged against a frozen clock. Deliberately NOT moved by the as-of worker because it is an as-of decision with a large blast radius across every axis (concentration 4,864, stock 2,554, country 2,219, theme 1,643, sector 1,461, asset_class 471). **This is a recency-gate over a frozen clock — the [[recency-gate-frozen-feed-time-bomb]] shape: it detonates with no code change.** Decide the clock policy, then rebuild every consumer. [fund_score: build_exposure_xray_panel.py:41 + Makefile]
  **Owner summary:** The freshness labels on our exposure data are measured against a date that was hardcoded in June and never moves, so "current" is being judged against a stopped clock. Correcting it would relabel a lot of funds at once, so it needs a deliberate call rather than a quiet fix.
- [ ] (data) **`confidence_state` still hardcoded `"high"` on the X-Ray `asset_class` axis — 2,400 rows already older than the 180-day bar** — the as-of fix (v0.5, 2026-08-26) made concentration and sector-active-share age-aware but the `asset_class` axis kept `pl.lit("high")`; `style` has the same shape but 0 affected rows today. Same defect class as the one just fixed, just a different axis — a confident label on a figure whose age is never checked. [fund_score: exposure_xray.py]
  **Owner summary:** We just fixed "we say we're confident regardless of how old the number is" in two places; a third place still does it, on 2,400 rows. Small, same fix.
- [ ] (bug) **A fund's own X-Ray concentration block carries inconsistent `fund_ticker` values — 1,160 of 2,621 funds** — found 2026-08-26. Deliberately NOT changed by the as-of worker, and it was right not to: the diversification-panel ticker is the share class the fund's L2 blend was actually fit to, so overwriting it would destroy provenance. Needs an adjudication of which ticker each row should CLAIM, not a blind overwrite. [fund_score: exposure_xray.py]
  **Owner summary:** Within one fund's own numbers, different rows label themselves with different share-class tickers. It's not wrong data — each row is honest about where it came from — but it reads as inconsistent and needs a deliberate rule.

- [ ] (story) **Bet-mix window disclosure: the bets table states a CURRENT tracking-error number over a mix measured through an OLDER window, and nothing on the page says so** — re-filed 2026-08-26 from `specs/done/unify-te-decomposition-global-basis.md:594-601`, whose backend shipped 2026-07-30/31 while this web-side residual was never started. The table scales to the fresh `te_current` but the MIX comes from `fit_window_end`, so a reader sees a current figure describing a stale composition. The spec's chosen policy requires a quiet, non-jargon "bet mix measured through &lt;date&gt;" affordance (brand bar: jargon behind affordances, never an inline lag figure). **Two more things ride the same fix:** funds suppressed by the alignment gate must render the honest missing-reason path rather than an empty table, and the FCNTX/FBGRX anatomy+bets coherence read the spec deferred "until after the serving reload" has still never been done — the reload happened 2026-08-25 (manifest 58), so it is now doable. [web: bets table / anatomy, V4 movement 01]
  **Owner summary:** On the page, one number is current and the picture next to it is a few months old, and we don't tell anyone. Nothing here is wrong exactly — it's that two things measured at different times sit side by side looking like they match. Small honest-labelling fix; we owed it since July.
- [ ] (story) **"Where it comes from" held-names line — part (b) of the direction-badge contract, never started** — re-filed 2026-08-26 from `specs/done/unify-te-decomposition-global-basis.md:586-591`. The one-line explanation naming the REAL held names behind a bet (holdings ∩ basis-factor constituents) needs a new backend join (fund holdings × basis-factor constituents). Feasible for the 23 geography/sector factors off existing country/sector holdings attributes; the 6 macro/commodity factors are correctly "exposure from returns — no single holding" and must say so rather than showing nothing. **Sizing it is its own call — do not lean-loop this.** [fund_score: new join → serving; web: bets table]
  **Owner summary:** When we tell someone the fund is making a bet on, say, energy, we can't yet show them which of its actual holdings make up that bet. This would name them. It needs new plumbing on the data side, so it's a real piece of work, not a tweak — worth sizing before committing.
- [ ] (data) **`positioning_changes` check has been FAILING on `main` since 2026-08-24, unmeasured — the L6 `no_expansion` promotion rebuilt the panel AFTER the last check run (found 2026-08-25 by the `sector-identity-defect-recovery` final data gate; PROVEN pre-existing, NOT caused by that spec).** `make check FEATURE=positioning_changes` reports 1 PASS / 1 FAIL (`reports/check_runs/check_run_20260825_143713.md`). The FAIL is Check 6 (X-Ray coherence): shared=3554, `<=1pp`=3503 → **98.5650%**, worst-entity 26.687755pp. Replicated in-run on BOTH vintages using the check's own constants — **BEFORE (pre-sid panel + pre-sid `holdings_complete`) and AFTER are identical to the last digit**, so the sector write did not move it. Why it was invisible: `29969d7` rebuilt `positioning_changes_panel.parquet` at 2026-08-24 19:10; the last positioning check ran at 17:32, *before that panel existed*. So the promotion changed the panel's basis and nothing re-ran the check. Two things to settle: (a) is the 98.57% coherence bar still the right bar on the `no_expansion` basis, or is the CHECK stale rather than the data; (b) a rebuild that changes a panel's basis must re-run that panel's checks in the same pass — that is the reusable fix. [fund_score: `scripts/checks/` positioning_changes + the L6 no_expansion promotion]
  **Owner summary:** One of our automated data checks has been failing quietly for a day. It is not new breakage — a change we shipped on the 24th changed how that panel is built, and the check that grades it never ran again afterwards, so nobody saw the red. We need to decide whether the panel is wrong or the check's bar is now measuring the wrong thing, and make basis changes re-run their own checks.
- [ ] (data) **Burford Capital is served under two different sector labels and the sector-consensus overlay CANNOT reach it — its defective fund-quarters have no `holdings_complete` rows (found + measured 2026-08-25 by the `sector-identity-defect-recovery` final data gate; MEASURED-AND-FILED, deliberately not fixed there).** `GG00BMGYLN96` is the same wrong-filed-CUSIP class as SharkNinja / Shift4 / Genie, but out of reach by construction: the consensus map is derived from `gold/holdings_complete.parquet`, and Burford's three defective fund-quarters (`S000008311`, `S000008325`, `S000036499`, all `2026-03-31`) each carry **0 rows** there, while gold's 143 Burford rows are single-sector Financial Services — so the map never sees a contradiction to resolve. Served consequence today: **3 rows / $245,354.64** carry the wrong sector on `fund_holdings_full_staging`; it is 1 of the 2 remaining multi-sector ISINs on that table (the other, Genie, is correct by design). Two candidate fixes, both bigger than a label patch: widen the map's derivation basis beyond `holdings_complete`, or close the `holdings_inclusion_contract` gap that drops those fund-quarters from gold in the first place — the second is the root cause and probably affects more than sector. Disclosed in `src/lib/db/schema/serving.ts`. [fund_score: `sector_attach.pinned_us_consensus_map` derivation basis / holdings inclusion contract]
  **Owner summary:** One more company shows two different sectors at once. The fix we just shipped can't reach it, because the fund-quarters that carry the bad rows are missing from the book that fix reads. Small money ($245k across 3 rows) but it is the "confidently wrong" class, and the underlying reason — rows dropping out of our main holdings book — likely costs us more than this one label.
  **SIZING THE ROOT CAUSE 2026-08-25 (finalize round, null-safe re-measure).** The gap is not Burford-shaped, it is table-shaped: of **1,361,548** distinct `(series_id, as_of, isin, cusip)` keys on `fund_holdings_full_staging`, **113,098 (8.3%) have NO matching row in `gold/holdings_complete`**. Burford's three fund-quarters are three of them. That is the number to work against — decide whether those keys are legitimately outside the gold inclusion contract (and if so, why the product table still serves them) or whether gold is under-including. Anything derived FROM `holdings_complete` — the US-consensus sector map included — is structurally blind to all 113,098. (Measured by `fund_score-wt-sid/reports/feature_pipeline/sid_serving/verify_serving6_nullsafe.py`; the earlier 778,292-key figure in that directory's `verify_serving.py` §B is a null-dropping join and understates the comparable set — see `FINALIZE_NOTE.md`.)
- [ ] (data) **`ROP` binds Roche Holding's Swiss line onto Roper Technologies' US ticker — a wrong-company binding on the TICKER side, reached by NO cusip path (traced 2026-08-25 in-run by `sector-identity-defect-recovery`, deliberately deferred per its ADDENDUM R-5).** On the written book, `security_id`/`security_ticker` `ROP` claims **two** ISINs: `US7766961061` (real Roper, 644 rows / 644 funds, Technology) and `CH1499059983` (Roche Holding AG, 4 rows / 4 funds, `inv_country='CH'`, Healthcare). `S000015906` carries the Roche line at **0.6343% of NAV**. Ruled out at the source, each checked: `CH1499059983` files cusips `N/A` / `000000000` / `H69293266`; `H69293266` is absent from `cusip_sector_map`, its `cusip_reference` row is all-null, and no `H69293` prefix winner exists; `isin_reference[CH1499059983].ticker` is null; `fmp_isin_us_ticker_bridge` maps it to **RHHBF, not ROP**. So it is produced by none of `cusip_mapping.py`, `_names_match`, the binding gate, or the FMP ISIN bridge — it needs its own ~20-minute trace of where `security_id`/`security_ticker` is ASSIGNED (`build_holdings_complete` / `exposure_xray` security-id construction, or an upstream alias path: `sharadar_ticker_alias.parquet`, `nport_cusip_ticker_map.parquet`). **Not currently wrong-labelled**: the whole-book identity bridge's tie rule sees 2 claimants and honestly DROPS the key. **`ROP` is 1 of 360 excluded tie keys** (180 `security_id` + 180 `security_ticker`) — that set is the census of ticker-level wrong-company-binding candidates and should be triaged value-weighted in the same pass. [fund_score: security_id construction + `reference/identity_bridge.py`]
  **Owner summary:** A Swiss Roche listing is sitting under Roper Technologies' ticker in one fund. Nothing is mislabelled today — our tie rule notices the clash and refuses to guess — but 360 tickers are in that same "refused to guess" pile, and each one is a place we might be showing the wrong company's data. Worth one focused session to size the pile by dollars and fix the ones that matter.
- [ ] (bug) **`build_holdings_complete.py` is not byte-deterministic: two runs of the SAME code on unchanged raw N-PORT disagree on `valUSD` / `weight` / `pct_nav` and on ROW ORDER (proven 2026-08-25 by `sector-identity-defect-recovery`'s rebuild-twice check; PRE-EXISTING, filed not fixed — a sort-tiebreak change reorders the frame for every consumer).** 1,272 / 1,548,667 / 1,238 rows differ across two runs 16 min apart; **max relative delta 8.3e-15**, i.e. DuckDB parallel-reduction order plus unstable sort ties, not a data change. The `sector` column IS bit-identical, so it did not touch that spec's verdict — but it puts permanent floating noise into every future rebuild-twice determinism check on this panel, which is the instrument we rely on to catch REAL nondeterminism ([[rebuild-twice-proves-determinism]]). Fix the tie-break/reduction order deliberately, in a pass that re-derives the downstream panels, not as a drive-by. [fund_score: `scripts/pipeline/build_holdings_complete.py`]
  **Owner summary:** Rebuilding our main holdings book twice from identical inputs gives very slightly different numbers in the last decimal place, and a different row order. Nothing is wrong for a user. It matters because "rebuild it twice and compare" is the test we use to catch genuinely unstable data, and this noise makes that test harder to read.
- [ ] (chore) **`cusip_reference.parquet` records no `match_method` / `source` provenance column, so which rung of the resolution ladder bound a CUSIP can only be recovered by REPLAYING the ladder (found 2026-08-25 by the `sector-identity-defect-recovery` dispatcher pre-check; noted as optional in its ADDENDUM R-2 and left, because the adopted remedy never opened `cusip_mapping`'s ladder for a write).** Concrete cost: a 10-minute replay instead of a one-line `group_by` to establish that all seven candidate CUSIPs were exact step-3 vendor hits and never reached the step-4 6-char-prefix fallback — which is the distinction between "the vendor is right and the FILER wrote the wrong CUSIP" and "our fallback guessed". Add the column the next time that module is opened for a write. [fund_score: `src/fundscore/reference/cusip_mapping.py`]
  **Owner summary:** Our security-ID reference table doesn't record HOW each ID was matched, so when we investigate a wrong match we have to re-run the whole matching process to find out. Cheap to add next time we're in that file; it turns a 10-minute forensic exercise into a one-line query.
- [ ] (data) **`cusip_reference`'s 6-char-prefix fallback binds FOUR different PIMCO RAE mutual funds to the ticker MFUS — a wrong-company binding, proven at the source (found 2026-08-24 by the `recent-changes-no-lookthrough` identity audit; upstream of that spec and NOT fixable inside it).** Gold `data/gold/cusip_reference.parquet` maps **five** distinct `72202L*` CUSIPs — `363, 421, 462, 512, 645` — to ticker **MFUS** under one identical name, `"PIMCO RAFI DYNAMIC MULTI-FACTOR US EQUITY ETF"`. Only `72202L363` is that ETF. The other four are four **separate** PIMCO RAE mutual funds, each proven distinct by its own LEI in the raw N-PORT store: `421 → S000048957 PIMCO RAE US Small Fund` (4 filers), `462 → S000048956 PIMCO RAE US Fund` (2), `512 → S000048955 PIMCO RAE International Fund` (5), `645 → S000048952 PIMCO RAE Emerging Markets Fund` (4). **Root cause is exact and reproducible:** the Sharadar reference contains only the three real ETF CUSIPs (`363/371/389` → `MFUS/MFDX/MFEM`), so the four fund CUSIPs miss the exact join and fall to step 4, the **6-character CUSIP-prefix fallback** in `src/fundscore/reference/cusip_mapping.py` (`_prefix_match`) — every `72202L` shares the PIMCO issuer prefix. The name gate `_gate_ticker_bindings` did **not** stop it (`cusip_ticker_rejected_bindings.parquet` holds **0** rows for `72202L`) because it rejects only when names are known on both sides and *none* match, and the filers' line names are trust-level (`"PIMCO FUNDS"`), which shares the token PIMCO with the ETF's name. **A prefix that has three candidate tickers is a tied claimant and should resolve to an honest null, not to whichever candidate sorts first** ([[deterministic-wrong-worse-than-nondeterministic]]). **Measured blast radius, both directions:** on today's SERVED expansion basis the mis-binding is invisible, because a held wrapper is replaced by its constituents before it can be named — but the moment Recent Changes stands on the fund's own filed book (the `recent-changes-no-lookthrough` measurement), it becomes the sentence the user reads: FAEQX's **rank-1** surfaced row renders "MFUS entered +2.31pp" for a position the fund itself files as *PIMCO RAE US Small Fund*. Full-universe audit of that arm: 465 surfaced wrapper rows → 12 with contested identity → **1 adjudicated a genuinely wrong served name (this one)**; the other 11 are stale filer LEIs where our ticker is right. Evidence + the reusable detector: `fund_score-wt-l6b/data/_tmp/recent-changes-no-lookthrough/evidence/identity_{coherence,adjudicate,verdict}.py` and `cusip_72202L_block_evidence.parquet`. **Sweep, do not point-fix:** 8,793 tickers in `cusip_reference` carry >1 CUSIP; most are legitimately bonds under an equity issuer, so the fix is to the prefix layer's ambiguity handling (and the gate's token-overlap rule), then re-measure the fund/ETF slice specifically. [fund_score: src/fundscore/reference/cusip_mapping.py `_prefix_match` + `_gate_ticker_bindings` → data/gold/cusip_reference.parquet]
  **Owner summary:** When we can't find a security's exact ID in our reference data, we fall back to matching just the first six characters — which is the issuer, not the security. That has quietly bound four different PIMCO funds to a fifth, unrelated PIMCO ETF. Nothing on the site is wrong today because that section currently looks through funds to the shares underneath. But the change we are measuring right now would put those names directly in front of readers, and the very first line on one fund's page would name the wrong fund. This is the confidently-wrong class we rank above an honest gap, and the fix belongs in the reference data, not in the section.
  **UPDATE 2026-08-24 (K-2), a SECOND proven bad block + the root cause of why the gate misses them.** `cusip_reference` also binds **both** `885216671` (filed **nowhere**) and `885216739` (filed **only** as *Thornburg Capital Management Fund*, LEI `549300GLR0WG6ALV5277` → `S000051223`) to ticker **TFGZ** under the name *THORNBURG FOCUS GROWTH FUND*. Served consequence, measured: THOAX +6.78pp, THMGX +4.18pp, TXUE +5.44pp would each have displayed a **growth-fund** purchase for what is an affiliated **cash** vehicle. **ROOT CAUSE OF THE GATE'S BLINDNESS, newly identified:** `reference.cusip_mapping._names_match` returns **True** for *THORNBURG FOCUS GROWTH FUND* vs *THORNBURG CAPITAL MANAGEMENT FUND* — they share the distinctive token `THORNBURG`. That predicate is correct for **issuers** and wrong for **funds**, where the distinguishing words are exactly the ones it discards. This is why `cusip_ticker_rejected_bindings.parquet` holds 0 rows for both the 72202L and 885216 blocks: the gate is structurally unable to reject a same-sponsor collision. **So the sweep is bigger than the prefix layer** — fix `_names_match`'s fund-name case (a shortening test, as used in `lookthrough_window.fund_name_is_shortening`) as well as the prefix ambiguity, then re-measure the fund/ETF slice. Suppressed downstream for now by ruling K-1, which is a *display* remedy, not a fix: the binding is still wrong in gold and any other consumer still reads it.
- [ ] (bug) **`attach_sector` has two latent traps in the new consensus overlay — a supported parameter now raises, and the map memo can go stale (found 2026-08-21 by the codex gate on `8590fc5`; both P2, neither live today).** (1) **Custom `isin_col` raises.** `attach_sector(..., isin_col='my_isin')` passes that name to `apply_us_consensus_sector`, but `pinned_us_consensus_map()` always returns the map keyed on the canonical `isin` — so the overlay does `cmap['my_isin']` and dies with `ColumnNotFoundError`. Reproduced by codex directly. **No production caller uses a custom `isin_col` today** (verified: the 5 callers all use the default), so nothing is broken now — but a previously-supported path is a landmine, and the only ways round it are `us_consensus=False` or hand-building a renamed map. Fix: rename the map's key column to `isin_col` inside the overlay. (2) **Memo key omits the reference vintages.** `_PINNED_MAP_CACHE` keys on `holdings_complete` (path, mtime_ns, size) plus the cusip/isin reference **path strings only** — not their mtimes. The map is DERIVED from those reference files, so a long-running process that rebuilds `cusip_reference`/`isin_reference` in place keeps serving the pre-rebuild map and relabels on stale sector evidence, silently. Today's builders are single-shot so it cannot fire; it becomes live the moment anything long-running or a rebuild-then-relabel script exists. Fix: include the reference files' (mtime_ns, size) in the key. **Deliberately NOT fixed in `8590fc5`**: the four canonical artifacts were produced by that exact module, and editing it post-verification would break the provenance chain between the committed code and the verified data. Fix both together, with a test per trap, before the next rebuild of any sector consumer. [fund_score: src/fundscore/reference/sector_attach.py]
  **Owner summary:** Two small booby-traps in the code that decides a stock's sector. Neither can fire today — nothing in the pipeline uses the affected settings — but both are the kind that fail silently later rather than loudly now. We chose not to touch that file after the data was already built and checked against it, so they're queued to fix together before the next rebuild.
- [ ] (bug) **Three latent fail-opens in the L16 passive-sector-parity code — one is a wrong-company-binding trap, MEASURED at zero impact on the shipped write (found 2026-08-21 by the codex gate on the L16 commit; all P2, none live today).** (1) **The identity bridge's tie detection is scoped to the ISINs you ASK for, so a subset call cannot see a tie that lives outside it.** `isin_identity_bridge()` filters `holdings_complete` to the requested ISINs *before* detecting keys that claim more than one ISIN — so a ticker shared between a requested ISIN and a non-requested one reads as clean and binds, silently relabelling the OTHER company's passive rows. That is the wrong-company-binding class ([[fmp-foreign-sector-collision-defects]]), made worse by being deterministic and therefore invisible. **Reproduced deliberately**: request both of `ROP`'s claimants (`CH1499059983`, `US7766961061`) → tie detected, key excluded; request only `US7766961061` → tie invisible, `ROP` binds. **Measured on the shipped write: 0 of the 14-ISIN bridge's 53 keys are tied in the whole-book bridge (which excludes 360), so nothing relabelled on 2026-08-21 is wrong** — the write stands. Fix: derive key conflicts over the FULL identity table, then restrict the OUTPUT to the requested ISINs. (2) **`--passive-blend` can promote a non-canonical book to gold.** `relabel_passive_sector_basis.py` mirrors the canonical-write guard for `--holdings-complete` but not for `--passive-blend`: point it at a staging/sample parquet, omit `--out`, and it reads that book and writes it to `data/gold/passive_blend_holdings.parquet`. Add the same guard. (3) **The new `cross_basis_sector_parity` gate can pass on a crossed pair.** It collapses each `(series_id, ISIN)` with `unique(keep="first")`, so when a shared security carries two labels on one side and the retained row happens to match, a genuinely crossed row is dropped before comparison — the gate under-reports exactly the multi-label case it exists to catch. (Pre-write the two definitions agree — set-level and unique-level both give 159 — so no measurement in the record changes.) Fix: compare distinct sector SETS, or compare row-level labels before de-duping. **Deliberately NOT fixed in the L16 commit**: the gate had already passed twice on that exact tree and the canonical `passive_blend_holdings` bytes were produced by it, so editing post-verification would break the provenance chain between committed code and verified data — the same call made for the `attach_sector` traps above. Fix all three together, with a test per trap, before the next bridge or parity-gate consumer. [fund_score: src/fundscore/reference/identity_bridge.py; scripts/pipeline/relabel_passive_sector_basis.py; scripts/checks/l16_cross_basis_parity.py]
  **Owner summary:** Three small booby-traps in the code that just put the passive side of the comparison onto the same sector basis as the fund side. The one worth naming: the tool that matches a ticker to a company only checks for ambiguity among the companies you hand it, so asked about a smaller set it can confidently give a ticker to the wrong company. We measured it against what we actually shipped — none of the fourteen securities is affected, so the data on disk is right — but it is the silent, confident kind of wrong, so it is queued rather than shrugged off. We deliberately left the code untouched after it had been reviewed and the data built from it, and will fix all three together with tests.
- [ ] (bug) **X-Ray stock-ladder tie-break is non-deterministic — equal-weight ties pick an arbitrary winner (found 2026-08-21 by the L14 seg-6 final data gate; PRE-EXISTING, not caused by that write).** On `exposure_xray_panel`'s `vs_benchmark` baseline, two stocks tying on exactly equal exposure weight resolve to an arbitrary winner, so a rebuild-twice diff is not clean: 2 `stock::` rows differed across two post-write runs (**KRYS vs FCFS** at 0.0327; **DHR vs V** at 0.0101). **The control proves it is ambient**: the same code on the pre-consensus input produced **3** such rows (PEGA/PLOW, EOG/GIII, DHR/V), so the post-write panel is marginally *more* stable — the L14 write neither caused nor worsened it. `coverage_state`/`confidence_state` are stable on the shared row set (0 flips on an inner join), and no sector row is affected. Fix: a deterministic tie-break (e.g. secondary sort on ticker) so rebuild-twice is bit-clean and a real regression can't hide behind ambient churn. **Same class, second site (found 2026-08-21 by the L14 seg-6 finalize check):** `build_holdings_complete.py` sorts on `(series_id, quarter_end, weight desc)`, which is not a TOTAL order — ULP-level jitter in `weight` across rebuilds reshuffles exact-weight neighbours. Measured on the seg-6 write vs its backup: **1,038 rows (0.060%) sit at a different position across 194 funds, max displacement 11 slots, and ALL 1,038 have `weight` unchanged to 1e-12** — pure tie reorder, no value moved. Key-aligned the two files are identical apart from `sector` (floats ≤1.2e-14 relative), so no keyed consumer is affected; the cost is that a positional diff of this artifact is noisy and the spec-level claim "row order preserved" is not literally true. Fix both sites with the same secondary sort key. [fund_score: build_exposure_xray_panel.py stock-ladder ranking; build_holdings_complete.py final sort]
  **Owner summary:** When two stocks are tied on exactly the same weight, the X-Ray picks one of them arbitrarily, so rebuilding the same data twice can produce slightly different output. It affects a couple of rows, it predates the sector work we just shipped, and nothing users see is wrong — but it means our "did anything change?" check has background noise in it, which is how a real problem eventually hides.
- [ ] (story) **EODHD consolidation — OWNER RULED 2026-08-21: "We can display it. Go with EODHD."** Adopts the 2026-07-16 validation spike (`fund_score/reports/product/eodhd_validation_spike.md`), which measured EODHD on the real held universe (5,528 funds · 5,815 US names/$57T · 19,554 foreign ISINs/$14.6T) and recommended consolidating **three vendors → one**. Measured then: US sector agreement **98.85%** (beats FMP's 98.2%, 100% coverage, byte-identical 11-label taxonomy) · **foreign sector 97.53%** · identity via ISIN **99.24%** · fund-NAV coverage **95.9% vs Tiingo 96.0%** · survivorship, delisting truncation, splits and dividend adjustment all PASS (delisting **beats Tiingo**, which carried SGEN 19 days stale) · **foreign price levels PASS** — SHEL.LSE 3,160 **GBX** correctly labelled, ASML €1,598 ⇄ $1,815 ADR reconcile. **Gate 5 (foreign USD returns) was left at "inputs validated, build pending" — that is the entire foreign-pricing gap, and it is a BUILD, not a purchase.** Staged order from the spike, in risk order: **(1) replace FMP with EODHD for the international layer, drop FMP** — strict win (display rights, CUSIP-via-ISIN which FMP lacks, same taxonomy); **(2) migrate US sector + identity off Sharadar reference**, keeping `sector_rollup.py` as a safety net; **(3) migrate US stock + ETF prices**, adding a $0.01 floor + rolling-median band for the illiquid tail (Sharadar's only real edge, and it lives in **<0.53%** of held value); **(4) fund NAV LAST and carefully — only after the mutual-fund distribution validation layer exists.** ⚠ **Stage 4 IS the `price-panel-distribution-coherence` spec** — same work, sequence them together or it gets built twice. ⚠ **Nothing is built today**: no fetcher, no data on disk, one stale comment in `build_holdings_complete.py` predating the spike; `EOD_API_KEY` has been in `.env` since 2026-07-16. ⚠ **FMP removal means the sector work standing on it (L14, L1) must be RE-POINTED, not extended.** Cost ~$399/mo Internal Use per the spike; the display tier was never quoted because the drafted inquiry (`reports/legal/eodhd_licensing_inquiry_email.md`) was never sent — **display rights are recorded as the OWNER'S ruling, not a document on file.** **RECEIPTS DEPENDENCY 2026-08-26: stage 1 (the international PRICE layer) is now a prerequisite of per-stock receipts Segment 1 (owner ruling D-1: EODHD-primary / US-fallback); ingestion spec dispatched → specs/queue/eodhd-international-prices.md — that spec covers stage 1 only, this item stays Open for stages 2-4.** [fund_score: new EODHD vendor layer; retires FMP, then Sharadar reference, then Sharadar SEP, then Tiingo]
  **Owner summary:** You bought EODHD in July and we measured it properly at the time — it matched or beat what we already pay for on every axis that matters, and crucially it covers international stocks and their currencies, which is the thing we cannot do today. The recommendation was to consolidate three vendors into it. That recommendation then sat for five weeks and nothing was built. You've now ruled to go ahead. The order matters: international data first (and drop the vendor it replaces), then US sector, then US prices, and fund prices last because those need a safety check built first — the same check already queued for the fake one-day chart moves.
- [ ] (data) **As-of mislabel: 812 served rows carry a holdings date NEWER than the book actually used, most of them stamped HIGH CONFIDENCE (found 2026-08-20 by L10 Segment 0; RIDES BEYOND L10's SCOPE — needs its own owner).** 812 of 2,610 served concentration rows label a `holdings_as_of` newer than the snapshot the figure was computed on: **21 off by more than a year, 9 by more than two, and 45 of the worst 48 stamped `coverage_state: available` + `confidence_state: high`.** Worst specimen: **JFEAX's figure was computed on the 2022-10-31 book and served labelled 2026-04-30** — a four-year-old number presented as current, at high confidence. **The reason this is filed separately from L10: the same mislabel rides `active_share` and `hhi`, which L10's spec does not touch**, so fixing L10 alone would leave two other served stats carrying the same false stamp. Verify the propagation before scoping the fix (L10's checkpoint was asked to confirm it). A confidently-stamped wrong date is the wrongness class this project ranks above an honest gap — the reader has no way to discount it. [fund_score: diversification/concentration as-of stamping → fact_assembler]
  **Owner summary:** Some funds show a portfolio statistic calculated from data years older than the date printed next to it — one fund's figure comes from October 2022 and is labelled April 2026 — and nearly all the worst cases are marked "high confidence". A stale number that admits it's stale is survivable; one that claims to be current isn't. It's filed on its own because two other statistics on the page carry the same bad stamp and wouldn't be fixed by the work already queued.
- [ ] (data) **CSIUX serves a fabricated +62.72% one-day step and NO branch of the plan owns it (found 2026-08-20 by the capital-gain re-check; UNOWNED — this is why it is filed separately).** Price hygiene excised only the *reversion* leg (2022-07-14), so the served `passive_alt_daily_nav` **still carries the fabricated print step on 2022-07-13** and the stale 1.9632 level after it. MAPOX, the sibling case, is fully excised (0 served rows around its prints) — so the machinery can handle this shape and simply did not here. CSIUX was adjudicated OUT of the capital-gain rule's scope (it is a bad print, not an early-factor artifact), and the capital-gain report's "price_hygiene already handles them" claim is true only for the kept 76 of the 2025-09 family — **not for CSIUX**. Fix: excise the print leg as well as the reversion leg, or truncate at the print. **Check the same asymmetry on the other three abandoned tickers** (BRLIX, JOPSX, SMGAX — all four are wrong stamped amounts: JOPSX/BRLIX stamped ~3× too big serving +28.1%/+28.3% against a true ~0%, SMGAX ~½ too small fabricating a −26.5% LOSS). [fund_score: price_hygiene excision scope → passive_alt_daily_nav]
  **Owner summary:** One fund's chart shows a 62.7% one-day jump that never happened. We already spotted the problem and removed the *second* half of it — the day the price came back — but left the fake jump itself in the served data. A sibling fund with the identical problem was cleaned up properly, so this is an oversight rather than a limitation. It matters because nobody currently owns it: it was ruled out of scope of the capital-gain work, and the report that ruled it out implied hygiene had it covered, which it doesn't.
- [ ] (bug) **D8-9: `return_attribution` row order is non-deterministic — 56 funds (filed 2026-08-20; FIRST DIAGNOSIS WAS WRONG AND VACUOUS, corrected at checkpoint; pre-existing, NOT L6's).** Two same-process runs of `_return_attribution_by_series()` return the same row SET in a different row ORDER, so the served section can reorder between builds with no code change. Reproduced on MAIN's pre-L6 code (`75980a3`): 43 of 4,106 funds differ in order, 0 in set; no L6 commit or working-tree hunk touches that function. **True mechanism: exact-duplicate `(period, dimension, value)` float keys** — overlapping-theme twins carrying bit-identical aggregates (e.g. `S000000137`: `theme::mag_7` and `theme::ev_battery_chain` both at contribution `−26.954114716242962`) shuffled by an unstable `.unique()`/sort. **True exposure: 56 of 1,939 funds.** ⚠ **The original filing said "all 107 moved positions have `total = null`" and "1,939 of 5,819 funds exposed" — both vacuous**: served rows carry **no `total` field at all**, so the first is true of every row by construction, and 1,939 is merely the count of funds that HAVE a `return_attribution` section. The real sort key `contribution_to_active_return_bps` is null on **0** served funds. A fix designed against "nulls sort last" would not fix this. Fix: make the sort total by adding a deterministic tiebreak on a stable key. **Related upstream smell worth its own look:** distinct themes carrying bit-identical weight/return/contribution is itself suspicious independent of ordering. [fund_score: fact_assembler `_return_attribution_by_series`]
  **Owner summary:** The "where did the return come from" section can list its rows in a different order each time we rebuild, with no change to the data. It happens when two themes have byte-identical numbers and the sort has no way to break the tie. It affects 56 funds — our first estimate of 1,939 was wrong, because the check we used couldn't have detected anything. Nothing shows a wrong number; the ordering just isn't stable.
- [ ] (data) **Per-stock returns: at least $540B of held value is UNPRICED but RECOVERABLE from a bridge we already have (measured 2026-08-20, owner-asked).** Return attribution joins prices from `sharadar_sep` **by ticker** (`build_return_attribution.py:55`). Measured on the densest quarter (2026-03-31; 2,471 series, $12.74T): **87.1% of held value is priceable per-stock, 12.9% — $1,644B — is not.** The gap is NOT what the line previously told the owner ("the vendor covers US exchanges; the rest is genuinely unreachable"). **81% of the unpriced rows (198,546 rows, $1,340B) carry NO TICKER AT ALL** — they never reach a price lookup to fail it. Splitting the $1,644B by the ISIN-keyed `fmp_isin_us_ticker_bridge`: **RECOVERABLE $540.4B (32.9%) — 26,732 rows / 1,047 securities / 1,808 funds** whose ISIN resolves to a US ticker **already in our price store** (TSM $46.6B across three name spellings, NVS $11.4B, BTI $10.6B, TTE, NXPI, LIN, HSBC); **$716.1B (43.6%) / 4,128 securities** where the bridge yields a US ticker we do NOT price (delisted / OTC — unquantified, needs its own pass); **$299.6B (18.2%) / 17,335 securities** with no bridge entry (the plausibly-honest foreign-exchange-only cohort); **$87.9B (5.3%)** with no ISIN on the holding row. **CORRECTED 2026-08-20 by L9 Segment 0 — the dispatcher's "off by sixty-fold" was a CATEGORY ERROR and is retracted.** The L9 ruling's "+30 funds" is a **floor-crossing (extensive)** number and is low by only **~1.6x** (true **+47** on the plain fix, **+116** with ADR pricing); the **1,808 funds** is the **intensive** margin (funds with at least one affected position). Both were right about different quantities. **The real defect is neither: of the 1,947 funds clearing the 0.80 floor, only 601 (30.9%) can be given a COMPLETE card — 1,346 (69.1%) are missing at least one priceable position from a table headed "every position", median 4.14% of NAV, max 17.10%.** Also corrected: **$19.8B of the $540B is a stale-quote mirage** (BBL last quoted 2022-01-28, ABBNY 2023, AKZOY 2019 — pricing them fabricates a 0% return), so recoverable is **$521.6B**; and **the denominator is NOT defective** — `equity_coverage_book` is already foreign-inclusive, the numerator is under-counted. **PRNEX crosses the ratified floor** (0.593 → 0.730 → 0.821). **The chokepoint is `build_holdings_snapshots.py`'s identity rule, not `build_return_attribution.py:55`, and 51 consumers ride that book** (incl. the L2 passive solver and the serving scope) — so any fix must be an ADDITIVE recovered book, never an in-place migration. **Two things to settle before building:** (1) **basis/commensurability — is a US ADR's USD return the right return for a foreign ORDINARY line?** It differs from the local-currency return by FX; defensible for a USD-reporting fund but it must be adjudicated and labelled, not assumed. (2) **the wrong-company trap** — bind on ISIN ONLY; `ROG` and `MC` are in the store as Rogers Corp and Moelis, not Roche and LVMH ([[fmp-foreign-sector-collision-defects]]). **This denominator is what the 0.80 priced-coverage floor (S2-a, ratified 2026-08-20) is computed on**, so a recoverable gap means the floor is excluding funds it should not. [fund_score: build_return_attribution ticker resolution + L9 receipts]
  **Owner summary:** You asked why we can't work out returns for foreign stocks. The honest answer is that a third of the gap isn't a foreign-data problem at all — it's ours. The sharpest version of it: of the funds that qualify for a holdings card today, **only 31% get a complete one** — the rest are missing holdings we could price, from a table that says "every position". For $540B of holdings across 1,808 funds, we already own the price data and simply never connect it to the holding, because the fund files the stock under its foreign identity and we never look up the American listing we do have. Taiwan Semiconductor alone is $47B of that. Some of the rest genuinely is unreachable — Roche's Swiss shares, most Japanese domestic names — but the "we just don't have foreign data" answer you were given is not right, and this same number is the denominator behind the 80% coverage floor you just approved.
- [ ] (bug) **D8-8: `build_positioning_changes_panel.py --sample N` crashes for small N (pre-existing; found 2026-08-20, PROVEN not L6's).** `--sample 25 --dry-run` dies at the sector-attach log line — `_cls.mean()` returns None on an empty holdings frame, because `--sample` takes `sorted(eq)[:N]` and the alphabetically-first N series can hold no classified equity. **Reproduces identically with `--no-te-impact`**, sits upstream of all TE code, and `git diff` confirms the line was never touched by the L6 work — so it is a latent defect the L6 round surfaced rather than caused. `--sample 400` works. Fix: guard the log line against an empty frame, or make `--sample` draw a representative slice rather than an alphabetical prefix. [fund_score: build_positioning_changes_panel.py sector-attach logging]
  **Owner summary:** A developer convenience flag for building a small test slice crashes when the slice is small enough to contain no classified holdings. Nothing users see; it makes quick local test runs fail confusingly. Pre-existing, not caused by the current work.
- [ ] (bug) **Degenerate twin: 144 funds' twin IS the reference leg, so movement 03 compares a number to itself (found 2026-08-18 by the movement-03 data audit).** 144 served funds have a twin of exactly `100% VT`, identical to the "World stocks (VT)" comparison leg — the page renders "$46,574 ... World stocks turned the same $10,000 into $46,574" and the growth chart draws the gold twin line on top of the grey reference line with no acknowledgement. ~446 more are NEAR-identities (234 SPY, 209 VTI vs IVV, 3 AGG vs BND). The false takeaway sentence is **FIXED** (`3c291ab` — suppressed when both capture ratios round to 100), but the open question is product: should the capture CARD, or the whole section, render at all when the twin is the reference? [fundscore-web: M03Neighbourhood + fact_assembler]
  **Owner summary:** For 144 funds the "cheapest alternative" we compare against IS world stocks, so the page compares a number to itself and draws two identical lines on one chart. We've stopped it claiming one beat the other; what's left is whether that comparison should appear at all.
- [ ] (story) **"Twin" means two different portfolios on one fund page, and one section's caption disparages the other's method (found 2026-08-18).** Movement 02 grades the fund against a POINT-IN-TIME twin refit as the fund changed; movement 03 shows TODAY'S mix held fixed and backcast. On PRNEX, over the *same* window, they differ by **$4,097 / 16.7%** ($28,600 vs $24,503) for the same named object — and the two figures sit close enough ($24,503 vs $25,229) to invite the false inference that 2008-2012 was flat, when M03's twin actually **lost 11.8%** over that stretch. M02's own caption warns against "today's mix projected backwards — which would flatter or punish the fund using a mirror that did not exist at the time", which is exactly M03's method. M03 discloses the WINDOW difference but never the CONSTRUCTION or COMPOSITION difference. Options: unify the construction, or rename them and show both with the delta. [fundscore-web: M02Record + M03Neighbourhood copy]
  **Owner summary:** The same word — "twin" — means two different things in two sections of one page, built two incompatible ways, and one section explicitly criticises the method the other uses. The numbers are close enough that a careful reader draws a conclusion that isn't true. This is a product-truth call, not a bug.
- [ ] (bug) **18 passive index ETFs are served a "judge the manager" neighbourhood, contradicting the assembler's own docstring (found 2026-08-18).** `AIEQ CSD CUT CVY CZA DIVI OUSA QQQM RFG RFV RPG RPV RSP RZG TAN VMAX WLDR XLG` carry `management_style='passive'` yet get movement 03, which reads "before you judge the manager, judge the space" and "only second a bet on the manager". TAN is an index ETF with no manager, and its "passive twin" is a rival passive ETF. `_neighbourhood_by_blend`'s docstring already states a twin is meaningless for passive vehicles, and the methodology page says the same. **One root cause, two symptoms:** these 18 are EXACTLY the 18 whose `passive_baseline` has no `source`/`asof_refit_date` — they entered through a path bypassing the L2 fit record, so their mix-as-of chip date is globally asserted rather than sourced from their own record. Fix: suppress in the assembler on `management_style == 'passive'`. [fund_score: fact_assembler neighbourhood]
  **Owner summary:** Eighteen index funds are being told to judge a manager they don't have. Our own code comments already say this shouldn't happen; a side path let them through, and the same gap means their "as of" date is assumed rather than looked up.
- [ ] (data) **Movement 03's headline dollar figure assumes DAILY REBALANCING for 18 years and never says so (found 2026-08-18).** `build_neighbourhood_panel.py` compounds `sum(w_t * ret_t)` daily = a daily-rebalanced constant-weight portfolio, while the copy says the mix is "held fixed" and is "what someone could buy today" — which reads as buy-and-hold. PRNEX displays **$25,229 (+5.25%/yr)** against a buy-and-hold **$26,539 (+5.55%/yr)**. Across 231 multi-leg blends: **44 exceed 0.25pp/yr, 25 exceed 0.50pp/yr, max +2.96pp/yr** — worst case `SOXX/VT` displays **$111,953** vs buy-and-hold **$177,659**, a **$65,706** gap on a $10,000 stake. Also assumes zero spread, commission and tax. Single-leg twins (2,860) unaffected. Fix: disclose ("rebalanced daily to those weights") or switch to buy-and-hold. [fund_score: build_neighbourhood_panel + web copy]
  **Owner summary:** The growth figure quietly assumes you rebalanced the mix every single day for 18 years. For most funds that barely matters; for the worst it is a $65,706 difference on a $10,000 investment. We either say so or change the assumption.
- [ ] (bug) **Movement 03 minor defects batch (found 2026-08-18 by the data audit; numbers all verified).** (a) `n_days_dropped` is emitted on **0 of 3,094** served rows though present on all 6,202 gold blends — `M03Neighbourhood` carries a disclosure paragraph that **can never render** (dead disclosure decays into false assurance); wire it through or delete the branch. (b) **7 funds display a twin but get no movement 03** — `FINX HIPS RZV DJD CQQQ BBMRX EMOP` — all 7 blend keys DO exist in gold, so the cause is a missing fit row, not the panel desync the methodology page describes. (c) Web parser is fail-OPEN on `drawdowns`/`years`/`capture` sub-arrays (section still renders with cards silently omitted); mirror the assembler's fail-closed contract. (d) Capture equal-weights a 35-day first stub and a 10-day last stub as "months" (2 of 217). (e) `underwater_months` is a calendar-month difference, up to ~1 month imprecise. (f) Year bars colour the twin's negative years red while world's stay grey — asymmetric encoding on two series presented as comparable. (g) `fmtDollarsMagnitude` uses `toLocaleString()` with no locale while the tooltip pins `en-US`. (h) `registry.ts` `nav-series` says "through 2026-05 / profile_nav_series_v1_2026-07-04"; served is **2026-07 / v3_2026-08-17**. [fundscore-web + fund_score]
  **Owner summary:** A batch of small, verified defects on the new neighbourhood section — including a disclosure paragraph that can never appear, seven funds silently missing the section, and a stale date on the methodology page.
- [ ] (data) **Price data is 39 days stale and movement 03 shows no price-as-of (found 2026-08-18).** Every leg's last priced day is **2026-07-10** (panel written 31 Jul; today 18 Aug). The section's only date is `MIX-AS-OF 30 JUN 2026`, which is the **refit** date, not the **price** date — a reader will conflate them, and "JULY 2026" reads as end-of-July when it is the 10th. Add a price-as-of affordance. [fund_score: fund_daily_adj_close refresh + web copy]
  **Owner summary:** Our prices are about six weeks old and the page shows no "prices through" date — the only date on it means something else entirely.
- [ ] (data) **Terminal + mid-series capital-gain / basis breaks — ONE item (OWNER DECISION 2026-08-17: thread (c) and the December capital-gain defect are one defect at two positions, adjudicate together).** When a fund makes a distribution or changes share basis, `adj_close` records the drop but is never back-adjusted, fabricating a cliff. **Terminal face (measured 2026-08-17 on v7): 31 served funds carry a fabricated chart tail; 4 also serve a headline verdict computed off it — AQLGX −290, CWCFX −210, PSMPX −150, QMGAX −70 bps.** Cohort boundary uncertain: the L15 scan gave 32 tickers at ≥25% requiring a dying feed, a looser scan gives 55 — reconcile before sizing. Mid-series face: fabricated weeks inside `te_current` windows (BIAGX served −230bps at HIGH confidence). **No existing rule fires** — no round trip (no excision), no excised neighbours (no bridge, so terminal truncation never looks), every step below the ratified 1.98× bar. **Candidate discriminator (costs no new magnitude threshold): a one-way step whose own matched passive benchmark did NOT move** — PLVPX −37.7% vs −2.6%, NTHFX −34.6% vs −0.2%, RIMHX **+28.8%** vs −1.4% (a liquidating fund does not gain 29% in a day). AQLGX is already in both faces, which is why splitting them risks two rules disagreeing on one fund. **BLOCKS a serving reload at owner level.** [fund_score: price_hygiene terminal/one-way trigger + benchmark corroboration]
  **Owner summary:** When a fund pays out or changes its share basis, our price feed shows a cliff that never really happened. It hits 31 fund charts at the end of the line, and 4 of those funds show a headline fee-vs-passive verdict calculated from the fake move. The giveaway is that the fund's own benchmark didn't budge on the same day — one fund appears to *gain* 29% the day it liquidated. You decided to fix this together with the December version of the same problem rather than write two rules that could disagree about the same fund.
- [ ] (data) **W3: extend the chart quarantine to `value_score` (OWNER DECISION 2026-08-17: withhold both).** 6 funds are withheld from `profile_nav_series` as unreliable yet still serve a headline verdict computed from that same history — MSVSX −150, SHXPX −150, JDSAX −130, HIACX −110, QMGAX −70, NGUAX −60 bps. The "the defect may fall outside the score window" defence does NOT hold: `n_weeks` runs **389–1,228** (7.5–23 years), so the estimation window fully contains the withheld period, and the fits look healthy (`replica_r2` 0.86–0.93) which is exactly why it reads as fine. Cost of consistency: 6 of 2,270 scores (0.26%). Make ONE rule govern both panels so they can never diverge again (`served_quarantine.py` already applies post-build to both). Note QMGAX is also in the capital-gain cohort above. [fund_score: served_quarantine → value_score serving]
  **Owner summary:** Six funds are too unreliable for us to draw a chart, but we still print a precise "you're losing 1.5% a year to fees" verdict for them — computed from the very history we refused to show. You chose consistency: if we won't chart it, we won't score it. Costs a quarter of one percent of our coverage.
- [ ] (chore) **Tighten the price-hygiene ratchet to 563 and re-tighten per fix (OWNER DECISION 2026-08-17).** `data/reference/price_hygiene_step_allowlist.json` records `max_fund_steps: 584` while the actual count is **563** — 21 steps of slack in which a new defect class could reach served data and still pass the gate green. Set to 563 now; make re-tightening an explicit step in any fix that lowers it (the capital-gain item and W3 both will). **Do NOT auto-ratchet on green builds** — the count scales with universe size, so a partial or sampled build would silently record a too-low ceiling and the next full build would fail on nothing. Also refresh the stale `_status` note, which still cites "522 flagged steps" from a superseded definition. [fund_score: price_hygiene_step_allowlist.json]
  **Owner summary:** Our tripwire for "new fake prices appeared" is set 21 steps looser than reality, so a new problem could land without setting it off. Tightening it to the true number, and re-tightening each time a fix lowers it.
- [ ] (data) **Risk-decomposition chain runs on an UNHYGIENED basis — do not regenerate the td cache until fixed (OWNER DECISION 2026-08-17: leave absent, file this).** `build_risk_decomposition.build_td()` reads the fund leg from the RAW canonical panel — which still contains the fabricated prints (MMTMX 2025-09-10 = 8.8122 confirmed present 2026-08-17; L15 hygiene is applied downstream at NAV-build, never baked into the panel) — and the ETF leg from Sharadar SFP + Tiingo gap-fill rather than the canonical ETF basis. `data/_tmp/risk_decomp_td.parquet` is currently ABSENT (deleted by a since-removed line in `build_l2_replica_quality.py`). **No serving impact today** — `idio_alpha_l2` (Jun 25), `theme_bet_attribution` (Aug 6) and `full_model_idio` (Jun 25) already exist on disk — but they were built on this basis and re-running them would rebuild served `theme_ride_bps` on it again. Keep the cache absent as a deliberate tripwire; **change the four consumers' error message** from "run build_risk_decomposition first" (which invites exactly the wrong action) to point at this item. [fund_score: build_risk_decomposition price basis + consumer error text]
  **Owner summary:** A whole family of our risk features is computed from price data that still contains the fake prints we just spent this work removing — the cleaning happens later in a different step. Nothing users see changes today, but rebuilding those features would bake the bad prices back in, so we're deliberately leaving a piece missing so nobody rebuilds them by accident, and making the error message say why.
- [ ] (data) **Size the run-selection cohort using the builder's OWN trim logic, then decide (OWNER DECISION 2026-08-17: measure first).** SPAX is unserved — absent from both `profile_nav_series` and `value_score` — because a new 14-month feed hole (2025-04 → 2026-06) makes the "most recent contiguous run" trim keep a 1-month stub over a good 28-month run. **No user-visible wrongness; a coverage gap.** Blast radius UNKNOWN: a naive month-gap reimplementation returned 310 candidates but is INVALIDATED — VWUSX/AMECX/NMANX appear in it yet are served with full 283-month histories, so that scan measured the assumption, not the code. MUIBX is unserved and a candidate, unconfirmed. Measure with the builder's actual trim before changing anything: run-selection governs every one of the 9,238 series, so a blind change is how a coverage fix becomes a coverage regression. Candidate rules: prefer the longest qualifying run, or the most-recent run clearing `MIN_MONTHS`. [fund_score: build_profile_nav_series run selection]
  **Owner summary:** One fund disappeared from the site because a gap opened in its price feed and our rule picked the one-month scrap after the gap instead of the two-plus years before it. Nothing shown is wrong — the fund is just missing. Before changing the rule that decides this for every fund, we're measuring how many others it affects; my first attempt at that count was wrong and I threw it out.
- [ ] (data) **B1: look-through carries `valUSD` UNSCALED — 68% of the gold holdings panel's USD is fabricated (pre-existing; found + CONFIRMED 2026-08-07 by the L1 sweep; FIX IN FLIGHT as L1 Segment 1b).** `build_holdings_complete.py` `lookthrough()` (lines ~172-202) scales `weight`/`pct_nav` when expanding master-feeder positions but sums the master's `valUSD` through unscaled; a SECOND copy lives in `src/fundscore/product/lookthrough_window.py:233-247` (`_regroup`). Measured: ~340-407 feeder series carry $49.7-50.6T against $0.8-1.6T filed (32×; worst single series 5,632×); panel total $73.94T vs filed ~$24-25T; row-level contamination extends into "clean" series (1,776 rows / $11.06B via the `valUSD/pct_nav ≈ filed netAssets` identity). **ZERO served fields affected** (reviewer-enumerated: top_holdings/fund_holdings_full/aum all read raw or metadata sources) — gold/research surfaces only, but it invalidates every USD-denominated acceptance number until fixed. The existing C2 country-coherence gate is DEGENERATE for this class (unscaled carry makes feeder≈master, passes trivially); the fix must land with the non-degenerate row identity as a permanent gate. [fund_score: build_holdings_complete.py + lookthrough_window.py]
  **Owner summary:** Our internal research panel double-counts fund-of-fund positions at the parent fund's full size instead of the investing fund's slice, inflating two-thirds of its dollar figures. Nothing a user sees is touched — the website reads different, correct sources — but our own analysis dollars were wrong, and the existing safety check was structurally blind to it. Fix is in flight with a new check that can actually catch this class.
- [ ] (data) **Per-sector confidence semantics + serialization (L1 Segment 5, re-filed 2026-08-09 — deferred pending owner decision P3).** All 3,540 served series carry ONE fund-wide sector confidence label; the fix makes it per-row, and the open call is the label's MEANING: "how much of the fund did we identify" (smaller step) vs "can the unidentified slice overturn THIS row's claim" (trust semantics, worker+dispatcher recommendation, uses the existing credible_tilt machinery). Either way: serialize the raw per-row bound to serving so web thresholds re-tune without a rebuild (the web gate field the F1 critic confirmed missing). [fund_score: exposure_xray confidence + fact_assembler serialization]
  **Owner summary:** The "how much do we trust this row" question from the P3 brief — everything is scoped and ready to build the moment you pick a meaning for the label ("adopt" or "smaller step").
- [ ] (data) **Canonical price panel holes: 15 missing ticker-dates across 7 ETFs, 12 recoverable from sharadar (filed 2026-08-09 by the L5 EDA; reviewer-verified).** In-window for the neighbourhood panel (dropped via the all-legs-priced intersection, max 2 days/blend): EDV+MUB 2008-07-14, EDV 2009-01-27, RXI 2010-05-06 — all 4 recoverable. Out-of-window but genuine canonical-panel defects: IXC 2006-07-14; MUB ×6 (2007-12→2008-02); EDV 2008-01-30 — all recoverable. **GMF/IAK/IJJ on 2010-05-06 are ABSENT FROM BOTH vendors** (plausible Flash-Crash busted trades) — known-honest, do NOT re-open as a lead. Fix at the canonical panel source ([[canonical-price-panel-and-l2-basis-split]]); consumers then drop their n_days_dropped automatically. [fund_score: fund_daily_adj_close builder]
  **Owner summary:** Our master price file is missing a dozen scattered days for a few ETFs (worst case: six days of one muni-bond fund around 2008). The new neighbourhood feature handles it honestly by skipping those days, and the fix belongs in the master file so everything downstream inherits it. Three gaps are provably unfixable — the data doesn't exist anywhere — and are marked so nobody chases them again.
- [ ] (data) **pctVal-scale coherence guard (S7-2, filed 2026-08-09): 8 filings write pctVal as a FRACTION where the schema wants a percent.** Detected by the window NAV gate as an identity (Σ valUSD/NAV == Σ pctVal, a factor-of-100 discrimination); $135.2M, pre-existing filer errors, unserved value path. Build the inverse sibling of the 2026-07-08 pct_nav [0,1.5] guard: detect and null (or rescale with evidence) fraction-misfiled pctVal at ingest. [fund_score: holdings ingest guards]
  **Owner summary:** A handful of funds' filings write "0.99" where the form asks for "99%". Our new safety check caught them; this files the small permanent guard so they're handled at the door instead of being spotted downstream.
- [ ] (data) **S7-4b: within-US wrong-company cusip binds — 3 ISINs / 15 rows / $10.1M served-wrong in gold (proven 2026-08-09; NOT fixable by domicile routing).** SharkNinja rows bound to SANCHEZ ENERGY's cusip (79970Y105), Shift4 to ENERPLUS (292766102), plus GE-Aerospace-cusip case — a wrongly FILED cusip binds through Sharadar inside the US branch. Same identity-adjudication class as the 635-group cross-company-reference item; adjudicate together. Distinct from the 3 same-issuer Sharadar-vs-FMP taxonomy splits also found (different class, benign). [fund_score: cusip_reference identity adjudication]
  **Owner summary:** Three foreign companies whose filings carry a wrong US identifier get another company's sector in our research panel — small dollars, but it's the "wrong company under a right name" class and belongs with the existing identity-cleanup item.
  **UPDATE 2026-08-25 — the SECTOR consequence is CLOSED; the item stays open for the BINDING itself.** `sector-identity-defect-recovery` shipped: a row whose filed CUSIP resolves to a different company than its ISIN and name now neither VOTES in the US-consensus nor RECEIVES the consensus label, so SharkNinja, Shift4 and the GE-Aerospace/Genie case all carry correct sectors in gold and in the product staging table (measured: 20/20 contradictory securities resolved, value coverage 1.0; 28 actually-mislabelled US rows / $14.4M / 28 funds corrected). **What is NOT fixed: `cusip_reference` still binds `79970Y105`→Sanchez Energy, `292766102`→Enerplus, `369604301`→GE Aerospace, and every consumer that reads TICKER or NAME off that binding is still wrong.** The sector overlay routes around it; it does not correct it. Keep this item for the binding, and adjudicate it with the 635-group cross-company item and the 360 excluded tie keys (ROP item above).
- [ ] (data) **Identity nondeterminism in holdings look-through: arbitrary winner on collapsed security slots (pre-existing; CONFIRMED 2026-08-07; FIX IN FLIGHT as L1 Segment 1b).** Two mechanisms: `.first()` over unordered group_by (`build_holdings_complete.py:190-197`) and `any_value()` in `_PULL_SQL` (~119-125). Root aggravator: `cusip_reference` binds junk sentinel cusip `999999999` → Sharadar `BAPG0TF0` ("Mountain Parent Inc. revolver"), collapsing 13 unrelated securities ($13.5B, incl. SocGen $9.85B) into ONE security_id whose name/sector/isin an arbitrary row wins — two identical-code builds differ on 151 isins / 15 sectors / 7,175 names, 12 rows/$13.13B re-binding to a *different company*. Same class as the l2_blend_etfs dedup lesson: adjudicate identity from the join surface; tied claimants → honest exclusion. [fund_score: build_holdings_complete.py + cusip_reference sentinel hygiene]
  **UPDATE 2026-08-25 (R-7, measured in-run by `sector-identity-defect-recovery`) — the `999999999` sentinel is INERT for SECTOR on this surface, and the reason is worth keeping: it is TWO independent reasons, not one.** (1) The 5 `WALDENCAST PLC Revolver` rows / $4,117,500 that file `cusip=999999999` (acc `0001735964-26-000010`) DO exist in the 2026 raw store, but with `seriesId` NULL and `isin` NULL — so they never enter `holdings_complete` (**0 rows there carry cusip `999999999`**). (2) Even if one did, `cusip_reference['999999999'].sector` is NULL, so it would yield a null US label and fall through exactly like `'N/A'` — the sentinel cannot inject a wrong SECTOR. **The residual risk is real and belongs to THIS item: `cusip_reference` maps `999999999` → ticker `BAPG0TF0` / "MOUNTAIN PARENT INC. REVOLVER", so any row that does carry it takes a wrong TICKER/NAME binding.** Separately, 491 SERVED rows still keep `999999999` as filed (sector-inert, verified). `000000000` and `N/A` are correctly ABSENT from `cusip_reference`; `999999999` is not — that asymmetry is the fix surface.
  **Owner summary:** When several securities collide into one identity slot, the pipeline picks a winner at random per build — so a rebuild can silently relabel billions in holdings as a different company. Pre-existing, mostly invisible to users, but it makes rebuilds unreproducible and can serve a wrong name/sector. Fix in flight: deterministic adjudication, and ties get honestly excluded instead of guessed.
- [ ] (data) **Twin-label defect: the record's passive leg is a point-in-time twin CASCADE but is labeled as a single current ETF — MUST FIX BEFORE F6 cutover (found + adjudicated 2026-08-07 by the F1 condition-5 critic).** The served `nav_series` passive leg is `passive_alt_daily_nav`, whose benchmark is a walk-forward sequence of annual point-in-time twin refits (`data/bronze/benchmark_calculation/level2/weights/`; PRNEX: XLE'12-16 → IXC'17 → IXC+VT → VEU+XLE → VT+XLE → IXC+VT → IGE+VT 68/32 current) — methodologically SUPERIOR (no hindsight) but `nav_series.passive_label` names only the lead ETF on 204/218 multi-leg funds and nothing served describes the cascade, so the page's "measured against IGE — raw index" is factually wrong about the plotted 2012-16 line. Verdict fit/fee correctly describe the final segment; cost leg stands (48.64bps). Fix upstream: serve basis metadata ("point-in-time passive twin, refit annually; currently 68% IGE + 32% VT" + optional composition history), then swap web copy. Critic suggested the lineage display as a trust differentiator. [fund_score: build_profile_nav_series passive_label / basis metadata + web copy]
  **Owner summary:** The performance chart compares each fund against the honest thing — the twin as it was fitted at each point in time, no hindsight — but the page CAPTIONS that line as if it were one current ETF, which is wrong about the earlier years. The math is right; the name is wrong. Fix the label to say what the yardstick really is; done well ("twin composition history") it becomes a trust feature no competitor shows.
- [ ] (data) **Active-share fail-open upstream: 17 funds serve exactly 0.5 vs an EMPTY benchmark at confidence `high`; discriminating fields dropped before serving (confirmed 2026-08-07).** Gold `diversification_panel` rows (e.g. MJFOX, MINDX) show `active_share=0.5`, `method='raw_herfindahl_no_lookthrough'`, `lookthrough_resolved_weight=0.0` — a 50% active share against nothing — yet `confidence_state='high'` reaches serving while `method`/`lookthrough_resolved_weight` do not, so the web tier CANNOT tell these apart (~24 more silently biased). Web currently gates the stat closed for everyone (F1). Fix upstream: propagate `method` + `lookthrough_resolved_weight` onto the served x-ray row and gate there; general pattern — `confidence_state` alone is not trustworthy provenance. [fund_score: diversification_panel → fact_assembler propagation + gate]
  **Owner summary:** A handful of funds report a "50% differs-from-index" stat that was computed against an empty index — and the served data carries no field that would let the site detect it, so the whole stat is currently hidden for everyone. Fix is to pass the two tell-tale fields through and suppress just the broken rows, restoring the stat for the ~98% of funds where it's sound.
- [ ] (data) **Canonical-ticker selection silently erases Dodge & Cox Stock's record (observation, F1 critic 2026-08-07; same class as the l2 share-class adjudication item).** DOXGX (Class X, 2024-) won canonical over DODGX (1965-), so the fund famous for its 60-year record shows a 2-year chart and a withheld verdict (β null). Deterministic-dedup identity question — adjudicate from the join surface with record-length/AUM in the key, or serve the share-class family's longest history with share-class fees. Fold into / sequence with the l2_blend_etfs share-class adjudication (queue L3). [fund_score: canonical ticker→series binding]
  **Owner summary:** Our "one ticker per fund" pick chose the brand-new share class of Dodge & Cox Stock, so one of the most storied funds in America shows up with a two-year history and no verdict. Nothing is wrong in the data — we just picked the wrong representative. Should be adjudicated alongside the existing share-class work.
- [ ] (chore) **Serve a `lookthrough_applied` flag (or `book_basis` + holdings_complete line count) on the concentration rows of `fund_profile_facts` (filed 2026-08-07 by the F1 worker; natural rider on L10's serving change).** The web can only PROVE look-through for `n_positions ≤ 10` funds (121); 637 further funds diverge ≥5pp from the feeder book but are indistinguishable payload-side from EC-long narrowing, so their look-through disclosure can't fire. All three candidate fields exist upstream; none reaches serving. [fund_score: fact_assembler concentration rows]
  **Owner summary:** The fund page now discloses when a "top 10 holdings" figure looks through into underlying funds — but it can only detect that for a fraction of the affected funds, because the data feed doesn't say when look-through was applied. One flag upstream makes the disclosure fire everywhere it should.
- [ ] (bug) **Web superlative hygiene follow-ups from the L11 audit (filed 2026-08-07; the te_decomposition "biggest bet" contract itself is correctly gated EVERYWHERE live).** (a) Four adjacent superlative claims ride metrics with NO confidence flag of their own: `ExposureXray.tsx:69-71`, `RiskAttribution.tsx:213-223`, `AttributionSection.tsx:211`, `XrayResult.tsx:1076,1164` — each asserts a largest/biggest on an ungated basis; decide per-surface whether the metric needs a separability flag or softer copy. (b) `CurrentPositioning.tsx:385-388` gates fail-OPEN (`!== false`) on a missing flag — dormant today (all served rows v0.2) but the exact class the section-flip protocol bans; flip to `=== true` like V4. [fundscore-web]
  **Owner summary:** The "single biggest bet" claim is now provably honest everywhere it appears — but four neighboring sentences make similar "largest" claims on measures that carry no honesty flag at all, and one older component would silently trust missing data if the feed ever changed shape. Small fixes; filed for triage.
- [ ] (data) **Cross-company reference bindings on served rows: 635 security_id groups span >1 issuer CUSIP prefix; at least 2 bind DIFFERENT companies (pre-existing; surfaced 2026-08-08 by the L1 final checkpoint).** Confirmed cross-company: `ES` = Eversource + Nu Holdings via misbound cusip 664397106; `3690` = Meituan + Korean Re. Reference-binding class (cusip_reference/ticker spine), NOT the L1 look-through fix's doing (identical rows pre-L1). Related: served identity columns still carry filed sentinel LITERALS (`isin='N/A'` on 374 rows/$0.27B; all-zeros cusips ~731K rows) — consumers must never treat them as identifiers (negation-filter hazard). Adjudicate the 635 from the join surface; same doctrine as the l2 dedup lesson (tied/misbound claimants → honest exclusion). [fund_score: cusip_reference / ticker→security binding]
  **Owner summary:** A few hundred securities share an identity slot with a different company because of one bad identifier link — in at least two proven cases a US utility is fused with a Brazilian bank, and Meituan with a Korean reinsurer. Pre-existing and mostly latent, but it's the same "wrong company under a right name" class we've fixed elsewhere; needs the same deterministic adjudication treatment.
- [ ] (data) **`PULL_WINDOW_SQL` (Positioning Changes path) still carries `any_value()` + no sentinel bar — PLUS two adjacent facets from the L1 delta-review (flagged 2026-08-08).** (a) `lookthrough_window.py`'s window pull — feeding `holdings_lookthrough_window.parquet` → Positioning Changes — retains the nondeterministic picker and unbarred sentinel identifiers purged from `build_holdings_complete`/`_PULL_SQL`; same representative-by-dominant-lot + sentinel-bar treatment, own rebuild + re-proof. (b) **`holdings_lookthrough_window.parquet` is STALE (built pre-B1)**: it still carries the original unscaled look-through valUSD, and L1's `lookthrough_asof` fix makes code and artifact diverge — latent today (its only consumer `build_positioning_changes_panel.py` never reads valUSD) but a rebuild without (a) would re-manufacture the old defects. (c) **passive_blend byte-level nondeterminism**: 21,102 rows flip `source_systems` LIST ORDER between identical-code builds (values set-equal; [[rebuild-twice-proves-determinism]] class) — sort the list at emit. [fund_score: src/fundscore/product/lookthrough_window.py + passive_holdings builder]
  **Owner summary:** The same two defects we just eliminated from the main holdings pipeline (random winner on identity collisions; junk placeholder IDs treated as real) still live in the sibling pipeline behind the "what changed recently" panel — and its data file is now a build behind the fixed code, plus one cosmetic rebuild-instability in the passive book. Known fix patterns, needs one focused pass.
- [ ] (story) **Rebuild `/screener` on `fund_profile_facts` for the demo — OWNER DECIDED 2026-08-08 (P2 answered: BUILD, not remove).** The current page renders the legacy 25-row synthetic `funds` table (invented analyst prose, retired Strong-Buy model) and is empty on any un-seeded DB. Owner wants a real screener for the local-MVP demo: rebuild around the Value Score verdict over the 5,819-fund served surface. To settle at spec time: relationship to the queued `exposure-screener` spec (which wants exposure, not fees/returns, as the primary screen key) — one page or two; which columns are free vs paid (respect the tier contract, incl. the P4 ruling that per-fund 3Y β-adj is paid); whether the legacy `funds` table dies with this (it has no other consumer). Owner: "we can flesh after this" — spec via /spec-story or fold into exposure-screener when speccing. [fundscore-web: /screener + src/lib/serving; overlaps specs/queue/exposure-screener]
  **Owner summary:** Decision made: the beta demo gets a real screener page built on the same data as the fund pages, replacing today's placeholder that shows 25 fake write-ups. Details (what you screen by first, what's free vs paid, whether it merges with the planned exposure-first screener) get settled when this is specced.
- [ ] (data) L2 energy pool has no natural-resources/materials candidate — TRNEX-class twins under-span their mandate (owner question 2026-07-28; investigated same day, read-only, scripts in session scratchpad). The `EQ.GLOBAL.ENERGY` allowlist (`src/fundscore/passive_match/etf_pools.py:65`) is broad-world ∪ energy only; MXI/XLB/VAW/GNR/GUNR/IGE/PICK/COPX appear in no energy pool (several nowhere in the module) and are never even priced into the solver panel (`runner.py:525`), so an energy+materials blend was NEVER a candidate — pool exclusion, not an objective artifact. Measured on the exact production window/method (reproduced the 2026-06-30 TRNEX solve to 4 decimals: IXC .5539/VT .4461, R² 0.9101, TE 556 bps): **IGE alone corr 0.948** (best of any candidate tested; IXC is 0.880); production rule over pool ∪ NR ETFs picks **IGE 68%/VT 32% — R² 0.943, TE 443 bps, wins the penalized objective by 20%**; IXC+MXI 0.936/469 confirms the materials sleeve (TRNEX holds 64.4% energy / 19.2% materials / 11.3% other) is the biggest hole; VT keeps a real seat for the non-resource sleeve either way. FIX: add IGE+GNR+MXI to `EQ.GLOBAL.ENERGY` (strict pool ADD — monotonic, can't worsen any fund per `etf_pools.py:49-54` pattern). **OWNER APPROVED 2026-07-28; SHIPPED 2026-07-29 (fund_score commit on `home/value-prop-refresh`: full-audit 21-ETF add list + EQ.CN.BROAD, 2026-06-30 re-solve, all gold panels rebuilt + gated — codex x3 + data-reviewer x4 PASS; ONLY the serving/Postgres reload remains, owner-gated)** (etf_pools.py add + etf_classification.py: IGE "Sector", GNR "Global Sector"; MXI already classified; pool routing + all_etf_tickers verified). Fee-vintage correction: the investigator's "stale 2017 fee rows" claim was WRONG — gold expense_ratio_history is current (IGE 39 bps @2025Q3, GNR 40 @2026Q1, MXI 39 @2025Q3), so no fee refresh needed and the blended IGE/VT twin fee is ≈28 bps ⇒ TRNEX surcharge ~32→~29 (not ~22 as first estimated). Remaining: companion all-pool gap audit (running), then ONE full refit at 2026-06-30 per the passive-match runbook — **changes served twins/R²/TE/surcharge for every fund routed to the energy pool (plus any audit-approved pool adds); owner-gated serving reload per docs/context/passive-match.md**. TRNEX effect: R² 0.906→~0.94, TE 6.3%→~4.4% (tighter Crescent; the fund honestly looks modestly better). [fund_score: passive_match pools + refit]
  **Owner summary:** You asked whether we ever tried an energy + materials mix as New Era's benchmark — we never did: the candidate list for energy funds simply contains no materials or natural-resources ETF. Tested properly, a natural-resources ETF (IGE) plus world stocks beats our current twin decisively (fit 0.94 vs 0.91, tracking error 4.4% vs 5.6%), and it wins under our own selection rule the moment it's allowed to compete. The fix is to add three tickers to the candidate list and refit — it changes the served comparison for every energy fund, so it runs as a gated refit with fresh fee data first.
- [ ] (data) **BETA BLOCKER (owner bar 2026-08-06).** V-spike price corruption degrades 174 L2-served funds' fits — one-week ±12-47% reversal pairs (net≈0) inside the production window in `data/gold/fund_daily_adj_close.parquet` (SLPAX ±23% Dec-2021, MOTNX ±46%, DFMLX −69%/+222% Nov-2021): bad NAV prints / distribution mis-adjustments injecting fake variance. Median served blend R² for affected funds 0.593 vs 0.858 clean — their twins/TE/verdicts are systematically degraded. Fund list in session scratchpad `pool_audit/fund_scan_v2.parquet` (2026-07-29 pool audit §7.1). Root-cause at the pricing source (adjustment pipeline), fix, then these funds heal at the next L2 refit. [fund_score: fund pricing / adj_close]
  **Owner summary:** 174 funds have a corrupted week in our price history (a bogus giant down-then-up spike, usually a mishandled distribution), which quietly wrecks their passive-twin fit and makes their verdicts look noisier than they are. Fix the price data at the source and they heal on the next refit.
- [ ] (data) **BETA BLOCKER (owner bar 2026-08-06).** Wrong price series served under fund tickers — pricing-resolver ticker collisions: WMSIX ("Wilmington Small-Cap Strategy") tracks a MUNI index at corr 0.97 (VTEAX; 0.33 vs VB), PMAAX ("Small Cap Growth") 0.96 vs MINO (PIMCO muni), CGBAX 0.91 vs IBTE, JSGTX 0.983 vs IWF — their served fits are meaningless and they dominate the EQ.US.BROAD.SMALL flag list (2026-07-29 pool audit §7.3). Sweep the resolver for collision class + audit how many more exist. Related but distinct: QQQ/DIA/MDY (UITs) + IAU/SLV (grantor trusts) have NO prices in any store (not N-PORT filers, not in the curated Sharadar 101 — QQQM is present as the natural Nasdaq-100 candidate), and VIG is unreachable under its ETF ticker (maps to Admiral-class ticker) — price-store coverage gaps that also blocked audit evaluation (§7.4-5). [fund_score: pricing resolver + ETF price store]
  **Owner summary:** A handful of funds are being graded against the WRONG price history entirely (a small-cap fund whose "prices" are actually a muni-bond fund's) — their pages are garbage until the ticker-resolution bug is fixed; and a few household ETFs (QQQ, DIA, gold/silver trusts) have no prices in our stores at all, so they can never be candidates.
- [ ] (data) **BETA BLOCKER (owner bar 2026-08-06).** Taxonomy misroutes put funds in the wrong L2 pool — surfaced fund-level by the 2026-07-29 pool audit (§5-6): MSCFX (Mairs & Power Small Cap, an equity fund) is served from the FI.US.IG_BROAD pool; WAINX + 2 more India funds and 3 LatAm funds sit in EQ.EM.BROAD instead of country routing; CSRIX (realty) in EQ.US.CONSDISC; AIIFX (alt income, blend R² −4.96) in EQ.US.FINANC; 17 funds whose best twin is an AOR-family allocation ETF are allocation funds misclassified as pure equity. Also the converse-gap set: 163 clean funds with max corr <0.85 against ALL 1,932 scanned ETFs — pre-merger SPAC, equity market-neutral, multi-strategy/tactical — need ALT/market-neutral classification so the R² floor suppresses their scores honestly instead of serving garbage twins. [fund_score: taxonomy / classification]
  **Owner summary:** Some funds are simply in the wrong bucket — an equity fund graded against bond ETFs, India funds graded against broad EM, and a set of hedge-fund-like funds that have no passive twin by construction but sit in equity pools anyway. Reclassify them so each is graded against the right shelf — or honestly not graded at all.
- [ ] (data) l2_passive_candidate_fit omits off-pool always-include blend legs — 15 funds (0.35% of 4,319; e.g. ROGSX/IGM+SPY, CONWX/VYM+…) have a blend second leg that entered via `always_include` (stated-benchmark ETFs: SPY/AGG/SHY/BNDX/IEFA/VTI) rather than the candidate pool; the builder (`src/fundscore/product/l2_passive_candidate_fit.py`) emits pool candidates only, so the served panel shows k_chosen=2 with ONE component and weights that don't sum to 1 — contradicting the serving docstring "every candidate the L2 solver actually considered" (fact_assembler.py:469). Blend composition itself (passive_alt_blend) is complete; this is the evidence panel under-reporting. Emit always-include legs as candidate rows (flagged `entered_via=always_include`) or document + serve the missing-leg weight. Surfaced by the 2026-07-29 pool-refit data review (non-blocking). [fund_score: l2_passive_candidate_fit]
  **Owner summary:** For a handful of funds the "why this passive alternative" evidence table shows only half the twin (the half that came from the candidate pool) — the composition users see elsewhere is right, but the evidence table under-explains it. Small fix: include the benchmark-leg rows too.
- [ ] (data) `value_offering_reframed_panel.l2_blend_etfs` is NON-DETERMINISTIC across rebuilds — two consecutive builds of the SAME code on the SAME inputs disagreed on **117 of 5,937 funds** (PABBX served `USMV` on one run and `SPY` on the next; PSGIX `USMV`/`IWO`; BACCX `IXC`/`VEU`). The differing values are not a re-ordering — the ETF **sets** differ (0 of 117 were set-identical). ROOT CAUSE (traced 2026-07-30): `build_value_offering_reframed.py` builds `tmap` as a concat of three ticker→series_id sources (`fund_metadata.primary_ticker` ∪ `hierarchical_skill_posteriors.fund` ∪ `class_ticker_mappings`), in which **6,654 series_ids map to >1 ticker** (share classes), then does `blend.join(tmap, on="ticker").unique(subset="series_id")` — so an ARBITRARY share class's L2 blend wins, and polars' unmaintained-order `unique` picks a different one run to run. Different share classes get different solver fits (fee-differentiated NAV series), hence genuinely different ETF sets. Discovered incidentally while verifying that a segment-2 guard was a no-op (the rebuild-twice diff surfaced it); **PRE-EXISTING and independent of the unify work** — the `l2` block is untouched by that diff. FIX: do NOT sort-then-unique — per the `deterministic-wrong-worse-than-nondeterministic` rule that just freezes one arbitrary winner. Adjudicate which share class is authoritative at the join surface (primary_ticker should win over posterior/CTM aliases), and fail honestly where claimants genuinely tie. Check the same concat→unique shape in sibling consumers before closing. MERGED 2026-08-06: the round-4 W3 "(bug) add a deterministic sort key before the unique" item was this SAME defect (116/117-fund cohort) — folded here; the sort-key quick fix is explicitly forbidden (it freezes an arbitrary wrong winner — see the dedup lesson). **BETA BLOCKER (owner bar 2026-08-06).** [fund_score: build_value_offering_reframed.py tmap/blend join]
  **Owner summary:** The named ETFs we show as a fund's cheap alternative can change between two identical rebuilds — 117 funds flip, and not just in ordering: one fund is shown a low-volatility ETF on one build and the S&P 500 on the next. The cause is that a fund's share classes each get their own passive match and we pick one at random. We need to decide which share class speaks for the fund rather than letting the tie break itself.
- [ ] (story) **Passive-fit coverage investigation — raise the share of funds with a GOOD twin (owner-requested 2026-07-31).** GOAL: more funds legitimately clearing the fit floor, NOT a greener metric. The fit floor (`l2_passive_candidate_fit_v1.0`: winner selection-corr <0.60 OR `replica_r2` <0.75 → `no_good_match`, fails closed) exists because serving a bad twin is worse than an honest null — any threshold change must be justified by evidence that the twin is genuinely usable, never by coverage convenience. **Grounding measured 2026-07-31 (do NOT re-derive):** on the VO panel 1,944/4,202 scored funds (46.3%) carry a measured `idio_risk_share` — below the check's 50% PASS bar, hence a standing WARN. The 2,258 missing split: **too_new 1,340 · not_comparable 394 · not in value_score 363 · our own decomposition gate 120 · fee-level 41.** Only the 394 `not_comparable` are twin-fit-addressable → **ceiling 46.3% → 55.6%**, and within them `replica_r2` is 0.70–0.75 on 120 (near-miss), 0.65–0.70 on 76, 0.60–0.65 on 42, **<0.60 on 152** (the last group are market-neutral / multi-strategy / SPAC-arb with no passive analogue — they need ALT classification and honest non-scoring, see the taxonomy-misroute item, not a better pool). Realistic pool-driven upside is ~2–4 coverage points, not nine. NOTE the 2026-07-29 21-ETF pool add is ALREADY live in the lakehouse (served refit `asof_refit_date=2026-06-30`; IGE/GNR/MXI appear in 9/6/15 blends) and was a fit-QUALITY fix (TRNEX R² 0.906→~0.94), not a coverage fix — coverage only moves when a fund crosses the floor from below. **LEVERS to evaluate (owner-listed; each needs measured yield + risk, then an owner call):** (1) **shorter fit window** — lets shorter-history funds qualify; costs fit precision and interacts with `te_decomposition`'s `MAX_ANCHOR_LAG_WEEKS=13` alignment gate and the ~7-week structural FF6 lag, so quantify the noise/precision trade before adopting. (2) **fall back to the stated/official benchmark when history is short** (`src/fundscore/stated_benchmark/` already exists) — BIGGEST CONCEPTUAL CALL: the product promise is "vs the best passive alternative you could actually buy," which is not the same question as "vs the benchmark the prospectus names"; if adopted it must be a distinctly LABELLED comparison, never silently mixed into the same field. (3) **candidate-pool expansion** — the companion all-pool gap audit is already running; target the 120 near-misses specifically and measure which are pool-limited vs structurally un-twinnable. (4) **revisit the R² threshold** — re-derive where 0.75/0.60 should sit from the relationship between fit quality and downstream correctness (do served TE / idio / verdicts actually degrade below the cut, and where?), not from the coverage it yields; report the served-figure damage at each candidate cut. **PREREQUISITE:** resolve the filed "222 `too_new` mislabels" defect first (Crescent-v2 item 3) — it decides whether headroom is ~4 points or materially more. I could NOT verify it 2026-07-31: `l2_replica_quality.n_obs` is a WINDOWED fit count (capped ~180w), not total history, so it cannot distinguish a genuinely young fund from an old one; this needs a real total-history field before the 222 is confirmed or refuted. **Prize is wider than the WARN:** the passive twin drives the Value Score itself, te_decomposition/the bets table and the fee-over-passive figure — twin coverage lifts every one of those surfaces, so scope the win across them, not just this metric. [fund_score: passive_match fit floor + window + pools + stated_benchmark fallback] [model: opus, effort: high]
  **Owner summary:** Fewer than half our funds currently get the "how much of this is real stock-picking" read, because it requires a passive twin that genuinely tracks the fund and many funds don't have one. This investigation asks how to give more funds a good twin — by shortening the measurement window, using the fund's official benchmark when its history is short, widening the candidate list, or revisiting where we set the quality bar. The rule is that we improve the actual match, not lower the bar to make the number look better: a wrong benchmark is worse than admitting we don't have one. Realistically this recovers a few points on its own, and the bigger prize is that a better twin improves the headline Value Score and the fee comparison too — not just this one metric.
- [ ] (data) fee_efficiency_score vintage/label coherence after refits — two warts surfaced by the 2026-07-29 pool-refit review: (a) rows are stamped `eval_date` (e.g. 2026-04-10) but a post-refit rebuild embeds blends solved on data through the refit date — as-of/label incoherence in a dated panel; (b) value_score no longer reads its `passive_fee_bps` (v0.3.2 computes the fee directly from the served blend after the stale-vintage desync defect — 64 funds up to ~54 bps net-fee distortion, fixed 2026-07-29), so decide fee_efficiency's remaining contract (fair-fee/peer content) and either re-stamp eval_date honestly on rebuilds or version the panel per refit. [fund_score: fee_efficiency]
  **Owner summary:** The fair-fee panel's date labels can quietly disagree with the data inside them after a benchmark refit, and its blend-fee column is no longer what the Value Score uses (the score now computes the twin's fee directly, immune to staleness). Tidy the panel's dating and its remaining role.
- [ ] (data) **BETA BLOCKER (owner bar 2026-08-06).** value_score SCORES ~139 funds on stale/mis-keyed ticker fees — the scoring-basis fee (`expense_ratio_history` ticker-keyed, resolve_fee `fee_basis=named_class`) can bind a DEAD predecessor series' fee to a reused ticker (SNWCX scored at 226 bps from a series ending 2021Q2; the live series S000073425 files 85 bps; TSWEX same pattern) and the 3% ER_CAP winsorization is a scoring choice that must never reach display. Display surfaces were healed 2026-07-29 (canonical_fee sources the display-basis series fee), but the SCORE still ingests the stale ticker fee for these funds — fees dominate the metric, so this misprices their value_bps/score100. Fix at source: expense_ratio_history ticker keying must resolve series reuse (join through class_ticker_mappings vintages), and resolve_fee should flag staleness (last quarter << as_of). Surfaced by the 2026-07-29 pool-refit review round 3 (warning class). [fund_score: expense pipeline / value_score fee resolver]
  **Owner summary:** About 139 funds' Value Scores are computed with the wrong fee — usually a dead predecessor fund's fee that got attached to a recycled ticker. The displayed fee numbers are now correct; the score's input still isn't for these funds. Fix the fee history's identity handling and their scores correct themselves.
- [ ] (data) Un-gated fallback fee path serves stale evals — `fact_assembler._fee_fairness_by_series` falls back to fee_efficiency's latest-eval `active_fee_over_passive_bps` for the 618 served funds NOT priced by canonical_fee; 13 contradict their displayed net ER (worst WIEMX 87 bps, evals back to 2021) and 34 serve a fee gap with NULL `passive_fee_bps` (a gap without its baseline in the payload). Same defect family the 2026-07-29 canonical_fee fix closed for the priced path. Gate the fallback (serve an honest missing_reason when the eval's actual disagrees with displayed net or passive is null) or extend canon coverage. Surfaced by the round-4 acceptance review (W1/W2, non-blocking). [fund_score: serving fact_assembler]
  **Owner summary:** ~600 funds that our new always-fresh fee source can't price still fall back to old dated fee evaluations; for 13 of them the displayed fee math visibly contradicts itself (some using 2021 numbers). Gate the fallback so those serve an honest "unavailable" instead.
- [ ] (bug) **LIVE mixed-basis: the 2026-07-29 pool refit published a new twin alongside attribution still computed against the OLD twin.** The refit rebuilt the twin/value/fee chain (passive_alt_blend, replica_quality, benchmark_nav, value_score, fee_efficiency, candidate_fit, positioning/fee-peer/VOR) and was reloaded to serving — but **`te_decomposition` (built 2026-07-15), `factor_attribution` (07-15) and `profile_nav_series`/`profile_nav_period_table` (07-04) were NOT rebuilt, and all four are in the serving path** (`serving/load.py:43-54`). So for funds whose twin changed, the served profile shows the NEW twin's identity/fit/fee next to bets, TE-decomposition shares and a growth chart measured against the OLD twin. Measured exposure: **877 funds took a new-pool ETF into their twin; 302 of them carry te_decomposition rows and 405 carry factor_attribution rows on the old basis** (TRNEX included — served twin IGE 68/VT 32 at fit 0.943, but its 9.4%/44-24-22-10 anatomy, its HOLD/SHIFT/PICK figures and its growth chart are all vs IXC+VT at fit 0.910). ROOT CAUSE: the refit runbook in `docs/context/passive-match.md` (corrected 2026-07-29 to 12 steps) still does not list these three panels — the same class of omission as the TD-cache and fee-efficiency gaps that correction caught. FIX: add them to the runbook, rebuild (`make build-profile-nav-series`, `make build-factor-attribution`, `make build-te-decomposition`, `make build-exposure-path`), re-gate, reload. **SEQUENCING RESOLVED (owner, 2026-07-30): the te_decomposition arm is FOLDED INTO the queued `unify-te-decomposition-global-basis` spec** — that spec must rebuild te_decomposition anyway (its `y` changes with the new twins regardless of basis), so unify rebuilds it exactly ONCE on the new twins AND the 35-factor basis. The earlier "partly throwaway" read was backwards. **This item's remaining scope = `factor_attribution` (405 funds) + `profile_nav_series`/`profile_nav_period_table` only** — both basis-independent, so rebuilding them is never wasted; do them independently and don't wait on unify. Runbook fix (adding all four panels to `docs/context/passive-match.md`) still belongs here. [fund_score: refit runbook + attribution panels; owner-gated reload]
    **te_decomposition arm CLOSED 2026-07-30** (unify segment 1, fund_score `feature/unify-te-decomposition-global-basis`): rebuilt on the new twins AND the 35-factor basis in one pass. The violation was much larger than this item estimated — v0.1 disagreed with the served `te_current` on **1,972/2,043 funds** (not ~302) and carried a stale `passive_alt_label` on **509**. v0.2 now matches value_score on `as_of`, `te_total_bps` (≤1e-9) and `passive_alt_label` for all 1,949 funds, **0 mismatches**. Still owner-gated on the serving reload, so the ~302 remain in violation *on the live site* until that runs.
  **Owner summary:** Yesterday's benchmark upgrade went live for the twin, the fees and the fit — but the bet-by-bet breakdown and the growth chart for those same funds are still measured against the OLD benchmark, because the refit runbook doesn't list those three panels. Roughly 300-400 funds are currently showing two benchmarks on one page. The fix is to rebuild those panels and reload; the runbook needs the missing steps added so the next quarterly refit doesn't repeat it.
- [ ] (data) te_decomposition serves 94 funds whose bets were fit on a 13–53 week STALE window — measured 2026-07-30 on the served panel (2,054 funds): 95.2% fit within 2 weeks of their `te_current` anchor, but 94 funds (4.6%) have `as_of − window_end` of 13–53 weeks (worst SSVSX/RSCYX/GAGVX/THBVX at 53 weeks — bets fit through 2025-05-02 against a 2026-05-09 anchor), every one sitting exactly on the `MIN_OBS_FIT=104` floor. ROOT CAUSE: the te-anchor gate is NAV-only (`build_te_decomposition.py:191`); the fit rows are then silently intersected with FF6+factor availability (`:196-206`) and a short aligned window passes with no gate — only `MIN_OBS_FIT` binds. So a fund's displayed bet MIX can be a year older than the tracking-error level it's scaled to, with all 11 invariants green. **FIX IS SPEC'D: the anchor-alignment gate in `unify-te-decomposition-global-basis` segment 4 suppresses these fail-honest.** File separately so it isn't lost if unify slips — the same gate is a ~10-line standalone fix on v0.1. Adjacent question worth one look: these funds' NAV series appear to end ~a year early, yet they pass `value_score` as `scored` — check whether the recency filter should exclude them upstream. [fund_score: build_te_decomposition alignment gate / value_score recency]
  **Owner summary:** For 94 funds (about 5%) the "where does your tracking error come from" table was computed on price data up to a year older than the tracking-error number it sits next to — the fund's bet mix is stale even though the headline figure is current. Our checks all pass because nothing verifies the two cover the same period. The fix is already written into the unify spec; filing it separately so it can ship on its own if that spec slips.
- [ ] (data) **LIKELY FIXED-BY-UNIFY — retest before closing (2026-07-30).** Tested RYLSX directly on the rebuilt 35-factor global basis: the served `Financial Services +318.7 bps` (rank 1, "high" confidence) + `US Mega Banks +258.6 bps` pair **disappears entirely** from the top-5, and `sector::consumer_cyclical +64 bps` — where leisure actually lives — surfaces at rank 4 behind Japan/Germany/China. Same pattern on the other archetypes: TRNEX's spurious `theme::mag_7 +105 bps` rank-1 bet and FBGRX's financial-services/real-estate leaders both vanish. Root cause is confirmed as the per-fund collinear basis, which `unify-te-decomposition-global-basis` replaces. Do NOT close on this evidence alone — consumer_cyclical is rank 4, not rank 1 — re-check on the full rebuild. Original item below.
- [ ] (chore) **Superlative-guard hardening (2 codex advisories deliberately deferred at the L11 round cap, 2026-08-08; the guard's core invariants are proven non-degenerate).** (a) `rollup` rows are filtered out BEFORE the flags-constant-per-series invariant — a stale `top_bet_gap`/`top_bet_confident` on a rollup row would pass while the serving assembler reads common fields from an arbitrary first row; include rollups in constancy, keep them out of the recompute. (b) The template scan verifies FIELD PRESENCE, not actual suppression — a future TE-sourced superlative template that merely lists `top_bet_confident` in `required_field_ids` would pass without proving the copy suppresses on False; upgrade to verifying a real gating/suppression condition when the first such template appears (the copy half is vacuous-but-armed today). [fund_score: scripts/checks/check_superlative_guard.py]
  **Owner summary:** The new "no fake biggest-bet claims" watchdog is live and proven able to catch real violations — these are two edge-hardenings its reviewer suggested that we consciously deferred to ship it: one narrow data path it doesn't yet watch, and a deeper form of template verification that only matters once a kind of template we don't have yet exists.
  *Also deferred at the same cap (2026-08-08, L1's NAV-identity gate):* codex advisory to weight the per-row `valUSD/pct_nav` median by row value rather than unweighted — the unweighted median could in principle be dominated by many tiny coherent rows masking a few large incoherent ones. Same file-and-harden-later treatment. [fund_score: scripts/checks/check_holdings_valusd_nav_identity.py]
- [ ] (data) **Name-bridge recall follow-up — 179 ISINs / 2,966 rows / $3.51B bridge-eligible but honest-NULL (FILED by the L1 final checkpoint 2026-08-09 at CORRECTED sizing; the first measurement was $527.7M, under-counted 6.7× on a degenerate resolved-name basis — quote $3.51B / 78.7% dollar recall).** Two causes, both mechanical: (1) the plurality-filed-name rule prefers descriptive line names over issuer names ("L'ORÉAL PRIME DE FIDÉLITÉ 2027" NULL beside three classified L'Oréal siblings in one fund-quarter) — prefer issuer-name variants; (2) suffix-stripper asymmetry — filed `PCL` strips but FMP's `Public Company Limited` → `… PUBLIC` doesn't (Gulf Development $869.7M, SCB X $523.2M), plus renames (Malco/Vedanta) and translated legal names (Magyar Telekom $264.4M). Bundle the small ruled items: `TR UNIT` token for the SVB liquidating-trust residual ($709K, current tokens can never fire on its filed text); CINS pseudo-ISIN handling via invCountry corroboration NOT a 3rd-char guard (the one live CINS binding, Aptiv $117.6M, is verified CORRECT — a blanket guard would null it); add the Adani partly-paid line to the ruled-ADOPT ledger. Direction is honest-null throughout — nothing wrong served; this is recall, not correctness. [fund_score: cusip_mapping name bridge + normalize_issuer_name]
  **Owner summary:** The name-matching that recovers sectors for foreign stocks deliberately refuses anything ambiguous — good — but it currently refuses $3.5B of holdings we could classify with two mechanical improvements (prefer the issuer's name over a line's description; make the abbreviation-stripping symmetric). Nothing is wrong on any page — these rows just show no sector where they could show the right one. A solid one-session follow-up once the beta-critical queue drains.
  **Owner summary:** Our data explicitly flags when the top two bets are too close to call a winner, and the page ignored that flag and named a winner anyway — on a 0.4 basis-point difference. Add a check so no page can claim a "biggest bet" the data won't support.
- [ ] (data) **Beta/weight sign disagreement has no detector — and country-of-registration is a weak proxy for exposure.** For TRNEX, 2 of 8 mappable bets disagree in sign between `te_decomposition` (returns beta) and `exposure_xray`/holdings (weight gap): **Australia** (beta over, holds 0.6% vs twin 1.7%) and **United Kingdom** (beta over, holds 6.5% vs 7.1%). Neither is a coverage artifact — country `fund_unclassified` is 0.0000 and the foreign book IS captured (38.4% of NAV, 55 non-US-ticker names). Root cause is conceptual: **filed country is domicile, not economic exposure.** This fund's "UK" is Shell/BP/TechnipFMC (energy, not Britain); its "Ireland" is mostly Linde (industrial gas); it holds Australian-registered names that mine elsewhere. Two asks: (a) add the sign-agreement join as a `/check-data` sibling-coherence check (both inputs are in gold, ~8 lines), auto-emitting a display annotation where they diverge; (b) decide whether country-by-domicile should carry a standing caveat or be supplemented by a revenue/listing-based view for global funds. Surfaced 2026-07-30 by the RIA critic + owner hypothesis. [fund_score: te_decomposition ↔ exposure_xray coherence]
  **Owner summary:** Our two views of "which countries does this fund bet on" disagree for a couple of bets — not because data is missing, but because the filed country tells you where a company is registered, not what its business is. A global resources fund's "UK" is Shell and BP. We should detect these disagreements automatically and say so on the page rather than letting the two views contradict each other.
- [ ] (data) Narrow-industry funds serve implausible top TE bets — RYLSX (Rydex LEISURE, holds NFLX/PM/MCD, zero financials held) leads with "Financial Services +320 bps (50% of factor variance, high confidence)" + near-duplicate "US Mega Banks +258 bps" (collinear FWL var-shares). Gold-exact and method-consistent, but an expert double-takes; review theme/sector bet labeling + collinearity handling for narrow industry funds before the section is marketed. Surfaced by the 2026-07-12 TE-flip data-quality-critic (P2). [fund_score: te_decomposition FWL basis / risk_model targets]
  **Owner summary:** For a handful of narrowly focused funds (e.g. a leisure-industry fund), our risk model names "Financial Services" as the biggest bet — statistically defensible, but jarring to anyone who knows the fund. Review how bets are labeled for narrow funds before this feature is marketed.
- [ ] (data) Exposure-path "Fees + trading" line is mostly a coverage artifact for foreign-heavy funds, and the COVERAGE_FLOOR gate never binds — ROOT CAUSE (traced 2026-07-27 on TRNEX): `holdings_snapshots.parquet` is US-ticker-keyed, so foreign local lines never enter the frozen book (TRNEX filed `pct_nav` sums to 0.58–0.71/quarter; 53–67 of 115–127 filed lines priced); `frozen_returns` renormalizes the priced sub-book to 1, and `weight_coverage` (COVERAGE_FLOOR 0.80, `exposure_path_attribution.py`) is measured vs the ALREADY-SUBSET snapshot book (reads 0.91–0.97 for TRNEX) instead of vs filed NAV (0.59–0.71) → the intended low-coverage suppression never fires for exactly the funds that need it (same bug class as the 2026-06-20 fix inside `frozen_returns`, one level up). Result: `reconciliation_gap_bps` — served on the profile as "Fees + trading" — absorbs what the unpriced 29–42% of NAV did differently. Measured: corr(gap, expense ratio) = 0.07 across 2,469 funds; median gap 112 bps where ≥90% of NAV is priced vs 277 in the 60–75% bucket (TRNEX +330, 78th pctile) and med-|gap| 560 below 60%. Fix: (1) gate on filed-NAV coverage (`pct_nav` sum), not priced-subset weight; (2) coverage-aware serving label — "Fees & trading" only when priced-NAV ≥ ~0.9, else "Fees, trading & unpriced holdings (~X% of NAV)" and suppress any "costs are the gap"-style takeaway; (3) the foreign-pricing vendor fix (EODHD path) shrinks the artifact at source later. Related to the foreign-classification CORE item above (same US-keyed-reference root) but ship the gate+label fix independently so the profile stops mislabeling now. [fund_score: product/exposure_path_attribution.py coverage denominator / serving fact_assembler + web copy]
  **Owner summary:** On foreign-heavy fund pages, the attribution line we call "fees and trading" is actually dominated by the third of the portfolio our US price data can't see — and the safety valve meant to suppress those cases checks the wrong denominator, so it never fires. Fix is two-part: measure coverage against the real filed portfolio, and when coverage is low, label the residual honestly instead of implying a 0.57% fee explains a 3.3% annual drag.
- [ ] (bug) Crescent v2 preview Hurdle mis-states its own bar — the preview says "must beat IXC by ≈32 bps/yr just to cover its extra fee" and draws AFTER-FEE excess bars against a +32 line, but after-fee excess already includes the fee difference: the honest bar on that basis is ZERO, so the current framing double-charges the surcharge (caught by the adversarial RIA panel on the V3 mockup, 2026-07-27; V3 is fixed — copy now "after all fees, beat the twin — the bar is zero; its $32 surcharge is already inside these numbers"). Two sibling label fixes from the same panel to carry into cutover: "Closest passive alternative IXC · 0.25%/yr" mislabels the BLEND's fee as IXC's (IXC alone is 40 bps; the twin is IXC 55%+VT 45%), and the twin growth series needs a HYPOTHETICAL · MIX-AS-OF chip (2026-06 refit weights backcast to 2017). [fundscore-web: src/components/fund/profile/v2 Hurdle framing + identity/twin labels]
  **Owner summary:** The preview tells users a fund must beat its twin by 0.32%/yr to cover its fee, but the numbers displayed already have fees taken out — the honest bar is zero, and the current framing counts the fee against the fund twice. Plus two labeling fixes: the twin's fee belongs to the two-ETF mix (not IXC alone), and the twin's long history should be marked hypothetical.
- [ ] (story) Returns-based realized-attribution fallback for low-coverage funds — the served "what the bets did" chain is frozen-holdings-based BY DESIGN (the bias/timing split needs point-in-time per-quarter exposures; a rolling NAV regression smears a fresh rotation into a persistent tilt — `version_c_model_brainstorm.md:93-97`; per-theme returns-based contributions are collinearity-unstable, cluster-level only — `value_offering_risk_replicability_v0_3.md:123-139`), but that trade buys constituent-pricing dependence, so foreign-heavy funds either get suppressed (448/2,484 low_coverage today, once the gate-denominator fix makes the gate honest this GROWS) or served a coverage-artifact residual. Documented fact: the returns side needs only fund+factor NAV series — no holdings, no constituent prices, and it's fresher (`13_risk_attribution.md:112-124`). Proposal to spec: when filed-NAV coverage < the floor, serve the returns-space attribution instead — aggregate replicable-vs-idio split + CLUSTER-level factor contributions (β × factor return over the window) on the unified 35-factor basis, labeled "estimated from returns — this fund's book is mostly foreign-listed", explicitly WITHOUT the picks-vs-tilts split and WITHOUT bias/timing (honest capability downgrade, not a silent one). Natural sequencing: after `unify-te-decomposition-global-basis` ships (same basis, same factor return series); the alternative — never showing realized attribution for ~20%+ of funds incl. most intl/EM — is the status quo this replaces. [fund_score: product/exposure_path_attribution.py + risk_model/global_* / new returns-attribution builder] [model: opus, effort: high]
  **Owner summary:** For the sizeable slice of funds whose holdings we can't price (mostly international), we today either hide the "what did the bets pay" story or serve a misleading residual. A returns-only version of the attribution — less granular but honest and coverage-proof — can cover exactly those funds, clearly labeled as the coarser read. Best built right after the already-queued risk-model unification since it reuses the same 35-factor machinery.
- [ ] (story) The One Ledger — full performance breakdown anchored on NAV, with fees broken out and static-vs-timing displayed (owner ask 2026-07-27) — one served decomposition per fund/window that always reconciles to the NAV-vs-twin gap: NET active (NAV, β-adj, exact incl. foreign) → + FEES (exact per-quarter filed ER from `series_expense_ratio_history.parquet` — break the known fee OUT of today's conflated `reconciliation_gap` residual) → factor sleeves with drill-downs (sector/theme/macro rows each splitting into STATIC tilt = `bias_bps` and MOVING-the-tilt = `timing_bps` — both already computed in `factor_attribution` AND already in the served payload, `fact_assembler.py:1693-94`, just never displayed; per-row drill to Brinson #10 stock/sector/theme receipts where priced) → stock selection (priced names, Brinson drill) → residual honestly split as "trading + unpriced sleeve", with the unpriced sleeve's COMPOSITION named via the FMP ISIN→sector enrichment (classification is GO even though foreign returns are NO-GO — "~35% of NAV, mostly foreign-listed energy" instead of a bare number). Display language lock: timing_bps is "the realized contribution of moving the exposure", NEVER "timing skill" (three probes killed timing-skill claims). Dependencies/sequencing: coverage-gate+label fix (above) first; `unify-te-decomposition-global-basis` gives one bet vocabulary; the returns-based fallback (above) covers low-coverage funds; EODHD foreign pricing later shrinks the unpriced-sleeve line toward pure fees+trading. TRNEX proof the drill-down pops: Energy total −666 = static −220 + moving −447; Big Oil Majors +466 = +325 static +141 moving. [fund_score: serving fact_assembler ledger assembly + web profile chain redesign; reference rendering docs/product/strategy/mockup_fund_profile_crescent_v3.html] [model: opus, effort: high]
  **Owner summary:** One performance ledger per fund that always adds up: start from the real after-fee gap vs the twin (which covers foreign holdings by construction), break out the fee we know exactly, show each bet family with "standing tilt" vs "moving the tilt around" — numbers we already compute but never show — with click-through receipts to individual stocks, and label the honest remainder for what it is (trading costs plus the foreign sleeve we can classify but not yet price). Turns three disconnected attribution views into one story a retail investor can follow line by line.
- [ ] (data) te_decomposition names a STALE passive alternative next to the new candidates table — 131/2,038 series serving both sections have `te_decomposition.passive_alt_label` ≠ the served blend rank-1 ETF (FCNTX: te_decomp says IWF, passive_baseline says SPY) because te_decomp predates the 2026-07-16 L2 canonical-panel winner flips. Each figure is self-labeled (payload not mislabeled), but two sections name a different "the passive alternative" on one page. Rebuild te_decomposition on the migrated blends BEFORE the frontend renders both sections — fold into the queued `unify-te-decomposition-global-basis` spec's stale-chain refresh rather than a standalone rebuild (do not duplicate). Surfaced by the serve-l2-passive-candidate-fit final data gate W2 (2026-07-23). [fund_score: build_te_decomposition inputs / specs/queue/unify-te-decomposition-global-basis.md]
  **Owner summary:** One fund page can currently name two different "closest passive" ETFs — the tracking-error section still reflects last quarter's winner while the new alternatives table shows the current one. The already-planned tracking-error rebuild fixes this; it needs to land before both sections appear on the same page.
- [ ] (data) 8 intl/EM ETF style tags honest-null on the new passive-candidates table — 441/2,969 served winner cards (14.9%; 2,702/37,963 candidate rows) omit `style_tag`/`one_word_summary` for IEMG/IEFA/EFG/EFV/FDN/MCHI/GMF/EPP because `fund_taxonomy`/`etf_classification_signal` carry holdings-derived focus dims that are WRONG for these ETFs (IEFA/IEMG/MCHI → CONSDISC, EFV → HEALTH); the builder correctly omits rather than serve a wrong tag (foreign-sector lesson), but the right tags are knowably curatable (the module's curated mechanism already covers VWO/VEA) and the gap reads incoherent on-page (EAGMX: VWO tagged "Emerging-markets" beside untagged IEMG). Fix: extend the curated style map for the 8 (12 total affected) ETFs + root-fix the upstream taxonomy focus dims for intl/EM ETFs. Surfaced by the serve-l2-passive-candidate-fit final data gate W1 (2026-07-23). [fund_score: product/l2_passive_candidate_fit.py curated styles / fund_taxonomy focus dims]
  **Owner summary:** On the new "closest passive alternative" table, about one winner card in seven shows no one-word style tag — all international/emerging-market ETFs whose auto-derived style would be wrong, so we honestly show nothing. Hand-curating the correct tags for eight well-known ETFs (and fixing the upstream classification) closes the gap and removes the odd look of a tagged and an untagged ETF side by side.
- [ ] (fast-follow) Serve-l2 candidate-fit serving-boundary hardening — two codex `--high` P2 advisories from the shipped serve-l2-passive-candidate-fit (fund_score `71b5e4b`), nil realized impact today, latent at the next quarterly refit: (1) `_passive_baseline`'s fail-closed coherence gate compares winner TICKER == blend rank-1 only — if a refit advances and the rank-1 ETF happens to stay the same, a stale `l2_passive_candidate_fit` panel would merge silently with current `passive_alt_blend` (contradictory `selected_weight` vs `etf_weights`, outdated floor verdict); also validate the candidate panel's `asof_refit_date` against the served refit (`served_l2_refit_date()`) at the serving boundary. (2) candidate rows expose `expense_ratio_bps` but drop `expense_ratio_source` (only `selected_summary` carries it) — non-selected candidates can't be fee-basis-labeled in the UI even though the gold panel mixes filed_erh / fmp_reference per row; carry the source on every candidate row. [fund_score: serving/fact_assembler.py `_l2_candidate_fit_by_series` / `_passive_baseline`]
  **Owner summary:** Two small hardening notes from the independent reviewer on the just-shipped alternatives table: make the staleness check strict enough to catch an out-of-date sibling artifact after the next quarterly refresh, and carry the "where this fee number comes from" label on every row, not just the winner. Nothing is wrong today.
- [ ] (data) N-CEN adviser pick is order-dependent → wrong fund-family identity — `ncen/parser.py` (~lines 250–258) takes the FIRST `<investmentAdvisers>` entry; DREVX's N-CEN lists Newton NIMNA (the SUB-adviser per its own 485BPOS + BNY fact sheet) first and BNY Mellon Investment Adviser third with `<subAdvisers>` empty → DREVX serves family "Newton Investment Management North America, LLC / 1 scored fund / too small to rank" when it should be member 27 of the RANKED BNY Mellon Investment Adviser family (74/115, 26 funds, $27.6B); `has_sub_adviser=false` also wrong. Measured blast radius today: 1 fund, but the mechanism is systemic for any filer listing the primary adviser non-first — the fund-family spec's called-out fragmentation risk realized. Fix the pick (e.g. registrant-sibling coherence gate / prospectus cross-check) + rebuild fund_family_panel/members + fund_metadata adviser fields. Surfaced by the 2026-07-12 fund-family-flip data-quality-critic (P1). [fund_score: ncen/parser.py / fund_family]
  **Owner summary:** We identify a fund's "family" from its SEC filing but take whichever management firm is listed first — sometimes a sub-adviser rather than the real manager. One fund is currently shown in the wrong family with a false "too small to rank" message; the fix makes family identity trustworthy across the catalog.
- [ ] (data) Metadata identity hygiene from the dedup review — three small source fixes: (1) null stale `fund_effective_ticker` relic claims at source (MDIIX/MCLRX/FEIRX-class VA relics; 3 occupied tickers RYCWX/TMAAX/EKOCX have witnessed owners outside the universe); (2) real primary_tickers for the two Federated Hermes MDT ETFs registered with placeholder `TBD`; (3) backfill `fund_name` for raw-store-only arrivals with no frozen metadata row (HEQQ S000090211 + 1 serve NULL names — recoverable from their filings). (4) NEW 2026-07-17 (fresher-β dedup review): adjudicate ticker→series identity for tied LIVE claimants now EXCLUDED from the β universe — EGRAX (S000005287 Floating-Rate vs S000029759 Global Macro ARA; previously served under the latter), DFLVX (S000000961 vs S000000988), and payload placeholder tickers TBD + ZZZ (junk claims in value_offering_payload). class_ticker_mappings is FROZEN at 2024-12-31 so recency can't adjudicate — needs the raw N-PORT/prospectus claim check. Surfaced 2026-07-10. [fund_score: fund_effective_ticker / fund_metadata / identity backfill]
  **Owner summary:** Three small identity cleanups — stale ticker claims, two placeholder tickers, missing fund names — each causing one-off wrong or empty labels. Low effort, prevents embarrassing single-fund errors.
- [ ] (data) 6 incoherent expense triples — bad FILED total/net ER at SEC source for single-class ETFs (e.g. First Trust Natural Gas ETF total ER 0.0% vs 40bps mgmt; MILITIA L/S 10.9bps vs 130bps). Per-filing parser fix to drive the coherence-gate baseline toward 0. [fund_score: mfrr]
  **Owner summary:** Six ETFs have internally impossible fee figures in the SEC source itself (e.g. a total fee below the management fee). Per-filing parser fixes drive our fee-coherence baseline toward zero exceptions.
- [ ] (data) Tighten the gold price-quarantine to catch 2–3× adjustment rescales — the X-Ray's fail-honest guard (>200%/wk) is a magnitude backstop, not full recall; `build_fund_daily_adj_close.py`'s quarantine uses a ~50× regime threshold, so a ~2× (+150%/wk) bad-price rescale (BSCL-class terminated / near-zero-price tickers, ~35 in-window) still slips into the canonical panel. Root-fix the quarantine to catch sub-50× rescales at source. Surfaced by the 2026-07-16 X-Ray data-reviewer. [fund_score: build_fund_daily_adj_close.py quarantine]
  **Owner summary:** Our price-cleaning step catches only very large data glitches; a handful of funds still carry medium-sized bad prices. Tightening the cleaner removes them at the source, so no downstream feature has to guard against them.
- [ ] (data) **RE-GROUNDED 2026-07-17 — the named test no longer exists (Gate A rebuilt by ab5aa01; replacement coverage_gate tests 10/10 green and keep LSHEX-style books). The RESIDUAL is the data, not the test: real LSHEX is still suppressed `insufficient_coverage` (3 periods) on the Jul-15 rebuild — root-cause its pricing/coverage gap.** Original: return_attribution north-star `test_gate_a_keeps_north_stars_and_domestic_concentrated` FAILS: LSHEX/TPL real domestic concentrated bet is dropped. Confirmed PRE-EXISTING (failing on `main` before the 2026-07-13 work) and NOT sector-related — LSHEX is suppressed `insufficient_coverage`, and Gate A keys on PRICED coverage (Sharadar SEP), not sector labels. A fresh rebuild does not fix it, so it is a genuine pricing/coverage gap worth root-causing. [fund_score: return_attribution]
  **Owner summary:** One genuinely concentrated fund is still honestly suppressed from return attribution because of a pricing-coverage gap. The old failing test was replaced and is green — the remaining work is root-causing that fund's data, not fixing a test.
- [ ] (data) Value Score 62-fund replica gap — 62 EQ funds have ≥200wk of returns but no `replica_r2` computed in `l2_replica_quality`, so they fall to `not_comparable` in `value_score` and get no score. Run/expand the L2 replica fit (mandate pools) for them to recover ~62 scored funds. [fund_score: l2_replica_quality / value_score]
  **Owner summary:** 62 otherwise-eligible funds show no Value Score because one intermediate model never ran for them. Running it recovers scores for ~62 funds — pure coverage upside.
- [ ] (data) FCNTX-class class recovery — ~85% of class-null fee rows still unrecovered (funds with no class-bearing `(series,document)` precedent in the SEC bulk dataset stay null). Explore per-filing R-file parsing or a ticker-based map. [fund_score: alpha/expense.py]
  **Owner summary:** For ~85% of a fund cohort we can't tie fees to the specific share class a customer would buy, so those pages show less precise fee info. Deeper filing parsing could recover many of them.
- [ ] (data) **RE-GROUNDED 2026-07-17 — the periodic-refresh half is DONE (refit advanced to 2026-06-30, quarterly cadence + fail-closed resolvers shipped). Remaining: the min-history/young-fund policy decision + re-measure how many of the 377 now match at the new refit.** Original: Passive-match coverage: 377 young scorable funds + bond-matched funds have no equity benchmark — after the Class-A fix (2026-07-13) funds with no passive blend correctly show no vs_benchmark comparison, which makes an honest coverage gap visible: **377 SCORABLE active funds** (median ~1yr return history — AKRE Focus ETF, Argent Large Cap, ARGA EM Value, American Century Insights) aren't in the L2 solver (latest refit 2026-02-28) because they're too young; **34 funds** are matched to BOND ETFs (AGG/SHY/SGOV) so the equity look-through is empty; **1,564 are scorability=PASSIVE index funds** (IVE/NASDX/DMIDX) correctly excluded by design. Not a rebuild gap today — the young funds recover as they accumulate history + a solver re-run. Also filed by the data-reviewer: GSIB (S000081041) holds 28.95% US but is matched to an all-foreign passive → passive-match quality, not coverage. Also: `passive_alt_blend.parquet` is stale (built 2026-04-09) — refresh feeds fact_assembler `_passive_baseline` / l2_passive_candidate_fit. Track for a periodic passive-match refresh + a min-history/solver-cadence decision. [fund_score: L2 solver universe / passive-match cadence]
  **Owner summary:** With the fabricated benchmark removed, ~400 mostly-new active funds honestly show "no passive comparison yet" because they're too young to match reliably; they'll light up as they age. A periodic passive-match refresh (and a decision on how young a fund can be matched) would recover them sooner.
- [ ] (data) X-Ray does NOT look through third-party ETF wrappers, but `holdings_lookthrough_window` (feeding positioning_changes) DOES. The 2026-07-13 gate makes the X-Ray honest about it (a wrapper's weight is charged to the error bound, so MSTQ — sole holding QQQ — no longer claims "underweight Magnificent 7"), but the real fix is to look through the wrapper so a QQQ-heavy fund gets its TRUE sector/theme/stock exposure instead of a suppressed panel. `etf_holdings_snapshots` already exists for the passive side. Would restore exposure for the 446 funds with >25% wrapper books. [fund_score: exposure_xray / holdings_complete]
  **Owner summary:** Funds that mostly hold ETFs instead of stocks currently show a suppressed exposure panel because we don't look inside the wrapper. Looking through would restore honest sector/theme/stock exposure for ~450 funds.
- [ ] (story) Auto-derive the methodology registry's as-of/method-version values from the serving pipeline so they can't drift — third manual drift-fix in a month (facts.ts side-stepped the registry 2026-07-13 for this reason; the registry itself re-derived 2026-07-16 night run). Candidate shape: emit a `methodology_asofs.json` from build_serving_facts (per-artifact method_version + as-of maxima, same panels the reviewer verified) and have registry.ts import it, keeping prose strings hand-written but dates/versions generated. [fundscore-web: src/lib/methodology/registry.ts / fund_score: build_serving_facts]
  **Owner summary:** The trust page's dates have now drifted stale twice and been hand-fixed twice. This wires the dates to come straight from the data pipeline so the page can never quietly fall out of sync again — only the explanatory prose stays hand-written.
- [ ] (bug) ff6 factor-source refresh — **OWNER DECIDED 2026-07-23: automate with a fail-loud staleness gate.** Add a `fetch-factor-data` Make target and wire it into `refresh-current-features`/`refresh-features`: attempt the Ken French download on each refresh (monthly-idempotent); if the source is unreachable, proceed only when on-disk `ff6_weekly`/`ff6_daily` are <45 days old, else FAIL the refresh visibly — never silently rebuild on stale factors. Original: Factor refresh loop never re-fetches the ff6 source (factor-alpha re-lags) — OWNER DECISION — `refresh-current-features`/`refresh-features` rebuild `build-factor-alpha` but there is **no `fetch-factor-data` Make target and no fetch step**, so the loop rebuilds factor-alpha from whatever `ff6_weekly` is already on disk. Even with the parser now fixed (87bdd3a), the automated refresh will perpetually rebuild the factor leg from a frozen ff6 and re-lag unless someone runs `scripts/pipeline/fetch_factor_data.py` by hand first. `fetch_factor_data.py` is currently a deliberate manual step (Ken French publishes ~monthly, and wiring a network download into every incremental price refresh adds a fragile external dependency). Decide: add a `fetch-factor-data` target + step into the refresh compound (auto-refresh, monthly-idempotent), or keep it manual and document the two-step "fetch factors, then refresh" runbook. Surfaced 2026-07-12 during the Ken French parser fix. [fund_score: Makefile refresh-current-features / fetch_factor_data.py]
  **Owner summary:** The parser that keeps our style-factor data current is fixed, but the automated refresh routine never actually re-downloads that source — so the layer will drift stale again on its own. Small decision: wire the download into the routine, or keep it a deliberate monthly manual step with a clear runbook. **Decided 2026-07-23: the download joins the automated refresh with a loud failure mode — if the source can't be fetched and our copy is older than ~45 days, the refresh stops instead of quietly rebuilding on stale data.**
- [ ] (chore) Pricing-store hygiene — **(2) DONE 2026-07-17: canonical-price entry added to the data dictionary (on fix/benchmark-nav-dedup-rebuild), incl. the duplicate-vintage hazard with the Jul-16 NAV corruption as the cautionary case.** Remaining: (1) the append-only `data/vendors/tiingo/daily_pricing/` store grows unboundedly (205M rows / 3,993 files / 86.9M duplicate re-fetch vintages); add a periodic in-store compaction — NOW HIGHER PRIORITY: the duplicate vintages are no longer a latent cost, they actively corrupted the Jul-16 passive-NAV rebuild via an un-deduped read; compaction removes the hazard class at source. Surfaced 2026-07-16, re-grounded 07-17. [fund_score: daily_pricing store]
  **Owner summary:** Two small maintenance items: the raw price folder keeps growing because we never prune old re-downloads, and we should officially document which cleaned price file everything is supposed to read from.
- [ ] (data) Holdings-direction vs active-β sign disagreement needs adjudication before retail launch — FCNTX renders a famous Technology UNDERWEIGHT on the holdings basis (25.9% held vs IWF 53.8%; MSFT −6.0pp, AAPL −8.6pp) while the attribution basis shows a POSITIVE avg active tech β (+0.146, per-quarter path peaks +0.38 in 2023–24) adding +461 bps/yr — two readings of the same exposure pointing opposite ways on one page with nothing reconciling them (DODGX us_megabanks shows the same held−3.1pp vs β+0.11 pattern, though there the TE table's t agrees). Baselines legitimately differ (joint-WLS return regression vs L2 blend vs holdings pp vs IWF) so not provably wrong; needs a data-scientist pass + an on-page basis-reconciliation note ("held weight and exposure β can disagree in sign — why") — related magnitude double-take: gross legs ±1800 bps/yr nearly canceling on a 4.8%TE fund, colliding with the +110 bps/yr gross-selection skill read (the known idio_alpha_bps tension). Surfaced by the 2026-07-12 attribution-flip data-quality-critic (P2). [fund_score: exposure_path_attribution basis / fundscore-web: basis note; data-scientist]
  **Owner summary:** Two sections of the fund page can describe the same bet in opposite directions — the portfolio shows a famous underweight to Technology while our return-based model says the fund behaves as if overweight, and nothing on the page reconciles them. Both measurements are defensible, but to a customer this reads as a contradiction; needs a research pass and a plain-English note before launch.

**Owner decisions.** Blocked on an owner call, not engineering — each is a paragraph of judgment that unblocks already-filed work. Once decided, re-file the resulting work into the Working set or the campaign. (Empty right now — both 2026-07-22 decisions were made and re-filed; new decision items go here.)

- [ ] (data) Fee/value takeaways gate on an *available* passive alternative, needlessly suppressing fee-missing funds (recover a few hundred fee rows) — `build_fund_takeaways.py` emits the fee-gap / passive-cost / value-offering takeaways only when `closest_passive` is `availability_status='available'` (the `passive_name is not None` gate), so a fund whose closest_passive is `missing_fee` (e.g. SPY's own ER absent from the fee feed) gets NO fee takeaway even though the reframed panel already carries the passive label (`l2_blend_etfs`), the fee bps, AND `source_inventory.passive_match='available'`. Since the passive LABEL now sources from the reframed panel (fund_score `ddcccbe`), that closest_passive-availability gate is vestigial for these rows — gating on the reframed passive-match fields instead (or accepting non-`unsupported` closest_passive) recovers the suppressed takeaways. Codex `--high` advisory (P2) on `ddcccbe`, 2026-08-06. Coverage change (ADDS fee takeaways) → measure the recovered count + spot-check the recovered funds before adopting. [fund_score: build_fund_takeaways.py fee gate]
  **Owner summary:** A few hundred funds get no fee comparison in their summary purely because the passive ETF we'd compare them to is missing its own fee in one feed — even though we already have everything needed to make the comparison. Loosening that one gate recovers those fee takeaways; it adds coverage, so measure it before shipping.

**Refresh campaign (one supervised arc — not lean-loop items).** ⚠ FIRST: the terminal-cascade blocker below (filed 2026-08-06 by the planning session, diagnosis done — campaign session executes).
- [ ] (data) **TERMINAL-CASCADE BLOCKER (2026-08-06).** Flat-tail detector misses runs broken by a stray final print — the sign guard correctly halted cascade step 25 (l2-passive-cand-fit) at 12:54: `build_l2_passive_candidate_fit` refused with `CNDA: winner VB recomputed correlation -0.0079 <= 0`. DIAGNOSED (read-only): CNDA's canonical series (2010-03-23→2025-05-16) is fabricated-flat at the tail — 48 of its last 60 closes are the identical 10.61 — but ends with ONE stray print (11.42), which breaks the `flat_tail.py` detector's ≥40-TRAILING-identical-closes condition (shipped `7e53131`), so the quarantine missed it and the in-window returns are noise; the recomputed winner corr is legitimately ~0 and the guard fired as designed. FIX AT SOURCE in the campaign worktree: harden the detector (tolerate ≤k stray prints inside/after the run, or detect the penultimate run), rebuild the price-panel step, resume `stage_cascade_resumable.sh` (skips fresh outputs, resumes at step 25). Do NOT weaken the sign guard. SWEEP FIRST: any other near-flat delisted series with a stray final print evades identically — count the cohort before resuming so step 25 doesn't fail serially, one fund per run. [fund_score campaign: alpha/flat_tail.py + build_fund_daily_adj_close + cascade step 25]
  **Owner summary:** The big rebuild stopped itself at a safety check — correctly: one dead fund's price history is fake-flat but slipped past our new fake-price detector because of a single stray final price. Fix the detector, re-clean prices, resume the rebuild from where it stopped. The safety check that caught it stays exactly as strict. The stale-producer / coherent-rebuild debt, worked as ONE campaign: land the VO-emitter branch first (screener-rebuild prerequisite), refresh the frozen evidence producers and ingest gaps, rebuild the dependent chain coherently, then close with the terminal reviewed panel-refresh pass and the owner-gated serving reload. Worked piecemeal, every member re-pays the reviewed-rebuild tax these items share; most were filed with "fold into the panel-refresh pass" notes already.
- [ ] (data) NAV panel serves dead-class tickers — ROOT-CAUSED 2026-07-10 (was: "NAV feed cuts off ~2025-04-30"; investigation in night-run-20260709.md): NOT an ingestion bug — Tiingo 200s with genuinely-ended series. The 317 frozen tickers in passive_alt_daily_nav = **284 dead series** (Putnam→Franklin mergers/closures — honest NAV end) + **23 retired share-class tickers** whose funds live under CURRENT tickers already fresh in the vendor store + **6 degraded classes** with fresh sibling-class coverage + **2 TRUE vendor gaps** (JMVNX, JCAVX: single-class, alive 2026 N-PORT, Tiingo stopped) + 1 recovered by the 2026-07-10 refresh + 2 unmapped. REMAINING FIX (UNBLOCKED — the Tiingo refresh completed 2026-07-12 17:42, store fresh through 2026-07-10): rebuild fund_daily_adj_close + the passive-match NAV pairing on the CURRENT-ticker basis (series→best-covered class ticker; use the 8993b29 deduped universe + class mappings — a per-series ticker can die while the series lives), then re-measure the profile-nav recovery (the original "~123 recoverable" premise mixes honest-dead with class-basis losses). JMVNX/JCAVX → alternate source (N-PORT monthly NAV) or honest null. Sibling: passive-blend micro-gaps item below shares the rebuild. [fund_score: passive-match / build_fund_adj_close ticker basis] [model: fable, effort: xhigh]
  **Owner summary:** Root-caused: most "stale price" funds are genuinely dead or renamed share classes, not a data bug. The fix is to rebuild price histories on funds' current tickers — recovering growth charts for live funds and stopping us from chasing dead ones.
- [ ] (data) Passive-blend micro-gaps fragment live-fund NAV tails — `passive_alt_daily_nav` drops 1–2 month runs of `benchmark_nav` for funds that ARE continuously priced in `fund_daily_adj_close` those same months, right before the series end (e.g. LSLTX: paired run 2002-12..2025-01, gap 2025-02/03, then a 1-month 2025-04 stub; MRTHX: 2017-01..2025-02, gap 2025-03, 2025-04 stub). 122/123 profile-nav "recoverable" funds show this fund-alive-in-gap pattern; the tiny post-gap stub then trips the MIN_MONTHS length filter and nulls the whole fund. Root-cause the passive-match/`benchmark_nav` build's month gaps for continuously-priced funds (a continuous blend would keep these funds served without any trim heuristics). Surfaced 2026-07-04 by the profile-nav-series coverage review. [fund_score: passive-match / build_benchmark_nav → passive_alt_daily_nav]
  **Owner summary:** Short gaps in our reconstructed index-blend prices cause ~120 live funds' growth charts to be suppressed entirely. Fixing the gap-handling at the source recovers those charts without lowering our no-fabrication standard.
- [ ] (data) SPY + DBC ingested — rebuild fund_metadata 3Y risk fields to light up benchmark-relative stats for 1,477 funds — RAW PRICES LANDED 2026-07-12 22:25 (SPY 6,669 rows 2000-01-03→2026-07-10; DBC 5,138 rows 2006-02-06→, via fetch_tiingo_pricing --ticker-list-override; store now uniformly fresh incl. SHY→2026-07-10 so the rf zero-fill latent is NOT armed). REMAINING (gated fix-data loop): rebuild build_gold_metadata risk fields (beta_3y/alpha/TE/IR/captures populate for the 1,430 SPY + 47 DBC funds incl. FCNTX), /check-data + data-reviewer on the new relative stats, correct the risk-behavior registry limitation wording (fundscore-web), then serving rebuild+reload picks it up. Original evidence: 2026-07-12 riskBehavior-flip DQ-critic (P1). [fund_score: build_gold_metadata / fundscore-web: registry]
  **Owner summary:** The missing S&P 500 benchmark prices are now loaded — done today. One reviewed rebuild remains to actually light up beta/tracking-error/capture stats for ~1,500 funds on the new risk panel, including our flagship example fund.
- [ ] (data) Member AUM stamps one N-CEN cycle stale (recoverable) — FCNTX/DREVX `aum_as_of_date` = 2024-12-31 while both registrants filed FY2025 N-CENs on 2026-03-12 (EDGAR-verified, not ingested; external FCNTX AUM $145.9B vs our $140.6B). Page discloses the range honestly; ingest the FY2025 N-CEN cycle + rebuild metadata AUM. Likely same root as the frozen universe-evidence producers item (sibling). Surfaced by the 2026-07-12 fund-family-flip data-quality-critic (P3). [fund_score: ncen ingest / fund_metadata AUM]
  **Owner summary:** Fund size figures are one annual filing cycle behind — the 2025 filings exist but aren't ingested (we show $140.6B for Contrafund vs ~$146B current). Cosmetic today and disclosed on-page, but easy to bring current.
- [ ] (data) **RE-GROUNDED 2026-07-30 (item 9): premise was WRONG — 3 of the 4 legacy outputs ARE consumed, and the producer is GONE (not re-runnable).** `fees`/`fund_metadata`/`tickers`/`turnover` were last produced 2025Q4 by an extraction path no longer in the codebase (current `mfrr_pipeline.py` emits only expense_ratios/fee_components/benchmarks/narratives per the manifest; no `extract_tickers` in code or git history; the ticker isn't even in the standard SEC MFRR TSVs). Consumer audit (corrected — the original "no consumers" was a literal-path grep that missed `build_gold_metadata.py`'s `load_*` helpers; codex P1 caught it): **`turnover`→`load_turnover`→`portfolio_turnover`; `tickers`→`load_tickers`→`mfrr_ticker` (coalesced BEFORE nport_ticker for `primary_ticker`) + alpha/expense.py; `fund_metadata`→`load_inception_dates`+`load_mfrr_identity`→`inception_date`/company name+CIK — all feed `gold/fund_metadata.parquet`. Only `fees` is genuinely unconsumed** (`load_fees` reads `fee_components`, the current panel, NOT this dir). DONE 2026-07-30: `fees` retired from `audit_data_freshness.MFRR_QUARTER_DIRS`; the other 3 KEPT in the audit (their 2025Q4 staleness is real, must not be silenced). **REMAINING (this item): re-source `tickers`/`fund_metadata`/`turnover` in `build_gold_metadata` from CURRENT feeds — N-PORT `class_ticker_mappings` (already fresh 2026-07-01) for tickers, N-CEN for inception/company identity, a current turnover source (N-PORT? TBD) — removing the frozen-MFRR-legacy dependency; then all four are fully retire-able. Impact of the current staleness is LOW (slow-changing fields: tickers stable, turnover ~annual, inception/identity static) but real.** [fund_score: build_gold_metadata load_tickers/load_turnover/load_inception_dates/load_mfrr_identity → current N-PORT/N-CEN feeds]
  **Owner summary:** Correcting an earlier mistake: three of these four datasets (fund tickers, turnover, and fund identity/inception) DO still feed our core fund-metadata table — they're just frozen at last-year's-Q4 with no way to rebuild them the old way. Only the fourth ("fees") was truly unused and is now retired. The real fix is to re-point those three at data we already keep current (N-PORT tickers, N-CEN identity) so they stop drifting stale; impact today is small because these fields barely change.
- [ ] (data) Manager-people gold is stale vs the serving universe — `manager_people_assignments` still contains adjudicated-dead series (MAVFX, TANX…) that source_inventory correctly excludes; `test_gold_universe_matches_source_inventory` red (pre-existing; inputs Jul-15, independent of the 07-17 restore). Also pre-existing reds worth one look in the same pass: `test_openfigi::test_resolve_batch_splits` (pure unit, batch-split logic) and the tracked the_take↔takeaways drift pair. **Full pre-existing red set re-measured on merged main 2026-08-22 — 5 of 1,379, each proven pre-existing by re-running them against pre-merge code on the same data (same 5 test IDs, not merely the same count):** `test_manager_people::test_gold_universe_matches_source_inventory`, `test_manager_people::test_gold_family_series_count_reconciles`, `test_openfigi::TestOpenFIGIClient::test_resolve_batch_splits`, `test_return_attribution::test_gate_a_keeps_north_stars`, `test_serving_fact_assembler::test_nav_series_matches_gold_and_matched_grid`. **NB the return-attribution item below claims its failing test 'was replaced and is green' — that claim is STALE**, `test_gate_a_keeps_north_stars` is red. Rebuild/trim manager gold against the current scope. [fund_score: manager_people / source_inventory]
  **Owner summary:** The fund-manager roster table still lists a handful of funds that have shut down, which trips one of our own consistency tests. A small refresh brings it back in line with the catalog we actually serve.
- [ ] (data) exposure_path active-universe lags fund_metadata — 22 funds with fresh 2026 holdings are excluded from `holdings_exposure_path` because `eq_active_universe()` → `active_retail_universe()` reads **unrefreshed** `fund_metadata`; rebuilding at a later as-of shifts the recency window and drops them even though their holdings are current (net −47 exposure_path series, 36 genuinely dormant + 22 metadata-lagged). Refresh `fund_metadata` alongside holdings, or have the active universe consider holdings recency. Surfaced 2026-07-03 by the NPORT-refresh sweep. [fund_score: build_exposure_path_attribution.eq_active_universe / universe]
  **Owner summary:** 22 funds with current portfolios get dropped from the trend analysis because a staleness check reads outdated reference data. Fixing the check recovers them.
- [ ] (data) `fund_taxonomy.parquet` (Jun 20) still reads the PRE-rollup `cusip_reference` sector as a classification SIGNAL. Not a served sector (so it is not a second sector truth in the UI), but the 4D classification could shift marginally for funds with material Travel-Services / Building-Products / Uranium weight. A refresh needs the full `run-taxonomy` LLM pipeline — deliberately NOT run inside the 2026-07-13 change. [fund_score: taxonomy]
  **Owner summary:** The fund-classification table still reads pre-correction sector data as one input signal. A refresh needs the full LLM classification pipeline, so it's tracked separately rather than folded into the quick sector fix.
- [ ] (data) **RE-GROUNDED 2026-07-17 — most of the cascade HAS been rebuilt (return_attribution + holdings_exposure_path + fund_alternatives Jul-15; exposure_divergence Jul-17). Still stale: theme_bet_attribution (Jun-25), fund_the_take + fund_takeaways (Jul-11, pre-relabel), fund_taxonomy (Jun-20).** Original: Downstream cascade still stale after the 2026-07-13 sector/coverage rebuild: `holdings_exposure_path` (Jul 7), theme_bet_attribution, return_attribution, exposure_divergence, alternatives, the_take. Several of their tests were ALREADY failing on `main` before that work (pre-existing staleness debt), but the sector relabel makes them formally incoherent with the rebuilt X-Ray. Rebuilt in that change: cusip_reference → exposure_xray → risk_decomposition → value_offering_reframed → positioning_changes → serving_facts. [fund_score: panel refresh]
  **Owner summary:** A few analytics tables are still computed from inputs that predate this month's sector fixes (theme attribution, the fund summaries pair, taxonomy) — internally consistent but stale. Rebuilding them brings every downstream number onto the corrected inputs.
- [ ] (data) fund_the_take ↔ fund_takeaways citation drift — re-measured 2026-07-17: now 183/1,431 (was 269) SERVED Take rows cite takeaway rows that no longer exist (invariants 3+9 fail; report was stale 25/25 evidence from the 2026-06-23 ship until regenerated 2026-07-11). Pre-existing: proven identical on pre-fix backup images; a later run rebuilt fund_takeaways without fund_the_take. Served impact: up to 269 funds' Take sentences may quote numerics from retired takeaways. Rebuild the pair coherently as its own gated fix or fold into the panel-refresh pass (sibling item). [fund_score: build_fund_takeaways.py + build_fund_the_take.py]
  **Owner summary:** Up to 269 fund summaries may quote supporting facts that have since been regenerated, so a summary sentence and its cited evidence can disagree. Rebuilding the two together restores the guarantee that every claim traces to live evidence.
- [ ] (data) Reviewed panel-refresh pass — gold panels no longer reproduce at their pinned as-ofs (input-state moved: 2026-07-10 dedup universe 5,799→5,706 et al.). Measured 2026-07-11 during the label fix: return_attribution −146k rows (−1.8%), exposure_xray 75 target rows + ~4.8k value cells, positioning change_z shifted on ~72k/78k rows, security_characteristics −3,752 rows, value_offering named-bet mass ~5k rows. Shipped panels are coherent but frozen pre-dedup. Refresh ALL panels together as a REVIEWED pass (numbers move, owner-visible), incl. the takeaways+the_take coherent pair rebuild (sibling item) and the mixed-state risk_model catalog internals (attribute_def/model_factor/target_membership regenerated 2026-07-11 without pre-images — converge here). Owner option to fold in: rename "EM ex-Japan" → "Broad EM" (data-reviewer advisory: Japan isn't EM, qualifier is vacuous; same migration pattern). **MUST fold in (2026-07-13): the exposure_xray Class-A zero-fill fix (eed1b00) already landed in gold, but its consumers on disk were built from the PRE-fix exposure_xray and still carry fabricated tilts — `value_offering_reframed_panel.named_bets` for 337 SERVED (status=scored) no-blend funds (+1,217 unsupported), plus risk_decomposition (33 rows), theme_bet_attribution/rollup (65/22), holdings_exposure_path (3,489 rows). Rebuild these from the fixed exposure_xray in this pass so the propagated fabrication clears before the manifest-35 reload.** Evidence: fund_score docs/status/data_gap_analysis.md 2026-07-11 + 2026-07-13 sections. Terminal step of the refresh queue: after the upstream refreshes above land, this pass + a serving rebuild/reload (manifest 35, owner-gated) put everything in front of customers. [fund_score: panel builders / risk_model catalog]
  **Owner summary:** Our published analytics were computed on a slightly older fund universe, so rebuilding anything today produces small unexplained shifts. One supervised, all-at-once refresh brings every analytic onto the current universe consistently — numbers will visibly move, so the owner reviews it.

- [ ] (data) **NEXT-CHAPTER ARC — the narrative stack is pinned 2 quarters behind the live holdings frontier (surfaced 2026-08-06 by the terminal panel-refresh).** The skill / value_offering_reframed / takeaways / the-take stack hard-anchors at the **2025-10-31** holdings frontier (`build_value_offering_reframed.py:64`, `build_fund_takeaways.py:58`, `build_fund_the_take.py:57` each hard-code `dt.date(2025,10,31)`), but the shared lakehouse has since ingested ~6 months of new N-PORT filings — `detect_holdings_frontier()` now returns **2026-04-30** (populated quarters 2025-12-31, 2026-03-31, 2026-04-30), and `holdings_complete` no longer even contains the 2025-10-31 vintage (only 284 straggler series ≤2025-10-31 vs 5,751 at the live frontier). This ONE stale-pin was the root of THREE separate frozen-frontier coherence bugs this campaign: the dead-fund L2-match halt (`70d0ae1`), the FCNTX/FBGRX north-star badge flip (`0e5aa4d`), and the fund_alternatives passive-label drift (`ddcccbe`). The stack is internally coherent AT 2025-10-31 today, but it drifts further from filed reality each quarter and any single-panel rebuild re-detonates the mismatch (that is exactly what the terminal cascade kept hitting). FIX: advance the ENTIRE narrative stack — skill posteriors, value_offering_reframed, takeaways, the-take, and the L2 solve they sit on — to the current frontier together as ONE reviewed arc; fund skill reads + bets refresh two quarters, so numbers move for customers (owner-visible). Distinct SUCCESSOR arc, not this campaign's scope. Prereq/pairing: fold in the "unify the 3 hard-coded frontier anchors" hardening chore so the advance is a one-line change. [fund_score: skill/value_offering_reframed/takeaways/the-take frontier + L2 refit]
  **Owner summary:** Our fund-analysis panels (skill, bets, the summary) are anchored to portfolios filed last October, but SEC filings have moved two quarters ahead and our price data no longer even holds the October snapshot. Everything is consistent at that older date today, yet it drifts further every quarter — and it was the single root cause behind three separate coherence bugs we fought this session. The fix is to advance the whole analysis stack to current filings at once: a supervised refresh where fund reads visibly update, run as its own arc rather than piecemeal.

**Fail-honest hardening sweep (batch as one session).** Latent defects with verified-nil live impact today — determinism, guards, null-vs-zero semantics. One focused pass with a single review; no served number should move, which is itself the acceptance check.
- [ ] (data) SWEEP: `unique(keep="first")` with NO explicit sort is a latent nondeterminism bug CLASS in this repo. polars' `unique()` is hash/parallel — "first" is only stable for byte-identical input, so the winner flips between rebuilds. Root case (fixed 2026-07-13): `sharadar.build_cusip_sector_map` wrote `cusip_sector_map.parquet` with a DIFFERENT ROW ORDER on every rebuild (6 orderings in 6 runs from identical input); `cusip_mapping._prefix_match` inherited it and silently reassigned the TICKER for ~3,000–3,800 prefix-matched CUSIPs per rebuild, with no code or data change. Fixed at both layers + `sharadar.py`'s ticker dedup (whose "prefer SEP over SFP" comment was never actually guaranteed) + both cluster-dedups (`value_offering_reframed.named_bets_from_panels`, `risk_decomposition.select_candidates`). **Remaining: grep the whole repo for `unique(` without `maintain_order=True` and audit each one that PICKS A WINNER (`keep=`/subset-based) — any such site is irreproducible.** [fund_score] NEW EVIDENCE 2026-07-17 (payload-restore data-reviewer W3): `build_holdings_complete.py`'s enrichment dedup (`unique(keep="first")` over an unordered DuckDB pull) re-bound isin/cusip/name/sector/lei on 307 series between two same-input runs (weights/membership stable; did not reach serving — staging md5-identical). Live confirmed instance for this sweep.
  **Owner summary:** A code pattern used in several places can silently pick a different "winner" row on every rebuild, making outputs unreproducible — one live instance re-bound identity fields for 307 funds between two identical runs. A one-pass audit fixes the whole class, not just the instances we've caught.
- [ ] (bug) build_gold_metadata rf leg silently zero-fills — `_load_risk_metrics` does `rf_return.fill_null(0.0)` (build_gold_metadata.py:1648), so months where SHY has no return get rf=0 instead of being dropped; the vendor store demonstrably runs mixed vintages (fund NAVs 2026-07-10 while SHY sat at 2026-05-08 mid-refresh), so a rebuild in that state would zero-fill trailing rf months and silently flatter Sharpe/Sortino. Fail-honest: drop unmatched months or gate the build on rf coverage. Latent today (shipped build predates the drift). Surfaced by the 2026-07-12 riskBehavior-flip data-quality-critic (P2). [fund_score: build_gold_metadata._load_risk_metrics]
  **Owner summary:** A latent calculation bug: if our price data were partially refreshed, the risk-adjusted-return math would quietly treat missing Treasury yields as zero and flatter every fund's Sharpe ratio. Nothing wrong is live today; we're closing the hole before a refresh can trigger it.
- [ ] (bug) Coherence guard missing on the holdings_complete sibling path — the misfiled-pct_nav guard (fund_score f7ec601) covers `holdings_snapshots` (→ positioning, top_holdings) but NOT `holdings_complete` (→ exposure_xray), a parallel N-PORT read. holdings_complete is currently clean (0 pct_nav<0, 0 >1.5, max 1.03) so nothing leaks today, but the same class of corrupt *latest* filing would reach exposure_xray unguarded. Port the identical `[0,1.5]` null guard to `build_holdings_complete.py` for symmetry (no current data change expected). Surfaced by the pct_nav-guard data-reviewer 2026-07-08. [fund_score: build_holdings_complete.py]
  **Owner summary:** A data-corruption guard was added to one of two parallel ingestion paths; the second is currently clean but unguarded. Porting the same guard means one bad SEC filing can't reach the portfolio X-ray unfiltered.
- [ ] (bug) Positioning aggregation turns a quarantined null pct_nav into 0 (false 0% / false exit) — [codex --high P2 from f7ec601] Polars `sum()` returns 0.0 for an all-null group, and `build_positioning_changes_panel.build_positions` pivots with `aggregate_function="sum"` before treating null/zero as absence, so a quarantined holding that is the ONLY value in a ticker/sector/theme/top-10 bucket at an endpoint reports 0% weight (or a false exit) instead of degraded/missing. Verified **zero served impact today** (only the dormant/unserved S000030654 has an all-null latest quarter; no served fund's corrupt quarter lands on a positioning endpoint). Harden positioning null-vs-zero semantics so an all-null bucket surfaces as missing/degraded, not 0. [fund_score: build_positioning_changes_panel.py]
  **Owner summary:** A latent bug where a quarantined bad data point could display as "position went to 0%" — a false exit story — instead of "data unavailable". No live fund is affected; fixing the handling prevents a future false narrative.
- [ ] (bug) build_gold_metadata fee circular-dep on clean rebuild [codex P2] — `_resolve_primary_class`/`load_fees` read `data/gold/fund_metadata.parquet`, the file the script is about to write, so a first clean `build-gold`/`full-rebuild` returns empty primary classes → all-null management fees (and gross-fee path) until a second pass. Derive primary classes from the in-memory metadata/ticker inputs for the current build instead of the prior output. [fund_score: build_gold_metadata.py]
  **Owner summary:** A from-scratch rebuild of our metadata would produce empty management-fee fields on the first pass because the build reads its own prior output. Only affects clean rebuilds — but those are exactly the moments we can least afford silent gaps.
- [ ] (bug) fact_assembler rank tie-break is unstable across identical-input runs — return_attribution section `rank_within_dimension` swaps tied rows between assembly runs on byte-identical gold (S000000137: mag_7 ↔ ev_battery_chain at ranks 7/8, bit-identical contributions). Cosmetic today; makes staging non-reproducible byte-for-byte and can flap served ordering on reloads. Add a deterministic secondary sort key (member_id) to the rank ordering. Surfaced by the 2026-07-11 label-fix identity verification. [fund_score: serving/fact_assembler.py]
  **Owner summary:** A reproducibility nit: tied rows can swap order between two identical builds, which makes our byte-for-byte verification noisy. One deterministic sort key fixes it; customers see at most flickering order of tied rows.

- [ ] (bug) **Serving-table DDL has THREE disagreeing definitions, and the loader's is the only true one** (surfaced 2026-08-07 by the W2 runbook worker; spot-verified by the dispatcher). `HOLDINGS_FULL_COLUMNS` (`fund_score src/fundscore/serving/load.py:75`) COPYs `position_direction`, which does NOT exist in fundscore-web's Drizzle `fundHoldingsFull` (`src/lib/db/schema/serving.ts`), in `schema.sql`, or in `drizzle/serving_layer_additive.sql`; `fund_profile_facts` is additionally 13 columns short in the checked-in SQL with 4 retired columns still present. Practical consequence, and why this is filed as a bug not a chore: if anyone ever creates the serving tables from Drizzle or the checked-in SQL — the obvious thing to do on a fresh prod/preview database — **the load fails or silently drops a column**, and the web layer cannot read `position_direction` at all. `apply_serving_schema.py` is the ONLY correct DDL source today and nothing says so. Fix: make one definition authoritative (generate the others from it, or delete the stale ones outright) and state the rule in `docs/RUNBOOK-serving-load.md` §3 + `CLAUDE.md`. Related to [[serving-db-ahead-of-branches]] — same family of failure, one layer down. [fundscore-web: src/lib/db/schema/serving.ts + drizzle/ / fund_score: serving/load.py + apply_serving_schema.py]
  **Owner summary:** There are three copies of "what columns the serving tables have" and they disagree; only the loader's copy is right. Anyone setting up a fresh database the obvious way would get tables the loader can't fill properly. Nothing is wrong on the live site today — this is a trap laid for the production load, which is exactly when we'd least like to find it.
- [ ] (chore) **`build_serving_facts.py` cannot load from an existing staging parquet** (surfaced 2026-08-07 by W2; dispatcher verified: `STAGING` at `build_serving_facts.py:32` is only WRITTEN at :147 and read at :135 for a section-population comparison — never as the load source). `fund_profile_facts` is re-assembled in-process by `assemble_fact_rows()` and COPYed from memory; only holdings and attribution are true parquet replays. So fence F4's literal form in `beta-execution-plan.md` — "preview/prod loads only from the verified staging the local reload used" — **is not achievable for the facts table today**: a prod load re-reads CURRENT gold, so any gold panel that moves between the S1-verified local reload and the prod load silently changes what prod serves. The runbook's substitute (freeze + `shasum` the staging artifact, load, then diff the re-written staging byte-for-byte against the frozen copy) DETECTS that divergence and is the [[rebuild-twice-proves-determinism]] technique, so it is a genuine guard rather than a hope — but it proves identity after the fact instead of preventing drift. Fix: a `--from-staging PATH` flag that COPYs the frozen parquet directly, making the fence literal. [fund_score: scripts/pipeline/build_serving_facts.py]
  **Owner summary:** Our rule says production must be loaded from the exact same verified file we checked locally. It turns out the main table doesn't work that way — it rebuilds itself from source data at load time, so if anything upstream shifted in between, production could quietly differ from what was approved. The runbook catches this by comparing before and after, which works; this item makes the rule literally true instead of checked-after-the-fact.
- [ ] (chore) **Serving-load operational gaps batch** (surfaced 2026-08-07 by W2, all documented in `docs/RUNBOOK-serving-load.md` §8; none blocks D1, all make it manual). **G2 — no target confirmation:** `apply_serving_schema.py`/`apply_auth_schema.py` take zero argparse (env-var only) and all five `apply-*.mjs` print nothing identifying the host; **every one falls back to LOCAL when `DATABASE_URL` is unset**, so a mistyped env silently targets the wrong database. Add a "you are about to write to <host>" confirmation. **G3 — no readback verification** for `fund_profile_facts`/`fund_attribution_blocks` (only `check_fund_holdings_full.py` checks a live DB); nothing compares per-section population local-vs-target, which is exactly the fail-open regression [[serving-db-ahead-of-branches]] describes. **G5 — no backup/restore path** (harmless on the first prod load into an empty target; real exposure on every reload after). **G6 — no script creates a gate-passing preview user**: `grant-early-access.mjs` writes the allowlist row only, the Supabase auth user is a manual dashboard step (preview has no mail sender), and automating it needs a service-role key nobody currently holds. **G7 — `apply_auth_schema.py` creates a trigger on the `auth` schema and it is unverified whether the pooled `postgres` role may do that on hosted Supabase** (fallback documented in the runbook). **G8 — no `.env.example` in either repo.** [fundscore-web: scripts/apply-*.mjs / fund_score: apply_*_schema.py + serving checks]
  **Owner summary:** Eight small rough edges in the production-load tooling — the scripts don't tell you which database they're about to write to (and quietly default to your laptop's), there's no automated "did it land correctly" check for two of the tables, and no backup step. None of them stops the launch; together they're the difference between a load that's safe to run tired and one that isn't.

- [ ] (bug) **`branch-guard.sh` resolves the WRONG repo when a compound command mentions another repo — and the mirror case fails OPEN** (found + mechanism CONFIRMED 2026-08-07 by the beta-drain dispatcher, not inferred). The hook resolves the commit's target repo by regexing the FIRST `git -C <path>` anywhere in the command string (`branch-guard.sh` ~line 36, greedy `.*git[[:space:]]+-C[[:space:]]+`), so a read-only `git -C <other-repo> status` appearing LATER in the same `&&`/`;` chain hijacks the resolution. Observed: a legitimate commit on worktree `fund_score-wt-w3` (branch `w3/query-serving-tables`) was blocked as "on 'main'" because the same command ended with `git -C ~/Projects/fund_score status`. That direction is harmless (fail closed). **The reverse is not: a command that commits on `main` in repo A while also touching `git -C <repoB>` where repo B sits on a feature branch is WAVED THROUGH** — the guard reads B's branch and approves A's default-branch commit. That is precisely the clobber this guard exists to prevent, and it is silent ([[negation-filters-absorb-new-row-types]] — a guard that fails open teaches you nothing). Fix: resolve the target from the `git` invocation that actually carries `commit`, not the first `-C` in the string; or refuse to guess when a command contains multiple distinct repo paths. Same parsing shape appears in `codex-commit-gate.sh` — audit it too. [fundscore-harness: plugins/fundscore-data/scripts/branch-guard.sh + codex-commit-gate.sh]
  **Owner summary:** The safety check that stops an AI session from committing to a shared main branch can be pointed at the wrong repository by an unrelated command in the same line — and in one direction it silently approves exactly the commit it's supposed to block. It cost us nothing today (it erred toward blocking), but it's a guard we rely on across both repos and it needs to be right.

- [ ] (bug) **The H3 env-guard is bypassed by `npx next build` / `npx next start` — and it has already caused real prod traffic** (found 2026-08-07 the hard way: the H4 worker ran `npx next start` to test its rate limiter, hit **production**, and sent ~290 requests before catching it). H3 (`f7526c0`) routes `npm run build` / `npm run start` through `scripts/next-env-guard.mjs` — but the guard lives in the **package.json scripts**, so the most natural alternative invocation walks straight past it and resolves `DATABASE_URL` from `.env.production.local` exactly as before. **This is the same lesson H1 already learned one item earlier**: its `drizzle-kit push` guard had to move from the npm script to `drizzle.config.ts` because `npx drizzle-kit push` routed around the script. H3 shipped with the weaker placement. Fix the same way: put the check in **`next.config.ts`**, which Next loads in-process for `build`, `start` and `dev` regardless of how it was invoked — the wrapper can stay as the thing that *promotes* `.env.local`, but the refusal must live where it cannot be skipped. (Incident impact was nil and was verified twice over: the connection failed at auth (`tenant/user postgres.henxcsknsjfadetomjeu not found`), AND prod has no ops tables to write to. The only real traffic was ~260 session lookups against prod's Supabase Auth REST API with no cookie.) [fundscore-web: next.config.ts + scripts/next-env-guard.mjs]
  **Owner summary:** The safeguard we shipped this morning to stop local work touching the production database only covers the `npm run` shortcuts — running the underlying tool directly skips it, which is exactly how an agent accidentally pointed a test server at production an hour later. Nothing was written and nothing was exposed, but the guard needs to sit where it cannot be walked around, the same way we had to move the database-schema guard earlier today.
- [ ] (bug) **`npm run build` — the mandatory build gate — silently connects to the PRODUCTION database** (found 2026-08-07 by the beta-drain dispatcher while reconciling a disagreement between the W3 and W4 workers; cause confirmed by running the build both ways, not inferred). Next loads env files as `.env.production.local` > `.env.local` > `.env.production` > `.env`, and `.env.production.local` sets `DATABASE_URL` to the prod Supabase pooler — so every `npm run build`, which `CLAUDE.md` requires before any change is called done, opens read queries against PROD. Two consequences: (1) **the gate's own output is misleading** — prod has no query tables, so `/q/[slug]` prerenders 0 paths and the build logs `no query catalog reachable at build`, which reads as a regression; forcing `DATABASE_URL` to local yields `● SSG` with all 14 paths (measured both ways 2026-08-07, which is exactly what W3 reported and what W4 saw the other side of). (2) Every agent and every developer running the build gate reaches production without intending to — read-only today, but it is the wrong default and it will be someone's write eventually. Fix: point the local/CI build at the local database by default and make reaching prod explicit and loud; a build that cannot reach its DB should say which host it tried. Cross-ref the same family of trap in the serving-load tooling (`apply-*` scripts default to LOCAL when `DATABASE_URL` is unset — the mirror mistake). [fundscore-web: .env.production.local + build config]
  **COMPOUNDING DEFECT found 2026-08-07 by the H1 worker — this is what makes the above silent rather than loud:** `src/app/(site)/q/[slug]/page.tsx:34` wraps `generateStaticParams` in a try/catch that logs and returns nothing. So a build that cannot reach its database does not fail — it **succeeds, shipping ZERO prerendered `/q/` slugs**, and the only trace is one log line. Combined with the prod-DB default above, that is exactly the sequence that produced the W3-vs-W4 disagreement this run had to reconcile: one worker built against a loaded local DB and reported 14 paths, the other built against the default (prod, no query tables) and saw the catch swallow it. Nothing else in the toolchain notices; `npm run db:check-serving` is now the only gate that can. Fix both together: the build should say which host it tried, and a prerender source that returns nothing should be loud.
  **Owner summary:** The build command everyone runs before calling work finished quietly talks to the production database. Nothing has been damaged — it only reads — but it means the build's report about which pages get pre-rendered is wrong on every machine, and it's a footgun pointed at prod that we should unload before the beta.

- [ ] (bug) **X-Ray solve result mixes two cadences under one as-of label** (surfaced 2026-08-07 by the data-reviewer at the W4 checkpoint; PRE-EXISTING solver behavior, not introduced by W4 — the HTTP port preserves it faithfully, which is why parity was clean). A result labeled `as_of_date=2026-06-30` (the cadence-1 L2 refit the fund pages serve) carries `performance.portfolio.window_end=2026-07-10` — the fund price panel's edge, cadence-2. The blend WEIGHTS are correctly clamped at the label (`portfolio_passive_solver.py:357`, `date <= as_of_date`, verified), so nothing leaks into the fit; but the PERFORMANCE block reports through a later date than the label the user is shown. It is disclosed via the window fields, so this is a labeling/display question rather than a wrong number: does the X-Ray show one as-of, and if so which — or does it show both and explain why they differ? Decide before the X-Ray is in front of beta users. [fundscore-web: X-Ray display / fund_score: portfolio_passive_solver performance block]
  **Owner summary:** The Portfolio X-Ray labels its answer "as of June 30" but the performance numbers inside it actually run to July 10, because the passive blend and the price history refresh on different schedules. Nothing is miscalculated and the dates are disclosed in the data — but a user seeing one date on the page while a second one drives part of the answer is the kind of small incoherence that costs trust. Needs a display decision before invitees see it.

- [ ] (bug) **Commit-hook parser: four KNOWN-OPEN shapes, shipped deliberately with the H2 branch** (codex gate 8, 2026-08-07 — the branch was committed over a `blocked` verdict by explicit owner decision after 8 review rounds; see the H2 commit message for the full accounting). All four are the same class the whole H2 arc has been closing — a shell shape the parser does not model, so a real commit reads as `NOCOMMIT`; and because the raw-text backstop **deliberately ignores quoted regions** (so a commit MESSAGE mentioning `git commit` does not false-block), it cannot cover the ones hiding inside quotes. **P1 — leading redirections**: `>out git commit` (`git-commit-target.py:483-486`). **P1 — fd-qualified redirections**: `git 2>/dev/null commit` — the redirection skip added in round 8 handles `>`/`<` but not an fd prefix (`:315-320`). **P2 — `env` wrappers**: `env FOO=1 git commit` is not followed (`:538-539`). **P2 — options on transparent prefixes**: `command -p git commit` (`:466`). Each is individually small; the reason they are filed rather than fixed is that **eight rounds demonstrated the enumeration does not terminate** — rounds 4 and 6 closed the *classes* that mattered (an unmodelled shape now costs a false block rather than a silent bypass, for anything the backstop can see), and the durable answer is the filed post-commit backstop, which is immune to parsing. Fix these opportunistically, or let the post-commit item subsume them. [fundscore-harness: plugins/fundscore-data/scripts/git-commit-target.py]
  **Owner summary:** Four known ways to write a commit that our pre-commit safety check still does not recognise. They were shipped knowingly, not missed: the check now blocks far more than it did (it started this run with a hole that let a commit land on main), and eight rounds of review showed that hunting the remaining shapes one at a time has no end. The durable fix is the "shout if a commit lands on main" alarm already filed, which cannot be fooled by how the command was written.
- [ ] (bug) **`SKIP_BRANCH_GUARD=1` / `SKIP_CODEX_GATE=1` are command-GLOBAL, not statement-scoped** (found 2026-08-07 by the H2 worker while testing its own fix; **pre-existing** — the override regex was preserved verbatim through the whole H2 rewrite specifically to avoid changing its semantics, so this is not H2's doing; deliberately NOT fixed there because narrowing an override is a behaviour change that could break existing dispatcher usage and deserves its own gate). Reproduced: `SKIP_BRANCH_GUARD=1 git -C <feature-repo> commit -m x; git -C <main-repo> commit -m y` → **ALLOW**. A `SKIP_` prefix on ONE statement exempts EVERY commit in the same command string, including unmarked ones landing on `main`. Same family as every other fail-open in this cluster: a guard acting on something it has not actually established. Practical risk is lower than the others because it requires someone to deliberately use the override — but a deliberate override of one statement silently disarming an unrelated one is exactly the surprise a safety hatch must not have. Fix is roughly one line: apply the override only to the segments it prefixes (the resolver already splits segments, so the scoping information exists). [fundscore-harness: plugins/fundscore-data/scripts/branch-guard.sh + codex-commit-gate.sh]
  **Owner summary:** The deliberate "yes I really mean it" escape hatch on our commit safety checks turns them off for the whole command line, not just the one command you attached it to. So an intentional override on a safe commit also silently waves through anything else in the same line. Nobody has been bitten by it; it is the kind of thing that bites once, badly.
- [ ] (chore) **Add a `post-commit` backstop that screams when a commit lands on a default branch** (proposed 2026-08-07 by the H2 worker at the end of its own fix round; NOT built there, deliberately). After H2, `git-commit-target.py` is ~440 lines of real shell parsing and is the ONLY thing between a session and a default-branch commit. Its 71 cases cover every shape the author could think of — and "shapes I could think of" is exactly the bound that failed TWICE in that round (a tightened detector opened `commit-graph` false-blocks, then six separate fail-opens: redirects ×2, `if/then`, brace group, `for/do`, `eval`). The safe belt-and-braces is **post-hoc, not another pre-filter**: a `post-commit` hook that detects a commit already on `main`/`master` and shouts, so a parser miss becomes loud and recoverable (`git reset --soft`) instead of silent. A second PRE-check would re-create the exact veto-authority problem H2 just removed — do NOT do that. [fundscore-harness: plugins/fundscore-data/scripts/]
  **Owner summary:** The guard that stops an AI session from committing to a shared main branch now depends on one fairly intricate piece of parsing. It is well tested, but the failure it can't rule out is "a command shape nobody imagined." This adds a cheap alarm AFTER the fact — if a commit ever does land on main, you hear about it immediately and it's trivially undone — rather than piling a second guess in front of it.
- [ ] (bug) **BETA BLOCKER — serving tables grant `anon` full CRUD + TRUNCATE, and two have RLS OFF entirely; the anon key ships to every browser** (found 2026-08-07 by the H1 worker while auditing DDL drift; **independently re-verified by the dispatcher against the live local DB**, exact state below). FIXED in `apply_serving_schema.py` on `feat/h1-serving-ddl-authority`, but **the fix only takes effect where that script RUNS — D1 must apply it to preview AND prod before/at the load.**
  Measured state (dispatcher, read-only, local serving DB):
  - `fund_holdings_full` — **RLS off, 0 policies**; `anon` + `authenticated` hold SELECT/INSERT/UPDATE/DELETE/TRUNCATE. The **paid** 1.4M-row holdings list is therefore readable AND destroyable by anyone holding the public anon key.
  - `fund_attribution_blocks` — identical: RLS off, 0 policies, same grants.
  - `fund_profile_facts` — RLS on, but policy `fpf_public_read` is `FOR SELECT TO public USING (true)` → **every paid JSONB section is world-readable** (`value_score` figures, `return_attribution`, `alternatives`, `te_decomposition`). Writes are denied by RLS-with-no-write-policy, so the write grants are inert HERE — that is luck, not design.
  - `serving_manifest` — same shape as facts (`sm_public_read`, `USING (true)`).
  **NOT a live breach today**: prod holds only `waitlist_signups` + `early_access` and preview has never been loaded, so the serving tables do not exist off this laptop. The danger is exactly D1 — it CREATES these tables on preview and prod, and without the fix it would create them in this state, publishing the paid dataset on day one of the beta. **A fifth definition site made it worse:** `apply_auth_schema.py` also set serving RLS and runs AFTER `apply_serving_schema.py` in the runbook, so it would have re-opened the hole; that block is removed. Verified nothing in the app reaches these tables via PostgREST (only `entitlements`, `early_access`), so revoking is safe — the app reads through `DATABASE_URL` as `postgres`. [fund_score: apply_serving_schema.py + apply_auth_schema.py / fundscore-web: D1 ordering]
  **Owner summary:** The database tables holding the paid fund data are currently wide open to the public key that ships inside every visitor's browser — the holdings list can be read *and deleted* by anyone who looks. Nothing is exposed right now, because those tables only exist on your laptop; the real danger is that loading them to production would have published the whole paid dataset on the beta's first day. The fix is written and must be applied as part of the production load.
- [ ] (bug) **Top-10 concentration has TWO served answers on the same claimed basis — 27.2% vs 31.0%** (surfaced 2026-08-07 by the W5 riders-spec worker, which correctly kept it out of its own scope). `Σ holdings.top_holdings[].weight` = **27.2%**, derived from `holdings_snapshots` which silently drops untickered / private / preferred lines; `exposure_xray.rows[] @ concentration::top10_weight` = **31.0%**, from the exposure engine, and it equals the filed book's top-10 exactly. Both are presented as "the top 10 holdings' share", so one of them is wrong on its own label — and **the V4 cutover spec currently names the 27.2% source**, i.e. the page would ship the understated one. Same root cause as the effective-positions basis defect in the riders spec (US-ticker-resolved book substituted for the filed `pctVal` book), so fix them together: the riders spec already emits `top10_weight_pct` from the same frame as effective positions specifically so the key-stats grid can source both cells from one place. Standing owner decision: product-displayed holding weights use SEC-filed `pctVal`. [fund_score: fact_assembler holdings teaser vs exposure engine / fundscore-web: movement 06 + cutover spec]
  **Owner summary:** The page can show two different numbers for "how much is in the top 10 holdings" — 27% or 31% — because one counts only the holdings we can match to a US ticker and the other counts the portfolio as filed. The filed one is right, and the new page is currently wired to the wrong one. Small fix, but it's the kind of discrepancy a sharp reader would catch and it undercuts the whole "we show you what's actually filed" promise.
- [ ] (bug) **Active ETFs lose their manager-skill read entirely — 461 served active funds** (surfaced 2026-08-07 by the W5 riders-spec worker; pre-existing, out of that spec's scope). `fact_assembler.py:2574` folds `is_etf` into the broad `is_passive` suppression, so an actively-managed ETF is treated as passive and its skill evidence is suppressed even though the fund IS in `hierarchical_skill_posteriors.parquet` with a valid read. Measured: **461 served active funds** are in the posteriors panel but serve no skill read for this reason. Actively-managed ETFs are a large and growing category, and suppressing the manager-skill section for them is exactly backwards — it is the section that most applies. Fix: separate "is an ETF wrapper" from "is passively managed"; the suppression should key on management style, not wrapper. Note the riders spec's population definition is written to track whatever this rule ends up being, so fixing it automatically corrects the percentile denominator rather than requiring a second change. [fund_score: serving/fact_assembler.py skill suppression rule]
  **Owner summary:** Any fund that trades as an ETF is currently assumed to be a passive index fund, so we hide the "did the manager add value" evidence for it — including for 461 funds that are actively managed and where we HAVE that evidence. Actively managed ETFs are one of the fastest-growing parts of the market, so this hides our most differentiated section from exactly the funds it was built for.
- [ ] (chore) **Solver deploy gate re-derives an AGGREGATE, not per-series** (codex P2 at the W4 final gate, 2026-08-07; PASSED the gate as an advisory, filed rather than fixed to avoid an unbounded fix loop — same family as the item below, fix them together). `check_solver_service_coherence.py:240-249` re-derives `live_max` = the single MAXIMUM `n_obs` across all candidates at the refit and compares it to the manifest's `refit_max_n_obs`, then trusts the manifest's own `etf_violations`/`fund_violations` counts. So a refit whose PER-SERIES distribution shifts while its maximum is unchanged (a partial re-emit of the refit partition) passes the gate. The residual risk is genuinely small — the BUILDER is per-series and refuses to write a snapshot with violations, so a violating manifest cannot exist, and the data-reviewer passed 2b on exactly that reasoning — but this is the third appearance of aggregate-substituted-for-per-series in this one build ([[aggregate-gate-masks-per-series-regression]]), which is the reason to close it rather than trust the pattern not to recur. Fix: have the gate re-derive per-series requirements from the lakehouse and check the deployed manifest's per-series counts, instead of comparing one scalar. [fund_score: scripts/checks/check_solver_service_coherence.py]
  **Owner summary:** The pre-deploy check that confirms the X-Ray's data still matches the fund pages compares one summary number rather than checking each investment series individually. The builder that creates the snapshot does check each one, so this is a backstop being weaker than the thing it backs up, not a hole in the main guard — but it's the third time this build confused a total for a per-item check, so it's worth closing properly.
- [ ] (chore) **Solver staleness gate leaves 53 never-candidate ETFs unguarded — needs a cross-snapshot ratchet** (surfaced + DEMONSTRATED 2026-08-07 by the data-reviewer at the W4 re-review; deliberately NOT fixed in that fix round, documented in-code instead). W4's `fit_sample_gate` bounds price-panel staleness per series against `max(n_obs)` from `l2_passive_candidate_fit` — a figure the lakehouse records, so no constant is invented. But that reference only exists for ETFs that were CANDIDATES at the refit: **127 of the 180 blend-universe ETFs**. Proof, executed: mutilating **EEM** (a never-candidate pool member) drops it 261→235 weekly obs and the builder **PASSES**; the identical mutilation of **AGG** (a candidate) **FAILS**. Non-blocking today because every ETF the fund pages actually fit or serve at this refit is inside the checked 127, and the real failure mode (frozen / partially re-downloaded feed) is panel-wide and trips 72–127 series against a one-week detection floor — the residual needs a regression surgically confined to never-candidate series. Closing it properly needs a **cross-snapshot ratchet** (hold each unchecked series against the PREVIOUS manifest's per-series counts) rather than a magic constant; that is a design call, which is why it was filed instead of improvised. The fund-side twin of this gap (5 probe funds → all fit-panel funds) WAS closed in the same round. [fund_score: scripts/service/build_solver_snapshot.py fit_sample_gate]
  **Owner summary:** The new guard that stops the Portfolio X-Ray from computing on stale prices covers every ETF we actually use for a fund's passive comparison, but not 53 spare ones in the pool that weren't used at the last refit — for those we have no recorded baseline to compare against. It's a narrow hole (it takes a very specific kind of data regression to hit) and it's documented in the code rather than hidden; closing it means having each snapshot check itself against the previous one, which is a small design decision rather than a quick patch.

- [ ] (chore) The narrative frontier date is hard-coded in 3 builders — collapse `dt.date(2025,10,31)` (`build_value_offering_reframed.py:64`, `build_fund_takeaways.py:58`, `build_fund_the_take.py:57`) into ONE shared constant so the stack can't drift out of lockstep and the eventual frontier advance (see the Refresh-campaign successor arc) is a one-line edit. No served number moves. Filed 2026-08-06 during the terminal panel-refresh. [fund_score: narrative frontier constant]
  **Owner summary:** The same anchor date is copy-pasted into three builders; one shared constant keeps them in lockstep and makes the eventual frontier advance a single edit.
- [ ] (chore) `fund_alternatives` publishes holdings-overlap evidence with no `holdings_as_of` provenance column — the v0.2 similarity types (cross_wrapper / similar_active) compute overlap from the LIVE N-PORT frontier while the row is stamped with the profile `eval_date`, so a consumer cannot tell which holdings vintage the overlap used and `--eval-date` is not fully reproducible for that path. Add a `holdings_as_of` (overlap frontier) column, honoring the module's own "one profile eval_date, source as-ofs in provenance" contract (`alternatives.py:39-42`). Surfaced by codex during the `ddcccbe` fix, 2026-08-06. No served number moves. [fund_score: build_fund_alternatives.py + alternatives.finalize]
  **Owner summary:** The "similar funds" evidence records which portfolio date it used only implicitly; an explicit as-of column makes its provenance legible and the build reproducible.
- [ ] (chore) Fee-coherence C1 gate is a COUNT ratchet that can mask a swap — `FEE_COHERENCE_MAX_C1` (`build_gold_metadata.py`) bounds the NUMBER of impossible gross<net pairs, so a rebuild that FIXES one series' C1 while BREAKING another passes silently at the same count ([[aggregate-gate-masks-per-series-regression]]). Make the gate SET-aware (diff the offending series-set vs the prior baseline, not just the count). Filed 2026-07-31 during the refresh; verified-nil-impact today (current C1 set is known single-fee-class filed garbage: ASEMX/BIGTX/GWURX). [fund_score: build_gold_metadata.enforce_fee_coherence]
  **Owner summary:** The fee-sanity check counts how many impossible fee pairs exist, so it can't tell if a rebuild quietly swapped one broken fund for another at the same total. Compare the actual set of offenders, not just the count.
- [ ] (data) ~2,414 older dead-fund flat-price tails remain in `fund_daily_adj_close` history — the flat-tail quarantine (`7e53131`) is scoped to the LIVE window (fabricated liveness that fools `is_live`), the only part that affects current serving; ~2,414 already-delisted series carry fabricated-flat tails deeper in history (Tiingo passthrough of a delisted ticker's last price). Harmless to current scores/identities (those funds are dead + outside the served universe) but a cleanliness debt for any backtest/replication reading the full panel. Batch-quarantine when convenient. Filed 2026-07-31. [fund_score: build_fund_daily_adj_close flat-tail scope]
  **Owner summary:** About 2,400 already-dead funds have fake-flat prices deep in their history; it touches nothing we serve today, but it's noise for any long-horizon analysis reading the full price panel — a cleanup chore for later.
- [ ] (chore) The codex commit gate reviews its OWN verdict artifacts under `--uncommitted` — `codex-review.sh --uncommitted` scans the working tree, which includes the untracked `feature-pipeline/reviews/codex-verdict-<HEAD>.json` it just wrote; a still-`blocked` verdict for HEAD then gets flagged as a P1 ("replace the blocked verdict"), so a blocked run cannot clear itself without manually deleting the stale verdict first (hit repeatedly while gating `ddcccbe`, 2026-08-06). The verdicts are a TRACKED audit trail, so they can't be gitignored. Fix: have `codex-review.sh` exclude `feature-pipeline/reviews/` from its review scope. [fundscore-harness: plugins/fundscore-data/scripts/codex-review.sh]
  **Owner summary:** The code-review gate reviews its own past verdict files and can get stuck flagging its own "blocked" note, forcing a manual delete to proceed. Tell the gate to ignore its own output folder.

**Deploy — UN-PAUSED 2026-08-06 (beta-critical).** The public site stays gated (landing + waitlist only; `LAUNCHED=false`). Of the three items that blocked opening the Portfolio X-Ray to a real user, two shipped (canonical-panel L2 refit + quarterly cadence; SPY/UIT expense ratios — see archive); the solver HTTP service below is the third. Owner chose a FULL-EXPERIENCE beta (2026-08-06) including the X-Ray, so this is back on the critical path. The PROD serving reload happens at the gated X-Ray launch.
- [ ] (story) **UN-PAUSED by owner 2026-08-06 (full-experience beta); PRD RESOLVED same day (all 4 questions: cheapest-of-Fly.io/Railway; owner verifies vendor licenses BEFORE any image push; compaction de-scope confirmed; freshness = fund-page cadence with an AUTOMATED as-of deploy gate) — ready to spec, spec-writer dispatched 2026-08-06.** Run the passive solver as an HTTP service so the app can deploy. (Still the eventual prerequisite for the public X-Ray; the PRD + 4 questions stand for when it resumes.) — was AWAITING OWNER: 4 open questions in `feature-pipeline/prds/solver-http-service.md` (vendor+cost, Sharadar/Tiingo license-into-container, confirm compaction de-scope, between-refit freshness stance). PRD red-teamed + revised 2026-07-16 night run; facts corrected therein: real input footprint ~690 MB across NINE files (not 2.2 GB — raw store no longer read post-d92213c) and solves are ~1–4 s, so sync HTTP needs no queue and compaction is no longer a prerequisite. `runSolver()` shells out with `spawn(uv, ["run","python",...], { cwd: FUND_SCORE_REPO })` — it needs the fund_score checkout, uv, Python, CVXPY and the pricing panel on local disk. That cannot run on Vercel (no Python runtime, no persistent filesystem, execution caps well under the current 170–220s). Everything ELSE in the app is a clean Vercel fit (RSC pages over Postgres). Fix: stand the solver up as a small always-on service (vendor = owner Q1 in the PRD; ~690 MB of inputs across nine files, verified — see PRD) exposing `POST /solve`, and replace `spawn()` with `fetch()` behind the same `SolveResponse` contract so nothing downstream changes. Compaction-first prerequisite RETIRED (superseded by the 2026-07-16 solver fix — warm solves ~1–4 s, no job queue needed; owner confirmation is PRD Q3). **CODE HALF DONE 2026-08-07 (beta-execution-plan W4; spec `specs/done/solver-http-service.md`; fund_score `6dc6dc7` on `w4/solver-http-service` in worktree `fund_score-wt-w4` + web `feature/crescent-profile-v2` — owner merges both).** Built: the authenticated service (`POST /solve` + `/healthz` + `/manifest`), the snapshot builder with a per-series fit-sample staleness gate, the container, the automated as-of coherence deploy gate, and the `spawn()`→`fetch()` bridge swap behind an UNCHANGED `SolveResponse` union. AC1 parity **17/17 deep-equal** incl. deterministic run-id hashes; boot verification 4/4; adversarial data-review FAIL→PASS on re-review (it broke the first two staleness-gate designs); codex `--high` PASS both repos. **Hosting priced: Fly `shared-cpu-2x` 2 GB $11.83/mo vs Railway $20.01/mo — Fly, sized from a MEASURED 767 MB peak RSS. Nothing provisioned.** Item STAYS OPEN — **D2 remains**: copy-mode snapshot bake, full `docker build`, re-run parity + AC2 probes INSIDE the container, **owner stop S4 (Sharadar/Tiingo license) before ANY image push**, deploy to Fly, run the deploy gate against the real serving DB (needs D1), AC3 end-to-end on a Vercel preview, and set `SOLVER_URL` + `SOLVER_SHARED_SECRET` in Vercel preview + production. Two residuals filed as their own chores (aggregate-vs-per-series in the deploy gate; 53 unchecked never-candidate ETFs). [fundscore-web: src/lib/serving/portfolio-solver.ts]
  **Owner summary:** The X-Ray currently only works on a laptop, because it launches a Python program that reads files off the local disk. To put it on the internet it needs to become its own small always-on service that the website calls over the network. **Built 2026-08-07: the service, its container, and an automatic check that its data can never silently drift from what the fund pages show. It answers identically to the laptop version on all 17 test portfolios. Nothing is deployed yet — hosting is picked (Fly, ~$12/mo, half of Railway) but no image has been pushed anywhere, because that needs your Sharadar/Tiingo license confirmation first.**

**Beta-launch group (filed 2026-08-06).** The launch thread the backlog lacked — everything between "local serving is coherent" and "a beta user on fundscore.ai sees the full experience." Owner scope decision 2026-08-06: FULL experience (fund pages + screener + X-Ray). Sequencing: prod data load + screener port start once the local reload verifies; the solver service is the un-paused Deploy item above; beta ops lands before any invite. The V4 page itself is driven by `specs/queue/profile-v2-production-cutover.md` (re-grounded on V4 2026-08-06), not a backlog item here.
- [ ] (story) Prod serving-data load + beta runbook — prod Supabase (`henxcsknsjfadetomjeu`) has ONLY `waitlist_signups`+`early_access`; the beta needs the four serving tables (`fund_profile_facts`, `fund_holdings_full`, `fund_attribution_blocks`, `serving_manifest`) loaded from the SAME verified staging parquet the local reload uses (TRUNCATE+COPY, one transaction, in-transaction coherence checks), plus env wiring (`LAUNCHED` stays false; access via `early_access` grants) and a WRITTEN prod-reload runbook — the serving-db-ahead-of-branches rules apply to prod doubly (never load from a branch missing another feature's emitters). ALSO loads the PREVIEW Supabase (`yqyyvhcrmcwarxweusbw`) with the same verified tables + a gate-passing test user: the solver-http-service spec's end-to-end acceptance (AC3) runs against preview, so the preview load is its external prerequisite (flagged in that spec 2026-08-06). **RUNBOOK HALF DONE 2026-08-07 (beta-execution-plan W2): `docs/RUNBOOK-serving-load.md` written + linked from `docs/DEPLOYMENT.md` §4.4; spec `specs/queue/prod-serving-data-load-beta-runbook.md`. Item STAYS OPEN — the loads themselves are item D1, owner-gated on S1, and NOTHING has been executed against preview or prod.** The runbook filed 8 gaps; two need a decision before D1 runs: **G1** — `build_serving_facts.py` re-assembles the facts in memory and never reads `serving_facts_staging.parquet` back, so a literal "replay the verified parquet" is impossible today (the runbook substitutes a checksum-freeze + byte-identity determinism proof; a `--from-staging PATH` flag would make it a real replay); **G4** — `src/lib/db/schema/serving.ts` is missing `fund_holdings_full.position_direction` which the loader COPYs, and `schema.sql` + `drizzle/serving_layer_additive.sql` are stale by 13 columns, so Drizzle must NOT create the serving tables (live drift defect, deserves its own item). Also confirmed mandatory and previously unlisted: `apply_auth_schema.py` (a missing `entitlements` table 500s every signed-in page). [fundscore-web: docs/DEPLOYMENT.md + scripts / fund_score: serving load target]
  **Owner summary:** Production's database has zero fund data — a beta user literally cannot see a fund page until we copy the verified serving tables up. This files the missing "ship the data to production" step with the same one-transaction discipline the local reload uses, plus a written runbook so future prod reloads aren't improvised.
- [ ] (bug) **`/screener` renders SYNTHETIC data and is empty in production — the beta cannot ship it as-is** (surfaced 2026-08-07 by the W3 screener-beta-port worker; the port's story assumed `/screener` read parquet, it does not). `src/app/(site)/screener/page.tsx` calls `getFundSummaries()` → the legacy `funds` table, which holds **25 rows of pre-pivot demo seed data**: fabricated multi-paragraph analyst notes ("FCNTX earns a FundScore of 78, placing it in the Strong Buy tier"), fabricated NAV/AUM/manager/turnover, and the RETIRED FundScore/Strong-Buy scoring model the product replaced with the Value Score. No serving load populates that table, so on preview/prod it is empty and `/screener` renders an empty screener behind a Header link every beta user can see. Two failure modes, both bad: locally (and on any DB that was ever seeded) it shows fabricated numbers — a direct data-integrity violation; remotely it shows nothing. It is the ONLY consumer of `funds`/`getFundSummaries`/`searchFunds`, so the table can be retired with it. Fix is a product call, not a port: rebuild the page on `fund_profile_facts` around the Value Score verdict (overlaps the queued `exposure-screener` spec, which makes exposure the primary screen key), or drop `/screener` from the beta nav until that lands. **Beta blocker either way** — a fabricated "Strong Buy" tier reaching an invited user is worse than a missing page. [fundscore-web: src/app/(site)/screener/page.tsx + src/lib/data/index.ts + src/lib/db/schema/funds.ts]
  **Owner summary:** The Screener page in the top nav is still the old demo: 25 made-up funds with made-up write-ups and the retired star-rating-style score we deliberately moved away from. On the real site the underlying table is empty, so a beta user clicking "Screener" gets a blank page. We either rebuild it on real Value Score data or take it out of the menu for the beta — but shipping invented "Strong Buy" verdicts to invited users is the one outcome we can't have.
- [ ] (chore) `/api/ops` has no rate limit — the beta-ops intake (shipped 2026-08-07) is the ONE `/api/` exception to the `early_access` gate, so it is an unauthenticated INSERT path on a public site. Same-origin is enforced via `sec-fetch-site`/`origin` (header-less requests are refused), but either header is forgeable by a non-browser client, and there is no per-IP or per-session throttle: a determined actor can grow `ops_pageviews`/`ops_error_events` without bound. Abuse fills tables — it does NOT leak data, reflect anything to the caller, or burn compute (one small capped row, no expensive path). Deliberately out of scope for the "deliberately small" beta-ops item and NOT judged a beta blocker for a small invite-only cohort; needs a real throttle before any ungated public launch. Cheapest durable fix: a per-IP-hash token bucket in Postgres (or Vercel KV if one is ever provisioned) on `src/app/api/ops/route.ts`, plus a retention/pruning job the ops item also skipped. [fundscore-web: src/app/api/ops/route.ts + src/lib/supabase/middleware.ts]
  **Owner summary:** The small endpoint that records pageviews and errors has to be reachable without a login (otherwise it can't see the landing page, where beta invitees arrive). That means someone could spam it with junk rows. Nothing leaks and nothing breaks — the tables just grow — so it's fine for a small private beta, but it needs a rate limit before the site is ever opened to the public.
- [ ] (story) Per-stock receipts backend for V4 movement 04 ("the manager") — V4's receipts table (per-stock holding-period receipts: filed holdings × prices, "the picking cleared the fee") has NO backend spec; the cutover spec's sub-blocks `top10VsIwf`/`positioningBetBridges` are fixture-only with no serving source. **OWNER DECIDED 2026-08-06 (batch 3a): BUILD before cutover — beta-critical. SEQUENCE AFTER the foreign-holdings enrichment lands** (else the panel gates weak for exactly the foreign-heavy funds where the proof matters most; building first = building twice). Honest gating for foreign-heavy books, same coverage doctrine as everything else. Spec-writer dispatched 2026-08-06. [fund_score: per-stock receipts panel + serving]
  **Owner summary:** V4's most persuasive section — the per-stock receipts proving the manager's picks did or didn't cover the fee — has no data pipeline behind it yet. Owner decided 2026-08-06: build it, so the beta ships the complete proof; the build runs after the foreign-holdings fix so international funds get honest receipts too.
- [ ] (story) Neighbourhood panel backend for V4 movement 03 — **OWNER DECIDED 2026-08-06 (batch 3b): IN for beta.** The movement places the fund among household alternatives (the current twin's full life, IVV, VT, BND: growth + drawdown/recovery through 2008→today) and has NO serving source (the mockup's figures were draft-computed). Small new gold panel + serving field with the usual gates; underlying price series already exist. Twin backcast must carry the HYPOTHETICAL · MIX-AS-OF labeling. Spec-writer dispatched 2026-08-06. [fund_score: neighbourhood panel + serving / fundscore-web: movement 03]
  **Owner summary:** The "what should I compare this to in real life" section — the fund next to the S&P 500, world stocks, and bonds, including how each fell and recovered in past crises. Data we already have, assembled into one served, honestly-labeled panel.

**Value-prop delivery group (filed 2026-07-17).** These deliver homepage promises (`docs/product/page_specs/home.md`) that the already-queued exposure specs (`serve-full-exposure-panel`, `portfolio-exposure-parity`, `exposure-screener`) do NOT cover. Confirmed gaps via a coverage-matrix pass — none duplicate a queued spec. NOT launch blockers; they build out the value prop after the gated launch, so they sit at the bottom of Open.
- [ ] (story) Portfolio X-Ray — cross-fund overlap & "cancel each other out" — make the hero promise literal: show which funds in a book duplicate each other (two funds holding the same names; an active fund that mirrors an ETF you also own). Pairwise holdings/factor overlap + a "do these funds diversify or cancel each other out" readout, on top of the aggregate concentration (top10/HHI/effective-N) that `portfolio-exposure-parity` already adds. NOT covered by any queued spec — parity does aggregate concentration only, not fund-vs-fund overlap. Spec'd but deferred: `docs/product/page_specs/portfolio_xray.md` §6. [fundscore-web + fund_score: portfolio look-through / overlap]
  **Owner summary:** The homepage's whole thesis — "you might own the AI trade seven times and call it diversification" — needs the X-Ray to actually show which of your funds overlap each other. We total up concentration today, but we don't yet show the fund-vs-fund duplication that makes "the same trade in seven wrappers" concrete and personal. This builds it.
- [ ] (story) Portfolio-aware screening — "funds that diversify your current holdings instead of duplicating them" — screen the universe against the user's own X-ray'd book (low overlap / complementary exposure to what they already hold), not just absolute or vs-passive exposure. Homepage screener promise + the deferred `portfolio_xray.md` §5 inline "find funds with less [X] for your portfolio" CTA. Depends on `exposure-screener` + the cross-fund overlap work above. [fundscore-web + fund_score: screener + portfolio context]
  **Owner summary:** Our screener searches the whole universe by exposure. The homepage also promises "funds that diversify what you already own," which needs the screen to know your portfolio and rank for what complements it — not just what matches a filter. It's the feature that connects the X-Ray and the screener into one loop.
- [ ] (story) Screener predicate — "active managers with broad-based security-selection value" — let the screener filter/rank on the security-selection-skill signal (idio / selection evidence), which ships as a fund-page feature but is not yet a screenable dimension. Homepage screener example NOT covered by `exposure-screener` (that spec is exposure / vs-passive only). Must stay backward-looking evidence, never a forecast or ranking-as-advice. [fund_score: expose selection-skill as a screen dimension / fundscore-web: screener]
  **Owner summary:** One of the homepage's screener examples asks for manager skill, not exposure — "active managers with broad-based selection value." We already show that evidence on a fund page, but you can't search on it yet. This makes it a searchable dimension, carefully framed as historical evidence rather than a prediction.
- [ ] (story) Fund attribution — "how much of the excess came from one bet" — a single-bet excess-return concentration readout ("N% of this fund's outperformance traced to semiconductor exposure, not broad selection"), the homepage's "did the manager earn it?" payoff. The single-fund TE / return decomposition already ships (`specs/done/te-decomposition-by-bet.md`, `attribution-factor-path-serving.md`); this is the concentration framing on top — what share of EXCESS RETURN (correctly labeled, not TE) traces to the top one/few bets vs broad selection. No forecast. [fund_score: return_attribution framing / fundscore-web: profile]
  **Owner summary:** The sharpest line on the homepage — "the manager outperformed, but 95% of it came from one sector, not skill" — is a framing we can almost tell but don't yet. The decomposition numbers exist; what's missing is the one-line "how concentrated was the outperformance in a single bet" readout on top of them. Small feature, but it's the punchline of the active-management story.
- [ ] (story) Monitoring & Alerts — the homepage's "know when the investment changes": notify a user when a fund they follow drifts from the role they expected it to play (positioning / exposure / factor / manager change past a threshold). NET-NEW experience — no route, spec, or prior backlog item exists anywhere. Reuses the shipped positioning-changes / exposure panels as the drift signal; new surface = a watch list + thresholds + a delivery channel. PRD `feature-pipeline/prds/monitoring-and-alerts.md` — **OWNER-DECIDED (all 7) 2026-07-27, ready to spec**: baseline = fund's own recent history; dimensions = sector/theme + manager + fee hike + factor/concentration/turnover; noise = quiet ~1/qtr/fund; scope = fund-by-fund explicit Follow; delivery = in-app feed + email (both from v1); tier = paid-only (whole surface). Spec notes: (1) owner included the noisier factor/concentration/turnover dims AND chose quiet — needs per-dimension threshold calibration; (2) email requires consent/unsubscribe/cadence scope. [fundscore-web + fund_score: new surface] [model: opus, effort: high]
  **Owner summary:** The homepage promises we'll tell you when a fund quietly turns into a different investment. Nothing like this exists yet — it's a whole new surface (a watchlist plus alerts on positioning drift), built on the change-detection we already compute per fund. All the product calls are now made (2026-07-27): follow individual funds, get a quiet ~once-a-quarter alert when one drifts, delivered in-app and by email, as a paid-only feature. Ready to write the spec.
- [ ] (story) Crescent v2 productionization — backend follow-ons for the five-block Fund Profile shipped on the v2 preview 2026-07-21 (web branch `feature/crescent-profile-v2`; concept `fund_score/docs/product/strategy/crescent_signature_artifacts.md`, status BUILDING). The page runs on served data EXCEPT the Archetype, which is a fixture (real A2 prototype outputs, 7 exemplar tickers, chipped). To take the fixture out and finish the system: (1) **archetype classifier data product** in fund_score — locked cutoffs from `reports/product/crescent_data_validation.md` (6 archetypes + borderline ±20% + Unclassifiable), new gold panel + serving field + check registration; prerequisites: FF6 style-panel refresh (11.2% coverage → Style Tilter under-detects; DODGX misfalls to Broad) and the naming/legal pass on pejoratives ("Closet Indexer"). (2) **Serve turnover** (in diversification_panel, not in fact_assembler) — two archetype rules key on it. (3) **Fix the 222 `too_new` mislabels** (funds with 4–30y pricing whose real blocker is twin fit — coverage_state should say so; DEFECT filed in the validation memo). (4) Runner-up twins for the TwinPanel = the `serve-l2-passive-candidate-fit` backend, SHIPPED 2026-07-23 (per-candidate corr/TE/beta + fit floor served in `passive_baseline.candidates`/`selected_summary`, staging; Postgres load owner-gated) — consume the served field, do not duplicate. (5) Production cutover of the five-block page folds into `specs/queue/profile-v2-production-cutover.md`; X-Ray crescent composite is a separate later story. [fund_score: archetype classifier + serving / fundscore-web: swap fixture → served field] Also fold in: delete the dead `ai_summary` fixture key from profile-v2-fcntx.json (section dropped from the Crescent page; ai-summary-generation spec rejected 2026-07-22).
  **Owner summary:** The new Crescent fund page is live on the local preview with real data everywhere except the archetype word, which still comes from the validated prototype's outputs for a handful of demo funds. This story builds the real classifier as a data product (plus the style-panel refresh and the legal pass on names like "Closet Indexer"), serves turnover, fixes the mislabeled "too new" funds, and then swaps the fixture out — after which the page is production-cutover ready.
- [ ] (story) Per-fund share-class comparison — a fund runs many share classes with the SAME portfolio but different ticker / net-ER / load / minimum (e.g. AB Global Bond `ANABX`/`ANAGX`/`ANAIX`; American Funds run up to 16). The profile today surfaces ONE representative class — the retail-accessible flagship the item-1 `primary_ticker` rule picks (Morningstar-style: retail before institutional, then flagship). Add a share-class table on the fund profile: every live class with its ticker, net ER, load, and minimum, so a user can find the class they can actually access and see the fee spread across classes. Fits the fee-transparency thesis (Morningstar/Vanguard both let a user self-select the class). Data: per-class ER exists (`data/mfrr/outputs/expense_ratios`, class-keyed) + class liveness (`fund_daily_adj_close`); load/minimum need a prospectus/MFRR source. NOT a launch blocker. [fund_score: per-class fee/load panel + serving / fundscore-web: profile share-class table] [owner-requested 2026-07-31, during the item-1 flagship-class decision]
  **Owner summary:** Every mutual fund has multiple share classes — same investments, different fees depending on how you buy it. We show one representative class per fund; this adds a small table listing every class with its ticker, fee, load, and minimum, so you can see the fee range and find the version you can actually buy. It's a natural fee-transparency feature and the industry standard (how Morningstar and Vanguard both present it).

## Specced (in queue)
Stories that became a queued spec — they've left the backlog for the spec queue and are **not**
re-triageable. `/implement-next` flips one to Done when it ships the linked spec.
Format: `- [~] (story) Title — context → specs/queue/<slug>.md`

- [~] (story) V4 serving riders — P(skill) population strip + effective-positions stat — OWNER DECIDED 2026-08-06 (batch 3b): IN for beta. **SPECCED 2026-08-07 (beta-execution-plan W5) → `specs/queue/v4-serving-riders-skill-strip-effective-positions.md`. The story's "two small serving additions, lean lane" framing did NOT survive grounding, and the spec is `lane: reviewed` / opus-high accordingly.** (1) **Effective positions is NOT an addition — it is already served AND already rendered** (`exposure_xray.rows[] @ concentration::effective_positions` → `format.ts:441` → `CurrentPositioning.tsx:422`), and it is on the WRONG BOOK: `diversification_panel.eff_n_raw` computes from `holdings_snapshots.weight` (US-ticker-resolved, equity-renormalised), not the filed `pctVal` basis the standing owner decision requires for product-displayed weights. Measured on PRNEX: the served figure used **57 positions to describe a fund that files 127**, giving **30.5** where the filed book gives **59.8**. **SIZING CORRECTED 2026-08-20 (L10 Segment 0, checkpoint-verified) — the "~2× every fund" reading generalised PRNEX and is RETRACTED:** the *direction* is near-universal (**86.8%** read too concentrated) but the *magnitude* is heterogeneous (**median 1.10×**, p75 1.65, **p90 8.3×**, max 130×; only 21% are ≥2×) and **6.2% are biased the OTHER way** — verified real, not a degenerate-book artifact. **The tail is the defect:** 163 served funds show <5 effective positions while filing ≥50 lines, 130 show <2, and 9 exceed their own filed line count — JFEAX files 288 lines and serves **1.0**, while JINTX files ONE line and serves **70.4**. So the rider REPLACES rather than adds (leaving `eff_n_raw` for the fee-efficiency multiplier, where the active basis is correct). Coverage improves 44.9% → **93.4%**. (2) The strip's percentile denominator changes the page's own sentence: the V4 mockup's "53% of graded funds sit below" reproduces only off the raw 8,150-row posteriors panel, 61% of which is funds we don't serve; over the **2,714 funds a reader can actually look up** the same fund reads **34.1%** and every band shifts. Spec takes the served population (a denominator you can't navigate to isn't checkable) and supersedes the mockup copy. Rider A coverage **46.6%** of served funds (66.3% of active), remainder honest-missing. **The cutover spec's §06 "effective positions not served — omit" is also wrong and needs correcting.** [fund_score: fact_assembler riders / fundscore-web: movements 04 + 06] → specs/queue/v4-serving-riders-skill-strip-effective-positions.md
  **Owner summary:** What looked like two small additions turned out to include a live wrong number. The "effective positions" figure the new page already shows is computed on only the holdings we can match to US tickers — for one fund that meant describing a 127-holding portfolio using 57 of them, reporting ~30 effective positions where the filed portfolio gives ~60. Nearly every fund reads more concentrated than it really is, though by how much varies enormously — for the typical fund it is about a tenth, but a few are off by many times over: one fund that files 288 holdings is currently described as having **one**. The spec now fixes that rather than adding alongside it, and it also found that the skill-percentile band in the mockup ("53% of funds sit below") only works if you count funds nobody can look up — against the funds we actually serve, the honest number is 34%.

- [~] (story) X-ray >100%-of-NAV and book-basis presentation — **OWNER DECIDED 2026-07-22/23, all four calls; doctrine: disclose as filed, never renormalize or suppress.** (a) serve the honest liability-explained >100% cohort as filed + a plain leverage flag; (b) show the SHORT book as well — carry short legs through gold and display both legs labeled (until shipped, label the long book "long positions only" with the filed net alongside); (c) keep dropping filed-incoherent rows but DOCUMENT the inclusion contract and surface the dropped count; (d) feeders anchor coherence to the master's own filing. Original: X-ray >100%-of-NAV and book-basis presentation — FOUR owner decisions from the 2026-07-10 country-coherence investigation (full evidence: fund_score docs/status/data_gap_analysis.md holdings section + check_exposure_xray_country_coherence.py WARN cohorts): (a) how to PRESENT the honest liability-explained >100% cohort (24 funds, RYZAX 178.9%) — serve as-is on the filed %-of-net basis vs flag vs suppress (no renormalization per the 2026-07-08 basis decision); (b) negative-leg serving semantics — holdings_complete carries only the LONG book, so 30 collar-style funds serve above their filed net totals (ASPCX +8.4pp; JAIGX filed −178% net, served +94%): long book or filed net book?; (c) holdings_complete row-inclusion contract for filed-incoherent rows (~75 funds file valUSD>totAssets, EDGU canonically — gold silently drops such rows today, undocumented); (d) feeder gate anchor — feeders (DFCSX/ECGIX) are G1/G2-exempt; link the master's own filing as the coherence anchor. [fund_score: exposure_xray / holdings_complete; owner] → specs/queue/holdings-book-basis-disclosure.md
  **Owner summary:** Four product decisions about presenting funds that use leverage or short positions (portfolios legitimately summing over 100% of assets). Needs owner judgment on presentation, not engineering — until decided, a small set of funds shows technically-true-but-confusing totals. **Decided 2026-07-22/23: show everything as filed — flag leverage, add the short book (specced), document what we exclude, and check feeders against their master's filing.**
- [~] (fast-follow) Look-through coverage-honesty hardening — two codex `--high` P2 advisories from the shipped look-through positioning feature (fund_score fd982cd), both honest-partial-coverage disclosure gaps. (1) `lookthrough_window.py:315` flags `is_unresolved_wrapper` only when `msid` resolves, so a held ETF outside the twin-map with a non-mapping/absent LEI (VOO/IVV/VEA) is counted as a resolved leaf → `lookthrough_coverage` over-reports; flag it honest-missing (or extend resolution), exclude from the resolved numerator. (2) `fact_assembler.py:785` emits coverage metadata only inside the per-`is_surfaced`-series dict, so a partial-coverage fund with zero surfaced shifts gets NO positioning_changes section → UI can't show its partial-coverage flag; emit `rows:[]` + coverage metadata for partial funds. Backend (fund_score), reviewed lane, depends_on look-through-positioning-changes. Realized impact nil today (data-reviewer verified), latent as data evolves. → specs/queue/look-through-coverage-honesty.md
  **Owner summary:** Two disclosure gaps in the fund-of-funds feature: coverage can be over-reported, and partially covered funds can lose their coverage flag entirely. Fixing both keeps the "we tell you what we can't see" promise intact as data evolves.
- [~] (story) Crescent v2 productionization — **SUPERSEDED 2026-07-30 into the cutover spec's V4 target.** The five-block preview on `feature/crescent-profile-v2` is no longer the thing to promote; V4 (`fund_score/docs/product/strategy/mockup_fund_profile_v4_2026-07-28.html`) is. The branch stays unmerged as the running preview + component donor. Its remaining backend prerequisites (archetype classifier, serve turnover, the 222 `too_new` mislabels) survive as V4 items where V4 still uses them — the archetype chip is absent from V4, so the classifier is no longer a cutover blocker. → specs/queue/profile-v2-production-cutover.md
  **Owner summary:** The redesign we were going to promote has itself been replaced by the newer V4 page, so this story now lives inside the cutover spec rather than standing alone. The old preview branch stays put as a working reference — nothing needs merging.
- [~] (story) Unify the TE decomposition onto the 35-factor global basis (ONE idio machine) — owner decision 2026-07-22 after the promotion investigation: the global basis merged 2026-06-25 (`3120e05`, headline idio into vo_reframe_v0.4) but the deferred te_decomposition rewire never happened, leaving a sector-crowded per-fund bets vocabulary AND two idio siblings on two bases. Spec: refresh the stale chain (inputs end ~mid-June; te anchors to value_score at 1e-9), repoint `build_te_decomposition` onto the fixed 35-factor vocabulary with an EDA-gated display rule + theme-handling decision, rewire vo_reframe idio to the repointed panel (v0.5), retire `global_decomposition` after a consumer census (theme_ride provenance!). Scoring basis untouched. → specs/queue/unify-te-decomposition-global-basis.md
  **Owner summary:** The bets table gets one universal risk vocabulary — macro and geography guaranteed a seat instead of being crowded out by sectors — and the site drops to exactly one "how much is stock-picking" number computed one way, everywhere. The model was already promoted a month ago; this finishes the wiring it was promised.
- [~] (data) **827 SURFACED false "entered"/"exited" rows across ~250 funds — a fund is shown SELLING a position it still holds** — found 2026-08-26 by the positioning check-data adjudication, which added a FAILING check (Check 10) so it stops hiding. **Mechanism:** `cusip='N/A'` is a STRING SENTINEL, not a null, so when a filer omits the CUSIP the ticker resolution drops out; a security held at BOTH endpoints then keys differently at each end and the year-over-year diff reads it as an EXIT. This is the [[negation-filters-absorb-new-row-types]] shape — a sentinel that a downstream comparison treats as a value. Bounded on ISIN (the identifier that survives): **827 surfaced rows / ~250 funds**, against a control leg of 24,381 true exits, so it is ~3.3% of exit claims and it lands on rows a reader actually sees. **Worked example, dispatcher-verified against the no-expansion frame:** DAACX (`S000073478`) is served "exited AAPL −4.89pp" — at 2025-01-31 Apple is `ticker=AAPL, cusip=037833100, isin=US0378331005, pct_nav=4.888%`; at 2026-01-31 the SAME `isin=US0378331005` is present at **`pct_nav=4.689%`** with `ticker=None, cusip=None`. **The fund never sold a share; the filing just stopped printing the CUSIP.** Second example from the adjudication: S000048264 "exited AAPL −9.71pp" while holding Apple at 9.62% of NAV. Remedy is a design change — resolve ticker via ISIN and re-derive the row as increased/decreased rather than exited — so it needs its own loop, not a patch. **SCOPED 2026-08-26 at the owner's request, and it is BIGGER than 827 rows.** Viability measured: of the **1,012,231 rows (38.9% of the 2.6M-row frame) carrying NO `security_ticker`, 1,002,884 (99.1%) DO carry an ISIN**; only 8,877 have neither ISIN nor CUSIP. So ISIN can carry the join — but that changes the MATCHING KEY of the entire comparison, touching ~1M rows. **The 827 are the visible tip (the surfaced subset); what the other ticker-less rows do today is NOT yet measured and must be before anyone builds.** Expect currently-separate rows to merge, magnitudes to move and the surfaced set to shift. **Needs a spec + the reviewed lane, with the "what else moves" measurement FIRST.** Not days-shaped — which is why suppressing the 827 now is worth doing rather than waiting for this. **This is the next item off the positioning thread.** [fund_score: positioning_changes.py / holdings lookthrough window] — SPECCED 2026-08-26 → specs/queue/positioning-isin-rekey.md (decision B; measurement-first)
  **⚠ READ BEFORE YOU FIX THIS — a trap fires at the moment of success (codex P2, 2026-08-26, filed not fixed).** The defect is guarded by a pinned-baseline ratchet in `check_positioning_changes_panel.py`. When you fix the root cause and run the instructed `--pin-baseline`, the pinned per-series maps become EMPTY — and in that state `prove_ratchet({}, 0, ...)` runs probes 1-3, which assume a non-empty baseline, mark the prover broken, and **flip your clean zero-defect artifact to FAIL**. Nothing is wrong with your fix when that happens; the prover is. Handle the zero-defect baseline in the probes as part of your change. It was left unfixed deliberately: the gate passes today, the state cannot occur until someone deliberately re-pins, and that someone is you, reading this.
  **Owner summary:** On roughly 250 funds we tell the reader the manager sold out of a position they in fact still own — Apple, in the two cases we checked, at nearly 5% and nearly 10% of the fund. Nothing was sold; the filing simply stopped printing one of the ID numbers we key on, and our comparison read the gap as a sale. It is the same fund-page section we just improved, so it is worth fixing next.

## Done

**Recent only — the 3 most recent shipped items** (full history: `backlog-archive.md`). When a new
item ships, add it on top and move the overflow entry to the TOP of `backlog-archive.md` — the
archive is the full changelog; this section is a bounded buffer so the operational backlog (loaded
on every `/triage`) stays small.

- [x] (data) **DONE 2026-08-09 — Foreign-holdings enrichment (L1) CLOSED: segments 1-4b, 6, 7 across four merges to fund_score main (`bad49ad`, `c000de4`, `0674802`, `bd39539`); segment 5 re-filed pending P3.** Headline: ~$40B of sector coverage recovered at validated >=95-97% precision (ISIN spine +$25.1B raw recall; wrappers via industry-corroboration +$11.97B; identity-gated name bridge +$12.96B); the sweep also caught and fixed a $50T pre-existing valUSD fabrication (unscaled master-feeder look-through, 68% of panel USD — zero served fields affected, reviewer-enumerated), made rebuilds identity-deterministic (dominant-lot representative, sentinel purges), unified ALL THREE holdings panels on one domicile-aware sector basis (killing six live wrong-company binds incl. Roche-served-under-Roper-Technologies; positioning classification 52%->97%), proved the Gulf gap honest ($14.88B, 99.974% by dollars), and left four permanent non-degenerate gates in make check (NAV-identity with absolute cap; superlative guard; rebuilt positioning grader; window parity tests). Method record: two segments HALTED at the dev/holdout precision bar and shipped honest nulls instead (C1 ties 81%; the venue rule); every checkpoint ran adversarially on the session model; three degenerate self-checks were caught and replaced with probes proven able to fire. Follow-ups filed, not buried: L14 domicile routing (five symptoms, PRE-RELOAD), $3.51B name-bridge recall chore, S7-2 pctVal guard, S7-4b within-US binds, check-hardening deferrals. [fund_score]
  **Owner summary:** The biggest beta blocker is done. A third of our equity funds understated their foreign holdings' sectors; now ~$40 billion of holdings are correctly classified, with every recovery rule validated on held-out samples and anything uncertain left honestly blank rather than guessed. The hunt also surfaced and fixed a huge internal accounting error (research-only, never user-facing), made our data pipeline reproducible run-to-run, and stopped several cases of one company being served under another company's identity. One follow-up must land before the next website data refresh; the rest are quality-of-life items.
- [x] (bug) **DONE 2026-08-08 — Superlative-guard consumer check SHIPPED (fund_score `06ae57a` on `l11/superlative-guard`, owner merges; beta-execution-plan item L11).** New `superlative_guard` in the check registry: independently recomputes `top_bet_gap`/`top_bet_confident` from raw `|te_alloc_bps|` (no trust in shipped rank) against the producer's own threshold, adds a rank-vs-allocation coherence invariant, scans served-copy template registries for ungated superlatives, and fails CLOSED on null flags. 14 tests incl. 7 synthetic-violation fixtures proving each invariant can fail; real gold clean (0 mismatches, 0 rank drift, 2,086 series). Codex --high pass ×4 with three fail-opens in the guard itself found and fixed in-loop; two residual advisories consciously deferred at the round cap → new Open chore. Web audit: every live "biggest bet" render correctly gated (V4 stricter than v2); 4 ungated adjacent superlatives + 1 dormant fail-open → new Open bug. The item's (b) half (largest-*factor*-bet phrasing) is enforced by the gate contract the check now polices. [fund_score 06ae57a]
  **Owner summary:** There's now an automated watchdog making sure we never tell a reader "this is the fund's single biggest bet" when the data can't actually separate #1 from #2 — the exact mistake the old mockup made. It double-checks the math from raw inputs rather than trusting the pipeline's own flags, and it's been battle-tested against injected violations. The live site was audited too: every current "biggest bet" sentence is honest.
- [x] (story) **DONE 2026-08-07 — Screener beta port SHIPPED (fundscore-web branch `feature/crescent-profile-v2` + fund_score branch `w3/query-serving-tables`; spec `specs/queue/screener-beta-port.md`; beta-execution-plan item W3).** Owner's default (Postgres-served, same pattern as the profiles) held — **no hard blocker**. Up-front EDA sized the query surface at **155 rows / 30 columns** (`query_canonical_catalog` 15×13, `query_canonical_results` 140×17 = 14 slugs × 10 + 1 refusal slug with 0), longest text field 122 chars — trivially small, so DuckDB, R2/httpfs and the MotherDuck plan were all retired rather than ported. **Coverage is identical to the DuckDB path**: 15/15 catalog rows, 140/140 result rows, 14/14 slugs, and all **110 distinct `series_id` present in `fund_profile_facts` (110/110)** — no honest-missing and no recoverable-missing remainder. Two new serving tables (DDL in fund_score `apply_serving_schema.py`, the authoritative source) load by TRUNCATE+COPY inside the SAME transaction as `fund_profile_facts` (`_load_query_tables`), plus a standalone `scripts/pipeline/load_query_serving.py` for loading them alone. `src/lib/serving/screener.ts` rewritten over Drizzle with an **unchanged exported API**, so `/q/[slug]`, `/search` and `/lens/[lens_slug]` needed no edits; `@duckdb/node-api` removed (a native binary out of the Vercel bundle). **One deliberate behavioural change:** the Value Score verdict now LEFT JOINs `fund_profile_facts` — the same row the fund page renders — instead of the stale `screener_funds.parquet` (2026-07-11). Those two sources had drifted, so the screener and a fund's own profile page contradicted each other; measured, the verdict changes on **13 of 140 rendered rows** and the contradiction is now impossible by construction. `value_score_100` is never selected (verdict free, precision paid) and `breakeven_state` is read from the served field instead of re-derived. Gates: parity script PASS (every transported column equal to the DuckDB read, 0 failures), lint pass, build pass, `/q/[slug]` **still ● SSG with all 14 prerendered paths** against a loaded DB, live `next start` smoke (10 ranked cards, refusal slug 404, `/search?q=` 307 → canonical slug). **D1 impact:** the load is the same one command (nothing new to run), but `/q/[slug]` prerendering now needs the build host's `DATABASE_URL` to reach a LOADED serving DB — redeploy after the load or the 14 slugs render on demand instead of statically. Out of scope and filed separately: `/screener`'s synthetic `funds` table (the story's premise about that page was factually wrong — see the new Open bug), re-emitting the 2026-07-02 canonical query panels (F1), and loading `screener_funds`/`screener_theme_exposure` (nothing reads them). Original: `/screener` and `/q/[slug]` read DuckDB over LOCAL parquet — they cannot run on Vercel; decide the approach and port. [fundscore-web: src/lib/serving/screener.ts / fund_score: serving/load.py + apply_serving_schema.py]
  **Owner summary:** The published-question pages — the ones that answer "which funds hold the most AI infrastructure above their passive blend" — used to work only on your laptop, reading analysis files off local disk. They now come out of the same database as the fund pages, so they deploy like everything else. Nothing was lost in the move: the same 15 questions, the same 140 ranked funds, verified column by column. It also fixed something we didn't know was broken — the value verdict shown next to a fund in the results came from an older file than the fund's own page, so the two could disagree; they now read the same row and can't. One thing did NOT get fixed, and it's the more urgent one: the "Screener" link in the nav is a separate, still-fake page (filed as its own item above).
