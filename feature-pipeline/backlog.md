# Triage Backlog

The intake list. `/triage` drains the **Open** section: it picks the top item and routes it by
`(type)`. A `(bug)` or `(data)` item is fixed end-to-end and moves straight to **Done**. A `(story)`
that gets turned into a queued spec moves to **Specced (in queue)** — it has left the backlog for the
spec queue and is no longer re-triageable — and only reaches **Done** when `/implement-next` ships
that spec. Each item lives in exactly one state at a time (never Open *and* specced).

**Format:** `- [ ] (type) Title — one-line context [area]`
**Optional model tag:** `[model: fable|opus|sonnet, effort: xhigh|high|...]` — a routing hint for judgment-heavy items. Fix loops run in the MAIN session, so the dispatcher should surface the tag ("this item recommends X — /model to switch, or proceed") rather than silently ignore it. Specs carry the same hint as `model:`/`effort:` frontmatter, which `/implement-next` passes to the implementer agents automatically.
**Types & routing:**
- `(bug)` → `fundscore-data:fix-bug` (fundscore-harness plugin) — lean loop: reproduce → fix → verify → review (Claude + codex) → commit.
- `(data)` → `fundscore-data:fix-data` (fundscore-harness plugin) — lean loop: root-cause at source → fix → rebuild → /check-data + data-reviewer → review (Claude + codex) → commit. (No synthetic data; fix upstream.)
- `(story)` → `spec-story`: clarity-gate → a lean spec in `specs/queue/` (or escalate to the owner if vague). Discovery-heavy, net-new product direction goes to `/critique-funds` → review-proposals → `/spec-approved` instead. Once a spec is queued, the item moves to **Specced (in queue)** (below), not Done.

Keep items small and single-purpose. Put the type first so the dispatcher can route without reading the whole line.

---

## Open

- [ ] (bug) Land the legacy value-offering emitter retirement (fund_score branch `feat/retire-legacy-value-offering-serving`, c4cf2a6, 2026-07-04, KEPT unmerged in the 2026-07-11 branch cleanup) — web dropped the legacy `value_offering` JSONB section + fee-gap scalar from `fund_profile_facts` in 2026-06, but fund_score `fact_assembler` ALL_COLUMNS still emits `value_offering`(+score/label/status, "kept additive" comment from 906f5eb predates the retire decision); the branch removes the emission (12 files, fact_assembler/load/screener_base) but is stale vs a fact_assembler that has since gained positioning_context/te_decomposition/fee_peer_percentile. Do NOT blind-merge: re-land through the gated fix loop (rebase or re-apply, full staging rebuild, check no column regression, codex gate). No live defect today (loads pass); this is emitter/schema drift hygiene. **2026-07-11 evidence (label-fix rebuild experiment):** the on-disk screener panels were BUILT FROM this branch — rebuilding build_screener_base on main re-adds vo_score/vo_label/fee_gap_bps and shifts screener_theme_exposure −464 rows; land this branch before any screener rebuild. [fund_score: serving/fact_assembler ALL_COLUMNS / load.py]
- [ ] (data) Reviewed panel-refresh pass — gold panels no longer reproduce at their pinned as-ofs (input-state moved: 2026-07-10 dedup universe 5,799→5,706 et al.). Measured 2026-07-11 during the label fix: return_attribution −146k rows (−1.8%), exposure_xray 75 target rows + ~4.8k value cells, positioning change_z shifted on ~72k/78k rows, security_characteristics −3,752 rows, value_offering named-bet mass ~5k rows. Shipped panels are coherent but frozen pre-dedup. Refresh ALL panels together as a REVIEWED pass (numbers move, owner-visible), incl. the takeaways+the_take coherent pair rebuild (sibling item) and the mixed-state risk_model catalog internals (attribute_def/model_factor/target_membership regenerated 2026-07-11 without pre-images — converge here). Owner option to fold in: rename "EM ex-Japan" → "Broad EM" (data-reviewer advisory: Japan isn't EM, qualifier is vacuous; same migration pattern). Evidence: fund_score docs/status/data_gap_analysis.md 2026-07-11 section. [fund_score: panel builders / risk_model catalog]
- [ ] (data) holdings_snapshots upstream ingestion gap — 53 LIVE N-PORT filers' recent filings never reached the panel (EDGAR-verified by the 2026-07-12 data-reviewer: S000041190/WisdomTree Japan Opps filed period 2026-03-31 on 2026-06-01, panel ends 2019-09-30; JPEM filed 2026-04-30, panel ends 2024-10-31; Direxion Brazil Bull 2X filed 2025-04-30, panel ends 2022-10-31; 10 of the 63 recency-gated funds are honestly dormant). RECOVERABLE-MISSING = DEFECT: fix the N-PORT→holdings_snapshots ingestion path + rebuild; the 53 recover automatically through the serving recency gate (005e24c). Penumbra: 116 kept funds serve books 366–548d old (healthy p90 = 92d) — some plausibly the same gap inside the window. Likely related to the frozen universe-evidence producers item (fund_metadata/CTM frozen at 2024-12-31 — possibly the same upstream staleness). Evidence: fund_score docs/status/data_gap_analysis.md 2026-07-12. [fund_score: nport ingestion → holdings_snapshots]
- [ ] (data) SPY + DBC absent from the Tiingo pricing store — every S&P 500-benchmarked fund (1,430) + DBC-benchmarked (47) silently lacks the 3Y benchmark-relative risk group (beta_3y/alpha/TE/IR/captures NULL) because `get_benchmark_etfs("S&P 500")→['SPY']` and SPY has ZERO rows in data/vendors/tiingo/daily_pricing/ (verified mid-refresh 2026-07-12; 1,477/1,477 of the mappable-benchmark NULL cohort explained by these two series). Page renders honest null; RECOVERABLE — ingest SPY/DBC (or remap S&P 500 to IVV/VOO/SPLG already in the store) + rebuild fund_metadata risk fields. Flagship casualty: FCNTX's risk expander has no relative group. Also correct the risk-behavior registry limitation wording (dominant cause is the proxy's missing prices, not unmappable benchmarks). Surfaced by the 2026-07-12 riskBehavior-flip data-quality-critic (P1). [fund_score: tiingo ingest / etf_pools / build_gold_metadata]
- [ ] (bug) build_gold_metadata rf leg silently zero-fills — `_load_risk_metrics` does `rf_return.fill_null(0.0)` (build_gold_metadata.py:1648), so months where SHY has no return get rf=0 instead of being dropped; the vendor store demonstrably runs mixed vintages (fund NAVs 2026-07-10 while SHY sat at 2026-05-08 mid-refresh), so a rebuild in that state would zero-fill trailing rf months and silently flatter Sharpe/Sortino. Fail-honest: drop unmatched months or gate the build on rf coverage. Latent today (shipped build predates the drift). Surfaced by the 2026-07-12 riskBehavior-flip data-quality-critic (P2). [fund_score: build_gold_metadata._load_risk_metrics]
- [ ] (data) N-CEN adviser pick is order-dependent → wrong fund-family identity — `ncen/parser.py` (~lines 250–258) takes the FIRST `<investmentAdvisers>` entry; DREVX's N-CEN lists Newton NIMNA (the SUB-adviser per its own 485BPOS + BNY fact sheet) first and BNY Mellon Investment Adviser third with `<subAdvisers>` empty → DREVX serves family "Newton Investment Management North America, LLC / 1 scored fund / too small to rank" when it should be member 27 of the RANKED BNY Mellon Investment Adviser family (74/115, 26 funds, $27.6B); `has_sub_adviser=false` also wrong. Measured blast radius today: 1 fund, but the mechanism is systemic for any filer listing the primary adviser non-first — the fund-family spec's called-out fragmentation risk realized. Fix the pick (e.g. registrant-sibling coherence gate / prospectus cross-check) + rebuild fund_family_panel/members + fund_metadata adviser fields. Surfaced by the 2026-07-12 fund-family-flip data-quality-critic (P1). [fund_score: ncen/parser.py / fund_family]
- [ ] (data) Member AUM stamps one N-CEN cycle stale (recoverable) — FCNTX/DREVX `aum_as_of_date` = 2024-12-31 while both registrants filed FY2025 N-CENs on 2026-03-12 (EDGAR-verified, not ingested; external FCNTX AUM $145.9B vs our $140.6B). Page discloses the range honestly; ingest the FY2025 N-CEN cycle + rebuild metadata AUM. Likely same root as the frozen universe-evidence producers item (sibling). Surfaced by the 2026-07-12 fund-family-flip data-quality-critic (P3). [fund_score: ncen ingest / fund_metadata AUM]
- [ ] (data) Narrow-industry funds serve implausible top TE bets — RYLSX (Rydex LEISURE, holds NFLX/PM/MCD, zero financials held) leads with "Financial Services +320 bps (50% of factor variance, high confidence)" + near-duplicate "US Mega Banks +258 bps" (collinear FWL var-shares). Gold-exact and method-consistent, but an expert double-takes; review theme/sector bet labeling + collinearity handling for narrow industry funds before the section is marketed. Surfaced by the 2026-07-12 TE-flip data-quality-critic (P2). [fund_score: te_decomposition FWL basis / risk_model targets]
- [ ] (data) fund_the_take ↔ fund_takeaways citation drift — 269/1,431 SERVED Take rows cite takeaway rows that no longer exist (invariants 3+9 fail; report was stale 25/25 evidence from the 2026-06-23 ship until regenerated 2026-07-11). Pre-existing: proven identical on pre-fix backup images; a later run rebuilt fund_takeaways without fund_the_take. Served impact: up to 269 funds' Take sentences may quote numerics from retired takeaways. Rebuild the pair coherently as its own gated fix or fold into the panel-refresh pass (sibling item). [fund_score: build_fund_takeaways.py + build_fund_the_take.py]
- [ ] (bug) fact_assembler rank tie-break is unstable across identical-input runs — return_attribution section `rank_within_dimension` swaps tied rows between assembly runs on byte-identical gold (S000000137: mag_7 ↔ ev_battery_chain at ranks 7/8, bit-identical contributions). Cosmetic today; makes staging non-reproducible byte-for-byte and can flap served ordering on reloads. Add a deterministic secondary sort key (member_id) to the rank ordering. Surfaced by the 2026-07-11 label-fix identity verification. [fund_score: serving/fact_assembler.py]
- [ ] (bug) te_decomposition build gate fails open — two codex P2 advisories from the shipped te-decomposition-by-bet gate (fund_score ee72d15): (1) `build_te_decomposition.py:217-220` runs `validate()` AFTER writing the parquet and only logs failures, so `make build-te-decomposition` exits 0 and can promote a bad/empty `data/gold/te_decomposition.parquet` on a future rebuild — fail the build on any invariant violation (and on 0 decomposed rows); (2) register a `te_decomposition` entry in `scripts/checks/run_checks.py` so `make check FEATURE=te_decomposition` actually exercises the panel's checks (today it selects nothing). Realized impact nil today (shipped build validated PASS); latent on rebuilds. [fund_score: build_te_decomposition.py / run_checks.py]
- [ ] (data) Refresh the stale universe-evidence producers — `fund_metadata.last_nport/ncen_filing_date` AND `nport/class_ticker_mappings.parquet` are FROZEN at 2024-12-31 (0 of 26,155 series ≥2025) while the raw N-PORT store spans →2026-05 for 13,737 series; the 8993b29 ticker-dedup works AROUND this via latest_raw_nport_recency but the witness tier is censored at the 2024-12 horizon. Refresh the producers (recoverable in-house — the filings are ingested): dissolves the tier2-vs-tier2 coin-flip shape (BRIF), fixes the GOFCX/PEQKX/PYCGX dead-vs-dead groups (live Victory successors exist but never enter), un-censors witness recency. Fold in codex residual: backdated-replay hardening (CTM witnesses by first_seen≤as_of; partition-year globs by build date). Surfaced by the fund-reorg dedup review 2026-07-10. [fund_score: metadata/ctm producers + source_inventory]
- [ ] (data) Metadata identity hygiene from the dedup review — three small source fixes: (1) null stale `fund_effective_ticker` relic claims at source (MDIIX/MCLRX/FEIRX-class VA relics; 3 occupied tickers RYCWX/TMAAX/EKOCX have witnessed owners outside the universe); (2) real primary_tickers for the two Federated Hermes MDT ETFs registered with placeholder `TBD`; (3) backfill `fund_name` for raw-store-only arrivals with no frozen metadata row (HEQQ S000090211 + 1 serve NULL names — recoverable from their filings). Surfaced 2026-07-10. [fund_score: fund_effective_ticker / fund_metadata / identity backfill]
- [ ] (data) vs_benchmark `reference_value = 0.0` on 35.5% of exposure_xray country::US rows (zero-vs-null suspicion) — 1,763/4,967 vs_benchmark country::US rows carry benchmark US weight EXACTLY 0.0 (e.g. RYZAX, an S&P 500 fund, shows benchmark US weight 0 → difference +158.6pp with confidence_state "high"). Smells like a zero-for-missing default-fill on the benchmark side of the panel — a possible no-synthetic-data violation (zero-vs-null class). Investigate the passive-blend holdings join in the vs_benchmark frame; missing benchmark weight must serve as null, never 0. Surfaced by the country-coherence data-reviewer 2026-07-10. **ESCALATED by the 2026-07-12 TE-flip data-quality-critic (P1)**: not just country rows — SECTOR passive weights are classification/coverage-depressed too (IENAX vs its IXC-led blend shows benchmark energy 76.2% when IXC/XOP are ≈97–100% energy funds → the page renders '+14.9 pp overweight energy' beside a NEGATIVE energy beta t −8.1; RXI, a pure consumer-discretionary ETF, shows only 54.6% consumer-cyclical for RYLSX). The new TE bets table JOINS these held/active weights, so the false signal now ships on a flagship section. Mitigation until fixed: baseline-composition chip (web item). [fund_score: exposure_xray vs_benchmark frame / passive_blend_holdings]
- [ ] (story) X-ray >100%-of-NAV and book-basis presentation — FOUR owner decisions from the 2026-07-10 country-coherence investigation (full evidence: fund_score docs/status/data_gap_analysis.md holdings section + check_exposure_xray_country_coherence.py WARN cohorts): (a) how to PRESENT the honest liability-explained >100% cohort (24 funds, RYZAX 178.9%) — serve as-is on the filed %-of-net basis vs flag vs suppress (no renormalization per the 2026-07-08 basis decision); (b) negative-leg serving semantics — holdings_complete carries only the LONG book, so 30 collar-style funds serve above their filed net totals (ASPCX +8.4pp; JAIGX filed −178% net, served +94%): long book or filed net book?; (c) holdings_complete row-inclusion contract for filed-incoherent rows (~75 funds file valUSD>totAssets, EDGU canonically — gold silently drops such rows today, undocumented); (d) feeder gate anchor — feeders (DFCSX/ECGIX) are G1/G2-exempt; link the master's own filing as the coherence anchor. [fund_score: exposure_xray / holdings_complete; owner]
- [ ] (data) exposure_path active-universe lags fund_metadata — 22 funds with fresh 2026 holdings are excluded from `holdings_exposure_path` because `eq_active_universe()` → `active_retail_universe()` reads **unrefreshed** `fund_metadata`; rebuilding at a later as-of shifts the recency window and drops them even though their holdings are current (net −47 exposure_path series, 36 genuinely dormant + 22 metadata-lagged). Refresh `fund_metadata` alongside holdings, or have the active universe consider holdings recency. Surfaced 2026-07-03 by the NPORT-refresh sweep. [fund_score: build_exposure_path_attribution.eq_active_universe / universe]
- [ ] (bug) Coherence guard missing on the holdings_complete sibling path — the misfiled-pct_nav guard (fund_score f7ec601) covers `holdings_snapshots` (→ positioning, top_holdings) but NOT `holdings_complete` (→ exposure_xray), a parallel N-PORT read. holdings_complete is currently clean (0 pct_nav<0, 0 >1.5, max 1.03) so nothing leaks today, but the same class of corrupt *latest* filing would reach exposure_xray unguarded. Port the identical `[0,1.5]` null guard to `build_holdings_complete.py` for symmetry (no current data change expected). Surfaced by the pct_nav-guard data-reviewer 2026-07-08. [fund_score: build_holdings_complete.py]
- [ ] (bug) Positioning aggregation turns a quarantined null pct_nav into 0 (false 0% / false exit) — [codex --high P2 from f7ec601] Polars `sum()` returns 0.0 for an all-null group, and `build_positioning_changes_panel.build_positions` pivots with `aggregate_function="sum"` before treating null/zero as absence, so a quarantined holding that is the ONLY value in a ticker/sector/theme/top-10 bucket at an endpoint reports 0% weight (or a false exit) instead of degraded/missing. Verified **zero served impact today** (only the dormant/unserved S000030654 has an all-null latest quarter; no served fund's corrupt quarter lands on a positioning endpoint). Harden positioning null-vs-zero semantics so an all-null bucket surfaces as missing/degraded, not 0. [fund_score: build_positioning_changes_panel.py]
- [ ] (bug) build_gold_metadata fee circular-dep on clean rebuild [codex P2] — `_resolve_primary_class`/`load_fees` read `data/gold/fund_metadata.parquet`, the file the script is about to write, so a first clean `build-gold`/`full-rebuild` returns empty primary classes → all-null management fees (and gross-fee path) until a second pass. Derive primary classes from the in-memory metadata/ticker inputs for the current build instead of the prior output. [fund_score: build_gold_metadata.py]
- [ ] (data) 6 incoherent expense triples — bad FILED total/net ER at SEC source for single-class ETFs (e.g. First Trust Natural Gas ETF total ER 0.0% vs 40bps mgmt; MILITIA L/S 10.9bps vs 130bps). Per-filing parser fix to drive the coherence-gate baseline toward 0. [fund_score: mfrr]
- [ ] (data) Run the price/alpha refresh loop — OPERATIONAL residual from the freshness-audit fix (fund_score 0d912b5). The panels are honestly stale, not defective: the full-universe Tiingo pull + panel rebuild hasn't run since ~2026-05-18, and the Tiingo vendor parquet itself caps at 2026-05-08 for the full universe (only a 40-ticker smoke `pricing_refresh_*` runs to 06-09), so recovering fresh prices needs a **live Tiingo re-fetch**, then rebuild the alpha panels (refresh Ken French factors first — `ff6_weekly` ~2026-04-24, publication-lagged). One command: `make refresh-current-features`. The freshness audit correctly keeps WARNing (gold.fund_daily_adj_close 2026-05-08 / rolling_alpha 2026-04-24 / rolling_factor_alpha 2026-01-30) until this runs. Needs network + Tiingo API quota; run yourself, not via a backgrounding subagent (memory: own-long-llm-extraction-runs). Filed in docs/status/data_gap_analysis.md. [fund_score: make refresh-current-features]
- [ ] (data) MFRR fees/fund_metadata/tickers/turnover stuck at 2025Q4 — surfaced (not caused) by the freshness-audit calibration (fund_score 0d912b5). The current `mfrr_pipeline.py` emits only expense_ratios/fee_components/benchmarks/narratives(+metrics) — all now at 2026Q1 — while these 4 outputs come from a **separate/legacy extraction path** last run at 2025Q4 (no 2026Q1 file). The 2026Q1 SEC bundle IS downloaded+extracted (data/mfrr/extracted/2026Q1/), so this is **recoverable** — re-run the legacy producer for 2026Q1. Consumers: `tickers` is read by alpha/expense.py + prepare_hierarchical.py (ticker→series map, low churn); `fees`/`fund_metadata`/`turnover` have **no** current consumers → decide with owner: refresh, or deprecate the 3 unconsumed outputs (and drop from `MFRR_QUARTER_DIRS` in the audit). [fund_score: mfrr legacy producer / audit_data_freshness.MFRR_QUARTER_DIRS]
- [ ] (data) FCNTX-class class recovery — ~85% of class-null fee rows still unrecovered (funds with no class-bearing `(series,document)` precedent in the SEC bulk dataset stay null). Explore per-filing R-file parsing or a ticker-based map. [fund_score: alpha/expense.py]
- [ ] (data) Value Score 62-fund replica gap — 62 EQ funds have ≥200wk of returns but no `replica_r2` computed in `l2_replica_quality`, so they fall to `not_comparable` in `value_score` and get no score. Run/expand the L2 replica fit (mandate pools) for them to recover ~62 scored funds. [fund_score: l2_replica_quality / value_score]
- [ ] (data) NAV panel serves dead-class tickers — ROOT-CAUSED 2026-07-10 (was: "NAV feed cuts off ~2025-04-30"; investigation in night-run-20260709.md): NOT an ingestion bug — Tiingo 200s with genuinely-ended series. The 317 frozen tickers in passive_alt_daily_nav = **284 dead series** (Putnam→Franklin mergers/closures — honest NAV end) + **23 retired share-class tickers** whose funds live under CURRENT tickers already fresh in the vendor store + **6 degraded classes** with fresh sibling-class coverage + **2 TRUE vendor gaps** (JMVNX, JCAVX: single-class, alive 2026 N-PORT, Tiingo stopped) + 1 recovered by the 2026-07-10 refresh + 2 unmapped. REMAINING FIX (after the running Tiingo refresh completes): rebuild fund_daily_adj_close + the passive-match NAV pairing on the CURRENT-ticker basis (series→best-covered class ticker; use the 8993b29 deduped universe + class mappings — a per-series ticker can die while the series lives), then re-measure the profile-nav recovery (the original "~123 recoverable" premise mixes honest-dead with class-basis losses). JMVNX/JCAVX → alternate source (N-PORT monthly NAV) or honest null. Sibling: passive-blend micro-gaps item below shares the rebuild. [fund_score: passive-match / build_fund_adj_close ticker basis] [model: fable, effort: xhigh]
- [ ] (data) Passive-blend micro-gaps fragment live-fund NAV tails — `passive_alt_daily_nav` drops 1–2 month runs of `benchmark_nav` for funds that ARE continuously priced in `fund_daily_adj_close` those same months, right before the series end (e.g. LSLTX: paired run 2002-12..2025-01, gap 2025-02/03, then a 1-month 2025-04 stub; MRTHX: 2017-01..2025-02, gap 2025-03, 2025-04 stub). 122/123 profile-nav "recoverable" funds show this fund-alive-in-gap pattern; the tiny post-gap stub then trips the MIN_MONTHS length filter and nulls the whole fund. Root-cause the passive-match/`benchmark_nav` build's month gaps for continuously-priced funds (a continuous blend would keep these funds served without any trim heuristics). Surfaced 2026-07-04 by the profile-nav-series coverage review. [fund_score: passive-match / build_benchmark_nav → passive_alt_daily_nav]
- [ ] (data) serve-l2-passive-candidate-fit serves non-match / inverse ETFs as the "closest passive alternative" — built out-of-band, **FAILED data-review 2026-07-04**; do NOT ship as-is (code preserved on branch `wip/concurrent-fund-family-l2`, spec back in `queue/`). BLOCKERS: (1) **No fit-quality floor** — 7.1% of the 3,299 served funds have selected `correlation < 0.7`, **9 negatively correlated** (RATE→AGG corr −0.909 beta −3.61 TE 3,190bps; GSCOX→IWD corr −0.22 TE 316%); presenting an inverse ETF as "closest passive alternative" is worse than honest suppression → add a min-correlation / min-R² / max-TE gate (else serve null). (2) **Contradictory fit numbers under one label** — `selected_summary` stacks 3 R² + 2 TE for the same fund×ETF from two undisclosed bases (solver weekly-5Y vs `l2_replica_quality` daily); the spec's own reconciliation invariant (`is_selected.corr² ≈ replica_r2 ±0.01`) FAILS for **2,011/2,692 (75%)**, max gap 0.882 — silently replaced by a tautological check → collapse to ONE labeled basis. (3) **check-data is GREEN but blind** (`check_l2_passive_candidate_fit.py` only tests bounds + copy-fidelity, not fit quality / cross-basis coherence) → add fit-quality + coherence checks. Also: corrupt source price **GSCOX 2022-06-13 (+756% one-day)** propagates untouched into corr/beta/TE (upstream `fund_daily_adj_close`); no `n_obs` floor (899 selected fits use <2yr). Recoverable-miss is small (7 funds); the failing axis is precision-of-served. **(4) NEW page-coherence evidence (2026-07-12 TE-flip data-quality-critic, P1/P2)**: one IENAX page serves TWO passive alternatives — TE section passive_alt_label=IXC (value_score L2 blend) while the Alternatives/passive_baseline section serves closest_passive::VEU with passive_fit_r2 0.9396 that is actually the IXC+XOP replica_r2 (misattributed R² next to the wrong ETF); VEU as an energy fund's closest passive is facially wrong. Scale: 605/2,054 te_decomp funds have te label ≠ passive_baseline rank-1 ETF. Add to the fix: a load-time cross-panel coherence gate (fail when te passive_alt ≠ alternatives closest_passive without an explicit different-question disclosure). [fund_score: product/l2_passive_candidate_fit.py + serving/fact_assembler._passive_baseline + scripts/reports/check_l2_passive_candidate_fit.py]

## Specced (in queue)
Stories that became a queued spec — they've left the backlog for the spec queue and are **not**
re-triageable. `/implement-next` flips one to Done when it ships the linked spec.
Format: `- [~] (story) Title — context → specs/queue/<slug>.md`

- [~] (fast-follow) Look-through coverage-honesty hardening — two codex `--high` P2 advisories from the shipped look-through positioning feature (fund_score fd982cd), both honest-partial-coverage disclosure gaps. (1) `lookthrough_window.py:315` flags `is_unresolved_wrapper` only when `msid` resolves, so a held ETF outside the twin-map with a non-mapping/absent LEI (VOO/IVV/VEA) is counted as a resolved leaf → `lookthrough_coverage` over-reports; flag it honest-missing (or extend resolution), exclude from the resolved numerator. (2) `fact_assembler.py:785` emits coverage metadata only inside the per-`is_surfaced`-series dict, so a partial-coverage fund with zero surfaced shifts gets NO positioning_changes section → UI can't show its partial-coverage flag; emit `rows:[]` + coverage metadata for partial funds. Backend (fund_score), reviewed lane, depends_on look-through-positioning-changes. Realized impact nil today (data-reviewer verified), latent as data evolves. → specs/queue/look-through-coverage-honesty.md

## Done

**Recent only — the 3 most recent shipped items** (full history: `backlog-archive.md`). When a new
item ships, add it on top and move the overflow entry to the TOP of `backlog-archive.md` — the
archive is the full changelog; this section is a bounded buffer so the operational backlog (loaded
on every `/triage`) stays small.

- [x] (bug) TE bets-table presentation batch from the 2026-07-12 DQ-critic — SHIPPED 2026-07-12 inside the positioning_context cutover flip (fundscore-web branch feat/profile-v2-section-flips): (a) blend-aware "Active vs blend (pp)" header + baseline-composition chip (weights only when the positioning cohort provably covers the full blend — qualifying_weight===1; else names-only from public l2_blend_etfs; DTEYX "IXN 50% + FDN 50%", IENAX "IXC + XOP" — mitigates the vs_benchmark P1 until the panel fix); (b) percentile copy rounded + shared cohortPhrase ("2nd percentile of the 160 funds benchmarked to IWF"; fee ruler imports the same helper); (c) split freshness stamp "returns through 2026-04-24 · built 2026-05-09" (window_end added to the TE proof point + golden assertion). Gates: lint/build/golden ALL PASS, codex high pass 0 P0/P1. [fundscore-web: CurrentPositioning.tsx / BetsTable.tsx / format.ts / gating.ts]
- [x] (bug×3) Serving hygiene trio — X-ray as-of / passive_fund reason / top-holdings recency — SHIPPED 2026-07-12 (fund_score 005e24c, branch fix/serving-hygiene-trio, merged+pushed; LIVE as manifest 34). (#25) X-ray header no longer reads eval-date-as-today: 2,737→0 funds (class-keyed stray filter — dormant funds' honest eval==real-date headers survive; producer fill_null(eval_date) also removed at source w/ per-series date joined pre-truncation, converges next rebuild; FCNTX header 2026-03-31). (#30) 1,717 truly-passive funds (1,661 vo-filed + 56 is_index_fund, 0 leaks) serve active_fee_over_passive_missing_reason='passive_fund' (VOO); condition uses RAW vo style, never identity's ETF fallback — two review rounds killed the broad-flag versions that would have mislabeled 451 then 18 active ETFs (ULTY-class). (#31) 63 funds w/ books older than frontier−548d no longer serve top_holdings (teaser untouched, 0 live-by-snapshot funds lost) + deterministic tie-break makes holdings/fees/xray byte-reproducible. Data-reviewer FAIL→PASS: B2 found 53/63 'dormant' funds are LIVE filers = upstream ingestion gap → filed as new Open item; codex ×3 pass 0 P0/P1. [fund_score 005e24c]
- [x] (data) theme::emerging_markets_exjapan label "EM ex-China" → "EM ex-Japan" — FIXED AT SOURCE + BYTE-IDENTITY MIGRATION 2026-07-11 (fund_score e4c877f, branch fix/em-exjapan-label, merged to main). Catalog name+rationale fixed (basket holds BABA/JD/PDD China + TW/IN/BR/MX/KR, no Japan — old label contradicted constituents); propagated to ALL 17 label-bearing artifacts (13,061 strings: xray 3,372, return_attr 1,853, positioning 2,177, te_decomp 260, divergence 938, staging 3,230 across 1,310 funds, screeners, prose panels incl. nested named_bets). Pinned rebuilds FAILED identity (input-state drift post-dedup → filed as the panel-refresh Open item); shipped as restore+keyed/prose patch (scripts/maintenance/label_fix_em_exjapan_20260711.py, idempotent) with verify 18/18 identical-mod-label. **te-decomposition RENDER spec UNBLOCKED** (gate WARN C2 resolved). Gates: make check 5 PASS/3 pre-existing WARN/0 FAIL; data-reviewer PASS 0-blocking (independent positional identity on 8M rows; 272-context collision audit — zero real ex-China fund names touched; advisories: consider "Broad EM" rename, manifest26 snapshot retains old label); codex ×3 → pass 0 P0/P1 (P1 en route was the PRE-EXISTING the_take citation drift the honest report exposed → new Open item + proven-pre-existing on backups). Postgres reload of the fixed staging pending owner sign-off. Exposed + filed: panel-refresh, the_take drift, tie-break nondeterminism, screener-from-retire-branch. [fund_score e4c877f]
