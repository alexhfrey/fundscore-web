# Triage Backlog

The intake list. `/triage` drains the **Open** section: it picks the top item and routes it by
`(type)`. A `(bug)` or `(data)` item is fixed end-to-end and moves straight to **Done**. A `(story)`
that gets turned into a queued spec moves to **Specced (in queue)** — it has left the backlog for the
spec queue and is no longer re-triageable — and only reaches **Done** when `/implement-next` ships
that spec. Each item lives in exactly one state at a time (never Open *and* specced).

**Format:** `- [ ] (type) Title — one-line context [area]`
**Optional model tag:** `[model: fable|opus|sonnet, effort: xhigh|high|...]` — a routing hint for judgment-heavy items. Fix loops run in the MAIN session, so the dispatcher should surface the tag ("this item recommends X — /model to switch, or proceed") rather than silently ignore it. Specs carry the same hint as `model:`/`effort:` frontmatter, which `/implement-next` passes to the implementer agents automatically.
**Types & routing:**
- `(bug)` → `fundscore-data:fix-bug` (fundscore-harness plugin) — lean loop: reproduce → fix → verify → review (Claude + codex) → commit.
- `(data)` → `fundscore-data:fix-data` (fundscore-harness plugin) — lean loop: root-cause at source → fix → rebuild → /check-data + data-reviewer → review (Claude + codex) → commit. (No synthetic data; fix upstream.)
- `(story)` → `spec-story`: clarity-gate → a lean spec in `specs/queue/` (or escalate to the owner if vague). Discovery-heavy, net-new product direction goes to `/critique-funds` → review-proposals → `/spec-approved` instead. Once a spec is queued, the item moves to **Specced (in queue)** (below), not Done.

Keep items small and single-purpose. Put the type first so the dispatcher can route without reading the whole line.

---

## Open

- [ ] (bug) Pipeline admin console pre-commit fixes [codex P1/P2] — findings from the 2026-07-09 machinery codex gate against the UNTRACKED admin-console WIP (`src/app/admin/pipeline/`, `src/lib/pipeline/admin.ts` — must be fixed before that feature is committed): [P1] `adminEnabled()` (admin.ts:80-82) gates the runner on `NODE_ENV !== "production" || PIPELINE_ADMIN_ENABLED === "1"` — one env var exposes an UNAUTHENTICATED endpoint that spawns arbitrary codex/claude processes and edits the backlog/specs; require a real auth check (session/token), not env-only. [P2] `moveSpec` (admin.ts:194-196) moves a spec out of `specs/queue/` but leaves the backlog's `## Specced (in queue)` line `[~]` pointing at the old path — reconcile the backlog line on move (or block the action for story-linked specs). [fundscore-web: src/lib/pipeline/admin.ts]
- [ ] (bug) exposure_xray served holdings-date shows eval_date (today) for ~51% of funds — `exposure_xray.py` fills a null `fund_holdings_as_of` with `eval_date` on stray stock rows, and `fact_assembler` serves `max(holdings_as_of)`, so the X-ray section's "as of" reads as **today** when the holdings are actually a quarter old (e.g. FCNTX holdings 2026-03-31 but xray header 2026-07-02). Pre-existing (backup 2597/5410, now 2774/5432 — same scale). Fix the null-fill (honest null, or the fund's real latest quarter) or the serving `max()`. Surfaced 2026-07-03 by the NPORT-refresh sweep. [fund_score: exposure_xray.py:341 / serving/fact_assembler.py:501]
- [ ] (data) exposure_xray country-region aggregate exceeds 100% of NAV for some funds — served US country exposure reads **RYZAX (S&P 500 Pure Value, *passive*) 158.6%** and **COHOX 152.6%**, yet every individual holding is ≤103% of NAV — so this is a double-count / look-through renormalization bug in the exposure_xray `country_region` aggregation, NOT misfiled pctVal. A long-only fund's country sum should not exceed ~100%. Pre-existing; surfaced by the pct_nav-guard data-reviewer 2026-07-08. Root-cause the country rollup (and add a coherence check that a country aggregate can't materially exceed 100%). [fund_score: exposure_xray country_region aggregation]
- [ ] (bug) VOO-class passive funds serve a null canonical with no explicit `missing_reason` — should label "passive fund, no active-fee-over-passive" instead of an empty `fair_fee`. Frontend-adjacent; coordinate the served shape. [fund_score serving + web]
- [ ] (bug) build_serving_facts.py clobbers the canonical staging parquet on partial/masked runs — `write_staging_parquet(rows, STAGING)` runs unconditionally, so a `--series` run or a run with a section input masked (change-isolation experiments) silently overwrites `serving_facts_staging.parquet` with an incomplete assembly. This bit the serve-full-holdings-backend build 2026-07-05: TWO out-of-band experiment runs (one masked staging overwrite 17:45/17:59, one full DB load from changeless code → manifest 26 stripped all 5,708 holdings_full gates) each failed data-reviewer checkpoint-3 until dispatcher rebuilds. Fix like build_fund_holdings_full.py: divert partial/masked output to a SAMPLE_OUT path (or refuse to write STAGING unless the build is full + unmasked). Codex advisories from the 5ddf855 sign-off belong to the same fix: [P2] `fact_assembler._holdings_full_teaser_by_series` returns `{}` when `fund_holdings_full_staging.parquet` is absent — silently assembles staging with the contract section missing; should FAIL FAST instead of treating the input as optional. [P3] `build_fund_holdings_full.py --series` crashes (None aggregates + assert) when the slice has zero filed holdings; handle empty → zero-coverage report + empty parquet. [fund_score: scripts/pipeline/build_serving_facts.py / serving/fact_assembler.py:389 / build_fund_holdings_full.py:247]
- [ ] (data) Fund-reorg duplicate series / ticker collisions — after a fund reorganization a successor + superseded series share a ticker (e.g. Pioneer→Victory Pioneer: old series stops filing ~2024Q4, successor takes the ticker at 2025Q4, neither is `is_defunct`). Surfaced in the fee-ripple sweep: it broke the alternatives grain (G2 — patched symptomatically by a dedup in `alternatives.finalize`) and shows up as **17 duplicate `canonical_ticker` rows (166 rows total) in `serving_facts_staging`** (GCSLX/INDCX/DHAMX/EGRAX/RWINX/… + placeholder tickers TBD/ZZZ + 134 null-ticker rows). Root-fix upstream: dedup the served universe/pool by current ticker→series (prefer the latest-filing series, drop the superseded one) so every downstream panel and `fund_profile_facts` has one row per ticker. Also refresh the now-stale "8,655 series" docstring in `alternatives.py` (universe re-scoped to 5,799 on 2026-06-29). Surfaced 2026-07-02 (fee-ripple data-reviewer + rebuild). [fund_score: source_inventory / universe / serving]
- [ ] (bug) full-history holdings_snapshots serves dormant funds' stale top-holdings — `holdings_snapshots` is (correctly) full-history for the time-series panels, but `fact_assembler._top_holdings_by_series` takes each series' latest quarter, so a dormant fund (e.g. S000060195, last filed 2024-12-31) is served with 2024-09-30 top-holdings — honestly labeled via `as_of_date`, so not fabrication, but stale holdings shouldn't surface on a live profile (the recent-2yr glob only covers holdings_complete/exposure_xray, not holdings_snapshots→top_holdings). Gate served top-holdings by the serving recency scope. Surfaced 2026-07-03 by the NPORT-refresh data-reviewer. [fund_score: serving/fact_assembler.py]
- [ ] (data) exposure_path active-universe lags fund_metadata — 22 funds with fresh 2026 holdings are excluded from `holdings_exposure_path` because `eq_active_universe()` → `active_retail_universe()` reads **unrefreshed** `fund_metadata`; rebuilding at a later as-of shifts the recency window and drops them even though their holdings are current (net −47 exposure_path series, 36 genuinely dormant + 22 metadata-lagged). Refresh `fund_metadata` alongside holdings, or have the active universe consider holdings recency. Surfaced 2026-07-03 by the NPORT-refresh sweep. [fund_score: build_exposure_path_attribution.eq_active_universe / universe]
- [ ] (bug) Coherence guard missing on the holdings_complete sibling path — the misfiled-pct_nav guard (fund_score f7ec601) covers `holdings_snapshots` (→ positioning, top_holdings) but NOT `holdings_complete` (→ exposure_xray), a parallel N-PORT read. holdings_complete is currently clean (0 pct_nav<0, 0 >1.5, max 1.03) so nothing leaks today, but the same class of corrupt *latest* filing would reach exposure_xray unguarded. Port the identical `[0,1.5]` null guard to `build_holdings_complete.py` for symmetry (no current data change expected). Surfaced by the pct_nav-guard data-reviewer 2026-07-08. [fund_score: build_holdings_complete.py]
- [ ] (bug) Positioning aggregation turns a quarantined null pct_nav into 0 (false 0% / false exit) — [codex --high P2 from f7ec601] Polars `sum()` returns 0.0 for an all-null group, and `build_positioning_changes_panel.build_positions` pivots with `aggregate_function="sum"` before treating null/zero as absence, so a quarantined holding that is the ONLY value in a ticker/sector/theme/top-10 bucket at an endpoint reports 0% weight (or a false exit) instead of degraded/missing. Verified **zero served impact today** (only the dormant/unserved S000030654 has an all-null latest quarter; no served fund's corrupt quarter lands on a positioning endpoint). Harden positioning null-vs-zero semantics so an all-null bucket surfaces as missing/degraded, not 0. [fund_score: build_positioning_changes_panel.py]
- [ ] (bug) build_gold_metadata fee circular-dep on clean rebuild [codex P2] — `_resolve_primary_class`/`load_fees` read `data/gold/fund_metadata.parquet`, the file the script is about to write, so a first clean `build-gold`/`full-rebuild` returns empty primary classes → all-null management fees (and gross-fee path) until a second pass. Derive primary classes from the in-memory metadata/ticker inputs for the current build instead of the prior output. [fund_score: build_gold_metadata.py]
- [ ] (data) 6 incoherent expense triples — bad FILED total/net ER at SEC source for single-class ETFs (e.g. First Trust Natural Gas ETF total ER 0.0% vs 40bps mgmt; MILITIA L/S 10.9bps vs 130bps). Per-filing parser fix to drive the coherence-gate baseline toward 0. [fund_score: mfrr]
- [ ] (data) Run the price/alpha refresh loop — OPERATIONAL residual from the freshness-audit fix (fund_score 0d912b5). The panels are honestly stale, not defective: the full-universe Tiingo pull + panel rebuild hasn't run since ~2026-05-18, and the Tiingo vendor parquet itself caps at 2026-05-08 for the full universe (only a 40-ticker smoke `pricing_refresh_*` runs to 06-09), so recovering fresh prices needs a **live Tiingo re-fetch**, then rebuild the alpha panels (refresh Ken French factors first — `ff6_weekly` ~2026-04-24, publication-lagged). One command: `make refresh-current-features`. The freshness audit correctly keeps WARNing (gold.fund_daily_adj_close 2026-05-08 / rolling_alpha 2026-04-24 / rolling_factor_alpha 2026-01-30) until this runs. Needs network + Tiingo API quota; run yourself, not via a backgrounding subagent (memory: own-long-llm-extraction-runs). Filed in docs/status/data_gap_analysis.md. [fund_score: make refresh-current-features]
- [ ] (data) MFRR fees/fund_metadata/tickers/turnover stuck at 2025Q4 — surfaced (not caused) by the freshness-audit calibration (fund_score 0d912b5). The current `mfrr_pipeline.py` emits only expense_ratios/fee_components/benchmarks/narratives(+metrics) — all now at 2026Q1 — while these 4 outputs come from a **separate/legacy extraction path** last run at 2025Q4 (no 2026Q1 file). The 2026Q1 SEC bundle IS downloaded+extracted (data/mfrr/extracted/2026Q1/), so this is **recoverable** — re-run the legacy producer for 2026Q1. Consumers: `tickers` is read by alpha/expense.py + prepare_hierarchical.py (ticker→series map, low churn); `fees`/`fund_metadata`/`turnover` have **no** current consumers → decide with owner: refresh, or deprecate the 3 unconsumed outputs (and drop from `MFRR_QUARTER_DIRS` in the audit). [fund_score: mfrr legacy producer / audit_data_freshness.MFRR_QUARTER_DIRS]
- [ ] (data) FCNTX-class class recovery — ~85% of class-null fee rows still unrecovered (funds with no class-bearing `(series,document)` precedent in the SEC bulk dataset stay null). Explore per-filing R-file parsing or a ticker-based map. [fund_score: alpha/expense.py]
- [ ] (data) Value Score 62-fund replica gap — 62 EQ funds have ≥200wk of returns but no `replica_r2` computed in `l2_replica_quality`, so they fall to `not_comparable` in `value_score` and get no score. Run/expand the L2 replica fit (mandate pools) for them to recover ~62 scored funds. [fund_score: l2_replica_quality / value_score]
- [ ] (data) NAV feed cuts off ~2025-04-30 for a batch of live funds — ~324 tickers' paired fund+passive series in `passive_alt_daily_nav` end on essentially the same day (2025-04-30) while the fresh majority (20,289 of 27,288 tickers) runs to 2026-05; both the fund's own `fund_daily_adj_close` and the paired series stop together, and codex confirmed some of these funds have 2025/2026 SEC filings (i.e. alive). A same-day cutoff across hundreds of funds ⇒ a fund-NAV **ingestion staleness/cutoff**, not organic liquidations. Consequence: their profile `nav_series` can only end ~13mo+ stale, so they honestly serve `nav_series:null` today (profile-nav-series v1). Root-cause which NAV vendor/pull freezes this cohort at 2025-04-30 and refresh so live funds carry current NAV (recovers ~123 profile-nav funds correctly, with fresh endpoints). Surfaced 2026-07-04 by the profile-nav-series coverage review (codex [P2] rejected a keep-longest-run band-aid as serving stale charts). [fund_score: NAV ingestion → passive_alt_daily_nav / fund_daily_adj_close]
- [ ] (data) Passive-blend micro-gaps fragment live-fund NAV tails — `passive_alt_daily_nav` drops 1–2 month runs of `benchmark_nav` for funds that ARE continuously priced in `fund_daily_adj_close` those same months, right before the series end (e.g. LSLTX: paired run 2002-12..2025-01, gap 2025-02/03, then a 1-month 2025-04 stub; MRTHX: 2017-01..2025-02, gap 2025-03, 2025-04 stub). 122/123 profile-nav "recoverable" funds show this fund-alive-in-gap pattern; the tiny post-gap stub then trips the MIN_MONTHS length filter and nulls the whole fund. Root-cause the passive-match/`benchmark_nav` build's month gaps for continuously-priced funds (a continuous blend would keep these funds served without any trim heuristics). Surfaced 2026-07-04 by the profile-nav-series coverage review. [fund_score: passive-match / build_benchmark_nav → passive_alt_daily_nav]
- [ ] (data) serve-l2-passive-candidate-fit serves non-match / inverse ETFs as the "closest passive alternative" — built out-of-band, **FAILED data-review 2026-07-04**; do NOT ship as-is (code preserved on branch `wip/concurrent-fund-family-l2`, spec back in `queue/`). BLOCKERS: (1) **No fit-quality floor** — 7.1% of the 3,299 served funds have selected `correlation < 0.7`, **9 negatively correlated** (RATE→AGG corr −0.909 beta −3.61 TE 3,190bps; GSCOX→IWD corr −0.22 TE 316%); presenting an inverse ETF as "closest passive alternative" is worse than honest suppression → add a min-correlation / min-R² / max-TE gate (else serve null). (2) **Contradictory fit numbers under one label** — `selected_summary` stacks 3 R² + 2 TE for the same fund×ETF from two undisclosed bases (solver weekly-5Y vs `l2_replica_quality` daily); the spec's own reconciliation invariant (`is_selected.corr² ≈ replica_r2 ±0.01`) FAILS for **2,011/2,692 (75%)**, max gap 0.882 — silently replaced by a tautological check → collapse to ONE labeled basis. (3) **check-data is GREEN but blind** (`check_l2_passive_candidate_fit.py` only tests bounds + copy-fidelity, not fit quality / cross-basis coherence) → add fit-quality + coherence checks. Also: corrupt source price **GSCOX 2022-06-13 (+756% one-day)** propagates untouched into corr/beta/TE (upstream `fund_daily_adj_close`); no `n_obs` floor (899 selected fits use <2yr). Recoverable-miss is small (7 funds); the failing axis is precision-of-served. [fund_score: product/l2_passive_candidate_fit.py + serving/fact_assembler._passive_baseline + scripts/reports/check_l2_passive_candidate_fit.py]
- [ ] (story) Fee-fairness peer band on the profile page — show where a fund's fee-over-passive sits vs its category peers (percentile), not just the raw bps. [web profile]

## Specced (in queue)
Stories that became a queued spec — they've left the backlog for the spec queue and are **not**
re-triageable. `/implement-next` flips one to Done when it ships the linked spec.
Format: `- [~] (story) Title — context → specs/queue/<slug>.md`

- [~] (story) Look-through positioning-changes panel for ETF-wrapper funds — positioning_changes (thin direct book, holdings_snapshots) reads incoherently vs exposure_xray (looks through wrapper ETFs, holdings_complete) for funds holding third-party ETFs (THEQ X-ray tech 29.7% vs positioning 3.0%; 10 funds >5pp / 17 >3pp / 37 >1pp of 3,215). SPECCED 2026-07-09: grounded in fund_score (feasibility CONFIRMED — the wrapper ETFs are themselves N-PORT filers with 20–23q of history; look-through machinery already exists in build_holdings_complete.py; buildable from existing data, no new ingestion), minimal PRD + 1 red-team round, owner resolved all 7 questions — Q1 replace-in-place, Q2 whole-universe change_z, Q3 partial-coverage+flag (honest-missing, never back-fill), Q4 extend twin map for QQQ/UITs, Q5 no FOF re-tag, Q6 windowed (endpoints trailing ~1yr, not full 104q), Q7 FOREIGN-INCLUSIVE basis matching X-ray (accepted: plain-international funds' positioning also changes; only plain-domestic unchanged). Backend track (fund_score), unblocked. PRD feature-pipeline/prds/look-through-positioning-changes.md → specs/queue/look-through-positioning-changes.md
- [~] (story) Serve the full holdings list per fund — the profile's "view all holdings" needs the complete classified position list (FCNTX: 280 rows at 2025-09-30) served per fund, not just top-10; decide gating (likely paid) and payload strategy (separate table like fund_attribution_blocks vs JSONB cap). Owner request 2026-07-01 (mock item 4). [fund_score serving + web profile] — BACKEND DONE 2026-07-05 (fund_score 5ddf855, MERGED TO MAIN 30e8c75 2026-07-06 + live via combined manifest 28; web schema mirror ca7820b + gating/golden 885e9ec): `fund_holdings_full` long table serves 5,708/5,799 funds (98.43%; 91 misses 100% honest, EDGAR-adjudicated), 1,398,936 as-filed rows, same-transaction load + in-transaction gate/teaser coherence check, FCNTX 428 rows @2026-03-31 byte-equal raw (META 10.2875%, SpaceX 5 lines 5.100%). Gates: checkpoints 3/3 PASS (data-reviewer ×3 rounds — two out-of-band clobbers caught+fixed), check_fund_holdings_full.py green, CODEX_GATE pass ×2 (fund_score: 0 P0/P1 + 2 advisories folded into the clobber bug item; web: clean). Spec serve-full-holdings-backend → done/. REMAINING: serve-full-holdings-web (frontend drawer + paid gate) now unblocked in queue/; owner decided basis (c) filed list / filed %-of-net-assets (PRD: feature-pipeline/prds/serve-full-holdings.md) → specs/queue/serve-full-holdings-web.md

## Done

**Recent only — the 3 most recent shipped items** (full history: `backlog-archive.md`). When a new
item ships, add it on top and move the overflow entry to the TOP of `backlog-archive.md` — the
archive is the full changelog; this section is a bounded buffer so the operational backlog (loaded
on every `/triage`) stays small.

- [x] (bug) apply_serving_schema fresh-DB + upgrade robustness [codex P1/P2] — FIXED 2026-07-09 (fund_score fix/apply-serving-schema-robustness 2e8fe61, MERGE TO MAIN PENDING). Defined the missing `asset_class_code` enum (EQ/FI/MU/MA/ALT/RE/OT — mirrors the web enum; the column stores taxonomy codes, not legacy long labels) and replaced the hand-maintained partial ADD COLUMN list with generation from `fact_assembler.SECTION_COLUMNS`, so every assembler section gets `ADD COLUMN IF NOT EXISTS` on an upgraded DB. Data-neutral DDL — no gold/product data touched, no rebuild. Unblocked the 2026-07-09 admin-run patch from stash@{0} via an isolated worktree (first use of `new-worktree.sh` + the branch/worktree doctrine; stash dropped after verifying full containment in the commit). Gates: 3 regression tests pass (every referenced enum defined; every SECTION_COLUMNS entry guarded); data-reviewer PASS (admin run, patch unchanged since); CODEX_GATE pass (high, 0 P0/P1, 2 advisories — the machine-local worktree data symlink correctly left uncommitted). [fund_score 2e8fe61]
- [x] (bug) implement-backend-spec pipeline: finalize can skip the mandatory codex gate + a killed finalize reports status:done — FIXED 2026-07-09 (fundscore-web a21897f, chore/pipeline-streamline). The workflow now FAILS CLOSED: finalize must return `codex_gate: pass` + `commit_sha` + `spec_moved_to_done` (all FINALIZE_SCHEMA-required) or the run returns `stopped` — never `done`; a null finalize result (session-limit death) also returns `stopped`; the gate protocol lives in the finalize prompt AND backend-implementer.md (gate BEFORE commit, max 2 rounds, an unrunnable gate is blocked — never a pass). Same batch also collapsed the verification tail (revision rounds 2→1; checkpoint-3 + /check-data folded into ONE final data gate). Machinery change per /review-prompts conventions. Gates: node --check on all 5 workflows; codex high review of the full diff — 0 P0/P1 in these files (1 P2 fixed pre-commit: spec_moved_to_done required; the unrelated admin-console [P1] filed as its own Open item). [fundscore-web a21897f]
- [x] (data) Misfiled out-of-range pct_nav coherence guard — FIXED 2026-07-08 (fund_score `fix/pct-nav-coherence-guard` f7ec601). Split from the FOF/ETF-wrapper item: `build_holdings_snapshots.py` already filtered valUSD>0 but carried filed N-PORT pctVal/100 into `pct_nav` blindly, so filings pairing a real positive valUSD with an internally-incoherent/misfiled pctVal corrupted the %-of-NAV display panels. Guard nulls pct_nav outside [0,1.5] at source (weight/valUSD untouched — honest-missing, never a synthetic fill): **272 rows** (269 negative incl. Enerpac S000001631 $19.0M @ pctVal −10212.53%, S000072700 whole-filing NAV-denom corruption 233%/231%/225%; +3 lines >150% of NAV). serving `_top_holdings_by_series` drops null-weight rows so a quarantined holding can't rank as a top holding. 1.5 ceiling keeps legit single-ETF-wrapper funds (~100-103% of NAV). Rebuilt holdings_snapshots + positioning_changes_panel (all invariants OVERALL PASS; +15 concentration rows correctly surface as removing corrupt cross-sectional outliers de-inflates change_z sd, 0 lost; 1 real change on dormant/unserved S000030654) + serving STAGING (no Postgres push). exposure_xray unaffected (reads already-clean holdings_complete). Gates: /check-data positioning OVERALL PASS (5/5 fund spot-checks to 0.00pp; z-tail WARN improved 11→10); **data-reviewer PASS** (272 nulls all trace to internally-incoherent raw N-PORT; served==gold==raw exact, FCNTX META 10.2875%; no synthetic data); **CODEX_GATE pass** (--high, 0 P0/P1, 1 advisory P2 = downstream null-vs-zero robustness, verified zero served impact). Residuals filed in data_gap_analysis.md: port the guard to holdings_complete (sibling, currently clean); exposure_xray RYZAX/COHOX country aggregate >100% (pre-existing); positioning null→0 hardening. The look-through incoherence → (story) in Open (owner: build a full-history looked-through positioning panel). Pre-existing unrelated test failure: test_risk_betas_eval_is_factor_date_not_holdings (stale risk_attribution date assertion, fails on pre-fix backup too). [fund_score f7ec601]
