# Night run 2026-07-16 — drain the Open backlog top-down (owner-prioritized order)

This file is the loop's single source of truth: read it at EVERY wakeup, fence-check, advance
ONE step, update the State section (including the heartbeat), reschedule. Untracked operational
state — do not commit this file.

Loop prompt (ScheduleWakeup, verbatim):
"Continue the overnight drain per feature-pipeline/night-run-20260716.md — read it, fence-check,
advance ONE step, update STATE (incl. heartbeat), reschedule; stop when the Open queue is drained
or everything remaining is blocked/fenced/owner-gated (then write the morning report, delete the
backstop cron, and stop the loop)."

## RESTART BACKSTOP (credit-limit resilience — owner directive 2026-07-16)

A recurring in-session cron (`11,41 * * * *`) fires the backstop prompt. Protocol:
- Every wakeup (loop OR backstop) MUST stamp `heartbeat:` in State with `date "+%Y-%m-%d %H:%M %Z"`.
- Backstop tick: if State `status:` is DONE/STOPPED, or heartbeat <50 min old → reply
  "backstop: loop alive — no-op", nothing else. Otherwise the wakeup chain is dead (usage limit,
  error): FIRST verify no step is still live (memory: verify-run-dead-before-resuming — check
  background tasks and agent-transcript mtimes for fresh writes before assuming dead), then resume:
  read this file, fence-check, advance ONE step, update State, and re-establish the chain by
  calling ScheduleWakeup with the loop prompt above.
- The cron is in-memory (this session only): the terminal must stay open and the machine awake.
  It survives usage-limit pauses (keeps firing; first post-reset tick resumes). Auto-expires 7 days.
- On normal completion: CronDelete the backstop job, then ScheduleWakeup stop.

## FENCE — LIFTED by owner 2026-07-16 23:5x ("There is no other session")

OWNER OVERRIDE: the recent-transcript/live-process signals were the owner's own closed/idle
sessions. fund_score work is UNBLOCKED immediately — no 2h-staleness wait.

Still-standing discipline (not about other sessions):
- All fund_score work in FRESH isolated worktrees off main (data/ symlink = SHARED lakehouse →
  strictly ONE fund_score item in flight). Scoped `git add` only; verify branch before commit.
- The main fund_score checkout's ~27 dirty files (report regens on feat/fmp-foreign-sector-map)
  are leftover state — don't sweep them into commits.
- Web-repo items: OK anytime, but run implement/fix work in isolated worktrees (Agent
  isolation: worktree) — never switch the main web checkout's branch.
- **NEVER `git push` the web repo — GitHub auto-deploy is connected (push = production deploy).**
  Don't push fund_score either; list unpushed commits in the morning report instead.
- backlog.md: re-read immediately before each edit; unique-string edits scoped to OUR item's line.
- Long builds (memory: own-long-llm-extraction-runs): run in THIS session via background Bash
  (nohup + FULL log redirection), poll via wakeups — never via a backgrounding subagent.
- Codex gates (memory: workflow-finalize-cannot-await-codex): the main loop OWNS codex as a
  background task (git add -N new modules first); iterate at --medium, gate once at high.

## QUEUE POLICY (no hand-curated list — the backlog IS the queue)

Each cycle: pick the TOPMOST eligible item in backlog.md **Open** (owner-prioritized top-down,
2026-07-13 ordering). Route by type per the backlog header: (bug)→fundscore-data:fix-bug,
(data)→fundscore-data:fix-data, (chore)→lean fix-bug-style, (story)→/spec-story (spec it; do not
auto-implement the resulting spec tonight). Fix loops run inline in this session (Fable).

INELIGIBLE tonight (skip; morning report must list them):
- Owner-decision items: X-ray >100%-of-NAV / book-basis (four owner decisions); factor-refresh
  ff6 re-fetch (marked OWNER DECISION).
- The three flagged NOT-lean-loop items at the bottom: holdings-direction vs active-β
  adjudication; serve-l2 fit-floor (failed data-review, wip branch); reviewed panel-refresh pass.
- Anything whose backlog line says BLOCKED, or whose repo fence is LIVE (revisit later in the
  night — fences may clear as other sessions go idle).
- The Specced (in queue) section and ai-summary-generation (owner must review) — out of scope.

Failure policy: gates fail → max 1 retry → mark ` — BLOCKED: <reason>` on the backlog line +
here, move on. Never lower a gate. Never spin. If an item exceeds ~3 wakeups without progress,
mark BLOCKED and move on.

## MORNING REPORT (on stop)

Write `feature-pipeline/run-summary-20260716.md` in the style of run-summary-20260709-11.md:
items shipped (with commits), items blocked + why, skipped/owner-gated list, unpushed commits in
both repos, memory lessons saved, what's waiting on the owner. Update backlog.md item states.
Then CronDelete the backstop, ScheduleWakeup stop.

## STATE

status: DONE (02:10 2026-07-17 — morning report written: run-summary-20260716.md; backstop cron deleted; loop stopped)
heartbeat: 2026-07-17 01:55 MDT (wakeup 17) — SPY-input bug half (a) FIXED + VERIFIED in
  worktree fund_score-wt-spy-input (branch fix/xray-spy-input-resolution): ETF-universe
  fallback in _resolve_row (series_id honestly None; series injected into the fund frame on
  the SAME weekly basis as blend legs; all guards apply). VERIFIED: {VTSAX,AGTHX,SPY} now
  cov=available, blend VTI 83.2+IWF 16.8, r2 0.997, fee gap 22bps coverage=partial (SPY ER
  missing = half (b), open); landing book byte-identical; SPY:100 → SPY 100% honest-degenerate;
  junk ticker still suppresses; 10/10 tests incl. 2 new integration tests. ADJUDICATED:
  VTSAX typed "etf" is series-true (VTSAX+VTI share S000002848; is_etf is series-level) —
  cosmetic class-level typing filed as note, no computation impact. Codex IN FLIGHT
  (bgn0iwxkp) → commit on pass, mark half (a) done on the item, then likely LAST substantive
  item of the night → morning report next.
(wakeup 16 was: L2-cadence chore SHIPPED f011953.)
  QUEUE DECISION: price-quarantine item DEFERRED tonight with reason — it rebuilds the
  canonical price panel (the basis under the OPEN northstars drift adjudication + everything
  shipped tonight); doing it before the owner adjudicates/merges would stack a second basis
  change on an unattributed drift. NOT blocked-blocked: first item for the next session after
  merges+adjudication. Pricing-store hygiene (compaction) similarly deferred (multi-hour store
  rewrite, same shared-store risk class at 2am). NEXT: (bug) SPY unsupported as X-Ray INPUT —
  taking half (a) input-resolution (bounded code fix, fund_score worktree, CLI-verifiable);
  half (b) UIT expense-ratio sourcing stays open (needs a data-source decision).
(wakeup 15 was: L2-cadence hardening chore:) BOTH fixes applied
  in worktree fund_score-wt-l2-hardening (branch fix/l2-cadence-hardening): (1)
  build_benchmark_nav ETF gap-fill + proxy splice moved raw-glob→canonical panel (loader
  exercised read-only: 61 ETFs gap-filled, 0 dup (ticker,date) rows, →2026-07-10; SHARED-GOLD
  REBUILD DEFERRED — passive_alt_daily_nav feeds value_score whose northstars drift is an
  open adjudication; rebuild goes with the quarterly runbook post-merge). (2)
  served_l2_refit_date fail-closed partial-partition guard (key col verified = 'fund' — first
  cut crashed on real schema; guard fires on synthetic sliver, passes real root 25,386 vs
  25,529) + 3 unit tests. 47 tests pass (NOTE: this worktree lacks the conftest prepend fix —
  must run PYTHONPATH=src; fix lives on the fresher-beta branch). Codex IN FLIGHT (bvld3jn30);
  on pass → commit, mark chore Done, next item.
(wakeup 14 was: fresher-β shipped d96098b on)
  fix/fresher-beta-rebuild (worktree kept for the morning merge — shared gold already
  reflects branch code, so merge PROMPTLY). Backlog: item→Done; FILED payload-contraction
  OWNER-DECISION item (71 funds) + northstars-drift item (routed to L2 migration); EGRAX/
  DFLVX/TBD/ZZZ appended to identity-hygiene evidence. Next eligible Open item: (chore) two
  L2-cadence hardening follow-ups (build_benchmark_nav → canonical basis; served_l2_refit_date
  partial-partition guard) — fund_score lean, new isolated worktree.
(wakeup 13, 01:20 — gates:) codex r2 PASS (P2 → basis logging added,
  byte-stable rebuild r5); data-reviewer r2 PASS 0-blocking (bindings independently verified,
  FBGRX/FCNTX bit-exact, guard test proven falsifiable, exclusion set reproduced; refined the
  71-fund finding → Jul-5 beta-skill payload rebuild 10,731→10,435 w/ .pre-beta-skill-bak
  backup as evidence, 49/71 live-priced — OWNER MUST CONFIRM intentional before ANY serving
  reload from this fe). Its conftest footgun fix applied (prepend src). IN FLIGHT bg task
  bm06znj4w: pytest re-verify + codex r3 on final diff (verdict JSON will land in worktree
  feature-pipeline/reviews/ — stdout was mistakenly discarded) + fresh make check for the
  pre-commit gate. ON COMPLETION: read codex verdict file; if pass → COMMIT on
  fix/fresher-beta-rebuild (2 builders, module, 2 test files, conftest, fresh check report),
  then backlog: item→Done + file 3 items (northstars drift ownered to L2 migration;
  payload-contraction/71-fund serving question; EGRAX/DFLVX/TBD/ZZZ identity-hygiene
  evidence) + morning-report note: MERGE the branch before anyone rebuilds these artifacts
  from main (shared gold already reflects branch code).
(wakeup 10 heartbeat was 00:25; sweep findings:) per-fund delta vs manifest-45
  baseline = lost 73 / gained 30 factor-β funds. Adjudicated: 21 dead/merged price series
  (honest); 2 = dup-ticker unique(keep="first") nondeterministic winner-flip → FIXED at source
  in build_factor_exposure.py (deterministic sort; live evidence for the backlog SWEEP item);
  ~50 live funds (TRP intl cluster) genuinely LEFT the value-offering universe (absent from
  the 07-05 payload AND today's vo panel — upstream universe change, NOT tonight's rebuild;
  the old factor artifact predated it). ⇒ MORNING REPORT + backlog: at the next serving
  reload ~52 live funds lose served risk-attribution vs manifest 45 — owner-visible coverage
  drop, belongs to the panel-refresh/universe item, do NOT band-aid. Deterministic rebuild:
  identical aggregates, 40/40 tests green. make check DONE: 5 PASS / 2 WARN (pre-existing
  xray-country + exposure-path) / 2 FAIL — data_freshness PRE-EXISTING (identical 07-15);
  value_score_northstars flipped PASS→FAIL at TODAY'S 17:43 L2 value_score rebuild (2022
  cohort median −80 vs ~−60 tolerance; NOT downstream of this diff) → FILE as new backlog
  item. Aggregate diagnostics clean (β 0 nulls/0 non-finite, r2 bounded, grain unique,
  invariant holds). GATES IN FLIGHT (wakeup 11, 00:45): data-reviewer agent + codex bg
  b6phwwkr2. ROUND 2 (00:55): codex r1 passed clean, but data-reviewer r1 FAILED — correct
  block: lowest-sid dedup froze CHGX/EGRAX onto defunct/wrong registrations (fund-A holdings
  × fund-B β). FIXED: winner = strictly-freshest panel holdings book; TIED claimants (both
  live) = ambiguous identity → ticker EXCLUDED + warned (transparent null > wrong binding).
  Excluded: EGRAX (1-fund honest serving drop — was served under Global Macro sid; disclosed),
  DFLVX (served sid had NO β → zero regression), TBD + ZZZ (placeholder junk tickers in the
  payload — hygiene evidence). CHGX/DHAMX/PXWIX verified on live registrations; 41 tests green
  (incl. NEW guard-falsification test the reviewer requested). Final: fe 876,800 rows/4,576
  funds; divergence 43,687/4,158. GATES r2 IN FLIGHT: data-reviewer re-check + codex re-run
  (boe54g9mx). On both pass: commit (2 builders + module + test + fresh check report), file
  3 backlog items (northstars drift; ~52-fund universe coverage drop; EGRAX/DFLVX/TBD/ZZZ →
  identity-hygiene evidence), mark item Done.
(wakeup 9: fresher-β rebuilt+green:)
  factor_exposure eval 2026-05-22 (877,212 rows / 4,578 funds; was 890,814 rows @ 04-24 —
  delta must be explained per-fund in the sweep); exposure_divergence rebuilt 43,699 rows,
  holdings max 2026-04-30 ≤ factor 2026-05-22, guard dropped 16 rows / 2 funds (2026-05-31
  sliver, honest+logged); era-stamped test literal (== "2026-04-24") replaced with
  gold-artifact assertion per repo convention; ALL 40 tests in both files PASS.
  NEXT STEP (wakeup 10): inconsistency sweep — per-fund delta vs serving_facts_staging.parquet
  baseline (old gold was overwritten in place — lesson: snapshot before rebuild), /check-data
  on factor_exposure + exposure_divergence, make check full-universe, provenance spot-checks;
  then data-reviewer + codex; commit scoped on fix/fresher-beta-rebuild (stage fresh check
  report for the pre-commit gate).
current item: (data) fresher-β rebuild — fix-data IN PROGRESS, before-state MEASURED.
  Worktree: /Users/alexfrey/Projects/fund_score-wt-fresher-beta (branch fix/fresher-beta-rebuild
  off main ebdb9c8, data/ symlinked). BEFORE: factor_exposure eval 2026-04-24 (890,814 rows);
  exposure_divergence 43,625 rows w/ holdings_as_of up to 2026-05-31 > factor_eval 2026-04-24;
  test_artifact_structural_invariants RED (holdings≤factor_eval all() fails);
  test_risk_betas_eval_is_factor_date_not_holdings RED ('2026-04-30' < '2026-04-24' fails).
  BOUNDS: ff6_weekly frontier 2026-05-22 (re-fetched 07-12); prices → 2026-07-10; holdings max
  2026-05-31 is a 3-fund sliver (broad frontier 2026-04-30). ⇒ PLAN: (1) re-eval factor
  exposures at the ff6 frontier 2026-05-22 (build_factor_exposure.py, heavy → background bash
  + full log); (2) root-fix build_exposure_divergence.py to pair each fund's newest holdings
  ≤ factor_eval_date (PIT constraint at source — the 2026-05-31 sliver pairs its prior
  snapshot); (3) rebuild divergence + downstream, re-run both tests green, sweep, gates.
  NEXT WAKEUP: read both builders, apply the constraint fix, launch the factor re-eval bg.
prior shipped tonight: methodology 729cb32; X-Ray example 2e8a419; PRD 5a788c2 (all on web main).
current item: (data) methodology stale as-of dates → corrections APPLIED (16 registry edits from
  the derivation truth table + per-product-frontier prose ×2 in methodology/page.tsx + facts.ts
  comment; risk-behavior left honest-unverifiable → build-date phrasing; takeaways/the-take/vo
  eval dates verified genuinely still 2025-10-31, left unchanged). Build+lint green. CODEX:
  pass + 3 substantive P2s ALL FIXED (passive-blend per-ETF source dates up-to 2026-04-30;
  return-attr "frontier" → return-window-end phrasing; risk-attr stale-β disclosed in asOf AND
  the method text's now-false "runs to a fresher date" claim qualified); rebuild green. GATE
  REMAINING: data-reviewer agent (reviewing pre-P2-fix diff; fixes only made strings more
  conservative) — on pass: commit in
  /Users/alexfrey/Projects/fundscore-web-wt-nightrun (branch night-run/20260716-drain, ==main
  after the ff-merge; copy changed files over, reconcile main tree same as 2e8a419), mark
  backlog item Done, note follow-up (auto-derive registry values) on the item line.
BRANCH CLEANUP (owner-requested, DONE ~00:00): web night-run branch ff-MERGED into main (main
  now 2 ahead of origin, NOT pushed — push = prod deploy, owner's call); web merged branches
  deleted (fee-peer-band-web, profile-v2-section-flips, te-decomp-flip, xray-as-of-copy).
  fund_score: main checkout switched sector-branch→main (local X-Ray now runs the FAST solver);
  11 merged branches deleted; worktrees compact-pricing + sector-rollup removed (untracked
  artifacts preserved into main checkout: eodhd licensing email + validation spike ×2 +
  codex-verdict json); KEPT unmerged: te-gate worktree (fix/te-decomp-build-gate a9b2dc0) + 6
  branches (design/value-offering-score, experiment ×2, feat/retire-legacy-value-offering-serving
  [backlog says keep], feature/multisignal-scoring, wip/concurrent-fund-family-l2).
FENCE: LIFTED (owner). fund_score items now eligible — next after methodology: top Open item
  (data) stale exposure/risk-attribution fresher-β rebuild.
prior: X-Ray default-example bug SHIPPED (2e8a419). CODEX_GATE pass (2 P2 advisories,
  both handled: state file stays untracked; story line's stale 2.2GB/compaction-first tail
  rewritten). Commits live on web branch `night-run/20260716-drain` in worktree
  /Users/alexfrey/Projects/fundscore-web-wt-nightrun: 2e8a419 (EXAMPLES fix), 5a788c2
  (solver-http-service PRD). Main checkout's working copies reconciled (component reverted,
  untracked PRD copy removed so the morning merge is clean). Branch-guard hook forbids main
  commits (classifier also blocks SKIP_BRANCH_GUARD) ⇒ ALL tonight's web commits go on this
  branch; owner merges once in the morning. backlog.md edits stay UNCOMMITTED in the main
  working tree (file carried another session's uncommitted edits before we started — never
  sweep foreign hunks). fund_score solve worktree removed. Next wakeup: fence-check → next
  eligible Open item — expected: (data) methodology page serves STALE as-of dates (line ~46,
  web repo), since SPY-unsupported (line 40) is still fund_score-fenced.
fence log: 23:05 initial survey — other-session transcripts active <90 min ago → fund_score
  fence LIVE at start; begin with topmost eligible WEB items, re-check fence each cycle.
dispatch state (runIds/agentIds/bg tasks): backstop cron job e2093cc6 (11,41 * * * *);
  red-team agent for solver-http-service PRD in flight (wakeup 1).

### Log
- 23:05 Setup: file created; backstop cron e2093cc6 registered; first work wakeup in ~2 min.
- 23:08 Wakeup 1: story solver-http-service. Dedup clean; queue-depth nudge: 9 specs already
  unstarted in specs/queue/ — recommend draining before speccing more. Grounding: verified on
  fund_score main the solver reads canonical panel (581M) + 3 small parquets ≈ 600MB (story's
  2.2GB = raw tiingo store, no longer read); solves ~1–4s ⇒ compaction-first premise stale.
  FINDING (morning report): main fund_score checkout is on pre-fix sector branch a6d7dcb —
  d92213c NOT an ancestor ⇒ local X-Ray currently runs the OLD slow raw-glob solver; fix is
  `git switch main` but checkout is fenced tonight. PRD written; red-team dispatched.
- 23:19 Wakeup 2: red-team returned 4 blocking clarifications (input inventory was 4/9 files —
  spot-verified the 5 omissions ≈78 MB incl. 2 crash-on-missing + 3 silent-fail-soft; as-of had
  3 independent pins; deep-equal parity untestable cross-platform; error contract undefined) +
  2 NEW owner questions (vendor-data licensing into a container registry; between-refit
  freshness stance). All 4 clarifications folded into the PRD in the single allowed revision;
  anti-fail-soft packaging gate added to acceptance. Owner questions remain ⇒ ESCALATED:
  backlog line marked AWAITING OWNER (4 questions), story stays Open. spec-story loop for this
  item ends here by design — no spec until owner answers.
- 23:36 Wakeup 3: fence re-check → still LIVE (4 other-session transcripts <2h). Took (bug)
  X-Ray default example (line 44, owner pre-scoped part (a)). One-line EXAMPLES swap applied;
  live-solve verification in isolated worktree (both books); build+lint green; codex gate
  dispatched as bg task blrr2fmap. MORNING-REPORT NOTES: (1) old book non-repro on main —
  filed-era bug, fix still owner-directed; (2) landing facts.ts DEMO_* constants are a
  2026-07-13 capture, current solve gives IWF 62.5/VEU 37.5, gap 23.05bps (~$230/$100k vs
  captured $227) — regenerate screenshots + constants together at next refresh (pre-existing
  drift from the as-of advance, NOT tonight's change).
- 23:35 Wakeup 4: codex pass → shipped. Item 44 marked [x] DONE(part a) on backlog (branch
  2e8a419); story line stale-tail rewritten per codex P2; PRD committed 5a788c2. Web commits
  now flow through branch night-run/20260716-drain (branch-guard doctrine) — MORNING: owner
  merges this branch into web main (fast, no conflicts expected: main copies reconciled).
